---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - pyproject.toml
  - helpertips/__init__.py
  - helpertips/listener.py
  - helpertips/parser.py
  - helpertips/store.py
  - helpertips/db.py
  - tests/test_parser.py
  - tests/test_store.py
  - tests/test_config.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Rodar 'pip install -e .' instala o pacote helpertips em modo editavel"
    - "Todos os imports entre modulos usam 'from helpertips.parser import parse_message' (nao 'from parser import')"
    - "python3 -m pytest tests/ passa apos a reestruturacao"
    - "Nenhum arquivo .py permanece na raiz do projeto (exceto scripts de entrada)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Configuracao do pacote Python com metadados e dependencias"
      contains: "[project]"
    - path: "helpertips/__init__.py"
      provides: "Marca helpertips/ como pacote Python"
    - path: "helpertips/listener.py"
      provides: "Listener movido para dentro do pacote"
      contains: "from helpertips.parser import parse_message"
    - path: "helpertips/parser.py"
      provides: "Parser movido para dentro do pacote"
      contains: "def parse_message"
    - path: "helpertips/store.py"
      provides: "Store movido para dentro do pacote"
      contains: "def upsert_signal"
    - path: "helpertips/db.py"
      provides: "DB movido para dentro do pacote"
      contains: "def get_connection"
  key_links:
    - from: "helpertips/listener.py"
      to: "helpertips/parser.py"
      via: "from helpertips.parser import parse_message"
      pattern: "from helpertips\\.parser import"
    - from: "helpertips/listener.py"
      to: "helpertips/store.py"
      via: "from helpertips.store import upsert_signal, get_stats"
      pattern: "from helpertips\\.store import"
    - from: "helpertips/listener.py"
      to: "helpertips/db.py"
      via: "from helpertips.db import validate_config, get_connection, ensure_schema"
      pattern: "from helpertips\\.db import"
    - from: "tests/"
      to: "helpertips/"
      via: "from helpertips.* import — resolvido pelo pip install -e ."
      pattern: "from helpertips\\."
---

<objective>
Reestruturar o projeto de arquivos soltos na raiz para um pacote Python `helpertips/` com `pyproject.toml`. Mover todos os modulos (`db.py`, `parser.py`, `store.py`, `listener.py`) para `helpertips/`, atualizar todos os imports internos e nos testes, e configurar `pip install -e .` para desenvolvimento.

Purpose: Estrutura de pacote (D-06, D-07, D-08, D-09) resolve imports automaticamente via pip install -e, elimina hacks de sys.path, e prepara o projeto para crescer na Fase 2 (dashboard como modulo separado).

Output: Pacote `helpertips/` funcional com pyproject.toml, todos os testes passando com imports via pacote.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-04-SUMMARY.md

<interfaces>
<!-- Arquivos atuais na raiz (criados por plans 01-04): -->
```
db.py          -> helpertips/db.py
parser.py      -> helpertips/parser.py
store.py       -> helpertips/store.py
listener.py    -> helpertips/listener.py
```

<!-- Imports atuais em listener.py: -->
```python
from db import validate_config, get_connection, ensure_schema
from parser import parse_message
from store import upsert_signal, get_stats
```

<!-- Imports atuais nos testes: -->
```python
# test_parser.py
from parser import parse_message

# test_store.py
from db import get_connection, ensure_schema
from store import upsert_signal, get_stats

# test_config.py
from db import validate_config
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar pyproject.toml e estrutura do pacote helpertips/</name>
  <files>pyproject.toml, helpertips/__init__.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (decisoes D-06, D-07, D-08, D-09)
    - requirements.txt
  </read_first>
  <action>
1. Criar `pyproject.toml` com o seguinte conteudo exato:

```toml
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "helpertips"
version = "0.1.0"
description = "Captura sinais de futebol virtual do Telegram e armazena no PostgreSQL"
requires-python = ">=3.12"
dependencies = [
    "telethon~=1.42",
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
```

2. Criar diretorio `helpertips/` e arquivo `helpertips/__init__.py`:

```python
"""HelperTips — Captura de sinais de futebol virtual do Telegram."""

__version__ = "0.1.0"
```

3. Rodar `pip install -e ".[dev]"` para instalar o pacote em modo editavel com dependencias de dev.

4. Verificar que `python3 -c "import helpertips; print(helpertips.__version__)"` imprime "0.1.0".
  </action>
  <verify>
    <automated>test -f pyproject.toml && test -f helpertips/__init__.py && python3 -c "import helpertips; print(helpertips.__version__)" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contem '[project]'
    - pyproject.toml contem 'name = "helpertips"'
    - pyproject.toml contem 'requires-python = ">=3.12"'
    - pyproject.toml contem 'telethon~=1.42'
    - pyproject.toml contem 'psycopg2-binary>=2.9'
    - pyproject.toml contem 'python-dotenv>=1.0'
    - pyproject.toml contem 'rich>=13.0'
    - pyproject.toml contem 'pytest>=7.0'
    - pyproject.toml contem '[tool.pytest.ini_options]'
    - pyproject.toml contem 'testpaths = ["tests"]'
    - helpertips/__init__.py contem '__version__ = "0.1.0"'
    - python3 -c "import helpertips" executa sem erro
  </acceptance_criteria>
  <done>pyproject.toml criado com todas as dependencias. Pacote helpertips instalavel via pip install -e .</done>
</task>

<task type="auto">
  <name>Task 2: Mover modulos para helpertips/ e atualizar todos os imports</name>
  <files>helpertips/db.py, helpertips/parser.py, helpertips/store.py, helpertips/listener.py, tests/test_parser.py, tests/test_store.py, tests/test_config.py</files>
  <read_first>
    - db.py
    - parser.py
    - store.py
    - listener.py
    - tests/test_parser.py
    - tests/test_store.py
    - tests/test_config.py
  </read_first>
  <action>
1. Mover os 4 modulos da raiz para `helpertips/` usando `git mv`:
   ```bash
   git mv db.py helpertips/db.py
   git mv parser.py helpertips/parser.py
   git mv store.py helpertips/store.py
   git mv listener.py helpertips/listener.py
   ```

2. Atualizar imports em `helpertips/listener.py` — trocar:
   - `from db import validate_config, get_connection, ensure_schema` -> `from helpertips.db import validate_config, get_connection, ensure_schema`
   - `from parser import parse_message` -> `from helpertips.parser import parse_message`
   - `from store import upsert_signal, get_stats` -> `from helpertips.store import upsert_signal, get_stats`

3. Atualizar imports em `helpertips/store.py` (se houver imports de db):
   - Verificar se store.py importa de db — se sim, atualizar para `from helpertips.db import ...`

4. Atualizar imports nos testes:
   - `tests/test_parser.py`: `from parser import parse_message` -> `from helpertips.parser import parse_message`
   - `tests/test_store.py`: `from db import get_connection, ensure_schema` -> `from helpertips.db import get_connection, ensure_schema` e `from store import upsert_signal, get_stats` -> `from helpertips.store import upsert_signal, get_stats`
   - `tests/test_config.py`: `from db import validate_config` -> `from helpertips.db import validate_config`

5. Remover `pytest.ini` da raiz (a configuracao agora esta em pyproject.toml `[tool.pytest.ini_options]`).

6. Manter `tests/` na raiz (conforme D-09), com `tests/__init__.py` e `tests/fixtures/` intactos.

7. Rodar `pip install -e ".[dev]"` novamente para atualizar paths.

8. Rodar `python3 -m pytest tests/ -v` para confirmar que todos os testes passam.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -v 2>&1 && test ! -f parser.py && test ! -f store.py && test ! -f db.py && test -f helpertips/parser.py && test -f helpertips/store.py && test -f helpertips/db.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo helpertips/parser.py existe e contem "def parse_message"
    - Arquivo helpertips/store.py existe e contem "def upsert_signal"
    - Arquivo helpertips/db.py existe e contem "def get_connection"
    - Arquivo helpertips/listener.py existe e contem "from helpertips.parser import parse_message"
    - Arquivo helpertips/listener.py contem "from helpertips.store import upsert_signal"
    - Arquivo helpertips/listener.py contem "from helpertips.db import validate_config"
    - tests/test_parser.py contem "from helpertips.parser import parse_message"
    - tests/test_store.py contem "from helpertips.store import"
    - tests/test_config.py contem "from helpertips.db import"
    - Nao existe parser.py na raiz
    - Nao existe store.py na raiz
    - Nao existe db.py na raiz
    - python3 -m pytest tests/ -v retorna exit code 0
  </acceptance_criteria>
  <done>Todos os modulos vivem em helpertips/. Imports atualizados em todos os arquivos. Testes passando via pip install -e . Nenhum .py avulso na raiz alem de scripts de entrada.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from helpertips.parser import parse_message; print('OK')"` — importa sem erro
2. `python3 -c "from helpertips.store import upsert_signal, get_stats; print('OK')"` — importa sem erro
3. `python3 -c "from helpertips.db import validate_config, get_connection, ensure_schema; print('OK')"` — importa sem erro
4. `python3 -m pytest tests/ -v` — todos os testes passam
5. `test ! -f parser.py && test ! -f store.py && test ! -f db.py` — nenhum modulo na raiz
6. `grep -r "from parser import\|from store import\|from db import" helpertips/ tests/` — retorna vazio (nenhum import antigo)
</verification>

<success_criteria>
- pyproject.toml existe com todas as dependencias e configuracao de pytest
- helpertips/ e um pacote Python valido (importavel)
- Todos os 4 modulos (db, parser, store, listener) estao em helpertips/
- Todos os imports internos usam prefixo "helpertips." (nao imports relativos sem prefixo)
- Todos os testes passam via python3 -m pytest tests/ -v
- pip install -e ".[dev]" funciona sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
