---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - helpertips/db.py
  - helpertips/store.py
  - helpertips/listener.py
  - tests/test_store.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Tabela parse_failures existe no PostgreSQL com colunas raw_text, received_at, failure_reason"
    - "Mensagens que o parser nao reconhece sao salvas em parse_failures com o motivo da falha"
    - "Taxa de cobertura (PARS-07) e calculada como parseados / (parseados + falhas) usando ambas as tabelas"
    - "Startup summary usa rich.table.Table com colunas formatadas e cores"
    - "Sinais capturados sao exibidos com cor verde (GREEN) ou vermelha (RED) no terminal"
    - "Logs do Telethon e modulos internos usam RichHandler integrado com logging stdlib"
  artifacts:
    - path: "helpertips/db.py"
      provides: "Schema atualizado com tabela parse_failures"
      contains: "CREATE TABLE IF NOT EXISTS parse_failures"
    - path: "helpertips/store.py"
      provides: "Funcao log_parse_failure para salvar falhas"
      exports: ["upsert_signal", "get_stats", "log_parse_failure"]
    - path: "helpertips/listener.py"
      provides: "Terminal com rich — Panel de startup, cores por resultado, RichHandler"
      contains: "from rich"
  key_links:
    - from: "helpertips/listener.py"
      to: "helpertips/store.py"
      via: "from helpertips.store import log_parse_failure"
      pattern: "log_parse_failure"
    - from: "helpertips/listener.py"
      to: "rich.console.Console"
      via: "Console para output formatado"
      pattern: "from rich"
    - from: "helpertips/db.py"
      to: "PostgreSQL parse_failures table"
      via: "CREATE TABLE IF NOT EXISTS parse_failures"
      pattern: "parse_failures"
---

<objective>
Adicionar tabela `parse_failures` para capturar mensagens nao parseadas (D-10 a D-13) e substituir output basico do terminal por `rich` library (D-03 a D-05). O startup summary vira um Panel/Table formatado, sinais aparecem com cores, e logs usam RichHandler.

Purpose: parse_failures e a "safety net" — se o formato do grupo mudar, nenhuma mensagem e perdida. Rich transforma o terminal de "wall of logs" para interface legivel e profissional.

Output: Schema com parse_failures, store com log_parse_failure(), listener com rich Panel/Table/RichHandler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-05-SUMMARY.md

<interfaces>
<!-- De helpertips/db.py (apos plan 05): -->
```python
def get_connection():
    """Retorna conexao psycopg2 usando vars DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD"""

def ensure_schema(conn):
    """Cria tabela signals + indexes se nao existem. Chama conn.commit()."""

def validate_config():
    """Verifica REQUIRED_VARS no os.environ. Levanta SystemExit(1) se alguma falta."""
```

<!-- De helpertips/store.py (apos plan 05): -->
```python
def upsert_signal(conn, data: dict) -> None:
    """INSERT ... ON CONFLICT (message_id) DO UPDATE. Faz commit apos cada escrita."""

def get_stats(conn) -> tuple:
    """Retorna (total, greens, reds, pending) da tabela signals."""
```

<!-- De helpertips/listener.py (apos plan 05): -->
```python
# Usa logging.basicConfig com format simples
# Usa print() para startup summary
# Contadores: parse_total, parse_success, parse_failures (in-memory)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabela parse_failures no schema e funcao log_parse_failure no store</name>
  <files>helpertips/db.py, helpertips/store.py, tests/test_store.py</files>
  <read_first>
    - helpertips/db.py
    - helpertips/store.py
    - tests/test_store.py
    - .planning/phases/01-foundation/01-CONTEXT.md (decisoes D-10, D-11, D-12, D-13)
  </read_first>
  <action>
1. Editar `helpertips/db.py` — na funcao `ensure_schema(conn)`, ADICIONAR apos o CREATE TABLE signals (nao remover nada existente):

```sql
CREATE TABLE IF NOT EXISTS parse_failures (
    id             SERIAL PRIMARY KEY,
    raw_text       TEXT NOT NULL,
    received_at    TIMESTAMPTZ DEFAULT NOW(),
    failure_reason TEXT
);

CREATE INDEX IF NOT EXISTS idx_parse_failures_received_at ON parse_failures(received_at);
```

A tabela parse_failures tem 3 colunas uteis (D-11):
- `raw_text TEXT NOT NULL` — texto original da mensagem que falhou
- `received_at TIMESTAMPTZ DEFAULT NOW()` — quando foi recebida
- `failure_reason TEXT` — motivo da falha (ex: "no_liga_match", "empty_text")

2. Editar `helpertips/store.py` — ADICIONAR funcao `log_parse_failure` (manter upsert_signal e get_stats intactos):

```python
def log_parse_failure(conn, raw_text: str, reason: str) -> None:
    """Salva mensagem que o parser nao conseguiu interpretar (D-10, D-11)."""
    with conn.cursor() as cur:
        cur.execute("""
            INSERT INTO parse_failures (raw_text, failure_reason)
            VALUES (%s, %s)
        """, (raw_text, reason))
    conn.commit()
```

3. Editar `helpertips/store.py` — ATUALIZAR funcao `get_stats` para incluir contagem de parse_failures (D-13):

Alterar o retorno de `get_stats` para incluir total_failures:
```python
def get_stats(conn) -> dict:
    """Retorna estatisticas de sinais e taxa de cobertura do parser."""
    with conn.cursor() as cur:
        cur.execute("""
            SELECT
                COUNT(*) AS total,
                COUNT(*) FILTER (WHERE resultado = 'GREEN') AS greens,
                COUNT(*) FILTER (WHERE resultado = 'RED')   AS reds,
                COUNT(*) FILTER (WHERE resultado IS NULL)   AS pending
            FROM signals
        """)
        total, greens, reds, pending = cur.fetchone()

        cur.execute("SELECT COUNT(*) FROM parse_failures")
        total_failures = cur.fetchone()[0]

    return {
        'total': total,
        'greens': greens,
        'reds': reds,
        'pending': pending,
        'parse_failures': total_failures,
        'coverage': (total / (total + total_failures) * 100) if (total + total_failures) > 0 else 100.0,
    }
```

ATENCAO: Essa mudanca de retorno (tuple -> dict) requer atualizar TODOS os consumidores de get_stats. No listener.py (Task 2), o startup summary sera atualizado. Nos testes test_store.py, os asserts tambem precisam ser atualizados.

4. Atualizar `tests/test_store.py`:
   - Adicionar `from helpertips.store import log_parse_failure` no import
   - Atualizar `test_get_stats_empty_table`: agora retorna dict `{'total': 0, 'greens': 0, 'reds': 0, 'pending': 0, 'parse_failures': 0, 'coverage': 100.0}`
   - Atualizar `test_get_stats_with_data`: verificar que retorna dict com chaves corretas
   - Adicionar `test_log_parse_failure`: chamar `log_parse_failure(conn, "texto estranho", "no_liga_match")`, verificar com SELECT que row existe em parse_failures com raw_text="texto estranho" e failure_reason="no_liga_match"
   - Adicionar `test_coverage_with_failures`: inserir 3 sinais + 1 parse_failure, verificar que coverage = 75.0 (3/(3+1)*100)
   - No fixture `db_conn`, adicionar cleanup de parse_failures: `conn.execute("DELETE FROM parse_failures")` apos o DELETE FROM signals

5. No fixture `db_conn` de test_store.py, adicionar `ensure_schema(conn)` para garantir que a tabela parse_failures tambem e criada.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_store.py -x -v 2>&1 || echo "NOTA: Testes de store requerem PostgreSQL — veja output para detalhes"</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/db.py contem "CREATE TABLE IF NOT EXISTS parse_failures"
    - helpertips/db.py contem "raw_text TEXT NOT NULL" (dentro do CREATE parse_failures)
    - helpertips/db.py contem "failure_reason TEXT"
    - helpertips/db.py contem "idx_parse_failures_received_at"
    - helpertips/store.py contem "def log_parse_failure(conn, raw_text"
    - helpertips/store.py contem "INSERT INTO parse_failures"
    - helpertips/store.py contem "'coverage'" (chave no dict de retorno de get_stats)
    - helpertips/store.py contem "SELECT COUNT(*) FROM parse_failures"
    - tests/test_store.py contem "def test_log_parse_failure"
    - tests/test_store.py contem "def test_coverage_with_failures"
    - tests/test_store.py contem "DELETE FROM parse_failures"
    - python3 -m pytest tests/test_store.py -x passa (quando DB disponivel)
  </acceptance_criteria>
  <done>Tabela parse_failures criada pelo ensure_schema. log_parse_failure salva falhas no banco. get_stats retorna dict com coverage calculado. Testes cobrem insert de falha e calculo de cobertura.</done>
</task>

<task type="auto">
  <name>Task 2: Rich terminal output — Panel de startup, RichHandler, cores por resultado</name>
  <files>helpertips/listener.py</files>
  <read_first>
    - helpertips/listener.py
    - helpertips/store.py (para ver o novo formato dict de get_stats)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisoes D-03, D-04, D-05)
  </read_first>
  <action>
1. Adicionar imports de `rich` no topo de `helpertips/listener.py`:

```python
from rich.console import Console
from rich.table import Table
from rich.panel import Panel
from rich.logging import RichHandler
```

2. Substituir `logging.basicConfig(...)` por configuracao com RichHandler (D-04):

```python
logging.basicConfig(
    level=logging.INFO,
    format="%(message)s",
    datefmt="[%X]",
    handlers=[RichHandler(rich_tracebacks=True, show_path=False)],
)
logger = logging.getLogger("helpertips")
```

Isso integra RichHandler com logging stdlib — logs do Telethon e modulos internos passam pelo mesmo handler formatado.

3. Criar instancia global de Console:
```python
console = Console()
```

4. Substituir a funcao `print_startup_summary(conn, group_title)` por versao com rich (D-03):

```python
def print_startup_summary(conn, group_title):
    stats = get_stats(conn)  # agora retorna dict

    table = Table(title="Estatisticas", show_header=True, header_style="bold cyan")
    table.add_column("Metrica", style="bold")
    table.add_column("Valor", justify="right")

    table.add_row("Total de sinais", str(stats['total']))
    table.add_row("[green]GREEN[/green]", str(stats['greens']))
    table.add_row("[red]RED[/red]", str(stats['reds']))
    table.add_row("Pendentes", str(stats['pending']))

    taxa = (stats['greens'] / stats['total'] * 100) if stats['total'] > 0 else 0.0
    table.add_row("Taxa de acerto", f"{taxa:.1f}%")
    table.add_row("Cobertura parser", f"{stats['coverage']:.1f}%")
    table.add_row("Falhas de parse", str(stats['parse_failures']))

    panel = Panel(
        table,
        title=f"[bold]HelperTips[/bold] - {group_title}",
        subtitle="Ctrl+C para encerrar",
        border_style="blue",
    )
    console.print(panel)
```

5. Atualizar o handler `handle_signal(event)` para usar rich ao logar sinais capturados (D-03, D-05):

Onde antes fazia `logger.info(f"Signal saved: ...")`, substituir por:
```python
# Apos upsert bem-sucedido:
resultado = parsed.get('resultado')
if resultado == 'GREEN':
    console.print(f"  [bold green]GREEN[/bold green] | {parsed['liga']} | {parsed['entrada']}")
elif resultado == 'RED':
    console.print(f"  [bold red]RED[/bold red] | {parsed['liga']} | {parsed['entrada']}")
else:
    console.print(f"  [yellow]NOVO[/yellow] | {parsed['liga']} | {parsed['entrada']}")
```

6. Atualizar o handler para usar `log_parse_failure` quando parse retorna None:

Onde antes fazia apenas `parse_failures += 1` e `logger.warning(...)`:
```python
if parsed is None:
    parse_fail_count += 1
    reason = "no_liga_match"  # parser retorna None quando nao encontra LIGA
    logger.warning(f"Parse falhou: {event.message.text[:80]}...")
    await asyncio.to_thread(log_parse_failure, conn, event.message.text, reason)
    return
```

Adicionar import: `from helpertips.store import upsert_signal, get_stats, log_parse_failure`

7. Atualizar o shutdown summary para usar console.print:
```python
# No bloco finally de main():
if parse_total > 0:
    coverage = (parse_success / parse_total * 100)
    console.print(f"\n[bold]Sessao encerrada[/bold] — cobertura: {coverage:.1f}% ({parse_success}/{parse_total})")
else:
    console.print("\n[bold]Sessao encerrada[/bold] — nenhuma mensagem processada")
```

8. Niveis de verbosidade (D-05):
   - INFO: startup summary, sinal capturado com sucesso
   - WARNING: falha de parse, desconexao temporaria
   - ERROR: falha de acesso ao grupo, erro de banco
  </action>
  <verify>
    <automated>python3 -c "from helpertips.listener import print_startup_summary; print('import OK')" && grep -q "from rich" helpertips/listener.py && grep -q "RichHandler" helpertips/listener.py && grep -q "Panel" helpertips/listener.py && grep -q "log_parse_failure" helpertips/listener.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/listener.py contem "from rich.console import Console"
    - helpertips/listener.py contem "from rich.table import Table"
    - helpertips/listener.py contem "from rich.panel import Panel"
    - helpertips/listener.py contem "from rich.logging import RichHandler"
    - helpertips/listener.py contem "RichHandler(rich_tracebacks=True"
    - helpertips/listener.py contem "console = Console()"
    - helpertips/listener.py contem "[bold green]GREEN[/bold green]" ou "[green]GREEN[/green]"
    - helpertips/listener.py contem "[bold red]RED[/bold red]" ou "[red]RED[/red]"
    - helpertips/listener.py contem "log_parse_failure" no import e no handler
    - helpertips/listener.py contem "stats['coverage']" ou "stats[" (usa dict de get_stats)
    - helpertips/listener.py contem "Panel(" (startup summary em Panel)
    - helpertips/listener.py contem "Table(" (estatisticas em Table)
    - helpertips/listener.py NAO contem "logging.basicConfig(level=logging.INFO, format='%(asctime)s" (formato antigo removido)
  </acceptance_criteria>
  <done>Terminal usa rich para toda saida. Startup summary e um Panel com Table de estatisticas. Sinais aparecem coloridos (verde/vermelho/amarelo). Logs usam RichHandler. Falhas de parse sao salvas no banco via log_parse_failure.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from helpertips.store import log_parse_failure; print('OK')"` — importa sem erro
2. `python3 -m pytest tests/test_store.py -x -v` — testes de store passam (incluindo novos testes de parse_failures)
3. `grep -q "CREATE TABLE IF NOT EXISTS parse_failures" helpertips/db.py` — tabela no schema
4. `grep -q "from rich" helpertips/listener.py` — rich importado
5. `grep -q "RichHandler" helpertips/listener.py` — logging com RichHandler
6. `grep -q "log_parse_failure" helpertips/listener.py` — falhas persistidas no banco
7. `python3 -m pytest tests/ -v` — suite completa passa
</verification>

<success_criteria>
- Tabela parse_failures existe no schema com raw_text, received_at, failure_reason
- log_parse_failure persiste mensagens nao parseadas no banco
- get_stats retorna dict incluindo parse_failures count e coverage %
- Startup summary usa rich Panel com Table formatada
- Sinais capturados aparecem com cores (GREEN=verde, RED=vermelho, NOVO=amarelo)
- Logging usa RichHandler integrado com stdlib logging
- Falhas de parse sao logadas como WARNING E salvas no banco
- Todos os testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
