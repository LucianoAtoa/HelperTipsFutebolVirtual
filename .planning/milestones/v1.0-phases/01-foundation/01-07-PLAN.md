---
phase: 01-foundation
plan: 07
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - tests/fixtures/sample_signals.txt
  - tests/conftest.py
  - tests/test_parser.py
  - helpertips/parser.py
autonomous: false
requirements: []

must_haves:
  truths:
    - "Fixtures contem mensagens reais copiadas do grupo {VIP} ExtremeTips"
    - "Regex do parser extrai liga, entrada, horario de mensagens reais (nao assumidas)"
    - "Todos os testes do parser passam com mensagens reais como input"
  artifacts:
    - path: "tests/fixtures/sample_signals.txt"
      provides: "Mensagens reais do grupo Telegram usadas como fixtures"
      min_lines: 20
    - path: "helpertips/parser.py"
      provides: "Parser com regex ajustado para formato real das mensagens"
      contains: "def parse_message"
  key_links:
    - from: "tests/test_parser.py"
      to: "tests/fixtures/sample_signals.txt"
      via: "Fixtures carregadas em conftest.py alimentam os testes"
      pattern: "sample_signal"
    - from: "helpertips/parser.py"
      to: "tests/fixtures/sample_signals.txt"
      via: "Regex patterns validados contra mensagens reais"
      pattern: "re\\.search"
---

<objective>
Substituir as fixtures placeholder do parser por mensagens reais do grupo {VIP} ExtremeTips (D-01, D-02). O usuario copia 10-20 mensagens reais, e o executor ajusta regex patterns e testes para o formato real.

Purpose: O parser so e confiavel se testado contra mensagens reais. Regex escrito contra formato assumido pode ter 100% de falha no primeiro uso real (Pitfall 6 do RESEARCH.md). Esta e a validacao critica da Fase 1.

Output: Fixtures atualizadas com mensagens reais, parser ajustado, testes verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-05-SUMMARY.md

<interfaces>
<!-- De helpertips/parser.py (apos plan 05): -->
```python
def parse_message(text: str, message_id: int) -> dict | None:
    """Retorna dict com keys: message_id, liga, entrada, horario, periodo, dia_semana, resultado, placar, raw_text
    Ou None se mensagem nao e um sinal reconhecivel."""
```

<!-- Regex patterns atuais (provavelmente placeholder): -->
```python
LIGA_PATTERN = r'(?:football_emoji)?LIGA\s*:\s*(.+?)(?:\n|$)'
ENTRADA_PATTERN = r'(?:dart_emoji)?Entrada\s*:\s*(.+?)(?:\n|$)'
HORARIO_PATTERN = r'(?:clock_emoji)?Hor[aa]rio\s*:\s*(\d{1,2}:\d{2})(?:\n|$)'
RESULTADO_PATTERN = r'(?:check_emoji|x_emoji)\s*(GREEN|RED)'
PLACAR_PATTERN = r'Placar\s*:\s*(\d+-\d+)'
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Usuario copia mensagens reais do grupo Telegram</name>
  <files>tests/fixtures/sample_signals.txt</files>
  <action>
O usuario deve copiar 10-20 mensagens reais do grupo {VIP} ExtremeTips no Telegram e colar em tests/fixtures/sample_signals.txt. O executor NAO pode fazer isso — requer acesso manual ao Telegram do usuario.

Instrucoes para o usuario:
1. Abra o Telegram e acesse o grupo {VIP} ExtremeTips
2. Copie 10-20 mensagens de sinais recentes, incluindo:
   - Pelo menos 3 sinais NOVOS (sem resultado ainda)
   - Pelo menos 3 sinais com resultado GREEN
   - Pelo menos 3 sinais com resultado RED
   - Pelo menos 1 mensagem que NAO e sinal (bom dia, aviso, etc.)
3. Cole as mensagens no arquivo tests/fixtures/sample_signals.txt
4. Separe cada mensagem com uma linha contendo apenas "---" e um rotulo:
   --- NOVO (sem resultado)
   [mensagem real]
   ---
   --- GREEN
   [mensagem com resultado GREEN]
   ---
   --- RED
   [mensagem com resultado RED]
   ---
   --- NAO-SINAL
   [mensagem que nao e sinal]
   ---
  </action>
  <verify>
    <automated>test -f tests/fixtures/sample_signals.txt && wc -l tests/fixtures/sample_signals.txt | awk '{if ($1 >= 20) print "PASS"; else print "FAIL — precisa de pelo menos 20 linhas"}'</automated>
  </verify>
  <done>Arquivo tests/fixtures/sample_signals.txt contem pelo menos 10 mensagens reais do grupo, com pelo menos 20 linhas de conteudo.</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar regex do parser e testes para formato real das mensagens</name>
  <files>helpertips/parser.py, tests/conftest.py, tests/test_parser.py</files>
  <read_first>
    - tests/fixtures/sample_signals.txt (mensagens reais — source of truth)
    - helpertips/parser.py (regex atuais para comparar)
    - tests/conftest.py (fixtures atuais)
    - tests/test_parser.py (testes atuais)
  </read_first>
  <action>
1. Ler `tests/fixtures/sample_signals.txt` com atencao — identificar:
   - Qual o formato real da linha de liga (ex: "LIGA:", "Liga:", emojis usados)
   - Qual o formato real da entrada (ex: "Entrada:", "ENTRADA:", "Gol:", "Over/Under")
   - Qual o formato real do horario (ex: "Horario:", "HORA:", formato HH:MM ou HH:MM:SS)
   - Como o resultado aparece (ex: "GREEN", "GREEEN", emoji check, "VITORIA")
   - Como o placar aparece (ex: "Placar: 2-1", "2x1", "2 x 1")
   - Caracteres especiais, emojis, espacos extras, quebras de linha

2. Atualizar `helpertips/parser.py`:
   - Ajustar LIGA_PATTERN para o formato real observado
   - Ajustar ENTRADA_PATTERN para o formato real
   - Ajustar HORARIO_PATTERN para o formato real
   - Ajustar RESULTADO_PATTERN para o formato real
   - Ajustar PLACAR_PATTERN para o formato real
   - Se o formato for muito diferente do assumido, reescrever a logica de parse_message mantendo a mesma assinatura (text, message_id) -> dict | None

3. Atualizar `tests/conftest.py`:
   - Carregar mensagens reais de sample_signals.txt
   - Parsear o arquivo separando por "---" delimiter
   - Criar fixtures nomeadas: signal_new_real, signal_green_real, signal_red_real, non_signal_real
   - Manter fixtures antigas como fallback (renomear para signal_new_assumed, etc.)

4. Atualizar `tests/test_parser.py`:
   - Usar as novas fixtures reais nos testes existentes
   - Ajustar valores esperados (liga, entrada, horario) para os valores reais das mensagens
   - Manter todos os test cases: PARS-01 a PARS-07, LIST-04
   - Adicionar testes para edge cases descobertos nas mensagens reais

5. Rodar `python3 -m pytest tests/test_parser.py -x -v` e iterar ate todos passarem.

IMPORTANTE: A assinatura de parse_message NAO deve mudar — `(text: str, message_id: int) -> dict | None`. O dict retornado deve ter as mesmas chaves: message_id, liga, entrada, horario, periodo, dia_semana, resultado, placar, raw_text. Isso garante compatibilidade com store.py e listener.py.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_parser.py -x -v && python3 -m pytest tests/ -v && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/sample_signals.txt contem pelo menos 10 mensagens reais (mais de 20 linhas)
    - helpertips/parser.py contem regex patterns atualizados (diferentes dos placeholder originais, a menos que o formato real coincida)
    - tests/conftest.py carrega mensagens de sample_signals.txt
    - tests/test_parser.py usa fixtures baseadas em mensagens reais
    - python3 -m pytest tests/test_parser.py -x -v retorna exit code 0
    - python3 -m pytest tests/ -v retorna exit code 0 (suite completa)
    - helpertips/parser.py mantem assinatura "def parse_message(text: str, message_id: int) -> dict | None"
  </acceptance_criteria>
  <done>Parser validado contra mensagens reais do grupo. Regex ajustado para formato real. Todos os testes passam com dados reais. Assinatura e contrato do parser preservados.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_parser.py -x -v` — todos os testes do parser passam com mensagens reais
2. `python3 -m pytest tests/ -v` — suite completa passa
3. `wc -l tests/fixtures/sample_signals.txt` — pelo menos 20 linhas (mensagens reais)
4. `python3 -c "from helpertips.parser import parse_message; print(type(parse_message('', 1)))"` — retorna None (contrato preservado)
</verification>

<success_criteria>
- Arquivo sample_signals.txt contem mensagens REAIS do grupo {VIP} ExtremeTips (nao inventadas)
- Parser regex ajustado para o formato real observado nas mensagens
- Todos os testes do parser passam com fixtures reais
- Assinatura de parse_message inalterada: (text, message_id) -> dict | None
- Chaves do dict retornado inalteradas: message_id, liga, entrada, horario, periodo, dia_semana, resultado, placar, raw_text
- Suite completa de testes passa
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
