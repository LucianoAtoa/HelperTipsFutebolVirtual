---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - requirements.txt
  - db.py
  - pytest.ini
  - tests/__init__.py
  - tests/test_config.py
autonomous: true
requirements:
  - OPER-02
  - OPER-03
  - DB-01

must_haves:
  truths:
    - "*.session and .env are listed in .gitignore before any code is committed"
    - "Missing .env variable causes a clean exit with a descriptive error message"
    - "Running ensure_schema() creates the signals table with all required columns and indexes"
    - "requirements.txt contains pinned versions of telethon, psycopg2-binary, python-dotenv, pytest"
  artifacts:
    - path: ".gitignore"
      provides: "Security — prevents .session and .env from being committed"
      contains: "*.session"
    - path: ".env.example"
      provides: "Template for required environment variables"
      contains: "TELEGRAM_API_ID"
    - path: "requirements.txt"
      provides: "Pinned Python dependencies"
      contains: "telethon"
    - path: "db.py"
      provides: "Database connection factory and schema creation"
      exports: ["get_connection", "ensure_schema"]
    - path: "pytest.ini"
      provides: "Test runner configuration"
      contains: "testpaths"
    - path: "tests/test_config.py"
      provides: "Unit tests for config validation"
      contains: "test_missing_var"
  key_links:
    - from: "db.py"
      to: "PostgreSQL"
      via: "psycopg2.connect using os.environ vars"
      pattern: "psycopg2\\.connect"
    - from: "db.py"
      to: "signals table"
      via: "CREATE TABLE IF NOT EXISTS signals"
      pattern: "CREATE TABLE IF NOT EXISTS signals"

user_setup:
  - service: telegram
    why: "Telethon requires API credentials to connect as a user client"
    env_vars:
      - name: TELEGRAM_API_ID
        source: "https://my.telegram.org -> API Development Tools -> App api_id"
      - name: TELEGRAM_API_HASH
        source: "https://my.telegram.org -> API Development Tools -> App api_hash"
      - name: TELEGRAM_GROUP_ID
        source: "Discovered at runtime via Telethon get_entity() — plan 04 handles this"
  - service: postgresql
    why: "Database storage for signals"
    env_vars:
      - name: DB_HOST
        source: "PostgreSQL server host (default: localhost)"
      - name: DB_PORT
        source: "PostgreSQL server port (default: 5432)"
      - name: DB_NAME
        source: "Database name — plan creates 'helpertips' database"
      - name: DB_USER
        source: "Database user — plan creates 'helpertips_user'"
      - name: DB_PASSWORD
        source: "Password for the database user — user chooses"
---

<objective>
Bootstrap the project infrastructure: .gitignore with security entries, .env configuration with startup validation, PostgreSQL schema for the signals table, dependency installation, and test infrastructure.

Purpose: Every subsequent plan depends on having dependencies installed, the database schema ready, and configuration validated. This is the foundation that makes the rest of Phase 1 possible.

Output: Working .gitignore, .env.example, requirements.txt, db.py (connection + schema), pytest.ini, and passing config validation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project bootstrap — .gitignore, .env.example, requirements.txt, and dependency installation</name>
  <files>.gitignore, .env.example, requirements.txt</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (section: .gitignore Minimum Content, section: Installation, section: Pattern 4)
  </read_first>
  <action>
1. Create `.gitignore` with these exact entries:
```
# Telegram session — contains full account auth state, NEVER commit
*.session
*.session-journal

# Credentials
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
```

2. Create `.env.example` with these exact entries (values empty, comments explain where to get each):
```
# Telegram API credentials — get from https://my.telegram.org
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_GROUP_ID=

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=helpertips
DB_USER=helpertips_user
DB_PASSWORD=
```

3. Install dependencies and generate requirements.txt:
```bash
pip3 install "telethon~=1.42" --upgrade "psycopg2-binary>=2.9" "python-dotenv>=1.0" pytest
pip3 freeze > requirements.txt
```

4. Verify requirements.txt contains all expected packages.
  </action>
  <verify>
    <automated>test -f .gitignore && grep -q "*.session" .gitignore && grep -q ".env" .gitignore && test -f .env.example && grep -q "TELEGRAM_API_ID" .env.example && test -f requirements.txt && grep -qi "telethon" requirements.txt && grep -qi "psycopg2" requirements.txt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains "*.session" line
    - .gitignore contains "*.session-journal" line
    - .gitignore contains ".env" line
    - .gitignore contains "__pycache__/" line
    - .env.example contains "TELEGRAM_API_ID="
    - .env.example contains "TELEGRAM_API_HASH="
    - .env.example contains "TELEGRAM_GROUP_ID="
    - .env.example contains "DB_HOST=localhost"
    - .env.example contains "DB_NAME=helpertips"
    - requirements.txt contains "Telethon" (case-insensitive)
    - requirements.txt contains "psycopg2-binary"
    - requirements.txt contains "python-dotenv"
    - requirements.txt contains "pytest"
  </acceptance_criteria>
  <done>All three files exist with correct content. Dependencies installed. `pip3 show telethon` shows version >=1.42.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Config validation module and db.py with schema creation</name>
  <files>db.py, tests/__init__.py, tests/test_config.py, pytest.ini</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (section: Pattern 4 .env Configuration, section: psycopg2 Connection Factory, section: Recommended Database Schema)
    - .env.example
  </read_first>
  <behavior>
    - test_missing_var_raises: When any of TELEGRAM_API_ID, TELEGRAM_API_HASH, TELEGRAM_GROUP_ID, DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD is missing from env, validate_config() raises SystemExit
    - test_all_vars_present_passes: When all required vars are set, validate_config() returns without error
    - test_empty_var_raises: When a required var is set to empty string "", validate_config() raises SystemExit
  </behavior>
  <action>
1. Create `pytest.ini`:
```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
```

2. Create `tests/__init__.py` (empty file).

3. Create `tests/test_config.py` with tests for `validate_config()` from `db.py`:
   - Use `monkeypatch.delenv` to remove each required var one at a time
   - Assert `SystemExit` is raised via `pytest.raises(SystemExit)`
   - Use `monkeypatch.setenv` to set all vars and verify no exception
   - Test that empty string value also triggers SystemExit

4. Create `db.py` with:
   - `REQUIRED_VARS` list: `['TELEGRAM_API_ID', 'TELEGRAM_API_HASH', 'TELEGRAM_GROUP_ID', 'DB_HOST', 'DB_PORT', 'DB_NAME', 'DB_USER', 'DB_PASSWORD']`
   - `validate_config()` function: checks each var via `os.environ.get(k)`, collects missing/empty, prints descriptive error listing all missing vars, raises `SystemExit(1)` if any missing
   - `get_connection()` function: returns `psycopg2.connect(host=os.environ['DB_HOST'], port=os.environ['DB_PORT'], dbname=os.environ['DB_NAME'], user=os.environ['DB_USER'], password=os.environ['DB_PASSWORD'])`
   - `ensure_schema(conn)` function: executes CREATE TABLE IF NOT EXISTS signals with columns:
     - `id SERIAL PRIMARY KEY`
     - `message_id BIGINT UNIQUE NOT NULL`
     - `liga TEXT`
     - `entrada TEXT`
     - `horario TEXT`
     - `periodo TEXT`
     - `dia_semana SMALLINT`
     - `resultado TEXT`
     - `placar TEXT`
     - `raw_text TEXT NOT NULL`
     - `received_at TIMESTAMPTZ DEFAULT NOW()`
     - `updated_at TIMESTAMPTZ DEFAULT NOW()`
   - Also creates indexes: `idx_signals_liga`, `idx_signals_entrada`, `idx_signals_resultado`, `idx_signals_received_at`
   - Calls `conn.commit()` after schema creation

5. Add `import os` and `from dotenv import load_dotenv` at top of db.py. Call `load_dotenv()` at module level.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - db.py contains "def validate_config():"
    - db.py contains "def get_connection():"
    - db.py contains "def ensure_schema(conn):"
    - db.py contains "REQUIRED_VARS" list with all 8 variable names
    - db.py contains "psycopg2.connect("
    - db.py contains "CREATE TABLE IF NOT EXISTS signals"
    - db.py contains "message_id BIGINT UNIQUE NOT NULL"
    - db.py contains "raw_text TEXT NOT NULL"
    - db.py contains "CREATE INDEX IF NOT EXISTS idx_signals_liga"
    - db.py contains "CREATE INDEX IF NOT EXISTS idx_signals_entrada"
    - db.py contains "CREATE INDEX IF NOT EXISTS idx_signals_resultado"
    - db.py contains "CREATE INDEX IF NOT EXISTS idx_signals_received_at"
    - db.py contains "conn.commit()"
    - db.py contains "load_dotenv()"
    - tests/test_config.py contains "def test_missing_var"
    - tests/test_config.py contains "pytest.raises(SystemExit)"
    - pytest.ini contains "testpaths = tests"
    - python3 -m pytest tests/test_config.py -x exits with code 0
  </acceptance_criteria>
  <done>db.py exports validate_config, get_connection, ensure_schema. All config tests pass. Schema SQL matches the exact column definitions from the research document.</done>
</task>

</tasks>

<verification>
1. `git ls-files --others --exclude-standard | grep -c .session` returns 0 (session files excluded)
2. `python3 -m pytest tests/test_config.py -x -v` — all tests pass
3. `python3 -c "from db import validate_config, get_connection, ensure_schema; print('imports OK')"` — prints "imports OK"
4. `grep -c "CREATE TABLE IF NOT EXISTS signals" db.py` returns 1
</verification>

<success_criteria>
- .gitignore blocks .session, .session-journal, and .env from git tracking
- .env.example documents all 8 required environment variables with sensible defaults for DB_HOST and DB_PORT
- requirements.txt has pinned versions of telethon>=1.42, psycopg2-binary, python-dotenv, pytest
- db.py has validate_config(), get_connection(), ensure_schema() — all importable
- ensure_schema() creates signals table with all 11 columns and 4 indexes
- Config validation tests pass: missing var raises SystemExit, all vars present succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
