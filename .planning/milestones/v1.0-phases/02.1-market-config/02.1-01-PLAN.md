---
phase: 02.1-market-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpertips/db.py
  - tests/test_db.py
autonomous: true
requirements:
  - MKT-01
must_haves:
  truths:
    - "ensure_schema() cria tabelas mercados e complementares no PostgreSQL"
    - "Seed data de 2 mercados + 14 complementares e inserido de forma idempotente"
    - "Reiniciar o listener ou dashboard nao duplica seed data"
  artifacts:
    - path: "helpertips/db.py"
      provides: "CREATE TABLE mercados + complementares + seed INSERT ON CONFLICT DO NOTHING"
      contains: "CREATE TABLE IF NOT EXISTS mercados"
    - path: "tests/test_db.py"
      provides: "Testes de integracao para novas tabelas"
      contains: "test_ensure_schema_mercados"
  key_links:
    - from: "helpertips/db.py"
      to: "PostgreSQL"
      via: "ensure_schema() CREATE TABLE + INSERT"
      pattern: "CREATE TABLE IF NOT EXISTS mercados"
---

<objective>
Estender ensure_schema() em db.py para criar as tabelas `mercados` e `complementares` com seed data dos 2 mercados principais e suas 14 complementares (7 cada).

Purpose: Fundacao de dados para toda a logica de complementares — sem as tabelas, nenhum calculo ou card funciona.
Output: Tabelas mercados + complementares criadas e populadas no PostgreSQL via ensure_schema().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-market-config/02.1-CONTEXT.md
@.planning/phases/02.1-market-config/02.1-RESEARCH.md

<interfaces>
<!-- Existing ensure_schema() pattern from helpertips/db.py -->
From helpertips/db.py:
```python
def ensure_schema(conn):
    """
    Create the signals table and required indexes if they do not exist.
    Commits the transaction after schema creation.
    """
    with conn.cursor() as cur:
        cur.execute("""
            CREATE TABLE IF NOT EXISTS signals ( ... );
            CREATE TABLE IF NOT EXISTS parse_failures ( ... );
        """)
    conn.commit()
```

From tests/test_queries.py (DB fixture pattern):
```python
@pytest.fixture
def db_conn():
    conn = get_connection()
    ensure_schema(conn)
    with conn.cursor() as cur:
        cur.execute("DELETE FROM signals")
    conn.commit()
    yield conn
    with conn.cursor() as cur:
        cur.execute("DELETE FROM signals")
    conn.commit()
    conn.close()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar testes de integracao para tabelas mercados e complementares</name>
  <files>tests/test_db.py</files>
  <read_first>
    - helpertips/db.py (ensure_schema atual)
    - tests/test_queries.py (padrao db_conn fixture)
    - tests/conftest.py (fixtures compartilhadas)
  </read_first>
  <behavior>
    - test_ensure_schema_mercados: apos ensure_schema(), tabela mercados existe com colunas id, slug, nome_display, odd_ref, ativo
    - test_ensure_schema_complementares: apos ensure_schema(), tabela complementares existe com colunas id, mercado_id, slug, nome_display, percentual, odd_ref, regra_validacao
    - test_seed_mercados_over_2_5: apos ensure_schema(), mercados contem registro com slug='over_2_5', nome_display='Over 2.5', odd_ref=2.30
    - test_seed_mercados_ambas_marcam: apos ensure_schema(), mercados contem registro com slug='ambas_marcam', nome_display='Ambas Marcam', odd_ref=2.10
    - test_seed_complementares_over_2_5_count: apos ensure_schema(), ha exatamente 7 complementares para mercado over_2_5
    - test_seed_complementares_ambas_marcam_count: apos ensure_schema(), ha exatamente 7 complementares para mercado ambas_marcam
    - test_seed_idempotent: chamar ensure_schema() duas vezes nao duplica registros (contagem permanece 2 mercados, 14 complementares)
    - test_seed_percentual_fracao: complementar 'over_3_5' de over_2_5 tem percentual=0.20 (nao 20)
  </behavior>
  <action>
    Criar arquivo tests/test_db.py com testes de integracao. Usar o mesmo padrao de fixture db_conn de test_queries.py (get_connection + ensure_schema + cleanup). Os testes devem:

    1. Verificar existencia das tabelas via `SELECT * FROM information_schema.columns WHERE table_name = 'mercados'`
    2. Verificar seed data com `SELECT slug, nome_display, odd_ref FROM mercados ORDER BY slug`
    3. Verificar contagem de complementares: `SELECT COUNT(*) FROM complementares c JOIN mercados m ON c.mercado_id = m.id WHERE m.slug = %s`
    4. Verificar idempotencia: chamar ensure_schema() novamente e recontar
    5. Verificar percentual como fracao: `SELECT percentual FROM complementares WHERE slug = 'over_3_5' AND mercado_id = (SELECT id FROM mercados WHERE slug = 'over_2_5')` deve retornar Decimal('0.2000') ou similar

    Fixture deve fazer cleanup com `DELETE FROM complementares; DELETE FROM mercados;` no teardown (nessa ordem por FK).

    IMPORTANTE: Os testes vao falhar inicialmente (RED) porque as tabelas ainda nao existem em ensure_schema().
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_db.py -x -q 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_db.py contem `def test_ensure_schema_mercados`
    - tests/test_db.py contem `def test_seed_mercados_over_2_5`
    - tests/test_db.py contem `def test_seed_complementares_over_2_5_count`
    - tests/test_db.py contem `def test_seed_idempotent`
    - tests/test_db.py contem `def test_seed_percentual_fracao`
    - tests/test_db.py contem `DELETE FROM complementares` no teardown
  </acceptance_criteria>
  <done>Arquivo tests/test_db.py criado com 8 testes. Testes falham porque ensure_schema() ainda nao cria as tabelas (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Estender ensure_schema() com CREATE TABLE mercados + complementares + seed data</name>
  <files>helpertips/db.py</files>
  <read_first>
    - helpertips/db.py (ensure_schema atual — adicionar ao bloco cur.execute existente)
    - .planning/phases/02.1-market-config/02.1-RESEARCH.md (Pattern 1 — SQL exato)
    - /Users/luciano/Downloads/documentacao_entradas_mercado.md (tabela de percentuais secao 5)
  </read_first>
  <action>
    Adicionar ao bloco `cur.execute(""" ... """)` existente em ensure_schema(), APOS as tabelas signals e parse_failures:

    1. CREATE TABLE IF NOT EXISTS mercados:
       - id SERIAL PRIMARY KEY
       - slug TEXT UNIQUE NOT NULL
       - nome_display TEXT NOT NULL
       - odd_ref NUMERIC(6,2) NOT NULL
       - ativo BOOLEAN NOT NULL DEFAULT TRUE

    2. CREATE TABLE IF NOT EXISTS complementares:
       - id SERIAL PRIMARY KEY
       - mercado_id INTEGER NOT NULL REFERENCES mercados(id)
       - slug TEXT NOT NULL
       - nome_display TEXT NOT NULL
       - percentual NUMERIC(5,4) NOT NULL
       - odd_ref NUMERIC(8,2) NOT NULL
       - regra_validacao TEXT NOT NULL
       - UNIQUE (mercado_id, slug)

    3. Seed mercados principais (ON CONFLICT (slug) DO NOTHING):
       - ('over_2_5', 'Over 2.5', 2.30)
       - ('ambas_marcam', 'Ambas Marcam', 2.10)

    4. Seed complementares Over 2.5 (ON CONFLICT (mercado_id, slug) DO NOTHING):
       - ('over_3_5', 'Over 3.5', 0.20, 4.00, 'over_3_5')
       - ('empate_3_3_4_4', 'Empate 3-3 / 4-4', 0.01, 30.00, 'empate_3_3_4_4')
       - ('over_5_plus', 'Over 5+', 0.10, 15.00, 'over_5_plus')
       - ('gols_casa_4', 'Total Gols Casa = 4', 0.01, 25.00, 'gols_casa_4')
       - ('gols_fora_4', 'Total Gols Fora = 4', 0.01, 25.00, 'gols_fora_4')
       - ('gols_casa_5_plus', 'Total Gols Casa 5+', 0.01, 40.00, 'gols_casa_5_plus')
       - ('gols_fora_5_plus', 'Total Gols Fora 5+', 0.01, 40.00, 'gols_fora_5_plus')

    5. Seed complementares Ambas Marcam (mesmos slugs, percentuais diferentes):
       - ('over_3_5', 'Over 3.5', 0.10, 4.00, 'over_3_5')
       - ('empate_3_3_4_4', 'Empate 3-3 / 4-4', 0.01, 30.00, 'empate_3_3_4_4')
       - ('over_5_plus', 'Over 5+', 0.05, 15.00, 'over_5_plus')
       - ('gols_casa_4', 'Total Gols Casa = 4', 0.01, 25.00, 'gols_casa_4')
       - ('gols_fora_4', 'Total Gols Fora = 4', 0.01, 25.00, 'gols_fora_4')
       - ('gols_casa_5_plus', 'Total Gols Casa 5+', 0.01, 40.00, 'gols_casa_5_plus')
       - ('gols_fora_5_plus', 'Total Gols Fora 5+', 0.01, 40.00, 'gols_fora_5_plus')

    Usar o padrao de INSERT ... SELECT do RESEARCH.md Pattern 1: JOIN com mercados para pegar mercado_id, evitando hardcode de IDs.

    ATENCAO: Percentuais sao fracoes decimais (0.20, nao 20). Per D-21 e Pitfall 6 do RESEARCH.md.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_db.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/db.py contem `CREATE TABLE IF NOT EXISTS mercados`
    - helpertips/db.py contem `CREATE TABLE IF NOT EXISTS complementares`
    - helpertips/db.py contem `ON CONFLICT (slug) DO NOTHING` (mercados)
    - helpertips/db.py contem `ON CONFLICT (mercado_id, slug) DO NOTHING` (complementares)
    - helpertips/db.py contem `0.20` (percentual Over 3.5 do Over 2.5, nao 20)
    - helpertips/db.py contem `0.10` (percentual Over 3.5 do Ambas Marcam)
    - helpertips/db.py contem `0.05` (percentual Over 5+ do Ambas Marcam)
    - Todos os testes em tests/test_db.py passam (GREEN)
  </acceptance_criteria>
  <done>ensure_schema() cria tabelas mercados + complementares com seed de 2 mercados e 14 complementares. Testes de integracao passam. Seed idempotente.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_db.py -x -q` — todos os testes passam
- `python3 -m pytest tests/ -x -q` — suite completa nao quebrou
- `psql` query manual: `SELECT COUNT(*) FROM mercados` retorna 2, `SELECT COUNT(*) FROM complementares` retorna 14
</verification>

<success_criteria>
- Tabelas mercados e complementares existem no PostgreSQL apos ensure_schema()
- 2 mercados principais e 14 complementares populados como seed
- Seed e idempotente (ON CONFLICT DO NOTHING)
- Percentuais armazenados como fracao decimal (0.20, nao 20)
- Suite de testes existente continua passando
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-market-config/02.1-01-SUMMARY.md`
</output>
