---
phase: 02.1-market-config
plan: "04"
subsystem: ui
tags: [dash, dash-bootstrap-components, plotly, roi, complementares, dashboard]

# Dependency graph
requires:
  - phase: 02.1-03
    provides: get_complementares_config() e calculate_roi_complementares() em queries.py
provides:
  - Card ROI Complementares no dashboard com tabela de breakdown por mercado (7 complementares)
  - Linha Total Combinado (principal + complementares) per D-17
  - Coloracao dinamica de lucros (text-success/text-danger/text-muted)
  - Estado vazio tratado com dbc.Alert "Selecione uma entrada"
  - _entrada_para_slug() helper para mapear dropdown para slug de mercado
  - _build_comp_table() helper para renderizar dbc.Table com totais
affects: [phase-03, dashboard-extensoes-futuras]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Card ROI Complementares segue exatamente o padrao do card Simulacao de ROI existente"
    - "_entrada_para_slug mapeia nome de dropdown para slug de mercado — unico ponto de manutencao para novos mercados"
    - "Output de callback expandido: novos Outputs adicionados ao final da lista de Outputs do master callback"
    - "ROI Total Combinado calculado inline no callback usando roi['profit'] + comp['profit']"

key-files:
  created: []
  modified:
    - helpertips/dashboard.py

key-decisions:
  - "Card ROI Complementares inserido no layout apos card Simulacao de ROI e antes de Charts Row — sequencia logica: principal > complementares > graficos"
  - "Output roi-comp-container adicionado como ultimo Output do master callback — minimiza diff no callback existente"
  - "ROI Total Combinado calculado inline no callback (nao em queries.py) — dado calculado sob-demanda, nao precisa de funcao separada"
  - "_build_comp_table como helper de modulo (nao funcao interna do callback) — reutilizavel e testavel de forma independente"
  - "dbc.Alert com color='info' para estado vazio (sem entrada) e 'warning' para sem sinais no periodo — distingue os dois estados vazios"

patterns-established:
  - "Callback expandido: novos Outputs adicionados ao final, return como tupla com ordem correspondente"
  - "Helpers de layout (_build_comp_table) definidos no nivel de modulo, antes do layout"
  - "Coloracao dinamica de valores monetarios: usar expressoes ternarias inline nas f-strings de className"

requirements-completed:
  - MKT-06
  - MKT-07

# Metrics
duration: 79min
completed: 2026-04-03
---

# Phase 02.1 Plan 04: Market Config Dashboard Integration Summary

**Card ROI Complementares com tabela de breakdown por mercado e linha Total Combinado (principal + complementares) integrado ao master callback do dashboard Dash**

## Performance

- **Duration:** 79 min (inclui leitura de contexto e verificacao)
- **Started:** 2026-04-03T16:12:26Z
- **Completed:** 2026-04-03T16:34:49Z
- **Tasks:** 1 auto + 1 checkpoint (auto-aprovado)
- **Files modified:** 1

## Accomplishments

- Card ROI Complementares adicionado ao layout do dashboard com `id="roi-comp-container"`
- Master callback expandido com `Output("roi-comp-container", "children")` e logica completa de ROI complementares
- Linha Total Combinado calcula e exibe lucro/ROI/investido somando principal + complementares (decisao D-17)
- Coloracao dinamica: `text-success` para positivo, `text-danger` para negativo, `text-info` para ROI% zero
- Estado vazio tratado: `dbc.Alert color="info"` quando sem filtro de entrada selecionado
- 106 testes passando sem modificacao

## Task Commits

Cada task commitada atomicamente:

1. **Task 1: Adicionar card ROI Complementares ao layout e expandir master callback** - `1f507a2` (feat)
2. **Task 2: Verificacao visual** - auto-aprovada (auto_advance: true)

**Metadados do plano:** [hash do commit de docs — gerado apos]

## Files Created/Modified

- `helpertips/dashboard.py` - Imports expandidos, helpers _entrada_para_slug/_build_comp_table adicionados, card ROI Complementares no layout, master callback com Output adicional e logica de ROI complementares + Total Combinado

## Decisions Made

- Card ROI Complementares inserido apos card Simulacao de ROI e antes de Charts Row — sequencia logica de exibicao
- Output `roi-comp-container` adicionado como ultimo Output do master callback para minimizar diff
- ROI Total Combinado calculado inline no callback usando `roi["profit"] + comp_profit` — dado calculado sob-demanda, nao merece funcao separada em queries.py
- `_build_comp_table` como helper de modulo (nao funcao interna) — reutilizavel e testavel independentemente
- `dbc.Alert color="info"` para estado vazio sem entrada e `color="warning"` para sem sinais no periodo — distingue os dois estados vazios semanticamente

## Deviations from Plan

Nenhum — plano executado exatamente como escrito.

## Issues Encountered

Nenhum — implementacao direta seguindo o plano e o contrato da UI-SPEC.

## Known Stubs

Nenhum stub identificado. O card ROI Complementares busca dados reais do banco via `get_complementares_config()` e `calculate_roi_complementares()`. O estado "sem sinais" (`total_invested == 0`) exibe alerta informativo, nao dados ficticiosos.

## Next Phase Readiness

- Phase 02.1 completa: schema (plan 01), validacao (plan 02), queries (plan 03), dashboard (plan 04)
- Dashboard funcional com ROI principal e ROI complementares integrados
- Proximo passo: Phase 03 (analise avancada) pode usar as funcoes de queries.py estabelecidas

---
*Phase: 02.1-market-config*
*Completed: 2026-04-03*
