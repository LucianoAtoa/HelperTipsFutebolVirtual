---
phase: 02.1-market-config
plan: 01
subsystem: database
tags: [postgres, psycopg2, schema, seed, tdd]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: ensure_schema() pattern, get_connection(), psycopg2 setup
provides:
  - Tabela mercados com 2 registros seed (over_2_5, ambas_marcam)
  - Tabela complementares com 14 registros seed (7 por mercado)
  - ensure_schema() idempotente para mercados e complementares
  - Testes de integração cobrindo schema + seed + idempotência + percentual fracionário
affects: [02.1-market-config-plan-02, 02.1-market-config-plan-03, 02.1-market-config-plan-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Seed data em SQL puro dentro de ensure_schema() com ON CONFLICT DO NOTHING — padrão idempotente"
    - "INSERT ... SELECT com VALUES() AS subquery para seed de FK sem hardcode de IDs"
    - "regra_validacao armazenada como enum-string para dispatch dict em Python (sem eval)"

key-files:
  created:
    - tests/test_db.py
  modified:
    - helpertips/db.py

key-decisions:
  - "Seed data via INSERT ... SELECT FROM mercados com subquery VALUES — evita hardcode de IDs sequenciais"
  - "Percentuais como NUMERIC(5,4) fração decimal (0.20, não 20) — fórmula multiplica diretamente sem divisão"
  - "regra_validacao como TEXT enum-string (over_3_5, empate_3_3_4_4 etc) — mapeado para lambdas em queries.py"
  - "ensure_schema() quebrado em múltiplas cur.execute() separadas para complementares Over 2.5 e Ambas Marcam"

patterns-established:
  - "Pattern: seed data de tabelas com FK usa INSERT ... SELECT m.id FROM mercados m WHERE m.slug = X"
  - "Pattern: fixture db_conn em test_db.py faz DELETE FROM complementares; DELETE FROM mercados no teardown (ordem FK)"

requirements-completed: [MKT-01]

# Metrics
duration: 8min
completed: 2026-04-03
---

# Phase 02.1 Plan 01: Schema Mercados e Complementares — Summary

**Tabelas mercados e complementares criadas via ensure_schema() com seed idempotente de 2 mercados e 14 complementares, percentuais como fração decimal NUMERIC(5,4)**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-03T15:06:45Z
- **Completed:** 2026-04-03T15:14:32Z
- **Tasks:** 2 (TDD: RED + GREEN)
- **Files modified:** 2

## Accomplishments

- Testes de integração criados (8 testes) cobrindo estrutura de tabelas, seed data, idempotência e tipo de percentual
- ensure_schema() estendido com CREATE TABLE mercados + complementares usando CREATE TABLE IF NOT EXISTS
- Seed data de 2 mercados (Over 2.5 odd 2.30, Ambas Marcam odd 2.10) com ON CONFLICT (slug) DO NOTHING
- Seed data de 14 complementares (7 por mercado) com percentuais como fração decimal (0.20, 0.10, 0.05, 0.01)
- Todos os 8 testes passam; suite existente (50 testes) inalterada

## Task Commits

Cada task foi commitada atomicamente:

1. **Task 1 (TDD RED): Criar testes de integração** - `d748ad8` (test)
2. **Task 2 (TDD GREEN): Estender ensure_schema()** - `efb0178` (feat)

**Metadados do plano:** (a seguir)

_Nota: TDD — commit test (RED) antes do feat (GREEN)_

## Files Created/Modified

- `/Users/luciano/helpertips/tests/test_db.py` — 8 testes de integração para tabelas mercados e complementares
- `/Users/luciano/helpertips/helpertips/db.py` — ensure_schema() estendido com CREATE TABLE + seed data

## Decisions Made

- **INSERT ... SELECT com subquery VALUES**: Para inserir complementares com FK para mercados sem hardcode de IDs. Padrão do RESEARCH.md Pattern 1.
- **ensure_schema() com múltiplas cur.execute()**: A instrução de seed das complementares Over 2.5 e Ambas Marcam ficou em chamadas separadas (não em um único bloco SQL) para clareza e manutenibilidade.
- **Percentuais NUMERIC(5,4)**: Armazenados como fração decimal (0.20, não 20) conforme D-21 e Pitfall 6 do RESEARCH.md. Fórmula de stake multiplica diretamente: `stake_principal * percentual`.
- **regra_validacao como TEXT enum-string**: Valores como 'over_3_5', 'empate_3_3_4_4' — mapeados para lambdas em queries.py (plano 02). Nunca eval().

## Deviations from Plan

Nenhum — plano executado exatamente como especificado.

## Issues Encountered

- Teste pré-existente `test_parse_placar_valid` em tests/test_queries.py falha com NameError (função `_parse_placar` não importada). Verificado que é falha pré-existente de outro agente paralelo (plano 02). Fora do escopo deste plano — não causado pelas alterações deste plano.

## User Setup Required

Nenhum — PostgreSQL já configurado via .env. ensure_schema() roda automaticamente no próximo start do listener ou dashboard.

## Next Phase Readiness

- Tabelas mercados e complementares prontas para uso no plano 02 (queries.py: get_complementares_config, validar_complementar, calculate_roi_complementares)
- Seed data populado e idempotente — restarts do listener/dashboard não duplicam dados
- Nenhum bloqueador

---
*Phase: 02.1-market-config*
*Completed: 2026-04-03*
