---
phase: 05-github-publication
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified: []
autonomous: false
requirements:
  - GH-01
  - GH-02

user_setup:
  - service: github
    why: "Confirmar publicacao do repositorio como publico"
    dashboard_config:
      - task: "Confirmar que deseja tornar o repo publico (contem codigo de sistema de apostas pessoal)"
        location: "Terminal — confirmacao verbal"

must_haves:
  truths:
    - "Repositorio esta publico no GitHub"
    - ".gitignore bloqueia *.session, .env, __pycache__, *.pyc"
    - "Push para main dispara GitHub Actions que roda lint e testes"
    - "CI passa verde (lint + testes)"
  artifacts:
    - path: "GitHub: LucianoAtoa/HelperTipsFutebolVirtual"
      provides: "Repositorio publico com codigo completo"
      contains: "visibility: PUBLIC"
  key_links:
    - from: "git local main"
      to: "GitHub remote main"
      via: "git push -u origin main"
      pattern: "origin.*HelperTipsFutebolVirtual"
    - from: ".github/workflows/ci.yml"
      to: "GitHub Actions runner"
      via: "push trigger no workflow"
      pattern: "conclusion.*success"
---

<objective>
Publicar o repositorio no GitHub e verificar que o CI funciona. Configurar remote, tornar publico (com confirmacao do usuario), fazer push de main, e validar que GitHub Actions passa verde.

Purpose: Satisfazer GH-01 (repo publico com .gitignore correto) e GH-02 (CI automatizado validando qualidade a cada push).
Output: Repositorio publico em github.com/LucianoAtoa/HelperTipsFutebolVirtual com CI verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-github-publication/05-RESEARCH.md
@.planning/phases/05-github-publication/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirmar publicacao do repositorio como publico</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (confirmar que *.session, .env, __pycache__, *.py[cod] estao bloqueados)
  </read_first>
  <action>
Antes de perguntar ao usuario, executar duas verificacoes de seguranca:

1. Confirmar .gitignore bloqueia patterns sensiveis:
```bash
git check-ignore -v helpertips.session .env __pycache__ dummy.pyc
```
Todos devem retornar match.

2. Confirmar que nenhum secret esta no historico git:
```bash
git log --all --full-diff -p -- .env '*.session' | head -5
```
Deve retornar vazio (ja auditado na Phase 4).

3. Apresentar ao usuario a decisao: tornar o repositorio LucianoAtoa/HelperTipsFutebolVirtual publico ou manter privado.
  </action>
  <decision>Tornar o repositorio LucianoAtoa/HelperTipsFutebolVirtual publico no GitHub</decision>
  <context>
GH-01 requer "repositorio publico". O repo atual existe no GitHub mas esta PRIVADO e vazio (sem branch padrao).

Antes de tornar publico, o executor deve verificar:
1. `git check-ignore -v helpertips.session .env __pycache__ dummy.pyc` — todos os patterns bloqueados
2. `git log --all --full-diff -p -- .env '*.session'` — nenhum secret no historico (ja auditado na Phase 4)

O repositorio contem codigo de um sistema pessoal de apostas em futebol virtual. Nao ha secrets no historico (limpo na Phase 4).
  </context>
  <options>
    <option id="public">
      <name>Tornar publico (recomendado)</name>
      <pros>Satisfaz GH-01 como especificado; permite CI gratuito no free tier; portfolio publico</pros>
      <cons>Codigo de apostas visivel publicamente</cons>
    </option>
    <option id="private">
      <name>Manter privado</name>
      <pros>Privacidade do sistema de apostas</pros>
      <cons>GH-01 nao sera satisfeito como escrito; GitHub Actions free tier tem limite de minutos para repos privados (2000 min/mes)</cons>
    </option>
  </options>
  <resume-signal>Responda "publico" ou "privado"</resume-signal>
  <verify>
    <automated>cd /Users/luciano/helpertips && git check-ignore -v helpertips.session .env __pycache__ dummy.pyc</automated>
  </verify>
  <acceptance_criteria>
    - Usuario confirmou a decisao de visibilidade do repositorio
    - `git check-ignore -v helpertips.session` retorna match
    - `git check-ignore -v .env` retorna match
  </acceptance_criteria>
  <done>Usuario decidiu a visibilidade do repositorio (publico ou privado)</done>
</task>

<task type="auto">
  <name>Task 2: Publicar repositorio no GitHub e verificar CI</name>
  <files>Nenhum arquivo local modificado — operacoes de git remote</files>
  <read_first>
    - .gitignore (confirmar patterns de seguranca)
    - .github/workflows/ci.yml (confirmar que existe da Plan 01)
    - README.md (confirmar que badge foi adicionado na Plan 01)
  </read_first>
  <action>
Executar na sequencia:

1. Verificar que nao ha remote configurado:
```bash
git remote -v
```
Se vazio, adicionar:
```bash
git remote add origin https://github.com/LucianoAtoa/HelperTipsFutebolVirtual.git
```

2. Se o usuario escolheu "publico" na Task 1, tornar o repo publico:
```bash
gh repo edit LucianoAtoa/HelperTipsFutebolVirtual --visibility public
```
Se escolheu "privado", pular este passo.

3. Push de main para o remote:
```bash
git push -u origin main
```

4. Verificar que o push disparou o CI — aguardar o workflow completar:
```bash
gh run list --limit 3
```
Esperar ate 3 minutos pelo resultado. Verificar com:
```bash
gh run list --limit 1 --json status,conclusion
```

5. Se CI falhar:
   - Verificar logs: `gh run view --log-failed`
   - Corrigir localmente, commit, push novamente
   - Causas comuns: dependencia faltando, violacao de lint nao corrigida

6. Verificar visibilidade final:
```bash
gh repo view LucianoAtoa/HelperTipsFutebolVirtual --json visibility -q '.visibility'
```
Deve retornar "PUBLIC" (se usuario confirmou publico) ou "PRIVATE".

7. Verificar .gitignore funciona:
```bash
git check-ignore -v helpertips.session .env __pycache__ dummy.pyc
```
Todos devem retornar match.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && gh repo view LucianoAtoa/HelperTipsFutebolVirtual --json visibility -q '.visibility' && gh run list --limit 1 --json conclusion -q '.[0].conclusion' && git check-ignore -v helpertips.session .env __pycache__ dummy.pyc</automated>
  </verify>
  <acceptance_criteria>
    - `git remote -v` mostra origin apontando para `LucianoAtoa/HelperTipsFutebolVirtual`
    - `gh repo view --json visibility -q '.visibility'` retorna "PUBLIC" (se confirmado) ou "PRIVATE"
    - `gh run list --limit 1 --json conclusion -q '.[0].conclusion'` retorna "success"
    - `git check-ignore -v helpertips.session` retorna match com `*.session`
    - `git check-ignore -v .env` retorna match com `.env`
    - `git check-ignore -v __pycache__` retorna match com `__pycache__/`
    - `git check-ignore -v dummy.pyc` retorna match com `*.py[cod]`
    - Badge no README aponta para URL correta do workflow (verificado na Plan 01)
  </acceptance_criteria>
  <done>Repositorio publicado no GitHub com CI verde. .gitignore bloqueia todos os patterns de seguranca. GH-01 e GH-02 satisfeitos.</done>
</task>

</tasks>

<verification>
1. `gh repo view LucianoAtoa/HelperTipsFutebolVirtual --json visibility` — visibilidade correta
2. `gh run list --limit 1 --json conclusion` — CI conclusion = success
3. `git check-ignore -v helpertips.session .env __pycache__ dummy.pyc` — todos bloqueados
4. `git remote -v` — origin configurado para o repo correto
5. Acessar `https://github.com/LucianoAtoa/HelperTipsFutebolVirtual/actions` — workflow visivel
</verification>

<success_criteria>
Repositorio acessivel no GitHub com visibilidade confirmada pelo usuario. GitHub Actions CI passou verde no primeiro (ou segundo) push. .gitignore bloqueia *.session, .env, __pycache__ e *.pyc. GH-01 e GH-02 completos.
</success_criteria>

<output>
After completion, create `.planning/phases/05-github-publication/05-02-SUMMARY.md`
</output>
