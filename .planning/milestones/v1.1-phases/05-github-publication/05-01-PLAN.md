---
phase: 05-github-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
  - README.md
  - "src/**/*.py (lint fixes via ruff --fix)"
autonomous: true
requirements:
  - GH-02

must_haves:
  truths:
    - "ruff check . executa sem erros no codigo existente"
    - "ci.yml existe com job de lint + pytest"
    - "Badge de CI visivel no README (substitui placeholder)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow com ruff lint + pytest"
      contains: "ruff check"
    - path: "pyproject.toml"
      provides: "Configuracao ruff lint"
      contains: "[tool.ruff]"
    - path: "README.md"
      provides: "Badge de status do CI"
      contains: "actions/workflows/ci.yml/badge.svg"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "ruff usa config do pyproject.toml"
      pattern: "ruff check"
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "badge URL referencia o nome do workflow file"
      pattern: "ci.yml/badge.svg"
---

<objective>
Preparar todos os arquivos locais para CI: configurar ruff no pyproject.toml, corrigir violacoes de lint no codigo existente, criar o workflow GitHub Actions, e adicionar badge de status no README.

Purpose: O CI precisa passar no primeiro push. Lint fixes e workflow devem estar prontos ANTES de publicar no GitHub.
Output: pyproject.toml com [tool.ruff], .github/workflows/ci.yml, README.md com badge, codigo lintado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-github-publication/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configurar ruff e corrigir lint do codigo existente</name>
  <files>pyproject.toml, helpertips/*.py, tests/*.py</files>
  <read_first>
    - pyproject.toml (estado atual — adicionar secao [tool.ruff] sem alterar o resto)
    - helpertips/*.py (arquivos que serao lintados)
    - tests/*.py (arquivos que serao lintados)
  </read_first>
  <action>
1. Adicionar a seguinte configuracao ao final do pyproject.toml (DEPOIS da secao [tool.pytest.ini_options] existente):

```toml
[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = []
```

Regras: E/W = pycodestyle, F = pyflakes, I = isort. Line-length 100 para evitar falsos positivos em linhas de dashboard Dash que tendem a ser mais longas.

2. Instalar ruff localmente (nao esta instalado):
```bash
pip install ruff
```

3. Rodar ruff com auto-fix para corrigir violacoes automaticamente:
```bash
ruff check --fix .
```

4. Verificar se restam violacoes manuais:
```bash
ruff check .
```

5. Se houver violacoes restantes apos --fix, corrigir manualmente. As mais comuns serao:
   - F841: variavel atribuida mas nao usada — remover ou prefixar com _
   - E501: linha muito longa apos 100 chars — quebrar a linha
   - F401: import nao utilizado — remover

6. Commit das correcoes de lint junto com a config ruff.

NAO alterar as secoes existentes do pyproject.toml ([build-system], [project], [project.optional-dependencies], [tool.pytest.ini_options]).
NAO usar flake8 ou isort separados — ruff substitui ambos.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && ruff check . && echo "LINT OK"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contem `[tool.ruff]` com `target-version = "py312"`
    - pyproject.toml contem `[tool.ruff.lint]` com `select = ["E", "F", "W", "I"]`
    - pyproject.toml contem `line-length = 100`
    - `ruff check .` retorna exit code 0 (zero violacoes)
    - `python -m pytest tests/ -q` continua passando (lint fixes nao quebraram nada)
    - Secoes existentes do pyproject.toml permanecem inalteradas
  </acceptance_criteria>
  <done>ruff configurado no pyproject.toml, zero violacoes de lint no codigo, testes continuam passando</done>
</task>

<task type="auto">
  <name>Task 2: Criar workflow CI e adicionar badge ao README</name>
  <files>.github/workflows/ci.yml, README.md</files>
  <read_first>
    - README.md (localizar placeholder do badge na linha 5: "<!-- CI badge sera adicionado na Phase 5 -->")
    - pyproject.toml (confirmar que [tool.ruff] ja foi adicionado pela Task 1)
  </read_first>
  <action>
1. Criar o diretorio e arquivo `.github/workflows/ci.yml` com o seguinte conteudo EXATO:

```yaml
name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install ruff pytest
          pip install -e .

      - name: Lint com ruff
        run: ruff check .

      - name: Testes com pytest
        run: pytest --tb=short -q
```

Detalhes:
- Trigger: push e pull_request para branch main
- Runner: ubuntu-latest
- Python: 3.12 (match do projeto)
- Cache: pip (reduz tempo de CI em runs subsequentes)
- Instala ruff e pytest separados (nao depende de [project.optional-dependencies])
- `pip install -e .` instala o projeto e suas dependencias de producao
- Testes de DB (test_db.py) vao ser skipped automaticamente — NAO precisa configurar PostgreSQL no CI
- NAO adicionar secrets do Telegram — testes nao fazem conexao real

2. No README.md, substituir a linha 5 (placeholder):
```
<!-- CI badge sera adicionado na Phase 5 -->
```
por:
```
[![CI](https://github.com/LucianoAtoa/HelperTipsFutebolVirtual/actions/workflows/ci.yml/badge.svg)](https://github.com/LucianoAtoa/HelperTipsFutebolVirtual/actions/workflows/ci.yml)
```

A URL do badge DEVE conter:
- Owner: `LucianoAtoa`
- Repo: `HelperTipsFutebolVirtual`
- Workflow file: `ci.yml` (match exato do nome do arquivo)

3. Commit: ci.yml + README.md badge update.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && test -f .github/workflows/ci.yml && grep "ruff check" .github/workflows/ci.yml && grep "badge.svg" README.md && echo "CI FILES OK"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.github/workflows/ci.yml` existe
    - ci.yml contem `name: CI`
    - ci.yml contem `actions/checkout@v4`
    - ci.yml contem `actions/setup-python@v5`
    - ci.yml contem `python-version: "3.12"`
    - ci.yml contem `cache: "pip"`
    - ci.yml contem `ruff check .`
    - ci.yml contem `pytest --tb=short -q`
    - ci.yml contem `pip install -e .`
    - README.md contem `actions/workflows/ci.yml/badge.svg`
    - README.md NAO contem `<!-- CI badge sera adicionado na Phase 5 -->`
    - `python -m pytest tests/ -q` continua passando
  </acceptance_criteria>
  <done>Workflow CI criado com lint + pytest, badge de status adicionado ao README substituindo o placeholder</done>
</task>

</tasks>

<verification>
1. `ruff check .` — zero violacoes (exit 0)
2. `python -m pytest tests/ -q` — todos os testes passam
3. `.github/workflows/ci.yml` existe com steps de lint e test
4. `grep 'badge.svg' README.md` — badge presente
5. `grep '\[tool.ruff\]' pyproject.toml` — config ruff presente
</verification>

<success_criteria>
Codigo lintado sem violacoes, workflow CI pronto para rodar, badge no README. Tudo preparado localmente para o push na Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/05-github-publication/05-01-SUMMARY.md`
</output>
