---
phase: 07-listener-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpertips/listener.py
  - deploy/05-setup-listener.sh
  - .env.example
  - tests/test_listener_logging.py
autonomous: false
requirements: [DEP-05, DEP-01]

must_haves:
  truths:
    - "listener.py usa RotatingFileHandler quando nao ha TTY (daemon/systemd)"
    - "listener.py usa RichHandler quando ha TTY (desenvolvimento local)"
    - "deploy/05-setup-listener.sh cria unit file systemd com Restart=on-failure e RestartSec=60"
    - "Script de notificacao de falha envia alerta via Telegram Bot API"
    - "logrotate configurado para /var/log/helpertips/listener.log"
    - "Servico systemd depende de network.target e postgresql.service"
    - ".env.example documenta variaveis de notificacao Telegram"
  artifacts:
    - path: "helpertips/listener.py"
      provides: "Logging condicional TTY vs daemon"
      contains: "sys.stdout.isatty()"
    - path: "deploy/05-setup-listener.sh"
      provides: "Setup completo do systemd service, logrotate e notificacao"
      contains: "helpertips-listener.service"
    - path: "tests/test_listener_logging.py"
      provides: "Testes unitarios para configure_logging()"
      contains: "test_"
    - path: ".env.example"
      provides: "Template com variaveis de notificacao"
      contains: "TELEGRAM_NOTIFY_TOKEN"
  key_links:
    - from: "deploy/05-setup-listener.sh"
      to: "/etc/systemd/system/helpertips-listener.service"
      via: "cat heredoc que cria o unit file"
      pattern: "helpertips-listener.service"
    - from: "helpertips/listener.py"
      to: "/var/log/helpertips/listener.log"
      via: "RotatingFileHandler quando not isatty()"
      pattern: "RotatingFileHandler"
    - from: "/etc/systemd/system/helpertips-listener.service"
      to: "helpertips-notify-failure@.service"
      via: "OnFailure= directive"
      pattern: "OnFailure="
---

<objective>
Preparar todo o codigo e scripts necessarios para o listener rodar como systemd service 24/7 na EC2.

Purpose: O listener precisa capturar sinais automaticamente sem intervencao manual, sobrevivendo a crashes e reboots. Esta e a razao de existir do HelperTips — perder sinais contradiz o core value.

Output: listener.py com logging condicional, deploy/05-setup-listener.sh completo (unit file + logrotate + notificacao), testes unitarios para logging, .env.example atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-listener-deployment/07-CONTEXT.md
@.planning/phases/07-listener-deployment/07-RESEARCH.md

@helpertips/listener.py
@deploy/01-provision-ec2.sh
@deploy/backup-helpertips.sh
@.env.example

<interfaces>
<!-- Codigo existente que sera modificado/referenciado -->

From helpertips/listener.py (linhas 24-31 — logging atual que sera substituido):
```python
logging.basicConfig(
    level=logging.INFO,
    format="%(message)s",
    datefmt="[%X]",
    handlers=[RichHandler(rich_tracebacks=True, show_path=False)],
)
logger = logging.getLogger("helpertips")
```

From helpertips/listener.py (linhas 10-11 — imports Rich que devem ser condicionais):
```python
from rich.console import Console
from rich.logging import RichHandler
```

From deploy/01-provision-ec2.sh (linhas 36-37 — diretorio de logs ja criado):
```bash
mkdir -p /var/log/helpertips
chown helpertips:helpertips /var/log/helpertips
```

From deploy/backup-helpertips.sh (linha 34 — path do .session):
```bash
SESSION_FILE="/home/helpertips/helpertips_listener.session"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Logging condicional TTY + testes unitarios</name>
  <files>helpertips/listener.py, tests/test_listener_logging.py</files>
  <read_first>
    - helpertips/listener.py (estado atual do logging, linhas 24-31)
    - tests/conftest.py (fixtures existentes)
    - pyproject.toml (configuracao pytest)
  </read_first>
  <behavior>
    - Test 1: Quando sys.stdout.isatty() retorna False, configure_logging() adiciona RotatingFileHandler ao root logger (nao RichHandler)
    - Test 2: Quando sys.stdout.isatty() retorna True, configure_logging() adiciona RichHandler ao root logger
    - Test 3: RotatingFileHandler aponta para "/var/log/helpertips/listener.log" com maxBytes=10485760 e backupCount=7
    - Test 4: Formato do RotatingFileHandler contem "%(asctime)s %(levelname)s %(name)s: %(message)s"
  </behavior>
  <action>
    **Passo 1 — Criar funcao `configure_logging()` em listener.py (per D-02):**

    Substituir o bloco de logging atual (linhas 24-31) por uma funcao `configure_logging()` que:

    ```python
    def configure_logging():
        """Configura logging: RichHandler em TTY, RotatingFileHandler em daemon."""
        root_logger = logging.getLogger()
        root_logger.setLevel(logging.INFO)
        # Limpar handlers existentes para evitar duplicacao
        root_logger.handlers.clear()

        if sys.stdout.isatty():
            from rich.logging import RichHandler
            handler = RichHandler(rich_tracebacks=True, show_path=False)
            handler.setLevel(logging.INFO)
            root_logger.addHandler(handler)
        else:
            from logging.handlers import RotatingFileHandler
            log_path = "/var/log/helpertips/listener.log"
            handler = RotatingFileHandler(
                log_path,
                maxBytes=10 * 1024 * 1024,  # 10MB
                backupCount=7,
                encoding="utf-8",
            )
            handler.setFormatter(logging.Formatter(
                "%(asctime)s %(levelname)s %(name)s: %(message)s",
                datefmt="%Y-%m-%d %H:%M:%S",
            ))
            handler.setLevel(logging.INFO)
            root_logger.addHandler(handler)
    ```

    Chamar `configure_logging()` no lugar do `logging.basicConfig(...)` atual. Mover `from rich.logging import RichHandler` da linha 11 para dentro do branch `if sys.stdout.isatty()` (import condicional — em daemon, rich nao e necessario para logging).

    Manter os outros imports Rich (`Console`, `Panel`, `Table`) no topo porque sao usados no `print_startup_summary` e `handle_signal` (output formatado). Porem, envolver os usos de `console.print()` com Rich markup em guard de TTY:
    - Em `print_startup_summary`: verificar `sys.stdout.isatty()` e, se False, usar `logger.info()` com texto plano em vez do Panel/Table
    - Em `handle_signal`: usar `logger.info()` com texto plano quando `not sys.stdout.isatty()`

    Adicionar `from logging.handlers import RotatingFileHandler` como import condicional dentro da funcao (nao no topo).

    **Passo 2 — Criar tests/test_listener_logging.py:**

    ```python
    """Testes para configure_logging() — logging condicional TTY vs daemon."""
    import logging
    from unittest.mock import patch

    from helpertips.listener import configure_logging

    def test_daemon_mode_uses_rotating_file_handler(tmp_path):
        """Sem TTY, deve usar RotatingFileHandler."""
        log_file = tmp_path / "listener.log"
        with patch("sys.stdout") as mock_stdout, \
             patch("helpertips.listener.configure_logging.__code__", ...):
            # Abordagem: mockar isatty e o path do log
            ...

    def test_tty_mode_uses_rich_handler():
        """Com TTY, deve usar RichHandler."""
        ...
    ```

    Usar `unittest.mock.patch` para:
    - Mockar `sys.stdout.isatty` retornando True ou False
    - Para o teste daemon: mockar o path do log para usar `tmp_path` (evitar escrever em /var/log real)
    - Verificar tipo do handler adicionado ao root logger apos chamar `configure_logging()`
    - Limpar handlers do root logger no teardown de cada teste

    **Importante:** O teste do RotatingFileHandler precisa de um path gravavel. Usar `monkeypatch` ou `patch` para substituir o path `/var/log/helpertips/listener.log` por `tmp_path / "listener.log"` durante o teste.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python -m pytest tests/test_listener_logging.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/listener.py contem `def configure_logging():`
    - helpertips/listener.py contem `sys.stdout.isatty()`
    - helpertips/listener.py contem `RotatingFileHandler`
    - helpertips/listener.py contem `maxBytes=10 * 1024 * 1024`
    - helpertips/listener.py contem `backupCount=7`
    - helpertips/listener.py NAO contem `logging.basicConfig(` como chamada top-level (substituido por configure_logging)
    - tests/test_listener_logging.py contem `test_daemon_mode` e `test_tty_mode`
    - `pytest tests/test_listener_logging.py -x` retorna exit code 0
    - `pytest tests/ -x -q` (suite completa) retorna exit code 0
  </acceptance_criteria>
  <done>configure_logging() detecta TTY e usa RichHandler (dev) ou RotatingFileHandler (daemon). 4 testes passando. Suite completa verde.</done>
</task>

<task type="auto">
  <name>Task 2: Script deploy/05-setup-listener.sh + .env.example</name>
  <files>deploy/05-setup-listener.sh, .env.example</files>
  <read_first>
    - deploy/01-provision-ec2.sh (padrao de scripts de deploy existentes)
    - deploy/04-setup-backup.sh (padrao mais recente, referencia para estilo)
    - deploy/backup-helpertips.sh (referencia path do .session)
    - .env.example (estado atual das variaveis)
    - .planning/phases/07-listener-deployment/07-RESEARCH.md (patterns systemd, logrotate, notificacao)
  </read_first>
  <action>
    **Passo 1 — Criar deploy/05-setup-listener.sh (per D-01, D-03, D-04):**

    Script bash com `set -euo pipefail` que cria 4 artefatos no sistema:

    **1a. Unit file principal** — `/etc/systemd/system/helpertips-listener.service`:
    ```ini
    [Unit]
    Description=HelperTips Telegram Listener
    After=network.target postgresql.service
    StartLimitBurst=5
    StartLimitIntervalSec=300
    OnFailure=helpertips-notify-failure@%n.service

    [Service]
    Type=simple
    User=helpertips
    Group=helpertips
    WorkingDirectory=/home/helpertips
    EnvironmentFile=/home/helpertips/.env
    ExecStart=/home/helpertips/venv/bin/python -m helpertips.listener
    Restart=on-failure
    RestartSec=60
    StandardOutput=null
    StandardError=journal
    NoNewPrivileges=true
    PrivateTmp=true

    [Install]
    WantedBy=multi-user.target
    ```

    Nota: `StandardOutput=null` per D-02 — logs vao para RotatingFileHandler, nao stdout. `StandardError=journal` preserva erros criticos no journald.

    CRITICO (per RESEARCH pitfall 3): `StartLimitBurst=5` e `StartLimitIntervalSec=300` DEVEM estar na secao `[Unit]`, NAO em `[Service]`. systemd ignora silenciosamente se colocados em `[Service]`.

    **1b. Unit file de notificacao** — `/etc/systemd/system/helpertips-notify-failure@.service`:
    ```ini
    [Unit]
    Description=HelperTips failure notifier for %i

    [Service]
    Type=oneshot
    User=helpertips
    EnvironmentFile=/home/helpertips/.env
    ExecStart=/usr/local/bin/helpertips-notify-failure.sh %i
    ```

    **1c. Script de notificacao** — `/usr/local/bin/helpertips-notify-failure.sh`:
    ```bash
    #!/bin/bash
    SERVICE_NAME="${1:-helpertips-listener}"
    BOT_TOKEN="${TELEGRAM_NOTIFY_TOKEN}"
    CHAT_ID="${TELEGRAM_NOTIFY_CHAT_ID}"

    if [[ -z "$BOT_TOKEN" || -z "$CHAT_ID" ]]; then
        echo "TELEGRAM_NOTIFY_TOKEN ou TELEGRAM_NOTIFY_CHAT_ID nao configurado" >&2
        exit 0
    fi

    HOSTNAME=$(hostname)
    MESSAGE="HelperTips ALERTA: servico ${SERVICE_NAME} falhou em ${HOSTNAME}. Verificar: sudo systemctl status ${SERVICE_NAME}"

    curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
        -d "chat_id=${CHAT_ID}" \
        -d "text=${MESSAGE}" \
        > /dev/null
    ```

    Chmod 755. Nota: exit 0 quando variaveis vazias — nao queremos que falha de notificacao cause loop.

    **1d. Logrotate** — `/etc/logrotate.d/helpertips`:
    ```
    /var/log/helpertips/listener.log {
        daily
        rotate 14
        compress
        delaycompress
        notifempty
        missingok
        create 0640 helpertips helpertips
    }
    ```

    **Passos finais do script:**
    - `systemctl daemon-reload`
    - NÃO habilitar nem iniciar o servico — apenas imprimir instrucoes
    - Imprimir mensagem: "Setup concluido. Proximo passo: autenticacao interativa via SSH (ver README-deploy.md)"
    - Imprimir: "Apos autenticacao: sudo systemctl enable --now helpertips-listener"

    **Passo 2 — Atualizar .env.example:**

    Adicionar ao final do arquivo, apos o bloco AWS:
    ```
    # Telegram Bot para alertas de falha do servico (opcional)
    # Criar bot via @BotFather, obter token e chat_id
    # TELEGRAM_NOTIFY_TOKEN=
    # TELEGRAM_NOTIFY_CHAT_ID=
    ```

    Comentados porque sao opcionais (per D-03 — script faz exit 0 se vazios).
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && bash -n deploy/05-setup-listener.sh && echo "Syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - deploy/05-setup-listener.sh existe e `bash -n` retorna exit code 0
    - deploy/05-setup-listener.sh contem `set -euo pipefail`
    - deploy/05-setup-listener.sh contem `helpertips-listener.service`
    - deploy/05-setup-listener.sh contem `StartLimitBurst=5` dentro de secao `[Unit]` (nao `[Service]`)
    - deploy/05-setup-listener.sh contem `StartLimitIntervalSec=300` dentro de secao `[Unit]`
    - deploy/05-setup-listener.sh contem `Restart=on-failure`
    - deploy/05-setup-listener.sh contem `RestartSec=60`
    - deploy/05-setup-listener.sh contem `OnFailure=helpertips-notify-failure@%n.service`
    - deploy/05-setup-listener.sh contem `EnvironmentFile=/home/helpertips/.env`
    - deploy/05-setup-listener.sh contem `ExecStart=/home/helpertips/venv/bin/python -m helpertips.listener`
    - deploy/05-setup-listener.sh contem `StandardOutput=null`
    - deploy/05-setup-listener.sh contem `helpertips-notify-failure@.service`
    - deploy/05-setup-listener.sh contem `helpertips-notify-failure.sh`
    - deploy/05-setup-listener.sh contem `/etc/logrotate.d/helpertips`
    - deploy/05-setup-listener.sh contem `systemctl daemon-reload`
    - deploy/05-setup-listener.sh NAO contem `systemctl enable` nem `systemctl start` (servico nao deve ser ativado antes da autenticacao)
    - .env.example contem `TELEGRAM_NOTIFY_TOKEN`
    - .env.example contem `TELEGRAM_NOTIFY_CHAT_ID`
    - `ruff check helpertips/listener.py` retorna exit code 0
    - `ruff check deploy/` nao falha (scripts bash, nao Python)
  </acceptance_criteria>
  <done>Script de deploy cria unit file systemd com Restart=on-failure/RestartSec=60, StartLimitBurst na secao [Unit], notificacao de falha via Telegram Bot, logrotate configurado. .env.example documenta variaveis de notificacao. Nenhum servico ativado automaticamente — requer autenticacao interativa primeiro.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Autenticacao interativa SSH + ativar systemd service</name>
  <files>N/A — acao humana na EC2, sem arquivos locais modificados</files>
  <action>
    Checkpoint de acao humana: autenticacao Telethon interativa via SSH e ativacao do systemd service na EC2.
    Codigo do listener com logging condicional (Task 1) e script de deploy completo (Task 2) prontos para execucao na EC2.
    O usuario deve seguir os passos detalhados em how-to-verify abaixo.
  </action>
  <what-built>
    Codigo do listener com logging condicional (Task 1) e script de deploy completo (Task 2) prontos para execucao na EC2.
  </what-built>
  <how-to-verify>
    **Passo A — Atualizar codigo na EC2:**
    ```bash
    ssh -i ~/.ssh/helpertips-key.pem ubuntu@32.194.158.134
    cd /home/helpertips/helpertips  # ou onde o repo esta clonado
    sudo -u helpertips git pull origin main
    sudo -u helpertips bash -c "cd /home/helpertips && source venv/bin/activate && pip install -e /home/helpertips/helpertips"
    ```

    **Passo B — Executar script de setup:**
    ```bash
    sudo bash deploy/05-setup-listener.sh
    ```
    Verificar output: "Setup concluido"

    **Passo C — Autenticacao interativa (per D-01, DEP-05):**
    ```bash
    sudo -u helpertips bash
    cd /home/helpertips
    source venv/bin/activate
    python -m helpertips.listener
    # Digitar phone number quando solicitado
    # Digitar codigo SMS/Telegram quando solicitado
    # Selecionar grupo {VIP} ExtremeTips quando listado
    # Confirmar que TELEGRAM_GROUP_ID foi salvo no .env
    # Aguardar pelo menos 1 sinal aparecer no terminal (GREEN/RED/NOVO)
    # Ctrl+C para encerrar
    ```

    **Passo D — Verificar .session e .env:**
    ```bash
    ls -la /home/helpertips/helpertips_listener.session
    # Deve existir com owner helpertips:helpertips
    grep TELEGRAM_GROUP_ID /home/helpertips/.env
    # Deve ter valor numerico preenchido
    ```

    **Passo E — Ativar servico systemd (per DEP-01):**
    ```bash
    exit  # voltar para usuario ubuntu
    sudo systemctl enable --now helpertips-listener
    sudo systemctl status helpertips-listener
    # Deve mostrar: active (running)
    ```

    **Passo F — Verificar sinal no banco (smoke test):**
    ```bash
    sudo -u helpertips psql -h localhost -U helpertips_user -d helpertips -c "SELECT COUNT(*) FROM signals;"
    # Deve retornar contagem > 0 (sinais capturados)
    ```

    **Passo G — Verificar logs:**
    ```bash
    sudo tail -5 /var/log/helpertips/listener.log
    # Deve mostrar linhas de log com formato: 2026-04-04 HH:MM:SS INFO helpertips: ...
    ```

    **Passo H (opcional) — Teste de reboot:**
    ```bash
    sudo reboot
    # Aguardar 2-3 minutos, reconectar via SSH
    sudo systemctl status helpertips-listener
    # Deve mostrar: active (running)
    ```
  </how-to-verify>
  <resume-signal>Digite "deployed" quando o servico estiver rodando, ou descreva problemas encontrados</resume-signal>
  <verify>
    <automated>echo "Checkpoint humano — verificacao manual na EC2 via SSH"</automated>
  </verify>
  <done>Servico helpertips-listener rodando como active (running) na EC2, .session gerado, sinais sendo capturados no banco.</done>
</task>

</tasks>

<verification>
**Verificacao automatizada (pre-deploy, local):**
- `pytest tests/test_listener_logging.py -x -v` — testes de logging passando
- `pytest tests/ -x -q` — suite completa verde
- `bash -n deploy/05-setup-listener.sh` — sintaxe valida
- `ruff check helpertips/` — sem violacoes de lint

**Verificacao no EC2 (pos-deploy, Task 3):**
- `systemctl status helpertips-listener` mostra `active (running)`
- `/var/log/helpertips/listener.log` contem entradas recentes
- `SELECT COUNT(*) FROM signals` retorna valor > 0
- `.session` existe em `/home/helpertips/` com permissoes corretas
</verification>

<success_criteria>
1. listener.py detecta TTY e usa handler de logging apropriado — testado automaticamente
2. deploy/05-setup-listener.sh cria todos os artefatos systemd sem erros de sintaxe
3. Servico helpertips-listener esta active (running) na EC2 apos autenticacao interativa
4. Sinais do Telegram estao sendo capturados no banco de dados da EC2
5. Logs aparecem em /var/log/helpertips/listener.log (nao no journald)
</success_criteria>

<output>
After completion, create `.planning/phases/07-listener-deployment/07-01-SUMMARY.md`
</output>
