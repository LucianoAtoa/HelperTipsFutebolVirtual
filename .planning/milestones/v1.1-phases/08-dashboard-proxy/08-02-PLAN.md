---
phase: 08-dashboard-proxy
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - deploy/07-setup-nginx.sh
  - .env.example
autonomous: false
requirements: [DEP-03, DEP-04]
must_haves:
  truths:
    - "Acessar http://<EC2-IP> sem credenciais retorna 401 Unauthorized"
    - "Acessar http://<EC2-IP> com credenciais corretas mostra o dashboard com dados reais"
    - "nginx reinicia automaticamente apos reboot do servidor"
  artifacts:
    - path: "deploy/07-setup-nginx.sh"
      provides: "Script idempotente para instalar nginx, configurar reverse proxy e HTTP Basic Auth"
      min_lines: 50
    - path: ".env.example"
      provides: "Template com DASHBOARD_USER e DASHBOARD_PASSWORD documentados"
      contains: "DASHBOARD_USER"
  key_links:
    - from: "deploy/07-setup-nginx.sh"
      to: "/etc/nginx/sites-available/helpertips"
      via: "nginx server block com proxy_pass e auth_basic"
      pattern: "proxy_pass.*127\\.0\\.0\\.1:8050"
    - from: "deploy/07-setup-nginx.sh"
      to: "/etc/nginx/.htpasswd"
      via: "htpasswd -cbB com credenciais do .env"
      pattern: "htpasswd.*-cbB"
---

<objective>
Configurar nginx como reverse proxy com HTTP Basic Auth para o dashboard gunicorn.

Purpose: DEP-03 (nginx reverse proxy porta 80 para localhost:8050) e DEP-04 (HTTP Basic Auth protege acesso publico). Apos esta task, acessar o IP da EC2 no browser pede usuario/senha antes de mostrar qualquer conteudo.
Output: deploy/07-setup-nginx.sh, .env.example atualizado com DASHBOARD_USER/DASHBOARD_PASSWORD
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-proxy/08-CONTEXT.md
@.planning/phases/08-dashboard-proxy/08-RESEARCH.md
@.planning/phases/08-dashboard-proxy/08-01-SUMMARY.md

@deploy/05-setup-listener.sh
@.env.example

<interfaces>
<!-- De 08-01: dashboard agora roda via gunicorn em 127.0.0.1:8050 -->
<!-- deploy/06-setup-dashboard.sh cria helpertips-dashboard.service -->

De .env.example (estado atual — precisa adicionar DASHBOARD_USER e DASHBOARD_PASSWORD):
```
# Dashboard
DASH_DEBUG=false
```

Nginx server block target (per D-02, D-03):
```nginx
server {
    listen 80;
    server_name _;
    auth_basic "HelperTips";
    auth_basic_user_file /etc/nginx/.htpasswd;
    location / {
        proxy_pass http://127.0.0.1:8050;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar deploy/07-setup-nginx.sh e atualizar .env.example</name>
  <files>deploy/07-setup-nginx.sh, .env.example</files>
  <read_first>deploy/05-setup-listener.sh, .env.example, .planning/phases/08-dashboard-proxy/08-RESEARCH.md</read_first>
  <action>
**Parte A — deploy/07-setup-nginx.sh** (per D-02, D-03, D-04):

Criar script seguindo padrao dos scripts 01-06. Conteudo:

1. Header: `#!/bin/bash`, `set -euo pipefail`, echo banner "=== HelperTips Nginx Setup ==="

2. Secao 1 — Instalar nginx e apache2-utils:
```bash
echo ">>> Instalando nginx e apache2-utils..."
apt install -y nginx apache2-utils
echo "nginx e apache2-utils instalados"
```

3. Secao 2 — Gerar .htpasswd a partir do .env (per D-03):
```bash
echo ">>> Gerando /etc/nginx/.htpasswd..."
source /home/helpertips/.env

if [[ -z "${DASHBOARD_USER:-}" || -z "${DASHBOARD_PASSWORD:-}" ]]; then
    echo "ERRO: DASHBOARD_USER e DASHBOARD_PASSWORD devem estar definidos em /home/helpertips/.env"
    exit 1
fi

htpasswd -cbB /etc/nginx/.htpasswd "$DASHBOARD_USER" "$DASHBOARD_PASSWORD"
chmod 640 /etc/nginx/.htpasswd
chown root:www-data /etc/nginx/.htpasswd
echo ".htpasswd gerado para usuario: $DASHBOARD_USER"
```

NOTA: Usar `www-data` como grupo (usuario nginx no Ubuntu 24.04), NAO `nginx` — pitfall documentado na pesquisa. Flags: `-c` cria arquivo, `-b` aceita senha no CLI (obrigatorio para scripting), `-B` usa bcrypt.

4. Secao 3 — Server block nginx `/etc/nginx/sites-available/helpertips` (per D-02):
```nginx
server {
    listen 80;
    server_name _;

    auth_basic "HelperTips";
    auth_basic_user_file /etc/nginx/.htpasswd;

    location / {
        proxy_pass         http://127.0.0.1:8050;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
    }
}
```
Headers: X-Forwarded-For, X-Real-IP, X-Forwarded-Proto, Host — todos per D-02. proxy_read_timeout 120s para igualar timeout gunicorn (evita 504 em callbacks lentos — pitfall 6 da pesquisa).

5. Secao 4 — Ativar site e desabilitar default:
```bash
rm -f /etc/nginx/sites-enabled/default
ln -sf /etc/nginx/sites-available/helpertips /etc/nginx/sites-enabled/helpertips
nginx -t && systemctl reload nginx
```

6. Secao 5 — Habilitar nginx no boot:
```bash
systemctl enable nginx
```

7. Secao 6 — Instrucoes finais com aviso sobre porta 8050:
```
echo "=== Nginx setup concluido ==="
echo ""
echo "Verificar:"
echo "  curl -I http://localhost/          # Deve retornar 401"
echo "  curl -u \$DASHBOARD_USER:\$DASHBOARD_PASSWORD http://localhost/ | head -5  # Deve retornar HTML Dash"
echo ""
echo "IMPORTANTE: Fechar porta 8050 no AWS Security Group (helpertips-sg)!"
echo "A porta 8050 foi aberta temporariamente na Phase 6."
echo "Com nginx ativo na porta 80, acesso direto a 8050 bypassa autenticacao."
echo "AWS Console -> EC2 -> Security Groups -> helpertips-sg -> Editar inbound rules -> Remover regra 8050"
```

Tornar executavel: `chmod +x deploy/07-setup-nginx.sh`

**Parte B — .env.example:**

Adicionar ao final da secao `# Dashboard` (apos a linha `DASH_DEBUG=false`):
```
DASHBOARD_USER=
DASHBOARD_PASSWORD=
```

NAO adicionar valores padrao — sao credenciais que o usuario define no .env do servidor.
  </action>
  <verify>
    <automated>bash -n /Users/luciano/helpertips/deploy/07-setup-nginx.sh && echo "SYNTAX OK" || echo "SYNTAX ERROR"</automated>
  </verify>
  <acceptance_criteria>
    - `test -x deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "proxy_pass.*127.0.0.1:8050" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "auth_basic" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "auth_basic_user_file" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "htpasswd -cbB" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "www-data" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "proxy_read_timeout 120" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "X-Forwarded-For" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "X-Real-IP" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "sites-enabled/default" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "8050.*Security Group" deploy/07-setup-nginx.sh` retorna 0
    - `grep -q "DASHBOARD_USER" .env.example` retorna 0
    - `grep -q "DASHBOARD_PASSWORD" .env.example` retorna 0
    - `bash -n deploy/07-setup-nginx.sh` passa sem erro
  </acceptance_criteria>
  <done>Script deploy/07-setup-nginx.sh criado com nginx reverse proxy (porta 80 -> 127.0.0.1:8050), HTTP Basic Auth via .htpasswd com bcrypt, aviso para fechar porta 8050. .env.example atualizado com DASHBOARD_USER e DASHBOARD_PASSWORD.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar dashboard acessivel via nginx com autenticacao</name>
  <what-built>
Deploy completo do dashboard em producao: gunicorn como systemd service (Plan 01) + nginx como reverse proxy com HTTP Basic Auth (Plan 02 Task 1). Scripts deploy/06-setup-dashboard.sh e deploy/07-setup-nginx.sh prontos para execucao na EC2.
  </what-built>
  <how-to-verify>
**Na EC2 (via SSH `ssh -i helpertips-key.pem ubuntu@32.194.158.134`):**

1. Fazer deploy do codigo atualizado:
   ```
   cd /home/helpertips && sudo -u helpertips git pull
   ```

2. Executar scripts de deploy:
   ```
   sudo bash deploy/06-setup-dashboard.sh
   sudo bash deploy/07-setup-nginx.sh
   ```
   (Lembrar de configurar DASHBOARD_USER e DASHBOARD_PASSWORD no /home/helpertips/.env antes do script 07)

3. Verificar servicos:
   ```
   systemctl status helpertips-dashboard   # Deve mostrar active (running)
   systemctl status nginx                  # Deve mostrar active (running)
   ```

4. Testar localmente na EC2:
   ```
   curl -I http://localhost/               # Deve retornar 401 Unauthorized
   curl -u user:pass http://localhost/ | head -5  # Deve retornar HTML do Dash
   ```

5. Testar no browser: Acessar `http://32.194.158.134` — deve pedir usuario/senha e, apos autenticacao, mostrar o dashboard com KPI cards e dados reais.

6. **IMPORTANTE:** Fechar porta 8050 no AWS Security Group (helpertips-sg) para impedir bypass da autenticacao.
  </how-to-verify>
  <acceptance_criteria>
    - `http://<IP>` sem credenciais retorna 401
    - `http://<IP>` com credenciais mostra dashboard com dados reais
    - `systemctl status helpertips-dashboard` mostra active (running)
    - Porta 8050 fechada no Security Group
  </acceptance_criteria>
  <resume-signal>Digitar "aprovado" ou descrever problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
1. `bash -n deploy/07-setup-nginx.sh` passa sem erro de sintaxe
2. Script contem nginx server block com proxy_pass, auth_basic, headers corretos
3. Script usa htpasswd com flags -cbB e grupo www-data
4. .env.example contem DASHBOARD_USER e DASHBOARD_PASSWORD
5. Script avisa sobre fechar porta 8050 no Security Group
</verification>

<success_criteria>
- deploy/07-setup-nginx.sh criado com nginx reverse proxy + HTTP Basic Auth
- .env.example atualizado com variaveis de autenticacao do dashboard
- Apos execucao na EC2: http://<IP> pede autenticacao, dashboard carrega com dados reais
- Porta 8050 fechada no Security Group (passo manual documentado)
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-proxy/08-02-SUMMARY.md`
</output>
