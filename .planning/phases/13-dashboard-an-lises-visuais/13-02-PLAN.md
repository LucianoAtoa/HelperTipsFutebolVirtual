---
phase: 13-dashboard-an-lises-visuais
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - helpertips/dashboard.py
  - tests/test_dashboard.py
autonomous: false
requirements: [DASH-05, DASH-06, DASH-07]

must_haves:
  truths:
    - "Secao analise por liga exibe grafico de barras empilhadas e tabela com P&L por liga"
    - "Secao equity curve exibe 3 linhas sobrepostas (principal, complementar, total)"
    - "Secao gale exibe donut chart e tabela com quantidade, percentual e lucro medio por tentativa"
    - "Todas as 3 secoes reagem aos filtros globais (periodo, mercado, liga)"
    - "Ordem das secoes: liga -> equity curve -> gale (per D-04)"
  artifacts:
    - path: "helpertips/dashboard.py"
      provides: "_build_phase13_section, callback master estendido"
      contains: "def _build_phase13_section"
    - path: "tests/test_dashboard.py"
      provides: "Teste estrutural phase13 IDs"
      contains: "phase13"
  key_links:
    - from: "helpertips/dashboard.py (_build_phase13_section)"
      to: "helpertips/queries.py (aggregate_pl_por_liga, aggregate_pl_por_tentativa)"
      via: "chamada direta no builder"
      pattern: "aggregate_pl_por_liga\\(pl_lista\\)"
    - from: "helpertips/dashboard.py (callback master)"
      to: "Output phase13-placeholder children"
      via: "_build_phase13_section retornando lista de dbc.Card"
      pattern: "Output.*phase13-placeholder.*children"
---

<objective>
Integrar as 3 secoes visuais (liga, equity curve, gale) no layout e callback master do dashboard, substituindo o phase13-placeholder com secoes reativas aos filtros globais.

Purpose: Completar DASH-05, DASH-06, DASH-07 — usuario visualiza analise por liga, evolucao temporal e distribuicao de gale com dados reais.
Output: Dashboard completo com 3 novas secoes visuais integradas ao callback master.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dashboard-an-lises-visuais/13-CONTEXT.md
@.planning/phases/13-dashboard-an-lises-visuais/13-RESEARCH.md
@.planning/phases/13-dashboard-an-lises-visuais/13-01-SUMMARY.md

@helpertips/dashboard.py
@helpertips/queries.py
@tests/test_dashboard.py

<interfaces>
<!-- Funcoes criadas no Plan 01 que serao integradas aqui -->

From helpertips/queries.py (Plan 01):
```python
def aggregate_pl_por_liga(pl_lista: list[dict]) -> list[dict]:
    """Retorna list[dict] ordenado por pl_total desc:
       liga, lucro_principal, lucro_complementar, pl_total, greens, reds, total, taxa_green"""

def aggregate_pl_por_tentativa(pl_lista: list[dict]) -> list[dict]:
    """Retorna list[dict] ordenado por tentativa:
       tentativa, greens, lucro_medio_green"""
```

From helpertips/dashboard.py (Plan 01):
```python
def _build_liga_chart(pl_por_liga: list[dict]) -> go.Figure
def _build_equity_curve_chart(equity: dict, dates: list[str] | None = None) -> go.Figure
def _build_gale_chart(gale_data: list[dict]) -> go.Figure
```

From helpertips/queries.py (existentes):
```python
def calculate_equity_curve_breakdown(signals, complementares_por_mercado, stake, odd_por_mercado, gale_on) -> dict
    # Retorna: {x, y_principal, y_complementar, y_total, colors}

def get_gale_analysis(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list
    # Retorna: [{tentativa, greens, total, win_rate}]

def calculate_pl_por_entrada(signals, complementares_por_mercado, stake, odd_por_mercado, gale_on) -> list[dict]
    # Retorna: [{mercado_slug, resultado, tentativa, placar, liga, entrada, lucro_principal, lucro_comp, lucro_total, ...}]
```

From helpertips/dashboard.py (callback master existente, L529-684):
- 12 Outputs: kpi-total, kpi-winrate, kpi-pl-total (children+className), kpi-roi (children+className), kpi-streak-green, kpi-streak-red, history-table, filter-liga, config-mercados-container, perf-table
- 10 Inputs: periodo-selector, filter-date-custom (start+end), filter-mercado, filter-liga, stake-input, odd-input, gale-toggle, perf-toggle-view, interval-refresh
- Return tuple com 12 valores na mesma ordem dos Outputs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _build_phase13_section + extensao callback master + imports</name>
  <files>helpertips/dashboard.py, tests/test_dashboard.py</files>
  <read_first>
    helpertips/dashboard.py
    helpertips/queries.py
    tests/test_dashboard.py
    .planning/phases/13-dashboard-an-lises-visuais/13-01-SUMMARY.md
  </read_first>
  <action>
    **Passo 1: Adicionar imports de queries.py**

    Na secao de imports (L32-44 de dashboard.py), adicionar ao bloco `from helpertips.queries import`:
    - `aggregate_pl_por_liga`
    - `aggregate_pl_por_tentativa`
    - `calculate_equity_curve_breakdown`
    - `get_gale_analysis`

    **Passo 2: Criar `_build_phase13_section`**

    Adicionar funcao apos os builders Plotly (criados no Plan 01), antes de `make_kpi_card`:

    ```python
    def _build_phase13_section(
        signals_placar: list[dict],
        pl_lista: list[dict],
        conn,
        liga: str | None,
        entrada: str | None,
        date_start: str | None,
        date_end: str | None,
        stake: float,
        odd_por_mercado: dict[str, float],
        comps_por_mercado: dict[str, list[dict]],
        gale_on: bool,
    ) -> list:
        """Constroi as 3 secoes Phase 13 (per D-04: liga -> equity -> gale)."""
        # --- DASH-05: Analise por Liga ---
        pl_por_liga = aggregate_pl_por_liga(pl_lista)
        fig_liga = _build_liga_chart(pl_por_liga)

        # Tabela de ligas com DataTable (coloracao condicional P&L)
        liga_table_data = [
            {
                "liga": r["liga"],
                "taxa_green": f"{r['taxa_green']:.1f}%",
                "pl_principal": round(r["lucro_principal"], 2),
                "pl_complementar": round(r["lucro_complementar"], 2),
                "pl_total": round(r["pl_total"], 2),
            }
            for r in pl_por_liga
        ]
        liga_table = dash_table.DataTable(
            data=liga_table_data,
            columns=[
                {"name": "Liga", "id": "liga"},
                {"name": "Taxa Green", "id": "taxa_green"},
                {"name": "P&L Principal (R$)", "id": "pl_principal", "type": "numeric"},
                {"name": "P&L Complementar (R$)", "id": "pl_complementar", "type": "numeric"},
                {"name": "P&L Total (R$)", "id": "pl_total", "type": "numeric"},
            ],
            style_header={"backgroundColor": "#303030", "color": "white", "fontWeight": "bold"},
            style_cell={"backgroundColor": "#222", "color": "white", "border": "1px solid #444", "textAlign": "center"},
            style_data_conditional=[
                {"if": {"filter_query": "{pl_total} > 0", "column_id": "pl_total"}, "color": "#28a745", "fontWeight": "bold"},
                {"if": {"filter_query": "{pl_total} < 0", "column_id": "pl_total"}, "color": "#dc3545", "fontWeight": "bold"},
                {"if": {"filter_query": "{pl_principal} > 0", "column_id": "pl_principal"}, "color": "#28a745"},
                {"if": {"filter_query": "{pl_principal} < 0", "column_id": "pl_principal"}, "color": "#dc3545"},
                {"if": {"filter_query": "{pl_complementar} > 0", "column_id": "pl_complementar"}, "color": "#28a745"},
                {"if": {"filter_query": "{pl_complementar} < 0", "column_id": "pl_complementar"}, "color": "#dc3545"},
            ],
            style_table={"overflowX": "auto"},
        ) if liga_table_data else html.P("Sem dados de liga para o periodo.", className="text-muted")

        card_liga = dbc.Card([
            dbc.CardHeader(html.H5("Analise por Liga", className="mb-0")),
            dbc.CardBody([
                dcc.Graph(id="liga-chart", figure=fig_liga, config={"displayModeBar": False}),
                html.Div(liga_table, className="mt-3"),
            ]),
        ], className="mb-3")

        # --- DASH-06: Equity Curve ---
        equity_data = calculate_equity_curve_breakdown(
            signals_placar, comps_por_mercado, stake, odd_por_mercado, gale_on,
        )
        # Extrair datas dos sinais para hover (Pitfall 1)
        resolved = [s for s in signals_placar if s.get("resultado") in ("GREEN", "RED")]
        dates = None
        if resolved:
            dates = []
            for s in resolved:
                ra = s.get("received_at")
                if ra is not None:
                    try:
                        dates.append(ra.strftime("%d/%m/%Y %H:%M"))
                    except AttributeError:
                        dates.append(str(ra))
                else:
                    dates.append("")

        fig_equity = _build_equity_curve_chart(equity_data, dates=dates)

        card_equity = dbc.Card([
            dbc.CardHeader(html.H5("Evolucao do Saldo (Equity Curve)", className="mb-0")),
            dbc.CardBody([
                dcc.Graph(id="equity-curve-chart", figure=fig_equity),
            ]),
        ], className="mb-3")

        # --- DASH-07: Analise de Gale ---
        gale_data = get_gale_analysis(conn, liga=liga, entrada=entrada,
                                       date_start=date_start, date_end=date_end)
        pl_por_tent = aggregate_pl_por_tentativa(pl_lista)

        fig_gale = _build_gale_chart(gale_data)

        # Merge gale_data com lucro_medio (per D-08/D-09, Padrao 6 do RESEARCH)
        pl_tent_map = {r["tentativa"]: r for r in pl_por_tent}
        gale_table_data = []
        for row in gale_data:
            t = row["tentativa"]
            pl_row = pl_tent_map.get(t, {})
            gale_table_data.append({
                "tentativa": f"{t}a",
                "greens": row["greens"],
                "total": row["total"],
                "win_rate": f"{row['win_rate']:.1f}%",
                "lucro_medio": f"R$ {pl_row.get('lucro_medio_green', 0.0):+.2f}",
            })

        gale_table = dbc.Table(
            [
                html.Thead(html.Tr([
                    html.Th("Tentativa"), html.Th("Greens"), html.Th("Total"),
                    html.Th("Win Rate"), html.Th("Lucro Medio Green"),
                ])),
                html.Tbody([
                    html.Tr([
                        html.Td(r["tentativa"]), html.Td(r["greens"]), html.Td(r["total"]),
                        html.Td(r["win_rate"]), html.Td(r["lucro_medio"]),
                    ]) for r in gale_table_data
                ]) if gale_table_data else html.Tbody(html.Tr(html.Td("Sem dados", colSpan=5))),
            ],
            color="dark", bordered=True, hover=True, size="sm",
        )

        card_gale = dbc.Card([
            dbc.CardHeader(html.H5("Analise de Gale", className="mb-0")),
            dbc.CardBody([
                dbc.Row([
                    dbc.Col(dcc.Graph(id="gale-donut-chart", figure=fig_gale, config={"displayModeBar": False}), md=6),
                    dbc.Col(gale_table, md=6),
                ]),
            ]),
        ], className="mb-3")

        # Ordem per D-04: Liga -> Equity Curve -> Gale
        return [card_liga, card_equity, card_gale]
    ```

    **Passo 3: Estender callback master**

    3a. Adicionar novo Output ao decorador `@callback` (L529-551):
    Inserir apos `Output("perf-table", "children")` (L541):
    ```python
    Output("phase13-placeholder",       "children"),  # DASH-05, DASH-06, DASH-07
    ```

    3b. No corpo do callback `update_dashboard` (L553+), adicionar logica ANTES do `finally`:

    Apos o bloco que calcula `perf_section` (step 18, ~L663), adicionar:

    ```python
        # 19. Phase 13 — Analise por Liga, Equity Curve, Gale (DASH-05, DASH-06, DASH-07)
        # Reutilizar signals_placar ja buscado para perf_section (D-08)
        # Para visao geral, signals_placar e pl_lista ja foram calculados acima
        # Para visao por mercado, precisamos buscar signals_placar se nao foi feito
        if "signals_placar" not in dir():
            signals_placar = get_signals_com_placar(conn, entrada=entrada,
                                                     liga=liga, date_start=date_start, date_end=date_end)
        if "comps_por_mercado" not in dir():
            comps_por_mercado = {}
            odds_por_mercado = {}
            for slug, nome in MERCADOS_CONFIG:
                cfg = get_mercado_config(conn, slug)
                if cfg:
                    odds_por_mercado[slug] = float(cfg["odd_ref"])
                    comps_por_mercado[slug] = get_complementares_config(conn, slug)
        if "pl_lista" not in dir():
            pl_lista = calculate_pl_por_entrada(signals_placar, comps_por_mercado, stake, odds_por_mercado, gale_on)

        phase13_section = _build_phase13_section(
            signals_placar=signals_placar,
            pl_lista=pl_lista,
            conn=conn,
            liga=liga, entrada=entrada,
            date_start=date_start, date_end=date_end,
            stake=stake, odd_por_mercado=odds_por_mercado,
            comps_por_mercado=comps_por_mercado,
            gale_on=gale_on,
        )
    ```

    **IMPORTANTE:** O callback master tem dois caminhos — visao geral (`else` no step 18, L249-261 de `_build_performance_section`) e visao por mercado (`if entrada`, L205-248). O `_build_performance_section` ja chama `get_signals_com_placar` e `calculate_pl_por_entrada` internamente para a visao geral. Para evitar chamada dupla, REFATORAR o callback para calcular `signals_placar`, `comps_por_mercado`, `odds_por_mercado` e `pl_lista` no nivel do callback (antes de `_build_performance_section`) e passar para ambos `_build_performance_section` e `_build_phase13_section`.

    A refatoracao envolve:
    1. Mover o calculo de `signals_placar`, `comps_por_mercado`, `odds_por_mercado` do interior de `_build_performance_section` para o callback master (logo apos step 14)
    2. Calcular `pl_lista = calculate_pl_por_entrada(...)` no callback master
    3. Passar `pl_lista` para `_build_performance_section` (que precisa aceitar o parametro adicional ou usar `_agregar_por_entrada(pl_lista)` diretamente)
    4. Reutilizar os mesmos dados para `_build_phase13_section`

    Se a refatoracao de `_build_performance_section` for muito invasiva, aceitar a chamada dupla a `get_signals_com_placar` como trade-off (performance minima pois os dados sao poucos). A prioridade e nao quebrar funcionalidade existente.

    3c. No return tuple (L671-684), adicionar `phase13_section` como ultimo elemento:
    ```python
        config_section,    # DASH-03
        perf_section,      # DASH-04
        phase13_section,   # DASH-05/06/07 — NOVO
    )
    ```

    **Passo 4: Testes estruturais**

    Adicionar em `tests/test_dashboard.py`:

    ```python
    def test_layout_has_phase13_component_ids():
        """Verifica que IDs dos componentes Phase 13 estao no layout."""
        from helpertips.dashboard import app
        ids = collect_ids(app.layout)
        assert "phase13-placeholder" in ids
        # Os graficos sao criados dinamicamente pelo callback, nao estao no layout estatico.
        # Verificar que o placeholder existe para receber o conteudo.

    def test_build_phase13_section_exists():
        """Verifica que _build_phase13_section e importavel."""
        from helpertips.dashboard import _build_phase13_section
        assert callable(_build_phase13_section)
    ```
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_dashboard.py tests/test_queries.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/dashboard.py contem `def _build_phase13_section(`
    - helpertips/dashboard.py contem `Output("phase13-placeholder"` no decorador @callback
    - helpertips/dashboard.py contem `from helpertips.queries import` com `aggregate_pl_por_liga`
    - helpertips/dashboard.py contem `from helpertips.queries import` com `aggregate_pl_por_tentativa`
    - helpertips/dashboard.py contem `from helpertips.queries import` com `calculate_equity_curve_breakdown`
    - helpertips/dashboard.py contem `from helpertips.queries import` com `get_gale_analysis`
    - helpertips/dashboard.py contem `id="liga-chart"` dentro de _build_phase13_section
    - helpertips/dashboard.py contem `id="equity-curve-chart"` dentro de _build_phase13_section
    - helpertips/dashboard.py contem `id="gale-donut-chart"` dentro de _build_phase13_section
    - helpertips/dashboard.py contem `color="dark"` (nao `dark=True`) para dbc.Table de gale (Pitfall 6)
    - Return tuple do callback tem 13 elementos (12 anteriores + phase13_section)
    - `python3 -m pytest tests/ -x -q` exit code 0
  </acceptance_criteria>
  <done>Callback master estendido com Output phase13-placeholder. _build_phase13_section retorna 3 dbc.Card (liga, equity, gale) na ordem D-04. Dados reutilizados do callback sem round-trips extras per D-08.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao visual do dashboard</name>
  <files>helpertips/dashboard.py</files>
  <action>Iniciar o dashboard e verificar visualmente as 3 novas secoes. Claude deve rodar `python -m helpertips.dashboard` para iniciar o servidor antes de solicitar verificacao.</action>
  <what-built>3 novas secoes visuais no dashboard: (1) Analise por Liga com grafico de barras empilhadas e tabela, (2) Equity Curve com 3 linhas sobrepostas, (3) Analise de Gale com donut chart e tabela. Todas reativas aos filtros globais.</what-built>
  <how-to-verify>
    1. Iniciar o dashboard: `cd /Users/luciano/helpertips && python -m helpertips.dashboard`
    2. Abrir http://localhost:8050 no navegador

    3. **Secao Liga (DASH-05):**
       - Verificar grafico de barras empilhadas com P&L principal (verde #00bc8c) e complementar (vermelho #e74c3c)
       - Verificar tabela abaixo com Liga, Taxa Green, P&L Principal, P&L Complementar, P&L Total
       - Hover no grafico mostra valor em R$ formatado

    4. **Secao Equity Curve (DASH-06):**
       - Verificar 3 linhas sobrepostas: Principal (verde), Complementar (vermelho), Total (laranja #f39c12)
       - Zoom/pan funciona (comportamento nativo Plotly per D-13)
       - Sem range slider (per D-11)

    5. **Secao Gale (DASH-07):**
       - Verificar donut chart com 4 tons de verde para tentativas 1-4
       - Tabela ao lado com Tentativa, Greens, Total, Win Rate, Lucro Medio Green

    6. **Reatividade:**
       - Mudar filtro de periodo (Hoje/Semana/Mes) — todas as 3 secoes atualizam
       - Mudar filtro de mercado (Over 2.5 / Ambas Marcam) — secoes atualizam
       - Mudar filtro de liga — secoes atualizam

    7. **Ordem (D-04):** Confirmar ordem das secoes: Config Mercados > Performance > **Liga > Equity Curve > Gale** > Historico AG Grid
  </how-to-verify>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <done>Usuario confirmou visualmente que as 3 secoes exibem graficos e tabelas corretos com cores, layout e reatividade conforme especificado.</done>
  <resume-signal>Digite "aprovado" ou descreva problemas visuais para correcao.</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/luciano/helpertips && python3 -m pytest tests/ -x -q
```
Suite completa verde. Dashboard funcional com 3 novas secoes visuais.
</verification>

<success_criteria>
- 3 secoes visuais integradas no dashboard: liga (DASH-05), equity curve (DASH-06), gale (DASH-07)
- Callback master com 13 Outputs (12 anteriores + phase13-placeholder)
- Todas as secoes reativas aos filtros globais (periodo, mercado, liga)
- Cores corretas per D-01, D-02, D-03
- Ordem per D-04: liga -> equity curve -> gale
- Sem accordions/tabs per D-05, scroll continuo
- Sem range slider per D-11, sem click-to-filter per D-12
- Suite completa verde
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-an-lises-visuais/13-02-SUMMARY.md`
</output>
