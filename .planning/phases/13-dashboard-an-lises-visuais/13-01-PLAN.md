---
phase: 13-dashboard-an-lises-visuais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpertips/queries.py
  - helpertips/dashboard.py
  - tests/test_queries.py
  - tests/test_dashboard.py
autonomous: true
requirements: [DASH-05, DASH-06, DASH-07]

must_haves:
  truths:
    - "aggregate_pl_por_liga agrupa P&L por liga com principal e complementar separados"
    - "aggregate_pl_por_tentativa calcula lucro medio green por tentativa"
    - "_build_liga_chart retorna go.Figure com barmode=stack e cores D-01/D-03"
    - "_build_equity_curve_chart retorna go.Figure com 3 traces (principal, complementar, total)"
    - "_build_gale_chart retorna go.Figure donut com 4 tons derivados de #00bc8c"
    - "Todos os builders retornam go.Figure() vazio para input vazio"
  artifacts:
    - path: "helpertips/queries.py"
      provides: "aggregate_pl_por_liga, aggregate_pl_por_tentativa"
      contains: "def aggregate_pl_por_liga"
    - path: "helpertips/dashboard.py"
      provides: "_build_liga_chart, _build_equity_curve_chart, _build_gale_chart"
      contains: "def _build_liga_chart"
    - path: "tests/test_queries.py"
      provides: "Testes para aggregate_pl_por_liga e aggregate_pl_por_tentativa"
      contains: "test_aggregate_pl_por_liga"
    - path: "tests/test_dashboard.py"
      provides: "Testes para builders Plotly Phase 13"
      contains: "test_build_liga_chart"
  key_links:
    - from: "helpertips/dashboard.py"
      to: "helpertips/queries.py"
      via: "import aggregate_pl_por_liga, aggregate_pl_por_tentativa"
      pattern: "from helpertips.queries import.*aggregate_pl_por"
---

<objective>
Criar funcoes puras de agregacao de dados e builders de figuras Plotly para as 3 secoes da Phase 13 (liga, equity curve, gale), com testes TDD cobrindo todos os comportamentos.

Purpose: Separar logica de dados e renderizacao da integracao no callback, seguindo o padrao estabelecido nas Phases 11-12 (helpers puros testaveis + integracao posterior).
Output: 2 funcoes de agregacao em queries.py, 3 builders Plotly em dashboard.py, testes completos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dashboard-an-lises-visuais/13-CONTEXT.md
@.planning/phases/13-dashboard-an-lises-visuais/13-RESEARCH.md

@helpertips/queries.py
@helpertips/dashboard.py
@tests/test_queries.py
@tests/test_dashboard.py

<interfaces>
<!-- Funcoes existentes em queries.py que serao reutilizadas -->

From helpertips/queries.py (L636):
```python
def calculate_pl_por_entrada(
    signals: list[dict],
    complementares_por_mercado: dict[str, list[dict]],
    stake: float,
    odd_por_mercado: dict[str, float],
    gale_on: bool,
) -> list[dict]:
    """Retorna list[dict] com chaves:
        mercado_slug, resultado, tentativa, placar, liga, entrada,
        investido_principal, retorno_principal, lucro_principal,
        investido_comp, retorno_comp, lucro_comp,
        investido_total, lucro_total"""
```

From helpertips/queries.py (L959):
```python
def calculate_equity_curve_breakdown(
    signals: list[dict],
    complementares_por_mercado: dict[str, list[dict]],
    stake: float,
    odd_por_mercado: dict[str, float],
    gale_on: bool,
) -> dict:
    """Retorna dict: x, y_principal, y_complementar, y_total, colors"""
```

From helpertips/queries.py (L1128):
```python
def get_gale_analysis(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Retorna list[dict]: tentativa (int), greens (int), total (int), win_rate (float 0-100)"""
```

From helpertips/dashboard.py (L29):
```python
import plotly.graph_objects as go  # ja importado
```

From helpertips/dashboard.py (L148):
```python
MERCADOS_CONFIG = [
    ("over_2_5", "Over 2.5"),
    ("ambas_marcam", "Ambas Marcam"),
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes TDD + funcoes de agregacao em queries.py</name>
  <files>helpertips/queries.py, tests/test_queries.py</files>
  <read_first>
    helpertips/queries.py
    tests/test_queries.py
  </read_first>
  <behavior>
    aggregate_pl_por_liga:
    - Input: lista de dicts de calculate_pl_por_entrada com chaves liga, lucro_principal, lucro_comp
    - Agrupa por liga, soma lucro_principal e lucro_comp separadamente
    - Retorna list[dict] ordenado por P&L total desc: {liga, lucro_principal, lucro_complementar, pl_total, greens, reds, total, taxa_green}
    - Input vazio retorna lista vazia
    - Sinais sem liga (None) agrupados como "Desconhecida"
    - Sinais com resultado None sao ignorados

    aggregate_pl_por_tentativa (per D-07):
    - Input: lista de dicts de calculate_pl_por_entrada com chaves tentativa, resultado, lucro_total
    - Agrupa por tentativa, conta greens, calcula lucro_medio_green = sum(lucro_total for GREEN) / count(GREEN)
    - Retorna list[dict] ordenado por tentativa: {tentativa, greens, lucro_medio_green}
    - Input vazio retorna lista vazia
    - Tentativa None tratada como 1
    - Tentativa sem greens: lucro_medio_green = 0.0
  </behavior>
  <action>
    **Fase RED — Testes primeiro:**

    Adicionar ao final de `tests/test_queries.py` os seguintes testes, usando o padrao try/except ImportError ja estabelecido nas Phases 11-12:

    ```python
    try:
        from helpertips.queries import aggregate_pl_por_liga
        _HAS_AGGREGATE_LIGA = True
    except ImportError:
        _HAS_AGGREGATE_LIGA = False

    try:
        from helpertips.queries import aggregate_pl_por_tentativa
        _HAS_AGGREGATE_TENT = True
    except ImportError:
        _HAS_AGGREGATE_TENT = False
    ```

    Testes para `aggregate_pl_por_liga`:
    1. `test_aggregate_pl_por_liga_basic` — 3 sinais de 2 ligas, verifica soma por liga e ordenacao por pl_total desc
    2. `test_aggregate_pl_por_liga_empty` — input vazio retorna []
    3. `test_aggregate_pl_por_liga_none_liga` — sinal com liga=None agrupado como "Desconhecida"
    4. `test_aggregate_pl_por_liga_taxa_green` — verifica calculo de taxa_green: greens/total * 100

    Fixture de dados para testes de liga:
    ```python
    SAMPLE_PL_LIGA = [
        {"liga": "Copa do Mundo", "resultado": "GREEN", "lucro_principal": 6.5, "lucro_comp": 2.0, "lucro_total": 8.5},
        {"liga": "Copa do Mundo", "resultado": "RED", "lucro_principal": -10.0, "lucro_comp": -3.0, "lucro_total": -13.0},
        {"liga": "Euro Cup", "resultado": "GREEN", "lucro_principal": 6.5, "lucro_comp": 1.5, "lucro_total": 8.0},
    ]
    ```

    Testes para `aggregate_pl_por_tentativa` (per D-07):
    5. `test_aggregate_pl_por_tentativa_basic` — 3 sinais (2 GREEN tent 1, 1 GREEN tent 2), verifica lucro_medio
    6. `test_aggregate_pl_por_tentativa_empty` — input vazio retorna []
    7. `test_aggregate_pl_por_tentativa_none_tentativa` — tentativa=None tratada como 1
    8. `test_aggregate_pl_por_tentativa_no_greens` — tentativa com apenas REDs: lucro_medio_green = 0.0

    Fixture de dados para testes de tentativa:
    ```python
    SAMPLE_PL_TENT = [
        {"tentativa": 1, "resultado": "GREEN", "lucro_total": 10.0},
        {"tentativa": 1, "resultado": "GREEN", "lucro_total": 8.0},
        {"tentativa": 2, "resultado": "GREEN", "lucro_total": 20.0},
        {"tentativa": 1, "resultado": "RED", "lucro_total": -10.0},
    ]
    ```

    **Fase GREEN — Implementacao:**

    Em `helpertips/queries.py`, adicionar ao final do arquivo (antes de qualquer `if __name__`):

    ```python
    def aggregate_pl_por_liga(pl_lista: list[dict]) -> list[dict]:
        """Agrega P&L por liga para analise dimensional (DASH-05)."""
        from collections import defaultdict
        grupos: dict[str, dict] = defaultdict(lambda: {
            "lucro_principal": 0.0, "lucro_complementar": 0.0,
            "greens": 0, "reds": 0,
        })
        for row in pl_lista:
            liga = row.get("liga") or "Desconhecida"
            resultado = row.get("resultado")
            if resultado not in ("GREEN", "RED"):
                continue
            grupos[liga]["lucro_principal"] += row.get("lucro_principal", 0.0)
            grupos[liga]["lucro_complementar"] += row.get("lucro_comp", 0.0)
            if resultado == "GREEN":
                grupos[liga]["greens"] += 1
            else:
                grupos[liga]["reds"] += 1

        result = []
        for liga, g in grupos.items():
            total = g["greens"] + g["reds"]
            pl_total = g["lucro_principal"] + g["lucro_complementar"]
            taxa = g["greens"] / total * 100 if total > 0 else 0.0
            result.append({
                "liga": liga,
                "lucro_principal": round(g["lucro_principal"], 2),
                "lucro_complementar": round(g["lucro_complementar"], 2),
                "pl_total": round(pl_total, 2),
                "greens": g["greens"],
                "reds": g["reds"],
                "total": total,
                "taxa_green": round(taxa, 1),
            })
        result.sort(key=lambda r: r["pl_total"], reverse=True)
        return result


    def aggregate_pl_por_tentativa(pl_lista: list[dict]) -> list[dict]:
        """Agrega P&L por tentativa para analise de gale (DASH-07, per D-07)."""
        from collections import defaultdict
        grupos: dict[int, dict] = defaultdict(lambda: {
            "greens": 0, "lucro_total_greens": 0.0,
        })
        for row in pl_lista:
            t = row.get("tentativa") or 1
            if row.get("resultado") == "GREEN":
                grupos[t]["greens"] += 1
                grupos[t]["lucro_total_greens"] += row.get("lucro_total", 0.0)

        result = []
        for tentativa in sorted(grupos.keys()):
            g = grupos[tentativa]
            lucro_medio = (
                g["lucro_total_greens"] / g["greens"] if g["greens"] > 0 else 0.0
            )
            result.append({
                "tentativa": tentativa,
                "greens": g["greens"],
                "lucro_medio_green": round(lucro_medio, 2),
            })
        return result
    ```

    Rodar testes apos implementacao para confirmar GREEN.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py -k "aggregate_pl_por_liga or aggregate_pl_por_tentativa" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_queries.py contem `def test_aggregate_pl_por_liga_basic`
    - tests/test_queries.py contem `def test_aggregate_pl_por_tentativa_basic`
    - helpertips/queries.py contem `def aggregate_pl_por_liga(pl_lista`
    - helpertips/queries.py contem `def aggregate_pl_por_tentativa(pl_lista`
    - `python3 -m pytest tests/test_queries.py -k "aggregate_pl_por" -x` exit code 0
    - aggregate_pl_por_liga retorna dicts com chaves: liga, lucro_principal, lucro_complementar, pl_total, greens, reds, total, taxa_green
    - aggregate_pl_por_tentativa retorna dicts com chaves: tentativa, greens, lucro_medio_green
  </acceptance_criteria>
  <done>8 testes novos passam. Funcoes aggregate_pl_por_liga e aggregate_pl_por_tentativa exportaveis de queries.py com comportamento correto para inputs normais, vazios e edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes TDD + builders de figuras Plotly em dashboard.py</name>
  <files>helpertips/dashboard.py, tests/test_dashboard.py</files>
  <read_first>
    helpertips/dashboard.py
    tests/test_dashboard.py
    helpertips/queries.py
  </read_first>
  <behavior>
    _build_liga_chart (DASH-05, per D-01/D-03):
    - Input: list[dict] de aggregate_pl_por_liga
    - Retorna go.Figure com 2 traces go.Bar (barmode='stack')
    - Trace 0: name="Principal", marker_color="#00bc8c"
    - Trace 1: name="Complementar", marker_color="#e74c3c"
    - Input vazio retorna go.Figure() sem traces
    - Layout: paper_bgcolor="#222", plot_bgcolor="#222", barmode="stack"

    _build_equity_curve_chart (DASH-06, per D-01/D-11):
    - Input: dict de calculate_equity_curve_breakdown (x, y_principal, y_complementar, y_total)
    - Retorna go.Figure com 3 traces go.Scatter mode="lines"
    - Trace 0: name="Principal", line.color="#00bc8c"
    - Trace 1: name="Complementar", line.color="#e74c3c"
    - Trace 2: name="Total", line.color="#f39c12"
    - Input com x vazio retorna go.Figure() sem traces
    - Sem range slider (per D-11)

    _build_gale_chart (DASH-07, per D-02):
    - Input: list[dict] de get_gale_analysis (tentativa, greens, total, win_rate)
    - Retorna go.Figure com 1 trace go.Pie hole=0.4
    - Cores: ["#00bc8c", "#00a07a", "#008567", "#006a53"]
    - Input vazio retorna go.Figure() sem traces
  </behavior>
  <action>
    **Fase RED — Testes primeiro:**

    Adicionar ao final de `tests/test_dashboard.py` os seguintes testes, usando try/except ImportError:

    ```python
    try:
        from helpertips.dashboard import _build_liga_chart
        _HAS_LIGA_CHART = True
    except ImportError:
        _HAS_LIGA_CHART = False

    try:
        from helpertips.dashboard import _build_equity_curve_chart
        _HAS_EQUITY_CHART = True
    except ImportError:
        _HAS_EQUITY_CHART = False

    try:
        from helpertips.dashboard import _build_gale_chart
        _HAS_GALE_CHART = True
    except ImportError:
        _HAS_GALE_CHART = False
    ```

    Testes para `_build_liga_chart`:
    1. `test_build_liga_chart_basic` — input com 2 ligas, verifica 2 traces, barmode="stack", cores "#00bc8c" e "#e74c3c"
    2. `test_build_liga_chart_empty` — input [] retorna go.Figure com 0 traces
    3. `test_build_liga_chart_dark_theme` — verifica paper_bgcolor="#222"

    Testes para `_build_equity_curve_chart`:
    4. `test_build_equity_curve_basic` — input com x=[1,2,3], verifica 3 traces Scatter, cores corretas
    5. `test_build_equity_curve_empty` — input com x=[] retorna go.Figure com 0 traces
    6. `test_build_equity_curve_colors` — trace 0 cor "#00bc8c", trace 1 "#e74c3c", trace 2 "#f39c12"

    Testes para `_build_gale_chart`:
    7. `test_build_gale_chart_basic` — input com 4 tentativas, verifica go.Pie com hole=0.4
    8. `test_build_gale_chart_empty` — input [] retorna go.Figure com 0 traces
    9. `test_build_gale_chart_colors` — verifica 4 tons derivados "#00bc8c", "#00a07a", "#008567", "#006a53"

    Fixtures:
    ```python
    SAMPLE_PL_LIGA = [
        {"liga": "Copa do Mundo", "lucro_principal": -3.5, "lucro_complementar": -1.0, "pl_total": -4.5,
         "greens": 1, "reds": 1, "total": 2, "taxa_green": 50.0},
        {"liga": "Euro Cup", "lucro_principal": 6.5, "lucro_complementar": 1.5, "pl_total": 8.0,
         "greens": 1, "reds": 0, "total": 1, "taxa_green": 100.0},
    ]

    SAMPLE_EQUITY = {
        "x": [1, 2, 3], "y_principal": [10, 5, 15],
        "y_complementar": [2, 1, 4], "y_total": [12, 6, 19],
        "colors": ["#28a745", "#dc3545", "#28a745"],
    }

    SAMPLE_GALE = [
        {"tentativa": 1, "greens": 50, "total": 60, "win_rate": 83.3},
        {"tentativa": 2, "greens": 20, "total": 30, "win_rate": 66.7},
        {"tentativa": 3, "greens": 5, "total": 15, "win_rate": 33.3},
        {"tentativa": 4, "greens": 2, "total": 10, "win_rate": 20.0},
    ]
    ```

    **Fase GREEN — Implementacao:**

    Em `helpertips/dashboard.py`, adicionar as 3 funcoes builder ANTES da secao de layout (apos `_build_performance_section`, antes de `make_kpi_card`). Nao e preciso novo import — `go` ja importado (L28).

    ```python
    def _build_liga_chart(pl_por_liga: list[dict]) -> go.Figure:
        """Grafico de barras empilhadas P&L por liga (DASH-05, D-01/D-03)."""
        if not pl_por_liga:
            return go.Figure()
        ligas = [row["liga"] for row in pl_por_liga]
        pl_principal = [row["lucro_principal"] for row in pl_por_liga]
        pl_comp = [row["lucro_complementar"] for row in pl_por_liga]
        fig = go.Figure()
        fig.add_trace(go.Bar(
            name="Principal", x=ligas, y=pl_principal,
            marker_color="#00bc8c",
            hovertemplate="<b>%{x}</b><br>P&L Principal: R$ %{y:+.2f}<extra></extra>",
        ))
        fig.add_trace(go.Bar(
            name="Complementar", x=ligas, y=pl_comp,
            marker_color="#e74c3c",
            hovertemplate="<b>%{x}</b><br>P&L Complementar: R$ %{y:+.2f}<extra></extra>",
        ))
        fig.update_layout(
            barmode="stack", paper_bgcolor="#222", plot_bgcolor="#222",
            font={"color": "white"}, legend={"orientation": "h"},
            margin={"t": 30, "b": 40},
        )
        return fig


    def _build_equity_curve_chart(equity: dict, dates: list[str] | None = None) -> go.Figure:
        """Equity curve com 3 linhas sobrepostas (DASH-06, D-01/D-11)."""
        if not equity.get("x"):
            return go.Figure()
        x_vals = equity["x"]
        # Se datas disponiveis, usar no hover; eixo X permanece com indices
        hover_extra_principal = ""
        hover_extra_comp = ""
        hover_extra_total = ""
        if dates and len(dates) == len(x_vals):
            # Customdata para incluir data no hover
            customdata_dates = dates
        else:
            customdata_dates = None

        fig = go.Figure()
        hover_tpl_p = "Sinal #%{x}<br>Principal: R$ %{y:.2f}<extra></extra>"
        hover_tpl_c = "Sinal #%{x}<br>Complementar: R$ %{y:.2f}<extra></extra>"
        hover_tpl_t = "Sinal #%{x}<br>Total: R$ %{y:.2f}<extra></extra>"
        if customdata_dates:
            hover_tpl_p = "%{customdata}<br>Principal: R$ %{y:.2f}<extra></extra>"
            hover_tpl_c = "%{customdata}<br>Complementar: R$ %{y:.2f}<extra></extra>"
            hover_tpl_t = "%{customdata}<br>Total: R$ %{y:.2f}<extra></extra>"
        fig.add_trace(go.Scatter(
            x=x_vals, y=equity["y_principal"], mode="lines", name="Principal",
            line={"color": "#00bc8c", "width": 2}, hovertemplate=hover_tpl_p,
            customdata=customdata_dates,
        ))
        fig.add_trace(go.Scatter(
            x=x_vals, y=equity["y_complementar"], mode="lines", name="Complementar",
            line={"color": "#e74c3c", "width": 2}, hovertemplate=hover_tpl_c,
            customdata=customdata_dates,
        ))
        fig.add_trace(go.Scatter(
            x=x_vals, y=equity["y_total"], mode="lines", name="Total",
            line={"color": "#f39c12", "width": 2}, hovertemplate=hover_tpl_t,
            customdata=customdata_dates,
        ))
        fig.update_layout(
            paper_bgcolor="#222", plot_bgcolor="#222",
            font={"color": "white"}, xaxis={"showgrid": False},
            yaxis={"gridcolor": "#444"}, legend={"orientation": "h"},
            margin={"t": 30, "b": 40},
        )
        return fig


    def _build_gale_chart(gale_data: list[dict]) -> go.Figure:
        """Donut de distribuicao de greens por tentativa (DASH-07, D-02)."""
        if not gale_data:
            return go.Figure()
        labels = [f"{row['tentativa']}a Tentativa" for row in gale_data]
        values = [row["greens"] for row in gale_data]
        colors = ["#00bc8c", "#00a07a", "#008567", "#006a53"][:len(gale_data)]
        fig = go.Figure(go.Pie(
            labels=labels, values=values, hole=0.4,
            marker={"colors": colors},
            hovertemplate="<b>%{label}</b><br>Greens: %{value}<br>%{percent}<extra></extra>",
        ))
        fig.update_layout(
            paper_bgcolor="#222", font={"color": "white"},
            showlegend=True, legend={"orientation": "h"},
            margin={"t": 30, "b": 40},
        )
        return fig
    ```

    Rodar testes apos implementacao para confirmar GREEN.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_dashboard.py -k "build_liga_chart or build_equity_curve or build_gale_chart" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_dashboard.py contem `def test_build_liga_chart_basic`
    - tests/test_dashboard.py contem `def test_build_equity_curve_basic`
    - tests/test_dashboard.py contem `def test_build_gale_chart_basic`
    - helpertips/dashboard.py contem `def _build_liga_chart(pl_por_liga`
    - helpertips/dashboard.py contem `def _build_equity_curve_chart(equity`
    - helpertips/dashboard.py contem `def _build_gale_chart(gale_data`
    - `python3 -m pytest tests/test_dashboard.py -k "build_liga_chart or build_equity_curve or build_gale_chart" -x` exit code 0
    - _build_liga_chart retorna go.Figure com barmode="stack"
    - _build_equity_curve_chart retorna go.Figure com 3 traces Scatter
    - _build_gale_chart retorna go.Figure com go.Pie hole=0.4
  </acceptance_criteria>
  <done>9 testes novos passam. 3 builders Plotly retornam figuras corretas com cores per D-01/D-02/D-03, tema dark, e hovertemplates ricos per D-10. Figuras vazias para input vazio per Pitfall 2.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py tests/test_dashboard.py -k "aggregate_pl_por or build_liga_chart or build_equity_curve or build_gale_chart" -x -q
```
Todos os 17 testes novos passam. Nenhum teste existente quebrado.
</verification>

<success_criteria>
- 2 funcoes de agregacao em queries.py com testes TDD (8 testes)
- 3 builders Plotly em dashboard.py com testes TDD (9 testes)
- Suite completa verde: `python3 -m pytest tests/ -x -q`
- Cores per D-01, D-02, D-03; tema dark; hovertemplates per D-10
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-an-lises-visuais/13-01-SUMMARY.md`
</output>
