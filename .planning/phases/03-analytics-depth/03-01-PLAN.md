---
phase: 03-analytics-depth
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - helpertips/queries.py
  - tests/test_queries.py
autonomous: true
requirements:
  - ANAL-01
  - ANAL-02
  - ANAL-05
  - ANAL-06
must_haves:
  truths:
    - "get_heatmap_data retorna matriz 24x7 com None para celulas sem dados"
    - "get_winrate_by_dow retorna win rate para cada dia da semana (0-6)"
    - "calculate_equity_curve converte sinais DESC em curva cronologica ASC com y_fixa e y_gale"
    - "calculate_streaks retorna streak atual, max_green e max_red em single-pass O(n)"
  artifacts:
    - path: "helpertips/queries.py"
      provides: "get_heatmap_data, get_winrate_by_dow, calculate_equity_curve, calculate_streaks"
      exports: ["get_heatmap_data", "get_winrate_by_dow", "calculate_equity_curve", "calculate_streaks"]
    - path: "tests/test_queries.py"
      provides: "Testes unitarios para as 4 funcoes novas"
      contains: "test_get_heatmap_data"
  key_links:
    - from: "helpertips/queries.py:get_heatmap_data"
      to: "helpertips/queries.py:_build_where"
      via: "reutiliza WHERE builder existente"
      pattern: "_build_where\\("
    - from: "helpertips/queries.py:calculate_equity_curve"
      to: "helpertips/queries.py:get_signal_history"
      via: "consome saida DESC de get_signal_history, reverte para ASC"
      pattern: "reversed\\("
---

<objective>
Implementar a camada de dados temporais e de equity/streaks para a Fase 3: 4 funcoes novas em queries.py cobrindo heatmap por hora/dia, win rate por dia da semana, curva de equity (Stake Fixa vs Gale), e calculo de streaks.

Purpose: Fornecer as funcoes de dados que o dashboard consumira na aba "Temporal" e parte da aba "Gale & Streaks" — tudo em puro Python/SQL, sem dependencia de Dash.
Output: 4 funcoes exportadas em queries.py + testes unitarios completos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-depth/03-CONTEXT.md
@.planning/phases/03-analytics-depth/03-RESEARCH.md
@.planning/phases/03-analytics-depth/03-VALIDATION.md

@helpertips/queries.py
@tests/test_queries.py

<interfaces>
<!-- Funcoes existentes em queries.py que as novas funcoes devem reutilizar -->

From helpertips/queries.py:
```python
def _build_where(liga=None, entrada=None, date_start=None, date_end=None):
    """Returns (where_sql: str, params: list) where where_sql starts with '1=1'"""

def get_signal_history(conn, liga=None, entrada=None, date_start=None,
                       date_end=None) -> list:
    """Retorna sinais em ORDER BY received_at DESC. Cada item e dict com keys:
       liga, entrada, horario, resultado, placar, tentativa, received_at"""

def calculate_roi(signals: list, stake: float, odd: float, gale_on: bool) -> dict:
    """ROI puro Python. Retorna {profit, total_invested, roi_pct, ...}
       Logica de Gale: stake * 2^(tentativa-1) por tentativa"""
```

From helpertips/store.py:
```python
def get_stats(conn) -> dict:
    """Retorna {total, greens, reds, pending, parse_failures, coverage}"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes RED para get_heatmap_data, get_winrate_by_dow, calculate_equity_curve, calculate_streaks</name>
  <files>tests/test_queries.py</files>
  <read_first>
    - tests/test_queries.py (estado atual dos testes — 718 linhas, seguir padrao de fixtures e asserts)
    - helpertips/queries.py (assinaturas existentes para consistencia)
    - .planning/phases/03-analytics-depth/03-RESEARCH.md (padroes verificados, SQL exato, algoritmos)
  </read_first>
  <behavior>
    - test_get_heatmap_data: dados mock com 3 sinais em horas/dias diferentes → matriz 24x7, celulas preenchidas com win_rate correto, demais None
    - test_get_heatmap_data_empty: sem sinais → z com todas celulas None, x = ["Dom","Seg",...,"Sab"], y = ["00h",...,"23h"]
    - test_get_winrate_by_dow: 4 sinais em 2 dias diferentes → retorna lista de dicts com {dia, dia_nome, greens, total, win_rate}
    - test_get_winrate_by_dow_empty: sem sinais → lista vazia
    - test_calculate_equity_curve: 3 sinais [GREEN, GREEN, RED] com stake=10, odd=1.90 → y_fixa=[9.0, 18.0, 8.0], y_gale=[9.0, 18.0, 8.0] (todos tentativa=1)
    - test_calculate_equity_curve_with_gale: sinais com tentativa=1,2,1 → y_gale difere de y_fixa (gale acumula custo)
    - test_equity_curve_reversal: sinais em ordem DESC → x=[1,2,3] com sinal mais antigo no indice 1 (confirma reversal)
    - test_equity_curve_empty: sem sinais resolvidos → {x:[], y_fixa:[], y_gale:[], colors:[]}
    - test_equity_curve_annotations: streak >= 5 consecutivos → annotations list nao vazio com x, y, text
    - test_calculate_streaks: [G,G,G,R,G] → current=1, current_type="GREEN", max_green=3, max_red=1
    - test_calculate_streaks_all_green: [G,G,G] → current=3, current_type="GREEN", max_green=3, max_red=0
    - test_calculate_streaks_empty: [] → current=0, current_type=None, max_green=0, max_red=0
  </behavior>
  <action>
Adicionar os seguintes testes em tests/test_queries.py, APOS os testes existentes:

**Testes puro Python (sem DB):**

1. `test_calculate_equity_curve` — Cria lista de 3 dicts simulando saida de get_signal_history (ordem DESC):
   ```python
   signals_desc = [
       {"resultado": "RED", "tentativa": 1},    # mais recente (DESC)
       {"resultado": "GREEN", "tentativa": 1},
       {"resultado": "GREEN", "tentativa": 1},   # mais antigo
   ]
   result = calculate_equity_curve(signals_desc, stake=10.0, odd=1.90)
   assert result["x"] == [1, 2, 3]
   assert result["y_fixa"] == [9.0, 18.0, 8.0]
   assert result["colors"] == ["#28a745", "#28a745", "#dc3545"]
   ```

2. `test_calculate_equity_curve_with_gale` — Sinais com tentativa variavel:
   ```python
   signals_desc = [
       {"resultado": "GREEN", "tentativa": 1},
       {"resultado": "GREEN", "tentativa": 2},   # gale 2a tentativa
       {"resultado": "RED", "tentativa": 1},
   ]
   # Reversal: RED(t=1), GREEN(t=2), GREEN(t=1) cronologico
   result = calculate_equity_curve(signals_desc, stake=10.0, odd=1.90)
   # RED t=1: fixa=-10, gale=-10
   # GREEN t=2: fixa=+9→-1, gale=winning=20*(0.9)-10=+8→-2
   # GREEN t=1: fixa=+9→+8, gale=+9→+7
   assert result["y_fixa"][-1] == 8.0
   assert result["y_gale"][-1] != result["y_fixa"][-1]  # gale diverge
   ```

3. `test_equity_curve_reversal` — Confirma que sinais DESC sao revertidos:
   ```python
   signals_desc = [
       {"resultado": "GREEN", "tentativa": 1, "id": "newest"},
       {"resultado": "RED", "tentativa": 1, "id": "oldest"},
   ]
   result = calculate_equity_curve(signals_desc, stake=10.0, odd=1.90)
   # Primeiro ponto deve ser RED (oldest), nao GREEN
   assert result["colors"][0] == "#dc3545"  # RED primeiro (cronologico)
   assert result["colors"][1] == "#28a745"  # GREEN depois
   ```

4. `test_equity_curve_empty` — Sem sinais resolvidos:
   ```python
   result = calculate_equity_curve([], stake=10.0, odd=1.90)
   assert result == {"x": [], "y_fixa": [], "y_gale": [], "colors": [], "annotations": []}
   ```

5. `test_equity_curve_annotations` — Streak >= 5:
   ```python
   signals_desc = [{"resultado": "GREEN", "tentativa": 1} for _ in range(6)]
   result = calculate_equity_curve(signals_desc, stake=10.0, odd=1.90)
   assert len(result["annotations"]) >= 1
   assert "5" in result["annotations"][0]["text"] or "streak" in result["annotations"][0]["text"].lower()
   ```

6. `test_calculate_streaks` — Caso misto:
   ```python
   signals_desc = [
       {"resultado": "GREEN"},  # mais recente (DESC)
       {"resultado": "RED"},
       {"resultado": "GREEN"},
       {"resultado": "GREEN"},
       {"resultado": "GREEN"},  # mais antigo
   ]
   result = calculate_streaks(signals_desc)
   assert result["current"] == 1
   assert result["current_type"] == "GREEN"
   assert result["max_green"] == 3
   assert result["max_red"] == 1
   ```

7. `test_calculate_streaks_all_green`:
   ```python
   signals_desc = [{"resultado": "GREEN"} for _ in range(3)]
   result = calculate_streaks(signals_desc)
   assert result == {"current": 3, "current_type": "GREEN", "max_green": 3, "max_red": 0}
   ```

8. `test_calculate_streaks_empty`:
   ```python
   result = calculate_streaks([])
   assert result == {"current": 0, "current_type": None, "max_green": 0, "max_red": 0}
   ```

**Testes com DB (usar fixture `db_conn` existente):**

9. `test_get_heatmap_data` — Inserir 3 sinais com received_at em horas/dias diferentes via INSERT direto, chamar get_heatmap_data(conn), verificar que z tem valores corretos nas celulas correspondentes e None nas demais.

10. `test_get_heatmap_data_empty` — Chamar sem inserir sinais. Verificar z = [[None]*7 for _ in range(24)].

11. `test_get_winrate_by_dow` — Inserir 4 sinais em 2 dias da semana diferentes, chamar get_winrate_by_dow(conn), verificar que retorna lista com dicts contendo win_rate por dia.

12. `test_get_winrate_by_dow_empty` — Sem sinais → lista vazia.

Importar as novas funcoes no topo do arquivo:
```python
from helpertips.queries import (
    ...,  # imports existentes
    get_heatmap_data,
    get_winrate_by_dow,
    calculate_equity_curve,
    calculate_streaks,
)
```

Rodar testes — DEVEM FALHAR (ImportError pois funcoes ainda nao existem).
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py::test_calculate_equity_curve tests/test_queries.py::test_calculate_streaks -x 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_queries.py contem `def test_calculate_equity_curve(`
    - tests/test_queries.py contem `def test_calculate_equity_curve_with_gale(`
    - tests/test_queries.py contem `def test_equity_curve_reversal(`
    - tests/test_queries.py contem `def test_equity_curve_empty(`
    - tests/test_queries.py contem `def test_equity_curve_annotations(`
    - tests/test_queries.py contem `def test_calculate_streaks(`
    - tests/test_queries.py contem `def test_calculate_streaks_all_green(`
    - tests/test_queries.py contem `def test_calculate_streaks_empty(`
    - tests/test_queries.py contem `def test_get_heatmap_data(`
    - tests/test_queries.py contem `def test_get_heatmap_data_empty(`
    - tests/test_queries.py contem `def test_get_winrate_by_dow(`
    - tests/test_queries.py contem `def test_get_winrate_by_dow_empty(`
    - tests/test_queries.py contem `from helpertips.queries import` com `calculate_equity_curve`
    - Testes falham com ImportError ou NameError (funcoes nao existem ainda) — RED phase
  </acceptance_criteria>
  <done>12 testes escritos em tests/test_queries.py, todos falhando (RED) porque as funcoes ainda nao existem em queries.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar get_heatmap_data, get_winrate_by_dow, calculate_equity_curve, calculate_streaks — GREEN</name>
  <files>helpertips/queries.py</files>
  <read_first>
    - helpertips/queries.py (estado atual — adicionar funcoes no final, seguir padrao de _build_where e docstrings)
    - .planning/phases/03-analytics-depth/03-RESEARCH.md (Padrao 2: Equity Curve reversal, Padrao 3: Heatmap 24x7, Padrao 5: Streaks)
    - tests/test_queries.py (testes RED recem-criados — implementar para satisfazer)
  </read_first>
  <action>
Adicionar 4 funcoes ao final de helpertips/queries.py:

**1. get_heatmap_data(conn, liga=None, entrada=None, date_start=None, date_end=None) -> dict:**
```python
def get_heatmap_data(conn, liga=None, entrada=None, date_start=None, date_end=None) -> dict:
    """Win rate por hora do dia x dia da semana. Retorna {z, x, y} para go.Heatmap."""
    where, params = _build_where(liga=liga, entrada=entrada,
                                 date_start=date_start, date_end=date_end)
    sql = f"""
        SELECT
            EXTRACT(HOUR FROM received_at)::INT AS hora,
            EXTRACT(DOW FROM received_at)::INT  AS dia,
            COUNT(*) FILTER (WHERE resultado = 'GREEN') AS greens,
            COUNT(*) FILTER (WHERE resultado IN ('GREEN', 'RED')) AS total
        FROM signals
        WHERE resultado IN ('GREEN', 'RED') AND {where}
        GROUP BY 1, 2
        ORDER BY 1, 2
    """
    with conn.cursor() as cur:
        cur.execute(sql, params)
        rows = cur.fetchall()

    z = [[None] * 7 for _ in range(24)]
    for hora, dia, greens, total in rows:
        z[hora][dia] = round(greens / total * 100, 1) if total > 0 else None

    dias = ["Dom", "Seg", "Ter", "Qua", "Qui", "Sex", "Sab"]
    horas = [f"{h:02d}h" for h in range(24)]
    return {"z": z, "x": dias, "y": horas}
```

**2. get_winrate_by_dow(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:**
```python
def get_winrate_by_dow(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Win rate por dia da semana. Retorna lista de dicts {dia, dia_nome, greens, total, win_rate}."""
    where, params = _build_where(liga=liga, entrada=entrada,
                                 date_start=date_start, date_end=date_end)
    sql = f"""
        SELECT
            EXTRACT(DOW FROM received_at)::INT AS dia,
            COUNT(*) FILTER (WHERE resultado = 'GREEN') AS greens,
            COUNT(*) FILTER (WHERE resultado IN ('GREEN', 'RED')) AS total
        FROM signals
        WHERE resultado IN ('GREEN', 'RED') AND {where}
        GROUP BY 1
        ORDER BY 1
    """
    dias_nomes = ["Dom", "Seg", "Ter", "Qua", "Qui", "Sex", "Sab"]
    with conn.cursor() as cur:
        cur.execute(sql, params)
        rows = cur.fetchall()

    return [
        {
            "dia": int(dia),
            "dia_nome": dias_nomes[int(dia)],
            "greens": int(g),
            "total": int(t),
            "win_rate": round(g / t * 100, 1) if t > 0 else 0.0,
        }
        for dia, g, t in rows
    ]
```

**3. calculate_equity_curve(signals_desc: list, stake: float, odd: float) -> dict:**
Funcao pura Python (sem DB). Recebe sinais em ordem DESC (saida de get_signal_history), filtra resolvidos, reverte para ASC, calcula bankroll acumulado para Stake Fixa e Gale.

Implementar conforme RESEARCH.md Padrao 2 (equity curve reversal):
- Filtrar sinais com resultado in ("GREEN", "RED")
- Reverter para ordem cronologica: `resolved_asc = list(reversed(resolved))`
- Iterar calculando bk_fixa e bk_gale:
  - Stake Fixa: GREEN = +stake*(odd-1), RED = -stake
  - Gale: accumulated = stake * (2^tentativa - 1), winning = stake * 2^(tentativa-1), GREEN = winning*(odd-1) - (accumulated-winning), RED = -accumulated
- Detectar streaks >= 5 e gerar annotations list: [{x, y, text}]
- Retornar: {x: list[int], y_fixa: list[float], y_gale: list[float], colors: list[str], annotations: list[dict]}
- Usar round(valor, 2) para y_fixa e y_gale
- Cores: "#28a745" para GREEN, "#dc3545" para RED

**4. calculate_streaks(signals_desc: list) -> dict:**
Funcao pura Python. Implementar conforme RESEARCH.md Padrao 5:
- Filtrar resolvidos, reverter para ASC
- Single-pass O(n): iterar comparando resultado[i] com resultado[i-1]
- Retornar: {current: int, current_type: str|None, max_green: int, max_red: int}

Nao alterar nenhuma funcao existente. Apenas adicionar ao final do arquivo.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - queries.py contem `def get_heatmap_data(`
    - queries.py contem `def get_winrate_by_dow(`
    - queries.py contem `def calculate_equity_curve(`
    - queries.py contem `def calculate_streaks(`
    - get_heatmap_data usa `_build_where(` para filtros
    - get_heatmap_data inicializa z com `[[None] * 7 for _ in range(24)]` (nao zeros)
    - calculate_equity_curve usa `list(reversed(` para converter DESC→ASC
    - calculate_streaks retorna dict com keys: current, current_type, max_green, max_red
    - Todos os testes em test_queries.py passam (incluindo os 12 novos + os 61 existentes)
  </acceptance_criteria>
  <done>4 funcoes implementadas em queries.py. Todos os testes passam (GREEN). Funcoes seguem padrao existente de _build_where, conn per call, docstrings.</done>
</task>

</tasks>

<verification>
python3 -m pytest tests/test_queries.py -x -q
# Todos os testes passam (61 existentes + 12 novos = 73)
</verification>

<success_criteria>
- get_heatmap_data retorna matriz 24x7 com None para celulas sem dados e win_rate (0-100) para celulas com dados
- get_winrate_by_dow retorna lista de dicts com dia_nome e win_rate
- calculate_equity_curve reverte DESC para ASC, calcula bankroll Stake Fixa e Gale, gera annotations para streaks >= 5
- calculate_streaks retorna streak atual e maximos historicos em O(n) single-pass
- Suite completa de testes passa
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-depth/03-01-SUMMARY.md`
</output>
