---
phase: 03-analytics-depth
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - helpertips/dashboard.py
  - tests/test_dashboard.py
autonomous: false
requirements:
  - ANAL-01
  - ANAL-02
  - ANAL-03
  - ANAL-04
  - ANAL-05
  - ANAL-06
  - ANAL-07
  - ANAL-08
  - OPER-01
must_haves:
  truths:
    - "Dashboard mostra 3 abas tematicas: Temporal, Gale & Streaks, Volume"
    - "Heatmap atualiza com filtros de liga/entrada/data"
    - "Equity curve mostra duas linhas sobrepostas: Stake Fixa e Gale"
    - "Gale analysis mostra barras horizontais por tentativa"
    - "Streak tracker mostra 3 mini-cards: atual, max green, max red"
    - "Volume chart mostra barras de sinais por dia"
    - "Cross-dimensional table mostra breakdown por liga+entrada"
    - "Badge de cobertura no header com cor por threshold"
    - "Click no badge abre modal com tabela de parse_failures"
  artifacts:
    - path: "helpertips/dashboard.py"
      provides: "Layout completo com tabs, badge, modal e callbacks"
      contains: "tabs-analytics"
    - path: "tests/test_dashboard.py"
      provides: "Testes de layout e badge thresholds"
      contains: "test_layout_has_required_component_ids"
  key_links:
    - from: "helpertips/dashboard.py:update_tabs"
      to: "helpertips/queries.py:get_heatmap_data"
      via: "callback separado para abas"
      pattern: "get_heatmap_data\\("
    - from: "helpertips/dashboard.py:update_tabs"
      to: "helpertips/queries.py:calculate_equity_curve"
      via: "callback de aba temporal"
      pattern: "calculate_equity_curve\\("
    - from: "helpertips/dashboard.py:update_dashboard"
      to: "helpertips/store.py:get_stats"
      via: "badge coverage no master callback"
      pattern: "get_stats\\("
    - from: "helpertips/dashboard.py:toggle_modal"
      to: "helpertips/queries.py:get_parse_failures_detail"
      via: "callback de modal toggle"
      pattern: "get_parse_failures_detail\\("
---

<objective>
Integrar todas as funcoes de dados (Plans 01-02) ao dashboard: layout com dbc.Tabs (3 abas tematicas), badge de cobertura do parser com modal, callback separado para abas com lazy render, e atualizacao do master callback para badge.

Purpose: Completar a interface visual da Fase 3, conectando os calculos de queries.py ao dashboard Dash existente.
Output: Dashboard funcional com abas analiticas, badge de cobertura, e modal de parse failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-depth/03-CONTEXT.md
@.planning/phases/03-analytics-depth/03-RESEARCH.md
@.planning/phases/03-analytics-depth/03-UI-SPEC.md
@.planning/phases/03-analytics-depth/03-01-SUMMARY.md
@.planning/phases/03-analytics-depth/03-02-SUMMARY.md

@helpertips/dashboard.py
@helpertips/queries.py
@tests/test_dashboard.py

<interfaces>
<!-- Funcoes criadas em Plans 01 e 02, disponiveis em queries.py -->

From helpertips/queries.py (novas — Plans 01-02):
```python
def get_heatmap_data(conn, liga=None, entrada=None, date_start=None, date_end=None) -> dict:
    """Retorna {z: list[list], x: list[str], y: list[str]} para go.Heatmap"""

def get_winrate_by_dow(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Retorna lista de {dia, dia_nome, greens, total, win_rate}"""

def calculate_equity_curve(signals_desc: list, stake: float, odd: float) -> dict:
    """Retorna {x, y_fixa, y_gale, colors, annotations}"""

def calculate_streaks(signals_desc: list) -> dict:
    """Retorna {current, current_type, max_green, max_red}"""

def get_gale_analysis(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Retorna lista de {tentativa, greens, total, win_rate}"""

def get_volume_by_day(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Retorna lista de {data, count}"""

def get_cross_dimensional(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Retorna lista de {liga, entrada, greens, total, win_rate} ordenada por win_rate DESC"""

def get_parse_failures_detail(conn, limit: int = 100) -> list:
    """Retorna lista de {raw_text, failure_reason, received_at}"""
```

From helpertips/queries.py (existentes):
```python
def get_signal_history(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Sinais em ORDER BY received_at DESC"""
```

From helpertips/store.py:
```python
def get_stats(conn) -> dict:
    """Retorna {total, greens, reds, pending, parse_failures, coverage}"""
```

From helpertips/dashboard.py (existentes):
```python
def make_kpi_card(title: str, value_id: str, color_class: str = "text-light"):
    """KPI card helper"""

def _build_comp_table(por_mercado: list[dict]) -> dbc.Table:
    """Tabela dbc.Table com breakdown"""

# Master callback update_dashboard: 14 Outputs, 8 Inputs
# app = dash.Dash(__name__, ...)
# app.layout = dbc.Container([...], fluid=True, className="py-3")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout — tabs, badge, modal + imports + callback separado para abas + modal callback + badge no master callback</name>
  <files>helpertips/dashboard.py</files>
  <read_first>
    - helpertips/dashboard.py (layout completo atual — 602 linhas, entender estrutura e posicao de insercao)
    - .planning/phases/03-analytics-depth/03-UI-SPEC.md (Component Inventory, Interaction Contract, Copywriting Contract, Spacing, Colors)
    - .planning/phases/03-analytics-depth/03-RESEARCH.md (Padrao 1: Callback separado, Padrao 6: Modal, Padrao 7: Badge no master, Anti-patterns)
    - .planning/phases/03-analytics-depth/03-CONTEXT.md (Decisoes D-01 a D-22)
    - helpertips/queries.py (assinaturas das funcoes novas para importar)
  </read_first>
  <action>
Modificar helpertips/dashboard.py com as seguintes alteracoes:

**1. Imports — adicionar ao bloco de imports existente:**
```python
from dash import dcc, html, Input, Output, State, callback, no_update, ctx
import plotly.graph_objects as go

from helpertips.queries import (
    get_filtered_stats,
    get_signal_history,
    get_distinct_values,
    calculate_roi,
    get_complementares_config,
    calculate_roi_complementares,
    # Novas funcoes — Phase 3
    get_heatmap_data,
    get_winrate_by_dow,
    calculate_equity_curve,
    calculate_streaks,
    get_gale_analysis,
    get_volume_by_day,
    get_cross_dimensional,
    get_parse_failures_detail,
)
from helpertips.store import get_stats
```

**2. Layout — Header: modificar o html.H2 existente para incluir badge:**
Substituir:
```python
html.H2("HelperTips — Futebol Virtual", className="text-center my-3"),
```
Por:
```python
html.H2([
    "HelperTips — Futebol Virtual ",
    dbc.Badge(
        id="badge-coverage",
        className="ms-2 fs-6",
        style={"cursor": "pointer"},
    ),
], className="text-center my-3"),
```

**3. Layout — Modal: adicionar ANTES do dcc.Interval (final do layout):**
```python
# Modal parse failures (OPER-01, D-21)
dbc.Modal([
    dbc.ModalHeader(dbc.ModalTitle("Falhas de Parse")),
    dbc.ModalBody(id="modal-failures-body"),
    dbc.ModalFooter(
        dbc.Button("Fechar", id="btn-close-modal", className="ms-auto", color="secondary")
    ),
], id="modal-parse-failures", is_open=False, size="lg", scrollable=True),
```

**4. Layout — Tabs: adicionar APOS o AG Grid row e ANTES do modal, conforme D-01:**
```python
# Analytics Tabs (D-01, D-02)
dbc.Row(
    dbc.Col(
        dbc.Tabs(
            id="tabs-analytics",
            active_tab="tab-temporal",
            className="mt-4",
            children=[
                dbc.Tab(label="Temporal", tab_id="tab-temporal", children=[
                    # Heatmap (ANAL-01) — ancora visual, full width, 400px
                    dbc.Card([
                        dbc.CardHeader(html.H5("Win Rate por Hora e Dia", className="fw-bold mb-0")),
                        dbc.CardBody(dcc.Graph(id="graph-heatmap", style={"height": "400px"})),
                    ], className="mt-3"),
                    # Equity curve (ANAL-05) — 350px
                    dbc.Card([
                        dbc.CardHeader(html.H5("Curva de Equity", className="fw-bold mb-0")),
                        dbc.CardBody(dcc.Graph(id="graph-equity", style={"height": "350px"})),
                    ], className="mt-3"),
                    # Win rate por dia da semana (ANAL-02) — 300px
                    dbc.Card([
                        dbc.CardHeader(html.H5("Win Rate por Dia da Semana", className="fw-bold mb-0")),
                        dbc.CardBody(dcc.Graph(id="graph-dow", style={"height": "300px"})),
                    ], className="mt-3"),
                ]),
                dbc.Tab(label="Gale & Streaks", tab_id="tab-gale", children=[
                    # Gale analysis (ANAL-07) — barras horizontais, 300px
                    dbc.Card([
                        dbc.CardHeader(html.H5("Recuperacao por Nivel de Gale", className="fw-bold mb-0")),
                        dbc.CardBody(dcc.Graph(id="graph-gale", style={"height": "300px"})),
                    ], className="mt-3"),
                    # Streaks (ANAL-06) — 3 mini-cards
                    dbc.Card([
                        dbc.CardHeader(html.H5("Streaks", className="fw-bold mb-0")),
                        dbc.CardBody(
                            dbc.Row([
                                dbc.Col(dbc.Card(dbc.CardBody([
                                    html.H6("Streak Atual", className="text-muted"),
                                    html.H3(id="kpi-streak-current", className="fw-bold"),
                                ]), className="text-center"), md=4),
                                dbc.Col(dbc.Card(dbc.CardBody([
                                    html.H6("Maior Sequencia GREEN", className="text-muted"),
                                    html.H3(id="kpi-streak-max-green", className="fw-bold text-success"),
                                ]), className="text-center"), md=4),
                                dbc.Col(dbc.Card(dbc.CardBody([
                                    html.H6("Maior Sequencia RED", className="text-muted"),
                                    html.H3(id="kpi-streak-max-red", className="fw-bold text-danger"),
                                ]), className="text-center"), md=4),
                            ]),
                        ),
                    ], className="mt-3"),
                ]),
                dbc.Tab(label="Volume", tab_id="tab-volume", children=[
                    # Volume chart (ANAL-08) — 280px
                    dbc.Card([
                        dbc.CardHeader(html.H5("Volume de Sinais", className="fw-bold mb-0")),
                        dbc.CardBody(dcc.Graph(id="graph-volume", style={"height": "280px"})),
                    ], className="mt-3"),
                    # Cross-dimensional breakdown (ANAL-04)
                    dbc.Card([
                        dbc.CardHeader(html.H5("Breakdown Cross-Dimensional", className="fw-bold mb-0")),
                        dbc.CardBody(id="table-cross-dimensional"),
                    ], className="mt-3"),
                ]),
            ],
        ),
        md=12,
    ),
    className="mb-4",
),
```

**5. Callback separado para abas (D-03 — lazy render via active_tab):**
Adicionar APOS o master callback existente:

```python
@callback(
    Output("graph-heatmap", "figure"),
    Output("graph-equity", "figure"),
    Output("graph-dow", "figure"),
    Output("graph-gale", "figure"),
    Output("kpi-streak-current", "children"),
    Output("kpi-streak-max-green", "children"),
    Output("kpi-streak-max-red", "children"),
    Output("graph-volume", "figure"),
    Output("table-cross-dimensional", "children"),
    Input("tabs-analytics", "active_tab"),
    Input("filter-liga", "value"),
    Input("filter-entrada", "value"),
    Input("filter-date", "start_date"),
    Input("filter-date", "end_date"),
    Input("stake-input", "value"),
    Input("odd-input", "value"),
    Input("interval-refresh", "n_intervals"),
)
def update_tabs(active_tab, liga, entrada, date_start, date_end, stake, odd, _n):
    """Callback separado para conteudo das abas. Lazy render: so calcula aba ativa."""
    conn = get_connection()
    try:
        stake = stake or 10.0
        odd = odd or 1.90

        if active_tab == "tab-temporal":
            # Heatmap (ANAL-01, D-15)
            heatmap_data = get_heatmap_data(conn, liga, entrada, date_start, date_end)
            heatmap_fig = _make_heatmap_figure(heatmap_data)

            # Equity curve (ANAL-05, D-05 a D-09)
            history = get_signal_history(conn, liga, entrada, date_start, date_end)
            equity_data = calculate_equity_curve(history, stake, odd)
            equity_fig = _make_equity_figure(equity_data)

            # Win rate por dia da semana (ANAL-02, D-16)
            dow_data = get_winrate_by_dow(conn, liga, entrada, date_start, date_end)
            dow_fig = _make_dow_figure(dow_data)

            return heatmap_fig, equity_fig, dow_fig, no_update, no_update, no_update, no_update, no_update, no_update

        elif active_tab == "tab-gale":
            # Gale analysis (ANAL-07, D-10 a D-12)
            gale_data = get_gale_analysis(conn, liga, entrada, date_start, date_end)
            gale_fig = _make_gale_figure(gale_data)

            # Streaks (ANAL-06, D-13)
            history = get_signal_history(conn, liga, entrada, date_start, date_end)
            streaks = calculate_streaks(history)
            current_label = _format_streak(streaks["current"], streaks["current_type"])
            max_green_label = f"{streaks['max_green']} wins" if streaks["max_green"] > 0 else "Sem dados"
            max_red_label = f"{streaks['max_red']} losses" if streaks["max_red"] > 0 else "Sem dados"

            return no_update, no_update, no_update, gale_fig, current_label, max_green_label, max_red_label, no_update, no_update

        elif active_tab == "tab-volume":
            # Volume (ANAL-08, D-18)
            vol_data = get_volume_by_day(conn, liga, entrada, date_start, date_end)
            vol_fig = _make_volume_figure(vol_data)

            # Cross-dimensional (ANAL-04, D-17)
            cross_data = get_cross_dimensional(conn, liga, entrada, date_start, date_end)
            cross_table = _build_cross_table(cross_data)

            return no_update, no_update, no_update, no_update, no_update, no_update, no_update, vol_fig, cross_table

        return (no_update,) * 9
    finally:
        conn.close()
```

**6. Helper functions para figuras (adicionar ANTES dos callbacks):**

```python
def _make_heatmap_figure(data: dict) -> go.Figure:
    if not any(cell is not None for row in data["z"] for cell in row):
        fig = go.Figure()
        fig.add_annotation(text="Dados insuficientes para o periodo selecionado",
                           xref="paper", yref="paper", x=0.5, y=0.5, showarrow=False,
                           font=dict(size=14, color="#aaa"))
        fig.update_layout(template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)")
        return fig
    fig = go.Figure(go.Heatmap(
        z=data["z"], x=data["x"], y=data["y"],
        colorscale="RdYlGn", zmin=0, zmax=100,
        colorbar=dict(title="Win Rate %"), hoverongaps=False,
    ))
    fig.update_layout(
        template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)",
        margin=dict(t=30, b=30, l=50, r=20),
        yaxis=dict(autorange="reversed"),
    )
    return fig
```

```python
def _make_equity_figure(data: dict) -> go.Figure:
    if not data["x"]:
        fig = go.Figure()
        fig.add_annotation(text="Nenhum sinal com resultado no periodo selecionado",
                           xref="paper", yref="paper", x=0.5, y=0.5, showarrow=False,
                           font=dict(size=14, color="#aaa"))
        fig.update_layout(template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)")
        return fig
    fig = go.Figure()
    # Stake Fixa (D-06: azul #17a2b8)
    fig.add_trace(go.Scatter(
        x=data["x"], y=data["y_fixa"], mode="lines+markers", name="Stake Fixa",
        line=dict(color="#17a2b8", width=2), marker=dict(color=data["colors"], size=8),
    ))
    # Gale (D-06: laranja #ffc107, linha tracejada)
    fig.add_trace(go.Scatter(
        x=data["x"], y=data["y_gale"], mode="lines+markers", name="Com Gale",
        line=dict(color="#ffc107", width=2, dash="dot"), marker=dict(color=data["colors"], size=8),
    ))
    # Breakeven (y=0)
    fig.add_trace(go.Scatter(
        x=[data["x"][0], data["x"][-1]], y=[0, 0], mode="lines", name="Breakeven",
        line=dict(color="#6c757d", dash="dash"), showlegend=True,
    ))
    # Annotations de streaks >= 5 (D-08)
    for ann in data.get("annotations", []):
        fig.add_annotation(x=ann["x"], y=ann["y"], text=ann["text"],
                           showarrow=True, arrowhead=2, font=dict(size=10, color="#ffc107"))
    fig.update_layout(
        template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)",
        margin=dict(t=30, b=30, l=60, r=20),
        xaxis_title="Sinal #", yaxis_title="Lucro (R$)",
        legend=dict(orientation="h", yanchor="bottom", y=1.02),
    )
    return fig
```

```python
def _make_dow_figure(data: list) -> go.Figure:
    if not data:
        fig = go.Figure()
        fig.add_annotation(text="Dados insuficientes para o periodo selecionado",
                           xref="paper", yref="paper", x=0.5, y=0.5, showarrow=False,
                           font=dict(size=14, color="#aaa"))
        fig.update_layout(template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)")
        return fig
    fig = go.Figure(go.Bar(
        x=[d["dia_nome"] for d in data], y=[d["win_rate"] for d in data],
        marker_color="#17a2b8",
        text=[f"{d['win_rate']:.1f}%" for d in data], textposition="auto",
    ))
    fig.update_layout(
        template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)",
        margin=dict(t=30, b=30, l=50, r=20),
        yaxis_title="Win Rate %", yaxis_range=[0, 100],
    )
    return fig
```

```python
def _make_gale_figure(data: list) -> go.Figure:
    if not data:
        fig = go.Figure()
        fig.add_annotation(text="Dados de tentativa indisponiveis",
                           xref="paper", yref="paper", x=0.5, y=0.5, showarrow=False,
                           font=dict(size=14, color="#aaa"))
        fig.update_layout(template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)")
        return fig
    labels = [f"{d['tentativa']}a tentativa" for d in data]
    win_rates = [d["win_rate"] for d in data]
    colors = ["#28a745", "#ffc107", "#fd7e14", "#dc3545"]
    bar_colors = [colors[min(i, len(colors)-1)] for i in range(len(data))]
    fig = go.Figure(go.Bar(
        x=win_rates, y=labels, orientation="h", marker_color=bar_colors,
        text=[f"{wr:.1f}% ({d['total']} sinais)" for wr, d in zip(win_rates, data)],
        textposition="inside",
    ))
    fig.update_layout(
        template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)",
        margin=dict(t=30, b=30, l=120, r=20),
        xaxis_title="Win Rate %", xaxis=dict(range=[0, 100], ticksuffix="%"),
    )
    return fig
```

```python
def _make_volume_figure(data: list) -> go.Figure:
    if not data:
        fig = go.Figure()
        fig.add_annotation(text="Nenhum sinal no periodo selecionado",
                           xref="paper", yref="paper", x=0.5, y=0.5, showarrow=False,
                           font=dict(size=14, color="#aaa"))
        fig.update_layout(template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)")
        return fig
    fig = go.Figure(go.Bar(
        x=[d["data"] for d in data], y=[d["count"] for d in data],
        marker_color="#17a2b8",
    ))
    fig.update_layout(
        template="plotly_dark", paper_bgcolor="rgba(0,0,0,0)", plot_bgcolor="rgba(0,0,0,0)",
        margin=dict(t=30, b=30, l=50, r=20),
        xaxis_title="Data", yaxis_title="Qtd. Sinais",
    )
    return fig
```

```python
def _format_streak(count: int, streak_type: str | None) -> str:
    if count == 0 or streak_type is None:
        return "Sem dados"
    return f"{count} {'wins' if streak_type == 'GREEN' else 'losses'}"
```

```python
def _build_cross_table(data: list) -> dbc.Table | html.P:
    if not data:
        return html.P("Aplique filtros para ver breakdown cruzado", className="text-muted text-center")
    header = html.Thead(html.Tr([
        html.Th("Liga"), html.Th("Entrada"),
        html.Th("Win Rate", className="text-end"),
        html.Th("Total Sinais", className="text-end"),
    ]))
    rows = []
    for d in data:
        row_class = "table-success" if d["win_rate"] > 60 else ""
        rows.append(html.Tr([
            html.Td(d["liga"]), html.Td(d["entrada"]),
            html.Td(f"{d['win_rate']:.1f}%", className="text-end"),
            html.Td(str(d["total"]), className="text-end"),
        ], className=row_class))
    return dbc.Table([header, html.Tbody(rows)], size="sm", bordered=False, hover=True, dark=True)
```

**7. Modal callback (D-21, D-22):**
```python
@callback(
    Output("modal-parse-failures", "is_open"),
    Output("modal-failures-body", "children"),
    Input("badge-coverage", "n_clicks"),
    Input("btn-close-modal", "n_clicks"),
    State("modal-parse-failures", "is_open"),
    prevent_initial_call=True,
)
def toggle_modal(badge_clicks, close_clicks, is_open):
    """Toggle modal de parse failures ao clicar no badge."""
    if ctx.triggered_id == "badge-coverage":
        conn = get_connection()
        try:
            failures = get_parse_failures_detail(conn, limit=50)
        finally:
            conn.close()
        if not failures:
            body = html.P("Nenhuma falha de parse registrada.", className="text-muted")
        else:
            header = html.Thead(html.Tr([html.Th("Texto"), html.Th("Motivo"), html.Th("Data")]))
            rows = [html.Tr([
                html.Td(f["raw_text"][:100], style={"maxWidth": "400px", "overflow": "hidden", "textOverflow": "ellipsis"}),
                html.Td(f["failure_reason"]),
                html.Td(str(f["received_at"])[:19]),
            ]) for f in failures]
            body = dbc.Table([header, html.Tbody(rows)], size="sm", bordered=False, hover=True, dark=True, responsive=True)
        return True, body
    return False, no_update
```

**8. Badge no master callback (D-19, D-20, D-22):**
Adicionar 2 novos Outputs ao master callback `update_dashboard`:
```python
Output("badge-coverage", "children"),
Output("badge-coverage", "color"),
```
Dentro do callback, APOS o bloco de stats e ANTES do return, adicionar:
```python
# Badge coverage (OPER-01, D-19, D-20) — sempre global, sem filtros
global_stats = get_stats(conn)
coverage = global_stats["coverage"]
badge_color = "success" if coverage >= 95 else ("warning" if coverage >= 90 else "danger")
badge_text = f"Cobertura: {coverage:.1f}%"
```
Adicionar `badge_text` e `badge_color` ao return statement do master callback (2 valores extras no final).

**Resumo das modificacoes em dashboard.py:**
1. Imports: adicionar 8 novas funcoes de queries.py + get_stats de store.py + State, no_update, ctx de dash
2. Header: badge dentro do H2
3. Layout: dbc.Tabs com 3 abas tematicas (apos AG Grid, antes do modal)
4. Layout: dbc.Modal para parse failures
5. 6 helper functions para figuras (_make_heatmap_figure, _make_equity_figure, _make_dow_figure, _make_gale_figure, _make_volume_figure, _format_streak, _build_cross_table)
6. Callback update_tabs: 9 Outputs, 8 Inputs, lazy render
7. Callback toggle_modal: 2 Outputs, 2 Inputs + 1 State
8. Master callback: +2 Outputs (badge)
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -c "from helpertips.dashboard import app; layout = app.layout; print('Layout OK')" && python3 -m pytest tests/test_dashboard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.py contem `id="tabs-analytics"`
    - dashboard.py contem `id="badge-coverage"`
    - dashboard.py contem `id="modal-parse-failures"`
    - dashboard.py contem `id="graph-heatmap"`
    - dashboard.py contem `id="graph-equity"`
    - dashboard.py contem `id="graph-dow"`
    - dashboard.py contem `id="graph-gale"`
    - dashboard.py contem `id="graph-volume"`
    - dashboard.py contem `id="kpi-streak-current"`
    - dashboard.py contem `id="kpi-streak-max-green"`
    - dashboard.py contem `id="kpi-streak-max-red"`
    - dashboard.py contem `id="table-cross-dimensional"`
    - dashboard.py contem `def update_tabs(`
    - dashboard.py contem `def toggle_modal(`
    - dashboard.py contem `from helpertips.store import get_stats`
    - dashboard.py contem `no_update` (lazy render)
    - dashboard.py contem `active_tab == "tab-temporal"` (lazy render)
    - `python3 -c "from helpertips.dashboard import app"` executa sem erro
  </acceptance_criteria>
  <done>Dashboard layout completo com 3 abas tematicas, badge de cobertura, modal de parse failures, callback separado para abas com lazy render, e badge integrado ao master callback.</done>
</task>

<task type="auto">
  <name>Task 2: Testes de layout e badge thresholds em test_dashboard.py</name>
  <files>tests/test_dashboard.py</files>
  <read_first>
    - tests/test_dashboard.py (estado atual — 204 linhas, entender padrao de testes)
    - helpertips/dashboard.py (layout atualizado na Task 1)
    - .planning/phases/03-analytics-depth/03-VALIDATION.md (IDs requeridos: tabs-analytics, badge-parser-coverage, modal-parse-failures)
  </read_first>
  <action>
Atualizar tests/test_dashboard.py:

**1. Atualizar test_layout_has_required_component_ids** — adicionar os novos IDs ao teste existente que verifica presenca de componentes no layout:
IDs a verificar: `tabs-analytics`, `badge-coverage`, `modal-parse-failures`, `graph-heatmap`, `graph-equity`, `graph-dow`, `graph-gale`, `graph-volume`, `kpi-streak-current`, `kpi-streak-max-green`, `kpi-streak-max-red`, `table-cross-dimensional`

**2. Adicionar test_coverage_badge_thresholds** — teste unitario que valida a logica de threshold do badge:
```python
def test_coverage_badge_thresholds():
    """Badge color: success >= 95%, warning >= 90%, danger < 90%."""
    # Testar a logica diretamente (nao precisa de DB)
    test_cases = [
        (100.0, "success"),
        (95.0, "success"),
        (94.9, "warning"),
        (90.0, "warning"),
        (89.9, "danger"),
        (50.0, "danger"),
        (0.0, "danger"),
    ]
    for coverage, expected_color in test_cases:
        color = "success" if coverage >= 95 else ("warning" if coverage >= 90 else "danger")
        assert color == expected_color, f"coverage={coverage}: expected {expected_color}, got {color}"
```

**3. Adicionar test_format_streak** — testar helper _format_streak:
```python
from helpertips.dashboard import _format_streak

def test_format_streak():
    assert _format_streak(5, "GREEN") == "5 wins"
    assert _format_streak(3, "RED") == "3 losses"
    assert _format_streak(0, None) == "Sem dados"
    assert _format_streak(0, "GREEN") == "Sem dados"
```
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_dashboard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_dashboard.py contem `"tabs-analytics"` na lista de IDs verificados
    - test_dashboard.py contem `"badge-coverage"` na lista de IDs verificados
    - test_dashboard.py contem `"modal-parse-failures"` na lista de IDs verificados
    - test_dashboard.py contem `def test_coverage_badge_thresholds(`
    - test_dashboard.py contem `def test_format_streak(`
    - Todos os testes passam
  </acceptance_criteria>
  <done>Testes de layout atualizados com novos IDs, teste de badge thresholds, e teste de _format_streak. Suite completa verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual do dashboard completo</name>
    <action>Verificacao visual — nenhuma automacao necessaria.</action>
  <done>Usuario aprovou o dashboard visualmente.</done>
  <what-built>Dashboard completo da Fase 3 com 3 abas analiticas (Temporal, Gale & Streaks, Volume), badge de cobertura do parser no header, e modal de parse failures.</what-built>
  <how-to-verify>
    1. Iniciar o dashboard: `cd /Users/luciano/helpertips && python3 -m helpertips.dashboard`
    2. Abrir http://localhost:8050 no browser
    3. Verificar que o header mostra "HelperTips" com badge de cobertura (cor verde/amarelo/vermelho)
    4. Clicar no badge — modal deve abrir com tabela de falhas de parse (ou mensagem "Nenhuma falha")
    5. Fechar o modal com botao "Fechar"
    6. Abaixo do AG Grid, verificar 3 abas: "Temporal", "Gale & Streaks", "Volume"
    7. Aba Temporal: heatmap, equity curve, win rate por dia da semana (podem mostrar "Dados insuficientes" se DB vazio)
    8. Aba Gale & Streaks: grafico de barras horizontais por tentativa + 3 mini-cards de streaks
    9. Aba Volume: grafico de barras de volume por dia + tabela cross-dimensional
    10. Mudar filtros (liga, entrada) — graficos das abas devem atualizar
    11. Os graficos existentes (bar chart, win rate por liga) devem estar intactos na secao fixa
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
cd /Users/luciano/helpertips && python3 -m pytest tests/ -x -q
# Suite completa passa (todos os testes existentes + novos)
</verification>

<success_criteria>
- Dashboard mostra 3 abas tematicas abaixo do AG Grid
- Cada aba renderiza graficos corretos (ou empty state quando DB vazio)
- Badge de cobertura no header com cor por threshold (verde/amarelo/vermelho)
- Click no badge abre modal com tabela de parse failures
- Callback separado para abas com lazy render (only active tab calculates)
- Filtros reativos atualizam graficos das abas
- Secao fixa (KPIs, filtros, ROI, AG Grid) permanece intacta
- Suite completa de testes passa
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-depth/03-03-SUMMARY.md`
</output>
