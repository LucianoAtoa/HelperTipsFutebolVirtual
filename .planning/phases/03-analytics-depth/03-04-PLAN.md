---
phase: 03-analytics-depth
plan: 04
type: execute
wave: 1
depends_on: ["03-03"]
files_modified:
  - helpertips/dashboard.py
  - tests/test_dashboard.py
autonomous: true
requirements: [ANAL-01, ANAL-02, ANAL-03, ANAL-04, ANAL-05, ANAL-06, ANAL-07, ANAL-08, OPER-01]
gap_closure: true

must_haves:
  truths:
    - "Usuario ve breakdown de win rate por periodo (1T/2T/FT) na aba Volume do dashboard"
    - "Dados de periodo atualizam quando filtros de liga/entrada/date sao alterados"
  artifacts:
    - path: "helpertips/dashboard.py"
      provides: "Componente table-periodo no layout + chamada get_winrate_by_periodo no callback update_tabs"
      contains: "table-periodo"
    - path: "tests/test_dashboard.py"
      provides: "Teste que table-periodo existe no layout"
      contains: "table-periodo"
  key_links:
    - from: "helpertips/dashboard.py:update_tabs"
      to: "helpertips/queries.py:get_winrate_by_periodo"
      via: "chamada no branch tab-volume do callback"
      pattern: "get_winrate_by_periodo\\(conn"
---

<objective>
Fechar gap ANAL-03: conectar `get_winrate_by_periodo` (ja implementada e testada em queries.py) ao dashboard, adicionando componente visual na aba Volume e wiring no callback `update_tabs`.

Purpose: A funcao de dados existe mas o usuario nao consegue ver o breakdown por periodo (1T/2T/FT) — este plano completa a entrega do requisito ANAL-03.
Output: Tabela de win rate por periodo visivel na aba Volume, atualizada com filtros reativos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-depth/03-VERIFICATION.md
@.planning/phases/03-analytics-depth/03-03-SUMMARY.md

<interfaces>
<!-- get_winrate_by_periodo ja existe e esta testada — executor NAO precisa implementa-la -->

From helpertips/queries.py (line 939):
```python
def get_winrate_by_periodo(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """
    Retorna list[dict] com chaves: periodo (str), greens (int), total (int), win_rate (float 0-100).
    Apenas periodos com ao menos 1 sinal resolvido sao incluidos.
    Exclui sinais com periodo NULL.
    """
```

From helpertips/dashboard.py — update_tabs callback (line 843-912):
- 9 Outputs atuais: graph-heatmap, graph-equity, graph-dow, graph-gale, kpi-streak-current, kpi-streak-max-green, kpi-streak-max-red, graph-volume, table-cross-dimensional
- 8 Inputs: tabs-analytics.active_tab, filter-liga.value, filter-entrada.value, filter-date.start_date, filter-date.end_date, stake-input.value, odd-input.value, interval-refresh.n_intervals
- Branch tab-volume (line 899-908): retorna vol_fig e cross_table, 7x no_update para as outras saidas

From helpertips/dashboard.py — tab-volume layout (line 405-416):
- Card "Volume de Sinais" com graph-volume
- Card "Breakdown Cross-Dimensional" com table-cross-dimensional
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar componente table-periodo no layout + Output no callback + wiring + teste</name>
  <files>helpertips/dashboard.py, tests/test_dashboard.py</files>
  <read_first>
    - helpertips/dashboard.py (linhas 355-420 para layout das abas, linhas 843-912 para callback update_tabs)
    - helpertips/queries.py (linhas 939-980 para assinatura e retorno de get_winrate_by_periodo)
    - tests/test_dashboard.py (linhas 68-117 para test_layout_has_required_component_ids)
  </read_first>
  <action>
**1. Layout — Adicionar card de periodo na aba Volume (dashboard.py, dentro de tab-volume):**

Inserir ANTES do card "Breakdown Cross-Dimensional" (entre o graph-volume e o table-cross-dimensional), um novo card:

```python
# Win rate por periodo (ANAL-03, D-02)
dbc.Card([
    dbc.CardHeader(html.H5("Win Rate por Periodo", className="fw-bold mb-0")),
    dbc.CardBody(id="table-periodo"),
], className="mt-3"),
```

**2. Callback — Adicionar Output e wiring (dashboard.py):**

2a. Adicionar novo Output na decoracao do callback `update_tabs` (linha ~843):
```python
Output("table-periodo", "children"),
```
Posicionar APOS `Output("table-cross-dimensional", "children")` (ultimo Output atual). Agora serao 10 Outputs totais.

2b. No branch `tab-temporal` (linha ~883), alterar o return de 9 para 10 valores:
```python
return heatmap_fig, equity_fig, dow_fig, no_update, no_update, no_update, no_update, no_update, no_update, no_update
```

2c. No branch `tab-gale` (linha ~897), alterar o return de 9 para 10 valores:
```python
return no_update, no_update, no_update, gale_fig, current_label, max_green_label, max_red_label, no_update, no_update, no_update
```

2d. No branch `tab-volume` (linha ~899-908), adicionar chamada a get_winrate_by_periodo e construir tabela:
```python
elif active_tab == "tab-volume":
    # Volume (ANAL-08, D-18)
    vol_data = get_volume_by_day(conn, liga, entrada, date_start, date_end)
    vol_fig = _make_volume_figure(vol_data)

    # Win rate por periodo (ANAL-03)
    periodo_data = get_winrate_by_periodo(conn, liga, entrada, date_start, date_end)
    periodo_table = _build_periodo_table(periodo_data)

    # Cross-dimensional (ANAL-04, D-17)
    cross_data = get_cross_dimensional(conn, liga, entrada, date_start, date_end)
    cross_table = _build_cross_table(cross_data)

    return no_update, no_update, no_update, no_update, no_update, no_update, no_update, vol_fig, cross_table, periodo_table
```

2e. No return default (linha ~910), alterar para 10 no_updates:
```python
return (no_update,) * 10
```

**3. Helper function — Criar _build_periodo_table (dashboard.py):**

Posicionar ANTES do callback update_tabs (na secao de helpers, proximo a `_build_cross_table`):

```python
def _build_periodo_table(periodo_data: list):
    """Constroi dbc.Table com win rate por periodo (1T/2T/FT). ANAL-03."""
    if not periodo_data:
        return html.P("Sem dados de periodo disponveis.", className="text-muted")
    header = html.Thead(html.Tr([
        html.Th("Periodo"),
        html.Th("Greens"),
        html.Th("Total"),
        html.Th("Win Rate"),
    ]))
    rows = []
    for row in periodo_data:
        wr = row["win_rate"]
        cls = "table-success" if wr >= 60 else ""
        rows.append(html.Tr([
            html.Td(row["periodo"]),
            html.Td(str(row["greens"])),
            html.Td(str(row["total"])),
            html.Td(f"{wr:.1f}%"),
        ], className=cls))
    body = html.Tbody(rows)
    return dbc.Table([header, body], size="sm", bordered=False, hover=True, dark=True, responsive=True)
```

**4. Teste — Adicionar "table-periodo" ao required_ids (tests/test_dashboard.py):**

No set `required_ids` dentro de `test_layout_has_required_component_ids` (linha ~70), adicionar:
```python
"table-periodo",
```
Na secao "# Phase 3 — Analytics Depth" do set (apos "table-cross-dimensional").
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_dashboard.py::test_layout_has_required_component_ids tests/test_queries.py::test_get_winrate_by_periodo -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `helpertips/dashboard.py` contem `id="table-periodo"` no layout da aba Volume
    - `helpertips/dashboard.py` contem `Output("table-periodo", "children")` na decoracao do callback update_tabs
    - `helpertips/dashboard.py` contem `get_winrate_by_periodo(conn,` no corpo do callback update_tabs, branch tab-volume
    - `helpertips/dashboard.py` contem `def _build_periodo_table(` como helper function
    - `tests/test_dashboard.py` contem `"table-periodo"` no set required_ids
    - `python3 -m pytest tests/ -x -q` passa todos os testes (132+ existentes + sem regressao)
  </acceptance_criteria>
  <done>Componente table-periodo visivel na aba Volume do dashboard, wired ao get_winrate_by_periodo via callback update_tabs, com filtros reativos e teste de layout atualizado. ANAL-03 completamente entregue ao usuario.</done>
</task>

</tasks>

<verification>
1. `grep -c "table-periodo" helpertips/dashboard.py` retorna >= 3 (layout id, Output, helper)
2. `grep "get_winrate_by_periodo(conn" helpertips/dashboard.py` retorna ao menos 1 match (chamada no callback)
3. `grep "table-periodo" tests/test_dashboard.py` retorna match no required_ids
4. `python3 -m pytest tests/ -x -q` — todos os testes passam sem regressao
</verification>

<success_criteria>
- ANAL-03 entregue: usuario ve tabela de win rate por periodo (1T/2T/FT) na aba Volume do dashboard
- Filtros de liga, entrada e data atualizam a tabela de periodo
- Todos os 132+ testes existentes continuam passando
- Novo ID "table-periodo" presente no teste de IDs do layout
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-analytics-depth/03-04-SUMMARY.md`
</output>
