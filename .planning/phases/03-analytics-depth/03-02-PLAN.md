---
phase: 03-analytics-depth
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - helpertips/queries.py
  - tests/test_queries.py
autonomous: true
requirements:
  - ANAL-03
  - ANAL-04
  - ANAL-07
  - ANAL-08
  - OPER-01
must_haves:
  truths:
    - "get_gale_analysis retorna taxa de recuperacao por tentativa (1-4), excluindo sinais sem tentativa"
    - "get_volume_by_day retorna contagem de sinais por dia para grafico de barras"
    - "get_cross_dimensional retorna breakdown de win rate por (liga, entrada)"
    - "get_parse_failures_detail retorna ultimas N falhas de parse para modal"
  artifacts:
    - path: "helpertips/queries.py"
      provides: "get_gale_analysis, get_volume_by_day, get_cross_dimensional, get_parse_failures_detail"
      exports: ["get_gale_analysis", "get_volume_by_day", "get_cross_dimensional", "get_parse_failures_detail"]
    - path: "tests/test_queries.py"
      provides: "Testes para as 4 funcoes novas"
      contains: "test_get_gale_analysis"
  key_links:
    - from: "helpertips/queries.py:get_gale_analysis"
      to: "signals.tentativa"
      via: "GROUP BY tentativa WHERE tentativa IS NOT NULL"
      pattern: "tentativa IS NOT NULL"
    - from: "helpertips/queries.py:get_cross_dimensional"
      to: "helpertips/queries.py:_build_where"
      via: "reutiliza WHERE builder existente"
      pattern: "_build_where\\("
---

<objective>
Implementar a segunda metade da camada de dados para a Fase 3: analise de Gale por nivel, volume de sinais por dia, breakdown cross-dimensional, e query de parse failures para o modal.

Purpose: Completar todas as funcoes de dados que o dashboard consumira nas abas "Gale & Streaks" e "Volume", e fornecer dados para o modal de falhas de parse (OPER-01).
Output: 4 funcoes exportadas em queries.py + testes unitarios completos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-depth/03-CONTEXT.md
@.planning/phases/03-analytics-depth/03-RESEARCH.md

@helpertips/queries.py
@tests/test_queries.py

<interfaces>
From helpertips/queries.py:
```python
def _build_where(liga=None, entrada=None, date_start=None, date_end=None):
    """Returns (where_sql: str, params: list) where where_sql starts with '1=1'"""

def get_signal_history(conn, liga=None, entrada=None, date_start=None,
                       date_end=None) -> list:
    """Retorna sinais em ORDER BY received_at DESC."""
```

From helpertips/db.py:
```python
def get_connection():
    """Retorna psycopg2 connection. Caller fecha no finally."""
```

Schema relevante (db.py ensure_schema):
- signals: liga, entrada, resultado, tentativa (SMALLINT nullable), received_at (TIMESTAMPTZ)
- parse_failures: raw_text, failure_reason, received_at
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes RED para get_gale_analysis, get_volume_by_day, get_cross_dimensional, get_parse_failures_detail</name>
  <files>tests/test_queries.py</files>
  <read_first>
    - tests/test_queries.py (estado atual — incluindo testes adicionados pelo Plan 01 se ja executado)
    - helpertips/queries.py (assinaturas existentes)
    - .planning/phases/03-analytics-depth/03-RESEARCH.md (Padrao 4: Gale Analysis, Query Parse Failures)
  </read_first>
  <behavior>
    - test_get_gale_analysis: 5 sinais com tentativa 1,1,2,2,3 e resultados variados → retorna lista com 3 dicts (tentativa 1,2,3) com win_rate correto
    - test_get_gale_analysis_excludes_null: sinais com tentativa=None excluidos → lista menor
    - test_get_gale_analysis_empty: sem sinais → lista vazia
    - test_get_volume_by_day: 5 sinais em 2 dias diferentes → retorna lista com 2 dicts {data, count}
    - test_get_volume_by_day_empty: sem sinais → lista vazia
    - test_get_cross_dimensional: sinais com 2 ligas e 2 entradas → retorna lista de dicts com {liga, entrada, win_rate, total} ordenada por win_rate DESC
    - test_get_cross_dimensional_empty: sem sinais → lista vazia
    - test_get_parse_failures_detail: inserir 3 parse_failures → retorna lista de dicts com {raw_text, failure_reason, received_at}
    - test_get_parse_failures_detail_limit: inserir 5, pedir limit=2 → retorna 2
    - test_get_parse_failures_detail_empty: sem falhas → lista vazia
  </behavior>
  <action>
Adicionar os seguintes testes em tests/test_queries.py:

Importar as novas funcoes:
```python
from helpertips.queries import (
    ...,
    get_gale_analysis,
    get_volume_by_day,
    get_cross_dimensional,
    get_parse_failures_detail,
)
```

**Testes com DB (usar fixture `db_conn` existente):**

1. `test_get_gale_analysis(db_conn)` — Inserir 5 sinais com tentativa e resultado:
   ```python
   # tentativa=1: 2 sinais (1 GREEN, 1 RED) → win_rate=50%
   # tentativa=2: 2 sinais (2 GREEN) → win_rate=100%
   # tentativa=3: 1 sinal (1 RED) → win_rate=0%
   result = get_gale_analysis(db_conn)
   assert len(result) == 3
   assert result[0]["tentativa"] == 1
   assert result[0]["win_rate"] == 50.0
   assert result[1]["tentativa"] == 2
   assert result[1]["win_rate"] == 100.0
   ```

2. `test_get_gale_analysis_excludes_null(db_conn)` — Inserir 2 sinais: um com tentativa=1, outro com tentativa=None. Resultado deve conter apenas 1 entry.

3. `test_get_gale_analysis_empty(db_conn)` — Sem sinais com tentativa: `assert get_gale_analysis(db_conn) == []`

4. `test_get_volume_by_day(db_conn)` — Inserir 5 sinais em 2 dias:
   ```python
   # Dia 1: 3 sinais, Dia 2: 2 sinais
   result = get_volume_by_day(db_conn)
   assert len(result) == 2
   assert result[0]["count"] + result[1]["count"] == 5
   ```

5. `test_get_volume_by_day_empty(db_conn)` — `assert get_volume_by_day(db_conn) == []`

6. `test_get_cross_dimensional(db_conn)` — Inserir sinais com combinacoes (Liga A, Over 2.5), (Liga A, Ambas Marcam), (Liga B, Over 2.5):
   ```python
   result = get_cross_dimensional(db_conn)
   assert len(result) >= 2
   # Cada entry tem liga, entrada, win_rate, total
   assert all(k in result[0] for k in ("liga", "entrada", "win_rate", "total"))
   # Ordenado por win_rate DESC
   assert result[0]["win_rate"] >= result[-1]["win_rate"]
   ```

7. `test_get_cross_dimensional_empty(db_conn)` — `assert get_cross_dimensional(db_conn) == []`

8. `test_get_parse_failures_detail(db_conn)` — Inserir 3 parse_failures via INSERT direto:
   ```python
   with db_conn.cursor() as cur:
       for i in range(3):
           cur.execute("INSERT INTO parse_failures (raw_text, failure_reason) VALUES (%s, %s)",
                       (f"msg {i}", "no_liga_match"))
   db_conn.commit()
   result = get_parse_failures_detail(db_conn)
   assert len(result) == 3
   assert "raw_text" in result[0]
   assert "failure_reason" in result[0]
   assert "received_at" in result[0]
   ```

9. `test_get_parse_failures_detail_limit(db_conn)` — Inserir 5, chamar com limit=2 → `assert len(result) == 2`

10. `test_get_parse_failures_detail_empty(db_conn)` — `assert get_parse_failures_detail(db_conn) == []`

Rodar testes — DEVEM FALHAR (ImportError).
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py::test_get_gale_analysis tests/test_queries.py::test_get_volume_by_day -x 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_queries.py contem `def test_get_gale_analysis(`
    - tests/test_queries.py contem `def test_get_gale_analysis_excludes_null(`
    - tests/test_queries.py contem `def test_get_gale_analysis_empty(`
    - tests/test_queries.py contem `def test_get_volume_by_day(`
    - tests/test_queries.py contem `def test_get_volume_by_day_empty(`
    - tests/test_queries.py contem `def test_get_cross_dimensional(`
    - tests/test_queries.py contem `def test_get_cross_dimensional_empty(`
    - tests/test_queries.py contem `def test_get_parse_failures_detail(`
    - tests/test_queries.py contem `def test_get_parse_failures_detail_limit(`
    - tests/test_queries.py contem `def test_get_parse_failures_detail_empty(`
    - Testes falham com ImportError ou NameError — RED phase
  </acceptance_criteria>
  <done>10 testes escritos, todos falhando (RED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar get_gale_analysis, get_volume_by_day, get_cross_dimensional, get_parse_failures_detail — GREEN</name>
  <files>helpertips/queries.py</files>
  <read_first>
    - helpertips/queries.py (estado atual — adicionar funcoes no final)
    - .planning/phases/03-analytics-depth/03-RESEARCH.md (Padrao 4: Gale Analysis, Query Parse Failures, Anti-patterns: tentativa NULL)
    - tests/test_queries.py (testes RED recem-criados)
  </read_first>
  <action>
Adicionar 4 funcoes ao final de helpertips/queries.py:

**1. get_gale_analysis(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:**
```python
def get_gale_analysis(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Taxa de recuperacao por nivel de tentativa (Gale). Exclui sinais sem tentativa."""
    where, params = _build_where(liga=liga, entrada=entrada,
                                 date_start=date_start, date_end=date_end)
    sql = f"""
        SELECT
            tentativa,
            COUNT(*) FILTER (WHERE resultado = 'GREEN') AS greens,
            COUNT(*) FILTER (WHERE resultado IN ('GREEN', 'RED')) AS total
        FROM signals
        WHERE resultado IN ('GREEN', 'RED')
          AND tentativa IS NOT NULL
          AND {where}
        GROUP BY tentativa
        ORDER BY tentativa
    """
    with conn.cursor() as cur:
        cur.execute(sql, params)
        rows = cur.fetchall()
    return [
        {
            "tentativa": int(t),
            "greens": int(g),
            "total": int(total),
            "win_rate": round(g / total * 100, 1) if total > 0 else 0.0,
        }
        for t, g, total in rows
    ]
```

**2. get_volume_by_day(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:**
```python
def get_volume_by_day(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Volume de sinais agrupado por dia. Retorna lista de {data, count}."""
    where, params = _build_where(liga=liga, entrada=entrada,
                                 date_start=date_start, date_end=date_end)
    sql = f"""
        SELECT
            DATE_TRUNC('day', received_at)::DATE AS data,
            COUNT(*) AS count
        FROM signals
        WHERE {where}
        GROUP BY 1
        ORDER BY 1
    """
    with conn.cursor() as cur:
        cur.execute(sql, params)
        rows = cur.fetchall()
    return [{"data": str(d), "count": int(c)} for d, c in rows]
```

**3. get_cross_dimensional(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:**
```python
def get_cross_dimensional(conn, liga=None, entrada=None, date_start=None, date_end=None) -> list:
    """Breakdown cross-dimensional: win rate e contagem por (liga, entrada). Ordenado por win_rate DESC."""
    where, params = _build_where(liga=liga, entrada=entrada,
                                 date_start=date_start, date_end=date_end)
    sql = f"""
        SELECT
            liga,
            entrada,
            COUNT(*) FILTER (WHERE resultado = 'GREEN') AS greens,
            COUNT(*) FILTER (WHERE resultado IN ('GREEN', 'RED')) AS total
        FROM signals
        WHERE resultado IN ('GREEN', 'RED') AND {where}
        GROUP BY liga, entrada
        HAVING COUNT(*) FILTER (WHERE resultado IN ('GREEN', 'RED')) > 0
        ORDER BY 3.0 / NULLIF(COUNT(*) FILTER (WHERE resultado IN ('GREEN', 'RED')), 0) DESC NULLS LAST
    """
    with conn.cursor() as cur:
        cur.execute(sql, params)
        rows = cur.fetchall()
    return [
        {
            "liga": liga_val,
            "entrada": entrada_val,
            "greens": int(g),
            "total": int(t),
            "win_rate": round(g / t * 100, 1) if t > 0 else 0.0,
        }
        for liga_val, entrada_val, g, t in rows
    ]
```

Nota: ORDER BY na query usa expressao SQL inline para ordenar por win_rate DESC. Alternativa mais limpa — ordenar em Python:
```python
    result = [...]
    result.sort(key=lambda r: r["win_rate"], reverse=True)
    return result
```

**4. get_parse_failures_detail(conn, limit: int = 100) -> list:**
```python
def get_parse_failures_detail(conn, limit: int = 100) -> list:
    """Retorna ultimas parse_failures para exibir no modal."""
    sql = """
        SELECT raw_text, failure_reason, received_at
        FROM parse_failures
        ORDER BY received_at DESC
        LIMIT %s
    """
    with conn.cursor() as cur:
        cur.execute(sql, (limit,))
        columns = [desc[0] for desc in cur.description]
        rows = cur.fetchall()
    return [dict(zip(columns, row)) for row in rows]
```

Nao alterar nenhuma funcao existente. Apenas adicionar ao final do arquivo.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - queries.py contem `def get_gale_analysis(`
    - queries.py contem `def get_volume_by_day(`
    - queries.py contem `def get_cross_dimensional(`
    - queries.py contem `def get_parse_failures_detail(`
    - get_gale_analysis tem `tentativa IS NOT NULL` no SQL
    - get_volume_by_day usa `DATE_TRUNC('day'`
    - get_cross_dimensional retorna lista ordenada por win_rate DESC
    - get_parse_failures_detail aceita parametro `limit`
    - Todos os testes passam (GREEN)
  </acceptance_criteria>
  <done>4 funcoes implementadas em queries.py. Todos os testes passam. Suite completa verde.</done>
</task>

</tasks>

<verification>
python3 -m pytest tests/test_queries.py -x -q
# Todos os testes passam (existentes + Plan 01 + Plan 02)
</verification>

<success_criteria>
- get_gale_analysis agrupa por tentativa e exclui NULL, retornando win_rate por nivel
- get_volume_by_day agrupa por dia com DATE_TRUNC e retorna contagem
- get_cross_dimensional retorna breakdown por (liga, entrada) ordenado por win_rate DESC
- get_parse_failures_detail retorna ultimas N falhas com limit parametrizavel
- Suite completa de testes passa
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-depth/03-02-SUMMARY.md`
</output>
