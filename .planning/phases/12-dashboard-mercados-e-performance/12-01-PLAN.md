---
phase: 12-dashboard-mercados-e-performance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dashboard.py
  - helpertips/dashboard.py
autonomous: true
requirements: [DASH-03, DASH-04]

must_haves:
  truths:
    - "Funcao _calcular_stakes_gale retorna T1=stake*pct, T2=T1*2, T3=T1*4, T4=T1*8"
    - "Funcao _agregar_por_entrada agrupa sinais por entrada com greens, reds, investido, lucro, taxa, roi"
    - "Funcao _get_colunas_visiveis retorna colunas corretas para cada modo (pct, qty, pl)"
    - "Funcao _build_config_card_mercado retorna dbc.Card com header e tabela de complementares"
    - "Funcao _build_performance_table retorna DataTable com style_data_conditional para cores"
  artifacts:
    - path: "tests/test_dashboard.py"
      provides: "Testes unitarios para logica pura das novas secoes"
      contains: "test_config_stakes_calculo"
    - path: "helpertips/dashboard.py"
      provides: "Funcoes helper puras para config mercados e performance"
      contains: "_calcular_stakes_gale"
  key_links:
    - from: "tests/test_dashboard.py"
      to: "helpertips/dashboard.py"
      via: "import das funcoes helper"
      pattern: "from helpertips.dashboard import _calcular_stakes_gale"
---

<objective>
Implementar as funcoes helper Python puras para as secoes Config Mercados (DASH-03) e Performance (DASH-04) com testes TDD red-first. Inclui: calculo de stakes T1-T4, agregacao de P&L por entrada, selecao de colunas por modo do toggle, e builders de componentes Dash para cards de config e tabela de performance.

Purpose: Isolar a logica pura (testavel sem banco/servidor) antes de integrar no callback master (Plan 02). Segue o padrao TDD estabelecido na Phase 11.
Output: Funcoes helper testadas em dashboard.py + testes expandidos em test_dashboard.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard-mercados-e-performance/12-CONTEXT.md
@.planning/phases/12-dashboard-mercados-e-performance/12-RESEARCH.md

@helpertips/dashboard.py
@tests/test_dashboard.py

<interfaces>
<!-- Funcoes existentes em queries.py que os helpers consumirao -->

From helpertips/queries.py:
```python
def get_mercado_config(conn, mercado_slug: str) -> dict | None:
    """Retorna dict com: id, slug, nome_display, odd_ref, ativo. None se nao existir."""

def get_complementares_config(conn, mercado_slug: str) -> list[dict]:
    """Retorna list[dict] com: id, slug, nome_display, percentual, odd_ref, regra_validacao."""

def calculate_roi(signals: list, stake: float, odd: float, gale_on: bool) -> dict:
    """Retorna dict com: profit, roi_pct, total_invested, greens, reds."""

def calculate_roi_complementares(signals: list, complementares_config: list[dict], stake: float, gale_on: bool) -> dict:
    """Retorna dict com: profit, roi_pct, total_invested, por_mercado (list[dict: slug, nome_display, greens, reds, lucro, investido])."""

def get_signals_com_placar(conn, liga=None, entrada=None, date_start=None, date_end=None, mercado_id=None) -> list[dict]:
    """Retorna sinais resolvidos (GREEN/RED) com mercado_slug. Ordenado por received_at ASC."""

def calculate_pl_por_entrada(signals, complementares_por_mercado, stake, odd_por_mercado, gale_on) -> list[dict]:
    """Retorna list[dict] por sinal: mercado_slug, resultado, tentativa, placar, liga, entrada,
       investido_principal, retorno_principal, lucro_principal, investido_comp, retorno_comp,
       lucro_comp, investido_total, lucro_total."""
```

From helpertips/dashboard.py (existente):
```python
_ENTRADA_SLUG_MAP = {"Over 2.5": "over_2_5", "Ambas Marcam": "ambas_marcam"}
def _entrada_para_slug(entrada: str | None) -> str | None: ...
def make_kpi_card(title: str, value_id: str, color_class: str = "text-light"): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes TDD red-first para helpers de config mercados e performance</name>
  <files>tests/test_dashboard.py</files>
  <read_first>
    - tests/test_dashboard.py (estado atual — 20 testes, patterns de collect_ids e imports)
    - helpertips/dashboard.py (funcoes existentes que serao estendidas)
    - .planning/phases/12-dashboard-mercados-e-performance/12-RESEARCH.md (secao Wave 0 Gaps para lista de testes necessarios)
  </read_first>
  <behavior>
    - test_config_stakes_calculo: _calcular_stakes_gale(100.0, 0.20) retorna (20.0, 40.0, 80.0, 160.0); _calcular_stakes_gale(50.0, 0.10) retorna (5.0, 10.0, 20.0, 40.0); _calcular_stakes_gale(10.0, 0.01) retorna (0.10, 0.20, 0.40, 0.80)
    - test_agregar_por_entrada_visao_geral: _agregar_por_entrada([{"entrada": "Over 2.5", "resultado": "GREEN", "investido_total": 10.0, "retorno_principal": 16.5, "retorno_comp": 2.0, "lucro_total": 8.5}, {"entrada": "Over 2.5", "resultado": "RED", "investido_total": 10.0, "retorno_principal": 0, "retorno_comp": 0, "lucro_total": -10.0}, {"entrada": "Ambas Marcam", "resultado": "GREEN", "investido_total": 10.0, "retorno_principal": 15.0, "retorno_comp": 1.5, "lucro_total": 6.5}]) retorna 2 grupos: Over 2.5 com greens=1, reds=1, Ambas Marcam com greens=1, reds=0
    - test_agregar_por_entrada_vazio: _agregar_por_entrada([]) retorna []
    - test_performance_toggle_colunas: _get_colunas_visiveis("pct") contem "taxa_green", "taxa_red", "roi"; _get_colunas_visiveis("qty") contem "greens", "reds", "total"; _get_colunas_visiveis("pl") contem "investido", "retorno", "lucro", "roi"
    - test_layout_has_required_component_ids: expandir required_ids com "config-mercados-container", "perf-toggle-view", "perf-table", "phase13-placeholder"
  </behavior>
  <action>
Adicionar ao final de tests/test_dashboard.py os seguintes testes. Usar try/except ImportError para imports das novas funcoes (mesmo padrao de _resolve_periodo existente no arquivo):

```python
try:
    from helpertips.dashboard import _calcular_stakes_gale
    _HAS_STAKES = True
except ImportError:
    _calcular_stakes_gale = None
    _HAS_STAKES = False

try:
    from helpertips.dashboard import _agregar_por_entrada
    _HAS_AGREGAR = True
except ImportError:
    _agregar_por_entrada = None
    _HAS_AGREGAR = False

try:
    from helpertips.dashboard import _get_colunas_visiveis
    _HAS_COLUNAS = True
except ImportError:
    _get_colunas_visiveis = None
    _HAS_COLUNAS = False
```

Testes a criar:

1. `test_config_stakes_calculo` — assert _HAS_STAKES primeiro, depois testar 3 cenarios:
   - _calcular_stakes_gale(100.0, 0.20) == (20.0, 40.0, 80.0, 160.0)
   - _calcular_stakes_gale(50.0, 0.10) == (5.0, 10.0, 20.0, 40.0)
   - _calcular_stakes_gale(10.0, 0.01) == pytest.approx((0.10, 0.20, 0.40, 0.80))

2. `test_agregar_por_entrada_visao_geral` — assert _HAS_AGREGAR primeiro, depois:
   - Passar lista de 3 dicts simulando saida de calculate_pl_por_entrada (2x Over 2.5 sendo 1 GREEN + 1 RED, 1x Ambas Marcam GREEN)
   - Verificar len(resultado) == 2
   - Verificar grupo Over 2.5: greens=1, reds=1
   - Verificar grupo Ambas Marcam: greens=1, reds=0
   - Verificar que cada grupo tem chaves: entrada, greens, reds, investido, retorno, lucro, taxa, roi

3. `test_agregar_por_entrada_vazio` — assert _HAS_AGREGAR, _agregar_por_entrada([]) == []

4. `test_performance_toggle_colunas` — assert _HAS_COLUNAS, depois:
   - cols_pct = _get_colunas_visiveis("pct"); assert "taxa_green" in cols_pct and "roi" in cols_pct
   - cols_qty = _get_colunas_visiveis("qty"); assert "greens" in cols_qty and "total" in cols_qty
   - cols_pl = _get_colunas_visiveis("pl"); assert "investido" in cols_pl and "lucro" in cols_pl

5. Expandir `test_layout_has_required_component_ids` — adicionar ao set `required_ids`:
   - "config-mercados-container"
   - "perf-toggle-view"
   - "perf-table"
   - "phase13-placeholder"

Rodar `python3 -m pytest tests/test_dashboard.py -x -q` — os novos testes devem FALHAR (RED) porque as funcoes nao existem ainda em dashboard.py. Os testes existentes devem continuar passando.

Commit RED: `test(12-01): add failing tests for config mercados and performance helpers`
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_dashboard.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_dashboard.py contem `def test_config_stakes_calculo`
    - tests/test_dashboard.py contem `def test_agregar_por_entrada_visao_geral`
    - tests/test_dashboard.py contem `def test_agregar_por_entrada_vazio`
    - tests/test_dashboard.py contem `def test_performance_toggle_colunas`
    - tests/test_dashboard.py contem `"config-mercados-container"` no set required_ids
    - tests/test_dashboard.py contem `"perf-toggle-view"` no set required_ids
    - tests/test_dashboard.py contem `"perf-table"` no set required_ids
    - tests/test_dashboard.py contem `"phase13-placeholder"` no set required_ids
    - Testes existentes (17 originais) continuam passando
    - Novos testes falham com assert (RED phase do TDD)
  </acceptance_criteria>
  <done>Testes RED criados cobrindo _calcular_stakes_gale, _agregar_por_entrada, _get_colunas_visiveis e IDs dos novos componentes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar helpers puros + placeholders no layout para testes GREEN</name>
  <files>helpertips/dashboard.py</files>
  <read_first>
    - helpertips/dashboard.py (estado atual completo — layout, callback, helpers existentes)
    - tests/test_dashboard.py (testes RED da Task 1 — contratos a satisfazer)
    - helpertips/queries.py linhas 379-450 (get_complementares_config e get_mercado_config — assinaturas e retorno)
    - .planning/phases/12-dashboard-mercados-e-performance/12-CONTEXT.md (decisoes D-01 a D-09)
    - .planning/phases/12-dashboard-mercados-e-performance/12-RESEARCH.md (patterns 1-5, pitfalls)
  </read_first>
  <behavior>
    - _calcular_stakes_gale(stake, pct) retorna (T1, T2, T3, T4) onde T1=stake*pct, T2=T1*2, T3=T1*4, T4=T1*8
    - _agregar_por_entrada(pl_lista) agrupa por entrada, retorna lista de dicts com greens, reds, investido, retorno, lucro, taxa, roi
    - _get_colunas_visiveis(modo) retorna lista de nomes de colunas para cada modo
    - Layout contem config-mercados-container, perf-toggle-view, perf-table, phase13-placeholder
  </behavior>
  <action>
Adicionar ao helpertips/dashboard.py as seguintes funcoes e componentes. TODAS as funcoes sao Python puro sem acesso ao banco.

**1. Funcao _calcular_stakes_gale (per D-03):**
```python
def _calcular_stakes_gale(stake_base: float, percentual: float) -> tuple[float, float, float, float]:
    """Retorna (T1, T2, T3, T4) para um complementar dado stake base e percentual."""
    t1 = stake_base * percentual
    return t1, t1 * 2, t1 * 4, t1 * 8
```
Colocar na secao Helpers, apos `_entrada_para_slug`.

**2. Funcao _agregar_por_entrada (per D-06 — visao geral):**
```python
from collections import defaultdict

def _agregar_por_entrada(pl_lista: list[dict]) -> list[dict]:
    """Agrega P&L por entrada (visao geral — mercado 'Todos')."""
    if not pl_lista:
        return []
    grupos: dict[str, dict] = defaultdict(lambda: {
        "greens": 0, "reds": 0,
        "investido": 0.0, "retorno": 0.0, "lucro": 0.0,
    })
    for row in pl_lista:
        g = grupos[row.get("entrada") or "?"]
        g["greens"] += 1 if row.get("resultado") == "GREEN" else 0
        g["reds"] += 1 if row.get("resultado") == "RED" else 0
        g["investido"] += row.get("investido_total", 0.0)
        g["retorno"] += row.get("retorno_principal", 0.0) + row.get("retorno_comp", 0.0)
        g["lucro"] += row.get("lucro_total", 0.0)
    result = []
    for entrada_nome, g in grupos.items():
        total = g["greens"] + g["reds"]
        taxa = g["greens"] / total * 100 if total > 0 else 0
        roi = g["lucro"] / g["investido"] * 100 if g["investido"] > 0 else 0
        result.append({
            "entrada": entrada_nome,
            "greens": g["greens"],
            "reds": g["reds"],
            "total": total,
            "investido": round(g["investido"], 2),
            "retorno": round(g["retorno"], 2),
            "lucro": round(g["lucro"], 2),
            "taxa_green": round(taxa, 1),
            "taxa_red": round(100 - taxa, 1) if total > 0 else 0,
            "roi": round(roi, 1),
        })
    return result
```

**3. Funcao _get_colunas_visiveis (per D-05):**
```python
COLUNAS_SEMPRE = ["entrada"]
COLUNAS_PCT = COLUNAS_SEMPRE + ["taxa_green", "taxa_red", "roi"]
COLUNAS_QTY = COLUNAS_SEMPRE + ["greens", "reds", "total"]
COLUNAS_PL = COLUNAS_SEMPRE + ["investido", "retorno", "lucro", "roi"]

def _get_colunas_visiveis(modo: str) -> list[str]:
    """Retorna lista de colunas visiveis para o modo do toggle de performance."""
    mapa = {"pct": COLUNAS_PCT, "qty": COLUNAS_QTY, "pl": COLUNAS_PL}
    return mapa.get(modo, COLUNAS_PCT)
```

**4. Adicionar import no topo:**
```python
from collections import defaultdict
from dash import dash_table  # para DataTable no Plan 02
```

**5. Adicionar IDs placeholder no layout (per D-08, D-09):**
Substituir `html.Div(id="analytics-placeholder")` (L242 atual) por:

```python
# Secoes Phase 12: Config Mercados + Performance (D-08)
html.Div(id="config-mercados-container", className="mb-3"),

# Toggle de visualizacao + Tabela de Performance (D-04)
dbc.Card(dbc.CardBody([
    html.H6("Performance das Entradas", className="card-title text-muted"),
    dbc.RadioItems(
        id="perf-toggle-view",
        options=[
            {"label": "Percentual", "value": "pct"},
            {"label": "Quantidade", "value": "qty"},
            {"label": "P&L (R$)", "value": "pl"},
        ],
        value="pct",
        inline=True,
        input_class_name="btn-check",
        label_class_name="btn btn-outline-secondary btn-sm",
        label_checked_class_name="active",
        className="mb-3",
    ),
    html.Div(id="perf-table"),
]), className="mb-3"),

# Placeholder para Phase 13 (D-08)
html.Div(id="phase13-placeholder"),
```

IMPORTANTE — Reordenar o layout conforme D-09: mover o AG Grid de historico para DEPOIS das novas secoes. Ordem final no layout:
1. Header
2. Filtros Globais
3. KPI Cards
4. Card Simulacao
5. config-mercados-container (NOVO)
6. Card Performance com perf-toggle-view + perf-table (NOVO)
7. phase13-placeholder (NOVO)
8. AG Grid historico (MOVIDO para ca)
9. Modal parse failures
10. dcc.Interval

Rodar `python3 -m pytest tests/test_dashboard.py -x -q` — TODOS os testes devem passar (GREEN).

Commit GREEN: `feat(12-01): implement config/performance helpers and layout placeholders`
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_dashboard.py -x -q && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/dashboard.py contem `def _calcular_stakes_gale(stake_base: float, percentual: float)`
    - helpertips/dashboard.py contem `def _agregar_por_entrada(pl_lista: list[dict])`
    - helpertips/dashboard.py contem `def _get_colunas_visiveis(modo: str)`
    - helpertips/dashboard.py contem `COLUNAS_PCT` e `COLUNAS_QTY` e `COLUNAS_PL`
    - helpertips/dashboard.py contem `id="config-mercados-container"`
    - helpertips/dashboard.py contem `id="perf-toggle-view"`
    - helpertips/dashboard.py contem `id="perf-table"`
    - helpertips/dashboard.py contem `id="phase13-placeholder"`
    - helpertips/dashboard.py contem `from collections import defaultdict`
    - AG Grid historico aparece DEPOIS de phase13-placeholder no layout
    - python3 -m pytest tests/test_dashboard.py -x -q retorna 0 (todos passam)
    - python3 -m pytest tests/ -x -q retorna 0 (suite completa verde)
  </acceptance_criteria>
  <done>Funcoes helper puras implementadas, IDs de componentes no layout, testes GREEN, suite completa passando</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_dashboard.py -x -q` — todos os testes passam (incluindo os novos)
- `python3 -m pytest tests/ -x -q` — suite completa verde
- `python3 -c "from helpertips.dashboard import _calcular_stakes_gale, _agregar_por_entrada, _get_colunas_visiveis; print('OK')"` — imprime OK
- `grep -c "config-mercados-container\|perf-toggle-view\|perf-table\|phase13-placeholder" helpertips/dashboard.py` — 4+ ocorrencias
</verification>

<success_criteria>
- Funcoes _calcular_stakes_gale, _agregar_por_entrada, _get_colunas_visiveis existem e sao testadas
- Layout contem os 4 novos IDs (config-mercados-container, perf-toggle-view, perf-table, phase13-placeholder)
- Ordem do layout segue D-09: filtros -> KPIs -> simulacao -> config -> performance -> placeholder 13 -> historico
- Suite completa de testes verde
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-mercados-e-performance/12-01-SUMMARY.md`
</output>
