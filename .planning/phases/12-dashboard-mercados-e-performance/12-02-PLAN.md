---
phase: 12-dashboard-mercados-e-performance
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - helpertips/dashboard.py
  - tests/test_dashboard.py
autonomous: false
requirements: [DASH-03, DASH-04]

must_haves:
  truths:
    - "Painel de configuracao mostra principal (odd, stake, progressao) e tabela de complementares com stakes T1-T4 para Over 2.5 e Ambas Marcam"
    - "Toggle Percentual/Quantidade/P&L muda a apresentacao da tabela de performance"
    - "Tabela de performance exibe greens, reds, taxa, investido, retorno, P&L e ROI para cada entrada"
    - "Visao por mercado filtra mostrando principal + cada complementar como linha separada"
    - "Stakes T1-T4 nos cards de config atualizam quando usuario muda stake no card de simulacao"
  artifacts:
    - path: "helpertips/dashboard.py"
      provides: "Dashboard completo com secoes config mercados e performance"
      contains: "_build_config_mercados_section"
    - path: "tests/test_dashboard.py"
      provides: "Testes de integracao para callback master estendido"
      contains: "test_build_config_card"
  key_links:
    - from: "helpertips/dashboard.py::update_dashboard"
      to: "helpertips/queries.py::get_mercado_config"
      via: "chamada dentro do callback master"
      pattern: "get_mercado_config\\(conn"
    - from: "helpertips/dashboard.py::update_dashboard"
      to: "helpertips/queries.py::get_signals_com_placar"
      via: "chamada adicional para dados de P&L"
      pattern: "get_signals_com_placar\\(conn"
    - from: "helpertips/dashboard.py::config-mercados-container"
      to: "helpertips/dashboard.py::update_dashboard"
      via: "Output do callback master"
      pattern: 'Output\\("config-mercados-container"'
    - from: "helpertips/dashboard.py::perf-table"
      to: "helpertips/dashboard.py::update_dashboard"
      via: "Output do callback master"
      pattern: 'Output\\("perf-table"'
---

<objective>
Integrar as secoes Config Mercados (DASH-03) e Performance (DASH-04) no callback master, construindo os componentes reativos que atualizam com filtros globais e parametros de simulacao. Estende o callback existente com novos Outputs que renderizam cards de config e tabela de performance.

Purpose: Completar a Phase 12 — usuario ve configuracao ativa dos mercados e analisa P&L por entrada em detalhe.
Output: Dashboard funcional com Config Mercados + Tabela Performance + verificacao visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard-mercados-e-performance/12-CONTEXT.md
@.planning/phases/12-dashboard-mercados-e-performance/12-RESEARCH.md
@.planning/phases/12-dashboard-mercados-e-performance/12-01-SUMMARY.md

@helpertips/dashboard.py
@helpertips/queries.py
@tests/test_dashboard.py

<interfaces>
<!-- Funcoes de Plan 01 ja disponiveis em dashboard.py -->
From helpertips/dashboard.py (apos Plan 01):
```python
def _calcular_stakes_gale(stake_base: float, percentual: float) -> tuple[float, float, float, float]:
    """Retorna (T1, T2, T3, T4)."""

def _agregar_por_entrada(pl_lista: list[dict]) -> list[dict]:
    """Agrega por entrada. Retorna list[dict] com: entrada, greens, reds, total, investido, retorno, lucro, taxa_green, taxa_red, roi."""

def _get_colunas_visiveis(modo: str) -> list[str]:
    """Retorna nomes de colunas para o modo do toggle."""

COLUNAS_PCT, COLUNAS_QTY, COLUNAS_PL  # constantes de colunas
```

<!-- Queries existentes em queries.py -->
From helpertips/queries.py:
```python
def get_mercado_config(conn, mercado_slug: str) -> dict | None:
    """Retorna dict com: id, slug, nome_display, odd_ref, ativo."""

def get_complementares_config(conn, mercado_slug: str) -> list[dict]:
    """Retorna list[dict] com: id, slug, nome_display, percentual, odd_ref, regra_validacao."""

def get_signals_com_placar(conn, liga=None, entrada=None, date_start=None, date_end=None, mercado_id=None) -> list[dict]:
    """Sinais resolvidos com mercado_slug. Ordenado ASC."""

def calculate_pl_por_entrada(signals, complementares_por_mercado, stake, odd_por_mercado, gale_on) -> list[dict]:
    """P&L detalhado por sinal."""

def calculate_roi(signals, stake, odd, gale_on) -> dict:
    """ROI principal."""

def calculate_roi_complementares(signals, complementares_config, stake, gale_on) -> dict:
    """ROI complementares. por_mercado = list[dict: slug, nome_display, greens, reds, lucro, investido]."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Builder functions + extensao do callback master com novos Outputs</name>
  <files>helpertips/dashboard.py, tests/test_dashboard.py</files>
  <read_first>
    - helpertips/dashboard.py (estado apos Plan 01 — helpers, layout com IDs placeholder, callback master atual com 10 Outputs)
    - helpertips/queries.py (assinaturas de get_mercado_config, get_complementares_config, get_signals_com_placar, calculate_pl_por_entrada, calculate_roi_complementares — linhas 379-760)
    - tests/test_dashboard.py (testes existentes apos Plan 01)
    - .planning/phases/12-dashboard-mercados-e-performance/12-CONTEXT.md (D-01 a D-09 — decisoes locked)
    - .planning/phases/12-dashboard-mercados-e-performance/12-RESEARCH.md (patterns 1-5, pitfalls 1-6)
  </read_first>
  <action>
**Parte A: Adicionar imports necessarios em dashboard.py**

Adicionar ao bloco de imports de `helpertips.queries`:
```python
from helpertips.queries import (
    calculate_pl_por_entrada,
    calculate_roi,
    calculate_roi_complementares,
    calculate_streaks,
    get_complementares_config,
    get_distinct_values,
    get_filtered_stats,
    get_mercado_config,          # NOVO
    get_parse_failures_detail,
    get_signal_history,
    get_signals_com_placar,      # NOVO
)
```

Adicionar import de dash_table:
```python
from dash import Input, Output, State, callback, ctx, dash_table, dcc, html, no_update
```

**Parte B: Funcao _build_config_card_mercado (per D-01, D-02, D-03)**

Adicionar na secao Helpers:
```python
MERCADOS_CONFIG = [
    ("over_2_5", "Over 2.5"),
    ("ambas_marcam", "Ambas Marcam"),
]

def _build_config_card_mercado(nome: str, odd_ref: float, stake: float, comps: list[dict]) -> dbc.Card:
    """Constroi card read-only de config de um mercado com tabela de complementares e stakes T1-T4."""
    header_text = f"Odd ref: {odd_ref:.2f} | Stake base: R$ {stake:.2f} | Progressao: 1x 2x 4x 8x"
    rows = []
    for comp in comps:
        pct = float(comp["percentual"])
        t1, t2, t3, t4 = _calcular_stakes_gale(stake, pct)
        rows.append(html.Tr([
            html.Td(comp["nome_display"]),
            html.Td(f"{pct * 100:.0f}%"),
            html.Td(f"{float(comp['odd_ref']):.2f}"),
            html.Td(f"R$ {t1:.2f}"),
            html.Td(f"R$ {t2:.2f}"),
            html.Td(f"R$ {t3:.2f}"),
            html.Td(f"R$ {t4:.2f}"),
        ]))
    thead = html.Thead(html.Tr([
        html.Th("Complementar"), html.Th("%"), html.Th("Odd Ref"),
        html.Th("T1"), html.Th("T2"), html.Th("T3"), html.Th("T4"),
    ]))
    return dbc.Card([
        dbc.CardHeader(html.H6(nome, className="mb-0")),
        dbc.CardBody([
            html.P(header_text, className="text-muted small"),
            dbc.Table([thead, html.Tbody(rows)], bordered=True, dark=True, hover=True, size="sm"),
        ]),
    ], className="mb-3")


def _build_config_mercados_section(conn, stake: float) -> list:
    """Retorna lista de dbc.Card para cada mercado ativo (per D-01)."""
    cards = [html.H5("Configuracao dos Mercados", className="text-muted mb-3")]
    for slug, nome in MERCADOS_CONFIG:
        config = get_mercado_config(conn, slug)
        if config is None:
            continue
        comps = get_complementares_config(conn, slug)
        cards.append(_build_config_card_mercado(
            nome, float(config["odd_ref"]), stake, comps
        ))
    return cards
```

**Parte C: Funcao _build_performance_section (per D-04, D-05, D-06, D-07)**

```python
def _build_performance_section(
    conn, history: list, entrada: str | None, stake: float, odd: float,
    gale_on: bool, toggle_mode: str,
) -> dash_table.DataTable | html.P:
    """Constroi DataTable de performance com colunas condicionais por toggle_mode."""
    if not history:
        return html.P("Sem dados para o periodo selecionado.", className="text-muted")

    if entrada:
        # Visao por mercado (D-06): principal + cada complementar como linha separada
        slug = _entrada_para_slug(entrada)
        if slug:
            comp_config = get_complementares_config(conn, slug)
            roi_princ = calculate_roi(history, stake, odd, gale_on)
            roi_comp = calculate_roi_complementares(history, comp_config, stake, gale_on)

            rows = []
            # Linha do principal
            p_total = roi_princ["greens"] + roi_princ["reds"]
            p_taxa = roi_princ["greens"] / p_total * 100 if p_total > 0 else 0
            p_roi = roi_princ["profit"] / roi_princ["total_invested"] * 100 if roi_princ["total_invested"] > 0 else 0
            rows.append({
                "entrada": f"{entrada} (Principal)",
                "greens": roi_princ["greens"],
                "reds": roi_princ["reds"],
                "total": p_total,
                "investido": round(roi_princ["total_invested"], 2),
                "retorno": round(roi_princ["total_invested"] + roi_princ["profit"], 2),
                "lucro": round(roi_princ["profit"], 2),
                "taxa_green": round(p_taxa, 1),
                "taxa_red": round(100 - p_taxa, 1) if p_total > 0 else 0,
                "roi": round(p_roi, 1),
            })
            # Linhas por complementar
            for pm in roi_comp.get("por_mercado", []):
                c_total = pm["greens"] + pm["reds"]
                c_taxa = pm["greens"] / c_total * 100 if c_total > 0 else 0
                c_roi = pm["lucro"] / pm["investido"] * 100 if pm["investido"] > 0 else 0
                rows.append({
                    "entrada": pm["nome_display"],
                    "greens": pm["greens"],
                    "reds": pm["reds"],
                    "total": c_total,
                    "investido": round(pm["investido"], 2),
                    "retorno": round(pm["investido"] + pm["lucro"], 2),
                    "lucro": round(pm["lucro"], 2),
                    "taxa_green": round(c_taxa, 1),
                    "taxa_red": round(100 - c_taxa, 1) if c_total > 0 else 0,
                    "roi": round(c_roi, 1),
                })
        else:
            rows = []
    else:
        # Visao geral (D-06): agrupado por entrada
        signals_placar = get_signals_com_placar(conn, entrada=entrada)
        # Obter configs de complementares e odds para todos os mercados
        comps_por_mercado = {}
        odds_por_mercado = {}
        for slug, nome in MERCADOS_CONFIG:
            cfg = get_mercado_config(conn, slug)
            if cfg:
                odds_por_mercado[slug] = float(cfg["odd_ref"])
                comps_por_mercado[slug] = get_complementares_config(conn, slug)
        pl_lista = calculate_pl_por_entrada(signals_placar, comps_por_mercado, stake, odds_por_mercado, gale_on)
        rows = _agregar_por_entrada(pl_lista)

    if not rows:
        return html.P("Sem dados para o periodo selecionado.", className="text-muted")

    # Mapas de nomes de colunas para headers legíveis
    col_headers = {
        "entrada": "Entrada",
        "greens": "Greens",
        "reds": "Reds",
        "total": "Total",
        "taxa_green": "Taxa Green (%)",
        "taxa_red": "Taxa Red (%)",
        "investido": "Investido (R$)",
        "retorno": "Retorno (R$)",
        "lucro": "P&L (R$)",
        "roi": "ROI (%)",
    }
    colunas_visiveis = _get_colunas_visiveis(toggle_mode)
    columns = [{"name": col_headers.get(c, c), "id": c} for c in colunas_visiveis]

    # Formatar valores monetarios nas rows para display
    for row in rows:
        for key in ("investido", "retorno", "lucro"):
            if key in row:
                row[key] = round(row[key], 2)

    return dash_table.DataTable(
        data=rows,
        columns=columns,
        style_header={"backgroundColor": "#303030", "color": "white", "fontWeight": "bold"},
        style_cell={"backgroundColor": "#222", "color": "white", "border": "1px solid #444", "textAlign": "center"},
        style_data_conditional=[
            {"if": {"filter_query": "{lucro} > 0", "column_id": "lucro"}, "color": "#28a745", "fontWeight": "bold"},
            {"if": {"filter_query": "{lucro} < 0", "column_id": "lucro"}, "color": "#dc3545", "fontWeight": "bold"},
            {"if": {"filter_query": "{roi} > 0", "column_id": "roi"}, "color": "#28a745"},
            {"if": {"filter_query": "{roi} < 0", "column_id": "roi"}, "color": "#dc3545"},
        ],
        page_size=20,
        style_table={"overflowX": "auto"},
    )
```

**Parte D: Estender callback master (per D-08, Pitfall 4 — aceitar re-fetch no toggle)**

Adicionar novos Outputs e Input ao callback `update_dashboard`:

Novos Outputs (adicionar apos os 10 existentes):
```python
Output("config-mercados-container", "children"),  # DASH-03
Output("perf-table", "children"),                  # DASH-04
```

Novo Input (adicionar antes de interval-refresh):
```python
Input("perf-toggle-view", "value"),
```

Atualizar assinatura do callback:
```python
def update_dashboard(periodo, custom_start, custom_end, mercado, liga, stake, odd, gale_on, perf_toggle, _n):
```

Adicionar no corpo do callback (dentro do try, apos a secao 16 — history rowData):
```python
        # 17. Config Mercados (DASH-03)
        config_section = _build_config_mercados_section(conn, stake)

        # 18. Performance (DASH-04)
        perf_section = _build_performance_section(
            conn, history, entrada, stake, odd, gale_on, perf_toggle or "pct"
        )
```

Atualizar o return tuple para incluir os 2 novos valores no final:
```python
    return (
        str(total),
        winrate,
        pl_text,
        pl_class,
        roi_text,
        roi_class,
        streak_green,
        streak_red,
        row_data,
        liga_options,
        config_section,   # NOVO
        perf_section,     # NOVO
    )
```

Atualizar comentario "17. Return tuple" para "19. Return tuple com todos os 12 Outputs na ordem".

**Parte E: Teste de integracao para _build_config_card_mercado**

Adicionar em tests/test_dashboard.py:
```python
def test_build_config_card_mercado():
    """DASH-03: Card de config mercado renderiza com header e tabela de complementares."""
    from helpertips.dashboard import _build_config_card_mercado
    comps = [
        {"nome_display": "Resultado Final", "percentual": 0.20, "odd_ref": 1.80, "regra_validacao": "resultado_final"},
        {"nome_display": "HT/FT", "percentual": 0.10, "odd_ref": 2.50, "regra_validacao": "ht_ft"},
    ]
    card = _build_config_card_mercado("Over 2.5", 2.30, 100.0, comps)
    assert card is not None
    # Verificar que eh um dbc.Card
    assert hasattr(card, 'children')
```

Rodar `python3 -m pytest tests/test_dashboard.py -x -q` e `python3 -m pytest tests/ -x -q`.

Commit: `feat(12-02): integrate config mercados and performance sections into callback master`
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_dashboard.py -x -q && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/dashboard.py contem `def _build_config_card_mercado(`
    - helpertips/dashboard.py contem `def _build_config_mercados_section(`
    - helpertips/dashboard.py contem `def _build_performance_section(`
    - helpertips/dashboard.py contem `Output("config-mercados-container", "children")`
    - helpertips/dashboard.py contem `Output("perf-table", "children")`
    - helpertips/dashboard.py contem `Input("perf-toggle-view", "value")`
    - helpertips/dashboard.py contem `get_mercado_config` no bloco de imports
    - helpertips/dashboard.py contem `get_signals_com_placar` no bloco de imports
    - helpertips/dashboard.py contem `MERCADOS_CONFIG`
    - helpertips/dashboard.py contem `dash_table` no import
    - tests/test_dashboard.py contem `def test_build_config_card_mercado`
    - python3 -m pytest tests/ -x -q retorna 0
  </acceptance_criteria>
  <done>Callback master estendido com 12 Outputs, secoes Config Mercados e Performance renderizam reativamente com filtros e simulacao</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao visual do dashboard com novas secoes</name>
  <files>helpertips/dashboard.py</files>
  <action>Iniciar o dashboard localmente para verificacao visual do usuario. Executar: `cd /Users/luciano/helpertips && python3 -m helpertips.dashboard`</action>
  <what-built>
Dashboard estendido com duas novas secoes:
1. Config Mercados (DASH-03): Dois cards read-only (Over 2.5 e Ambas Marcam) com header (odd ref, stake base, progressao) e tabela de complementares com stakes T1-T4 calculados dinamicamente
2. Performance das Entradas (DASH-04): Toggle Percentual/Quantidade/P&L(R$) + tabela DataTable com dados agregados por entrada (visao geral) ou principal+complementares (visao por mercado)
  </what-built>
  <how-to-verify>
1. Iniciar o dashboard: `cd /Users/luciano/helpertips && python3 -m helpertips.dashboard`
2. Abrir http://localhost:8050 no browser

**Config Mercados (DASH-03):**
3. Verificar que aparece secao "Configuracao dos Mercados" com 2 cards: Over 2.5 e Ambas Marcam
4. Cada card mostra header com odd ref, stake base R$ 10.00 e progressao 1x 2x 4x 8x
5. Cada card tem tabela com colunas: Complementar, %, Odd Ref, T1, T2, T3, T4
6. Alterar Stake para 50 no card de simulacao — verificar que T1-T4 atualizam nos cards de config

**Performance das Entradas (DASH-04):**
7. Verificar secao "Performance das Entradas" com toggle Percentual/Quantidade/P&L(R$)
8. Clicar "Percentual" — tabela mostra colunas: Entrada, Taxa Green, Taxa Red, ROI
9. Clicar "Quantidade" — tabela mostra: Entrada, Greens, Reds, Total
10. Clicar "P&L (R$)" — tabela mostra: Entrada, Investido, Retorno, P&L, ROI
11. Valores P&L positivos em verde, negativos em vermelho

**Visao por Mercado (D-06):**
12. Selecionar "Over 2.5" no dropdown de mercado
13. Tabela de performance deve mostrar linhas separadas: "Over 2.5 (Principal)" + uma linha por complementar
14. Selecionar "Todos" — tabela volta a agregar por entrada

**Ordem do Layout (D-09):**
15. Verificar scroll: Filtros -> KPIs -> Simulacao -> Config Mercados -> Performance -> Historico (AG Grid por ultimo)
  </how-to-verify>
  <verify>Dashboard acessivel em http://localhost:8050 com secoes Config Mercados e Performance visiveis</verify>
  <done>Usuario aprovou visualmente o dashboard com Config Mercados e Performance das Entradas funcionais</done>
  <resume-signal>Type "approved" ou descreva problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -x -q` — suite completa verde
- `python3 -c "from helpertips.dashboard import _build_config_mercados_section, _build_performance_section; print('OK')"` — imprime OK
- `grep -c 'Output("config-mercados-container"' helpertips/dashboard.py` — 1
- `grep -c 'Output("perf-table"' helpertips/dashboard.py` — 1
- `grep -c 'get_mercado_config' helpertips/dashboard.py` — 2+ (import + uso)
- `grep -c 'get_signals_com_placar' helpertips/dashboard.py` — 2+ (import + uso)
</verification>

<success_criteria>
- Secao Config Mercados renderiza 2 cards com dados do banco e stakes T1-T4 reativos ao stake-input
- Tabela de Performance exibe dados agregados com toggle de 3 modos funcionando
- Visao geral (Todos) agrupa por entrada; visao por mercado mostra principal + complementares
- Cores verde/vermelho nos valores de P&L e ROI
- Layout segue ordem D-09
- Suite de testes completa verde
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-mercados-e-performance/12-02-SUMMARY.md`
</output>
