---
phase: 17-p-gina-de-configura-es
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - helpertips/queries.py
  - tests/test_queries.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04, CFG-05]

must_haves:
  truths:
    - "get_mercado_config retorna stake_base, fator_progressao e max_tentativas alem das colunas existentes"
    - "calculate_preview_stakes calcula stakes T1-T4 para cada complementar sem acesso ao banco"
    - "calculate_total_risco calcula total em risco por tentativa (principal + complementares) sem acesso ao banco"
    - "save_mercado_config persiste stake_base, fator_progressao, max_tentativas no banco"
    - "save_complementares_config persiste percentual e odd_ref de cada complementar no banco"
  artifacts:
    - path: "helpertips/queries.py"
      provides: "get_mercado_config atualizado + calculate_preview_stakes + calculate_total_risco + save_mercado_config + save_complementares_config"
      exports: ["get_mercado_config", "calculate_preview_stakes", "calculate_total_risco", "save_mercado_config", "save_complementares_config"]
    - path: "tests/test_queries.py"
      provides: "Testes unitarios para todas as funcoes novas e atualizadas"
  key_links:
    - from: "helpertips/queries.py::calculate_preview_stakes"
      to: "helpertips/pages/config.py (Plan 02)"
      via: "callback Dash importa funcao pura"
      pattern: "calculate_preview_stakes"
    - from: "helpertips/queries.py::save_mercado_config"
      to: "helpertips/pages/config.py (Plan 02)"
      via: "callback Dash chama ao clicar Salvar"
      pattern: "save_mercado_config"
---

<objective>
TDD: Funcoes de query e calculo puro para a pagina de configuracoes de mercado.

Purpose: Criar a camada de dados que a UI (Plan 02) vai consumir — leitura de config completa, calculo de preview sem banco, e persistencia de edits.
Output: queries.py com 5 funcoes novas/atualizadas + testes completos em test_queries.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-p-gina-de-configura-es/17-UI-SPEC.md
@.planning/phases/16-navega-o-schema-db/16-01-SUMMARY.md

<interfaces>
<!-- Schema DB existente (Phase 16 migration) -->

Tabela mercados:
  id SERIAL PK, slug TEXT UNIQUE, nome_display TEXT, odd_ref NUMERIC(6,2),
  ativo BOOLEAN DEFAULT TRUE,
  stake_base NUMERIC(10,2) NOT NULL DEFAULT 10.00,
  fator_progressao NUMERIC(4,2) NOT NULL DEFAULT 2.00,
  max_tentativas SMALLINT NOT NULL DEFAULT 4

Tabela complementares:
  id SERIAL PK, mercado_id INTEGER FK mercados(id),
  slug TEXT, nome_display TEXT, percentual NUMERIC(5,4),
  odd_ref NUMERIC(8,2), regra_validacao TEXT,
  UNIQUE (mercado_id, slug)

Funcoes existentes em queries.py:
  get_mercado_config(conn, mercado_slug) -> dict | None
    Retorna: {id, slug, nome_display, odd_ref, ativo}
    NOTA: NAO retorna stake_base, fator_progressao, max_tentativas ainda

  get_complementares_config(conn, mercado_slug) -> list[dict]
    Retorna: [{id, slug, nome_display, percentual, odd_ref, regra_validacao}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — Atualizar get_mercado_config + funcoes puras de preview</name>
  <files>helpertips/queries.py, tests/test_queries.py</files>
  <read_first>
    - helpertips/queries.py (linhas 417-445: get_mercado_config atual)
    - helpertips/queries.py (linhas 375-409: get_complementares_config para entender padrao)
    - helpertips/db.py (linhas 96-215: schema mercados e complementares)
    - tests/test_queries.py (para entender padrao de testes existente)
  </read_first>
  <behavior>
    - Test: get_mercado_config retorna dict com chaves id, slug, nome_display, odd_ref, ativo, stake_base, fator_progressao, max_tentativas
    - Test: calculate_preview_stakes(stake_base=10.0, fator_progressao=2.0, max_tentativas=4, complementares=[{percentual: 0.20, nome_display: "Over 3.5"}, {percentual: 0.01, nome_display: "Empate"}]) retorna lista de dicts com nome_display e stakes T1-T4 calculados
    - Test: calculate_preview_stakes com stake_base=10, fator=2, max_tent=4, complementar percentual=0.20 retorna T1=2.0, T2=4.0, T3=8.0, T4=16.0
    - Test: calculate_preview_stakes com max_tentativas=2 retorna apenas T1 e T2 (nao T3/T4)
    - Test: calculate_total_risco(stake_base=10.0, fator_progressao=2.0, max_tentativas=4, odd_principal=2.30, complementares=[{percentual: 0.20, odd_ref: 4.0}]) retorna lista de dicts por tentativa com principal, complementares, total
    - Test: calculate_total_risco T1: principal=10.0, complementares=2.0, total=12.0; T2: principal=20.0, complementares=4.0, total=24.0
  </behavior>
  <action>
    1. TESTES PRIMEIRO (RED):
       Adicionar em tests/test_queries.py:
       - test_get_mercado_config_returns_config_columns: Mocka cursor que retorna row com 8 colunas (id, slug, nome_display, odd_ref, ativo, stake_base, fator_progressao, max_tentativas). Verifica que dict retornado tem todas as 8 chaves.
       - test_calculate_preview_stakes_basic: Chama calculate_preview_stakes(stake_base=10.0, fator_progressao=2.0, max_tentativas=4, complementares=[{"percentual": 0.20, "nome_display": "Over 3.5"}, {"percentual": 0.01, "nome_display": "Empate"}]). Verifica: resultado tem 2 items, primeiro item tem nome_display="Over 3.5", T1=2.0, T2=4.0, T3=8.0, T4=16.0. Segundo item: T1=0.1, T2=0.2, T3=0.4, T4=0.8.
       - test_calculate_preview_stakes_max_2: Chama com max_tentativas=2. Verifica que cada item tem apenas T1 e T2 (sem T3/T4).
       - test_calculate_total_risco_basic: Chama calculate_total_risco(stake_base=10.0, fator_progressao=2.0, max_tentativas=4, odd_principal=2.30, complementares=[{"percentual": 0.20, "odd_ref": 4.0}]). Verifica: 4 items (T1-T4). T1: principal=10.0, complementares=2.0, total=12.0. T2: principal=20.0, complementares=4.0, total=24.0.

    2. IMPLEMENTACAO (GREEN):
       Em queries.py:
       a) Atualizar get_mercado_config — mudar SELECT para incluir stake_base, fator_progressao, max_tentativas. Atualizar columns list para ["id", "slug", "nome_display", "odd_ref", "ativo", "stake_base", "fator_progressao", "max_tentativas"].
       b) Criar calculate_preview_stakes(stake_base, fator_progressao, max_tentativas, complementares) -> list[dict]:
          - Funcao PURA, sem DB, sem Dash
          - Para cada complementar: stake_comp_base = stake_base * float(percentual)
          - Para cada tentativa t (1..max_tentativas): stake_t = stake_comp_base * fator_progressao^(t-1)
          - Retorna: [{"nome_display": str, "T1": float, "T2": float, ...}]
          - Chaves Tn sao dinamicas: "T1" ate "T{max_tentativas}"
       c) Criar calculate_total_risco(stake_base, fator_progressao, max_tentativas, odd_principal, complementares) -> list[dict]:
          - Funcao PURA, sem DB, sem Dash
          - Para cada tentativa t (1..max_tentativas):
            - principal = stake_base * fator_progressao^(t-1)
            - comp_total = sum(stake_base * float(c["percentual"]) * fator_progressao^(t-1) for c in complementares)
            - total = principal + comp_total
          - Retorna: [{"tentativa": "T1", "principal": float, "complementares": float, "total": float}]

    3. Rodar testes, confirmar GREEN.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python -m pytest tests/test_queries.py -k "test_get_mercado_config_returns_config_columns or test_calculate_preview_stakes or test_calculate_total_risco" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - queries.py contem `SELECT id, slug, nome_display, odd_ref, ativo, stake_base, fator_progressao, max_tentativas`
    - queries.py contem `def calculate_preview_stakes(stake_base`
    - queries.py contem `def calculate_total_risco(stake_base`
    - test_queries.py contem `def test_calculate_preview_stakes_basic`
    - test_queries.py contem `def test_calculate_total_risco_basic`
    - Todos os testes novos passam com exit code 0
  </acceptance_criteria>
  <done>get_mercado_config retorna colunas de config, calculate_preview_stakes e calculate_total_risco funcionam como funcoes puras testadas</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — Funcoes de persistencia save_mercado_config e save_complementares_config</name>
  <files>helpertips/queries.py, tests/test_queries.py</files>
  <read_first>
    - helpertips/queries.py (funcoes de Task 1 recem-criadas)
    - helpertips/db.py (schema mercados e complementares)
    - tests/test_queries.py (testes de Task 1)
  </read_first>
  <behavior>
    - Test: save_mercado_config executa UPDATE em mercados SET stake_base, fator_progressao, max_tentativas WHERE slug = mercado_slug
    - Test: save_complementares_config executa UPDATE em complementares SET percentual, odd_ref WHERE id = comp_id para cada item da lista
    - Test: save_mercado_config com mercado_slug invalido nao levanta excecao (0 rows affected)
  </behavior>
  <action>
    1. TESTES PRIMEIRO (RED):
       Adicionar em tests/test_queries.py:
       - test_save_mercado_config: Mocka conn/cursor. Chama save_mercado_config(conn, "over_2_5", stake_base=15.0, fator_progressao=3.0, max_tentativas=3). Verifica que cursor.execute foi chamado com SQL contendo "UPDATE mercados SET stake_base" e params (15.0, 3.0, 3, "over_2_5").
       - test_save_complementares_config: Mocka conn/cursor. Chama save_complementares_config(conn, [{"id": 1, "percentual": 0.25, "odd_ref": 5.0}, {"id": 2, "percentual": 0.02, "odd_ref": 35.0}]). Verifica que cursor.execute foi chamado 2 vezes (uma por complementar), com SQL contendo "UPDATE complementares SET percentual" e params corretos.

    2. IMPLEMENTACAO (GREEN):
       Em queries.py:
       a) Criar save_mercado_config(conn, mercado_slug, stake_base, fator_progressao, max_tentativas) -> None:
          ```python
          sql = """
              UPDATE mercados
              SET stake_base = %s, fator_progressao = %s, max_tentativas = %s
              WHERE slug = %s
          """
          with conn.cursor() as cur:
              cur.execute(sql, (stake_base, fator_progressao, max_tentativas, mercado_slug))
          conn.commit()
          ```
       b) Criar save_complementares_config(conn, complementares: list[dict]) -> None:
          - complementares: lista de dicts com keys "id", "percentual", "odd_ref"
          ```python
          sql = """
              UPDATE complementares
              SET percentual = %s, odd_ref = %s
              WHERE id = %s
          """
          with conn.cursor() as cur:
              for comp in complementares:
                  cur.execute(sql, (comp["percentual"], comp["odd_ref"], comp["id"]))
          conn.commit()
          ```

    3. Rodar testes, confirmar GREEN.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python -m pytest tests/test_queries.py -k "test_save_mercado_config or test_save_complementares_config" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - queries.py contem `def save_mercado_config(conn, mercado_slug`
    - queries.py contem `def save_complementares_config(conn, complementares`
    - queries.py contem `UPDATE mercados SET stake_base`
    - queries.py contem `UPDATE complementares SET percentual`
    - test_queries.py contem `def test_save_mercado_config`
    - test_queries.py contem `def test_save_complementares_config`
    - Todos os testes novos passam com exit code 0
  </acceptance_criteria>
  <done>save_mercado_config e save_complementares_config persistem config no banco com testes que verificam SQL e parametros</done>
</task>

</tasks>

<verification>
cd /Users/luciano/helpertips && python -m pytest tests/test_queries.py -k "config_columns or preview_stakes or total_risco or save_mercado or save_complementares" -x -v
</verification>

<success_criteria>
- get_mercado_config retorna 8 chaves incluindo stake_base, fator_progressao, max_tentativas
- calculate_preview_stakes calcula stakes T1-T4 corretamente como funcao pura
- calculate_total_risco calcula risco total por tentativa como funcao pura
- save_mercado_config e save_complementares_config persistem via UPDATE SQL
- Todos os testes novos passam
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/17-p-gina-de-configura-es/17-01-SUMMARY.md`
</output>
