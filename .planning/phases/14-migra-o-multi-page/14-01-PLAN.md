---
phase: 14-migra-o-multi-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpertips/dashboard.py
  - helpertips/pages/__init__.py
  - helpertips/pages/home.py
  - tests/test_dashboard.py
autonomous: false
requirements: [MPA-01, MPA-02]

must_haves:
  truths:
    - "Dashboard abre no browser identico ao estado pre-migracao"
    - "URL / carrega pages/home.py com todos os callbacks funcionando"
    - "use_pages=True ativo no app com suppress_callback_exceptions=True"
    - "Nenhum teste existente quebra apos a migracao"
  artifacts:
    - path: "helpertips/dashboard.py"
      provides: "Shell minimo com use_pages=True, page_container, dcc.Location"
      contains: "use_pages=True"
    - path: "helpertips/pages/__init__.py"
      provides: "Subpacote Python para pages/"
    - path: "helpertips/pages/home.py"
      provides: "Todo o conteudo do dashboard atual (layout, callbacks, helpers)"
      contains: "dash.register_page"
    - path: "tests/test_dashboard.py"
      provides: "Testes atualizados com imports de pages.home + novos testes MPA"
  key_links:
    - from: "helpertips/dashboard.py"
      to: "helpertips/pages/home.py"
      via: "use_pages=True auto-descoberta de pages/"
      pattern: "use_pages=True"
    - from: "helpertips/pages/home.py"
      to: "helpertips/queries.py"
      via: "import direto (mesmo que antes)"
      pattern: "from helpertips.queries import"
    - from: "tests/test_dashboard.py"
      to: "helpertips/pages/home.py"
      via: "from helpertips.pages.home import"
      pattern: "from helpertips.pages.home import"
---

<objective>
Migrar dashboard.py para estrutura Dash Pages: dashboard.py vira shell minimo e todo o conteudo (helpers, layout, callbacks) migra para pages/home.py.

Purpose: Desbloquear Phase 15 (pagina de detalhe do sinal) que requer URL routing multi-page.
Output: Dashboard funcional com use_pages=True, zero regressoes, 34+ testes verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-migra-o-multi-page/14-RESEARCH.md

@helpertips/dashboard.py
@tests/test_dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar conteudo de dashboard.py para pages/home.py e reescrever shell</name>
  <files>helpertips/pages/__init__.py, helpertips/pages/home.py, helpertips/dashboard.py</files>

  <read_first>
    - helpertips/dashboard.py (estado atual completo — 1003 LOC)
    - .planning/phases/14-migra-o-multi-page/14-RESEARCH.md (patterns e pitfalls)
  </read_first>

  <action>
    Executar migracao atomica em 3 sub-passos:

    **Sub-passo A: Criar helpertips/pages/__init__.py**
    - Arquivo vazio (apenas docstring opcional). Necessario para Python tratar pages/ como subpacote importavel.

    **Sub-passo B: Criar helpertips/pages/home.py**
    - Primeira linha apos imports: `dash.register_page(__name__, path="/")`
    - MOVER todo o conteudo de dashboard.py (linhas 1-999 aproximadamente) para home.py, com estas mudancas:
      1. Remover bloco "App initialization" (linhas 52-61): `app = dash.Dash(...)`, `server = app.server` — ficam no shell
      2. Remover bloco `if __name__ == "__main__"` (linhas 1001-1003) — fica no shell
      3. Renomear `app.layout = dbc.Container([...])` (linha 607) para `layout = dbc.Container([...])` — CRITICO: sem prefixo `app.`
      4. Callbacks ja usam `@callback` (nao `@app.callback`) — nenhuma mudanca necessaria
      5. Manter TODOS os imports de queries.py, db.py, store.py exatamente como estao
      6. Adicionar `import dash` no topo (necessario para `dash.register_page`)
      7. O import de `plotly.graph_objects as go` continua necessario em home.py

    **Sub-passo C: Reescrever helpertips/dashboard.py como shell minimo**
    - Conteudo exato do novo dashboard.py:

    ```python
    """
    dashboard.py — Shell minimo Dash Pages para HelperTips.

    O conteudo do dashboard (layout, callbacks, helpers) esta em pages/home.py.
    Este arquivo inicializa o app com use_pages=True e page_container.

    Run as:
        python -m helpertips.dashboard
        DASH_DEBUG=true python -m helpertips.dashboard
    """

    import os

    import dash
    import dash_bootstrap_components as dbc
    from dash import dcc

    app = dash.Dash(
        __name__,
        use_pages=True,
        suppress_callback_exceptions=True,
        external_stylesheets=[dbc.themes.DARKLY],
        title="HelperTips — Futebol Virtual",
    )
    server = app.server  # WSGI callable para gunicorn

    app.layout = dbc.Container([
        dcc.Location(id="url-nav", refresh="callback-nav"),
        dash.page_container,
    ], fluid=True)

    if __name__ == "__main__":
        debug = os.getenv("DASH_DEBUG", "false").lower() == "true"
        app.run(debug=debug)
    ```

    ATENCAO: O titulo deve ser exatamente `"HelperTips \u2014 Futebol Virtual"` (com em-dash Unicode, nao hifen).
    ATENCAO: `refresh="callback-nav"` no dcc.Location (nao `True` nem `False`).
    ATENCAO: `suppress_callback_exceptions=True` eh OBRIGATORIO — sem isso, callbacks de pages/home.py levantam excecao no startup.
  </action>

  <verify>
    <automated>python3 -c "from helpertips.dashboard import app, server; print('use_pages:', app.config.use_pages); print('suppress:', app.config.suppress_callback_exceptions); assert app.config.use_pages == True; assert app.config.suppress_callback_exceptions == True; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `helpertips/pages/__init__.py` existe
    - `helpertips/pages/home.py` contem `dash.register_page(__name__, path="/")`
    - `helpertips/pages/home.py` contem `layout = dbc.Container` (sem prefixo `app.`)
    - `helpertips/pages/home.py` NAO contem `app = dash.Dash` nem `server = app.server`
    - `helpertips/dashboard.py` contem `use_pages=True`
    - `helpertips/dashboard.py` contem `suppress_callback_exceptions=True`
    - `helpertips/dashboard.py` contem `dash.page_container`
    - `helpertips/dashboard.py` contem `dcc.Location(id="url-nav", refresh="callback-nav")`
    - `helpertips/dashboard.py` NAO contem `@callback` (callbacks migraram para home.py)
    - `python3 -c "from helpertips.dashboard import app; assert app.config.use_pages == True"` passa sem erro
    - `python3 -c "from helpertips.pages.home import layout; assert layout is not None"` passa sem erro
  </acceptance_criteria>

  <done>dashboard.py eh shell minimo com use_pages=True; pages/home.py contem todo o conteudo anterior; app inicializa sem erro</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar imports dos testes e adicionar testes MPA-01/MPA-02</name>
  <files>tests/test_dashboard.py</files>

  <read_first>
    - tests/test_dashboard.py (estado atual — ver imports que referenciam helpertips.dashboard)
    - helpertips/pages/home.py (confirmar que funcoes auxiliares existem la)
  </read_first>

  <action>
    **Parte A: Atualizar imports existentes**

    Os seguintes imports de `helpertips.dashboard` devem mudar para `helpertips.pages.home`:

    1. Linha 37: `from helpertips.dashboard import _resolve_periodo` -> `from helpertips.pages.home import _resolve_periodo`
    2. Linha 379: `from helpertips.dashboard import _calcular_stakes_gale` -> `from helpertips.pages.home import _calcular_stakes_gale`
    3. Linha 385: `from helpertips.dashboard import _agregar_por_entrada` -> `from helpertips.pages.home import _agregar_por_entrada`
    4. Linha 391: `from helpertips.dashboard import _get_colunas_visiveis` -> `from helpertips.pages.home import _get_colunas_visiveis`
    5. Linha 519: `from helpertips.dashboard import _build_config_card_mercado` -> `from helpertips.pages.home import _build_config_card_mercado`
    6. Linha 551: `from helpertips.dashboard import _build_liga_chart` -> `from helpertips.pages.home import _build_liga_chart`
    7. Linha 556: `from helpertips.dashboard import _build_equity_curve_chart` -> `from helpertips.pages.home import _build_equity_curve_chart`
    8. Linha 561: `from helpertips.dashboard import _build_gale_chart` -> `from helpertips.pages.home import _build_gale_chart`
    9. Linha 698: `from helpertips.dashboard import _build_phase13_section` -> `from helpertips.pages.home import _build_phase13_section`

    MANTER sem mudanca:
    - Linha 31: `from helpertips.dashboard import app` — `app` continua em dashboard.py

    **Parte B: Atualizar testes que usam app.layout para IDs do dashboard**

    O `app.layout` agora eh o shell (Container com dcc.Location + page_container), NAO o layout completo do dashboard. Os seguintes testes devem usar `home_layout` em vez de `app.layout`:

    Adicionar import no topo do arquivo:
    ```python
    from helpertips.pages.home import layout as home_layout
    ```

    Testes a atualizar:
    - `test_layout_has_required_component_ids`: `collect_ids(app.layout)` -> `collect_ids(home_layout)`
    - `test_datepicker_collapse_initial_closed`: `find_collapse(app.layout, ...)` -> `find_collapse(home_layout, ...)`
    - `test_layout_has_phase12_component_ids`: `collect_ids(app.layout)` -> `collect_ids(home_layout)`
    - `test_layout_has_phase13_component_ids`: `collect_ids(app.layout)` -> `collect_ids(home_layout)`

    O teste `test_app_layout_renders` deve:
    - Manter verificacao de `app.layout is not None`
    - Manter verificacao do titulo `"HelperTips \u2014 Futebol Virtual"`

    **Parte C: Adicionar 3 novos testes MPA**

    Adicionar ao final de `tests/test_dashboard.py`:

    ```python
    # ---------------------------------------------------------------------------
    # Phase 14 — MPA-01, MPA-02: Dash Pages migration
    # ---------------------------------------------------------------------------

    def test_app_uses_pages():
        """MPA-01: use_pages=True e suppress_callback_exceptions=True no app."""
        assert app.config.use_pages is True, "app.config.use_pages deve ser True"
        assert app.config.suppress_callback_exceptions is True, (
            "app.config.suppress_callback_exceptions deve ser True"
        )

    def test_shell_has_url_nav():
        """MPA-01: Shell layout contem dcc.Location(id='url-nav') e dash.page_container."""
        ids = collect_ids(app.layout)
        assert "url-nav" in ids, f"url-nav ausente no shell layout. IDs: {sorted(ids)}"

    def test_home_page_registered():
        """MPA-02: pages/home.py registrado com path='/'."""
        import dash
        registry = dash.page_registry
        home_entries = [v for v in registry.values() if v.get("path") == "/"]
        assert len(home_entries) == 1, (
            f"Esperado 1 pagina com path='/', encontrado {len(home_entries)}. "
            f"Registry: {list(registry.keys())}"
        )
    ```

    **Parte D: Rodar suite completa e confirmar 37+ testes verdes**

    Executar: `python3 -m pytest tests/test_dashboard.py -x -q`
    Todos os 34 testes existentes + 3 novos = 37 testes devem passar.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_dashboard.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "from helpertips.dashboard import" tests/test_dashboard.py` retorna exatamente 1 (apenas `import app`)
    - `grep -c "from helpertips.pages.home import" tests/test_dashboard.py` retorna pelo menos 5 (funcoes auxiliares migradas)
    - `grep "test_app_uses_pages" tests/test_dashboard.py` encontra o teste
    - `grep "test_shell_has_url_nav" tests/test_dashboard.py` encontra o teste
    - `grep "test_home_page_registered" tests/test_dashboard.py` encontra o teste
    - `python3 -m pytest tests/test_dashboard.py -x -q` mostra "37 passed" (34 existentes + 3 novos)
    - Nenhum teste usa `app.layout` para verificar IDs do dashboard (usam `home_layout` de pages.home)
  </acceptance_criteria>

  <done>Todos os 37 testes passam; imports apontam para helpertips.pages.home; testes MPA-01 e MPA-02 cobertos</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual — dashboard identico ao pre-migracao</name>
  <files>helpertips/dashboard.py, helpertips/pages/home.py</files>
  <action>Verificacao humana do dashboard migrado. Tasks 1 e 2 completaram toda a automacao; agora o usuario confirma que o resultado visual eh identico ao pre-migracao.</action>
  <what-built>Dashboard migrado para Dash Pages (use_pages=True) com todo o conteudo em pages/home.py e shell minimo em dashboard.py</what-built>
  <how-to-verify>
    1. Executar: `python -m helpertips.dashboard`
    2. Abrir http://127.0.0.1:8050/ no browser
    3. Verificar que o dashboard carrega identico ao estado anterior:
       - Filtros globais (periodo, mercado, liga) funcionam
       - KPI cards mostram valores
       - Tabela de historico (AG Grid) renderiza
       - Graficos de analise por liga, equity curve e gale aparecem
    4. Abrir console do browser (F12) — nenhum erro JavaScript
    5. Verificar que URL eh exatamente "/" (nao "/home" ou outro path)
  </how-to-verify>
  <verify>
    <automated>python3 -m pytest tests/test_dashboard.py -x -q</automated>
  </verify>
  <done>Usuario confirma que dashboard visual eh identico ao pre-migracao e sem erros no console</done>
  <resume-signal>Digite "aprovado" se tudo identico, ou descreva diferencas encontradas</resume-signal>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_dashboard.py -x -q` — 37 testes passam
- `python3 -m pytest -x -q` — suite completa verde
- `python3 -c "from helpertips.dashboard import app; assert app.config.use_pages"` — sem erro
- `python3 -c "import dash; assert any(v['path'] == '/' for v in dash.page_registry.values())"` — home registrado
- Dashboard visual identico ao pre-migracao (checkpoint manual)
</verification>

<success_criteria>
1. dashboard.py tem menos de 30 linhas (shell minimo)
2. pages/home.py contem todo o conteudo anterior (~950 LOC)
3. 37 testes passam em test_dashboard.py (34 existentes + 3 novos MPA)
4. use_pages=True, suppress_callback_exceptions=True, page_container, dcc.Location(id="url-nav") presentes no shell
5. Dashboard abre em http://127.0.0.1:8050/ visualmente identico ao pre-migracao
</success_criteria>

<output>
After completion, create `.planning/phases/14-migra-o-multi-page/14-01-SUMMARY.md`
</output>
