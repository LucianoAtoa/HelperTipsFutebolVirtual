---
phase: 15-p-gina-de-detalhe-do-sinal
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - helpertips/queries.py
  - tests/test_queries.py
autonomous: true
requirements:
  - SIG-02
  - SIG-03
  - SIG-04
  - SIG-06

must_haves:
  truths:
    - "get_sinal_detalhado retorna dict completo para ID existente"
    - "get_sinal_detalhado retorna None para ID inexistente"
    - "calculate_pl_detalhado_por_sinal retorna breakdown principal + cada complementar + totais"
    - "Complementar sem placar retorna resultado N/A e lucro 0.0"
    - "Gale progressivo aplica fator correto na stake de cada tentativa"
  artifacts:
    - path: "helpertips/queries.py"
      provides: "get_sinal_detalhado e calculate_pl_detalhado_por_sinal"
      exports: ["get_sinal_detalhado", "calculate_pl_detalhado_por_sinal"]
    - path: "tests/test_queries.py"
      provides: "Testes TDD para as novas funcoes"
      contains: "test_get_sinal_detalhado"
  key_links:
    - from: "helpertips/queries.py"
      to: "signals table"
      via: "SELECT ... WHERE s.id = %s"
      pattern: "WHERE s\\.id = %s"
    - from: "helpertips/queries.py"
      to: "validar_complementar"
      via: "chamada interna"
      pattern: "validar_complementar"
---

<objective>
Criar as funcoes de dados para a pagina de detalhe do sinal: `get_sinal_detalhado` (query por ID) e `calculate_pl_detalhado_por_sinal` (breakdown P&L individual com linha por complementar).

Purpose: A pagina de detalhe (Plan 02) precisa de dados que nao existem no queries.py atual — `calculate_pl_por_entrada` retorna totais agregados sem breakdown por complementar individual.

Output: Duas novas funcoes exportadas em queries.py + testes TDD completos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-p-gina-de-detalhe-do-sinal/15-RESEARCH.md

@helpertips/queries.py
@tests/test_queries.py

<interfaces>
<!-- Funcoes existentes em queries.py que serao reutilizadas -->

From helpertips/queries.py:
```python
def validar_complementar(regra: str, placar: str | None, resultado_principal: str | None) -> str | None:
    """Determine GREEN/RED/None for a complementary market entry."""

def get_complementares_config(conn, mercado_slug: str) -> list[dict]:
    """Retorna lista de mercados complementares para um slug de mercado principal."""

def get_mercado_config(conn, mercado_slug: str) -> dict | None:
    """Retorna config de um mercado por slug. None se nao existir ou inativo."""

def calculate_pl_por_entrada(
    signals: list[dict],
    complementares_por_mercado: dict[str, list[dict]],
    stake: float,
    odd_por_mercado: dict[str, float],
    gale_on: bool,
) -> list[dict]:
    """Calcula P&L detalhado por sinal individual (totais agregados, sem breakdown por complementar)."""
```

From helpertips/db.py:
```python
def get_connection():
    """Retorna conexao psycopg2 ao PostgreSQL."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — get_sinal_detalhado + calculate_pl_detalhado_por_sinal</name>
  <files>helpertips/queries.py, tests/test_queries.py</files>

  <read_first>
    - helpertips/queries.py (ver calculate_pl_por_entrada linhas 636-760 como referencia de calculo P&L; ver validar_complementar linhas 321-365)
    - tests/test_queries.py (ver padrao de testes existente — imports, fixtures, convencoes)
  </read_first>

  <behavior>
    - test_get_sinal_detalhado_found: get_sinal_detalhado(conn, existing_id) retorna dict com keys [id, liga, entrada, resultado, placar, tentativa, received_at, mercado_id, mercado_slug]
    - test_get_sinal_detalhado_not_found: get_sinal_detalhado(conn, 999999) retorna None
    - test_calculate_pl_detalhado_green_t1: sinal GREEN tentativa 1 com stake=10.0 e odd=2.30 retorna principal.lucro=13.0, principal.investido=10.0, principal.retorno=23.0; complementares e uma lista com len == len(complementares_config); totais.investido = principal.investido + sum(comp.investido)
    - test_calculate_pl_detalhado_red: sinal RED tentativa 1 stake=10.0 retorna principal.lucro=-10.0, principal.retorno=0.0; todos os complementares tem resultado RED e lucro negativo
    - test_calculate_pl_detalhado_green_t2_gale: sinal GREEN tentativa 2 com gale_on=True: principal.investido=30.0 (stake * (2^2 - 1)), principal.stake_efetiva=20.0 (stake * 2^1)
    - test_calculate_pl_detalhado_sem_placar: sinal GREEN com placar=None: complementares retornam resultado="N/A" e lucro=0.0 e investido=0.0
    - test_calculate_pl_detalhado_complementar_individual: cada complementar no retorno tem keys [nome, odd, stake, resultado, lucro, investido, retorno]
  </behavior>

  <action>
    **RED phase — Escrever testes primeiro:**

    Adicionar ao final de `tests/test_queries.py` os imports:
    ```python
    from helpertips.queries import get_sinal_detalhado, calculate_pl_detalhado_por_sinal
    ```

    1. `test_get_sinal_detalhado_not_found` (puro DB): chamar com ID=999999, assert retorno is None.
       - Este teste precisa de conexao DB — usar o padrao existente do arquivo com `@pytest.mark.skipif` se DB indisponivel.

    2. `test_calculate_pl_detalhado_green_t1` (puro Python, sem DB):
       ```python
       sinal = {"resultado": "GREEN", "placar": "3-1", "tentativa": 1, "mercado_slug": "over_2_5"}
       comps_config = [
           {"nome_display": "Over 3.5", "percentual": 0.20, "odd_ref": 3.50, "regra_validacao": "over_3_5"},
           {"nome_display": "Over 4.5", "percentual": 0.01, "odd_ref": 7.00, "regra_validacao": "over_4_5"},
       ]
       result = calculate_pl_detalhado_por_sinal(sinal, comps_config, stake=10.0, odd_principal=2.30, gale_on=False)
       assert result["principal"]["investido"] == 10.0
       assert result["principal"]["retorno"] == 23.0
       assert result["principal"]["lucro"] == 13.0
       assert result["principal"]["resultado"] == "GREEN"
       assert len(result["complementares"]) == 2
       assert result["complementares"][0]["nome"] == "Over 3.5"
       # Over 3.5: stake=10*0.20=2.0, placar 3-1 -> casa 3 >= 4? Nao -> RED
       assert result["complementares"][0]["resultado"] == "RED"
       assert result["complementares"][0]["lucro"] == -2.0
       # Totais
       assert result["totais"]["investido"] == pytest.approx(result["principal"]["investido"] + sum(c["investido"] for c in result["complementares"]))
       ```

    3. `test_calculate_pl_detalhado_red` (puro Python):
       ```python
       sinal = {"resultado": "RED", "placar": "1-0", "tentativa": 1, "mercado_slug": "over_2_5"}
       result = calculate_pl_detalhado_por_sinal(sinal, comps_config, stake=10.0, odd_principal=2.30, gale_on=False)
       assert result["principal"]["lucro"] == -10.0
       assert result["principal"]["retorno"] == 0.0
       # Complementares: principal RED -> todos RED (regra D-08 de validar_complementar)
       for comp in result["complementares"]:
           assert comp["resultado"] == "RED"
       ```

    4. `test_calculate_pl_detalhado_green_t2_gale` (puro Python):
       ```python
       sinal = {"resultado": "GREEN", "placar": "4-2", "tentativa": 2, "mercado_slug": "over_2_5"}
       result = calculate_pl_detalhado_por_sinal(sinal, comps_config, stake=10.0, odd_principal=2.30, gale_on=True)
       # Gale T2: accumulated=10*(2^2-1)=30, winning=10*(2^1)=20
       assert result["principal"]["investido"] == 30.0
       ```

    5. `test_calculate_pl_detalhado_sem_placar` (puro Python):
       ```python
       sinal = {"resultado": "GREEN", "placar": None, "tentativa": 1, "mercado_slug": "over_2_5"}
       result = calculate_pl_detalhado_por_sinal(sinal, comps_config, stake=10.0, odd_principal=2.30, gale_on=False)
       # Principal: GREEN com placar=None -> principal e GREEN (nao depende de placar)
       assert result["principal"]["resultado"] == "GREEN"
       # Complementares: placar None -> validar_complementar retorna None -> exibir como "N/A"
       for comp in result["complementares"]:
           assert comp["resultado"] == "N/A"
           assert comp["lucro"] == 0.0
           assert comp["investido"] == 0.0
       ```

    6. `test_calculate_pl_detalhado_complementar_individual`: verificar que cada dict em complementares tem todas as keys necessarias: nome, odd, stake, resultado, lucro, investido, retorno.

    Rodar testes: `python3 -m pytest tests/test_queries.py -k "detalhado or sinal_detalhado" -x` — todos DEVEM falhar (ImportError).

    **GREEN phase — Implementar funcoes:**

    Adicionar ao final de `helpertips/queries.py` (antes da secao "Fase 3: Analytics Depth"):

    1. `get_sinal_detalhado(conn, signal_id: int) -> dict | None`:
       ```python
       def get_sinal_detalhado(conn, signal_id: int) -> dict | None:
           sql = """
               SELECT s.id, s.liga, s.entrada, s.resultado, s.placar,
                      s.tentativa, s.received_at, s.mercado_id,
                      m.slug AS mercado_slug
               FROM signals s
               LEFT JOIN mercados m ON s.mercado_id = m.id
               WHERE s.id = %s
           """
           with conn.cursor() as cur:
               cur.execute(sql, (signal_id,))
               row = cur.fetchone()
           if row is None:
               return None
           columns = ["id", "liga", "entrada", "resultado", "placar",
                      "tentativa", "received_at", "mercado_id", "mercado_slug"]
           return dict(zip(columns, row))
       ```

    2. `calculate_pl_detalhado_por_sinal(sinal, complementares_config, stake, odd_principal, gale_on) -> dict`:
       - Logica do principal: copiar exatamente o padrao de calculate_pl_por_entrada linhas 680-699 (Gale e Stake Fixa).
       - Logica de complementares: iterar complementares_config, chamar `validar_complementar(comp["regra_validacao"], sinal.get("placar"), sinal.get("resultado"))`. Se resultado_comp is None: tratar como "N/A" com investido=0.0, retorno=0.0, lucro=0.0 (complementar pendente/sem placar). Se GREEN ou RED: calcular normalmente como em calculate_pl_por_entrada linhas 707-741.
       - Cada complementar retorna dict: `{"nome": comp["nome_display"], "odd": odd_ref, "stake": stake_comp, "resultado": resultado_str, "lucro": lucro, "investido": investido, "retorno": retorno}`.
       - `resultado_str`: "GREEN", "RED", ou "N/A" (quando validar_complementar retorna None).
       - Totais: somar investido, retorno, lucro de principal + todos complementares (N/A contribui 0).
       - Retorno final: `{"principal": {...}, "complementares": [...], "totais": {...}}`.

    Rodar testes novamente: todos DEVEM passar.

    **REFACTOR phase:** Extrair logica comum de calculo Gale entre calculate_pl_por_entrada e calculate_pl_detalhado_por_sinal se a duplicacao for > 10 linhas. Caso contrario, manter separado (funcoes independentes, sem acoplamento).
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_queries.py -k "detalhado or sinal_detalhado" -x -q</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "def get_sinal_detalhado" helpertips/queries.py retorna 1
    - grep -c "def calculate_pl_detalhado_por_sinal" helpertips/queries.py retorna 1
    - grep "WHERE s.id = %s" helpertips/queries.py retorna match
    - grep -c "test_calculate_pl_detalhado" tests/test_queries.py retorna >= 5
    - grep -c "test_get_sinal_detalhado" tests/test_queries.py retorna >= 1
    - python3 -m pytest tests/test_queries.py -k "detalhado or sinal_detalhado" -x -q retorna 0 (exit code)
    - python3 -m pytest -x -q retorna 0 (suite completa nao quebra)
  </acceptance_criteria>

  <done>
    get_sinal_detalhado retorna dict completo para ID existente e None para inexistente. calculate_pl_detalhado_por_sinal retorna breakdown com principal, lista de complementares individuais e totais consolidados. Todos os testes passam incluindo suite completa.
  </done>
</task>

</tasks>

<verification>
python3 -m pytest tests/test_queries.py -k "detalhado or sinal_detalhado" -x -q
python3 -m pytest -x -q
</verification>

<success_criteria>
- Funcao get_sinal_detalhado busca sinal por ID com JOIN em mercados
- Funcao calculate_pl_detalhado_por_sinal retorna breakdown individual
- Complementares sem placar retornam "N/A" com lucro 0.0
- Gale progressivo aplica fator correto por tentativa
- Suite completa de testes verde
</success_criteria>

<output>
After completion, create `.planning/phases/15-p-gina-de-detalhe-do-sinal/15-01-SUMMARY.md`
</output>
