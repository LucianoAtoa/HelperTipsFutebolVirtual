---
phase: 15-p-gina-de-detalhe-do-sinal
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - helpertips/pages/sinal.py
  - helpertips/pages/home.py
  - tests/test_sinal_page.py
  - tests/test_dashboard.py
autonomous: false
requirements:
  - SIG-01
  - SIG-02
  - SIG-03
  - SIG-04
  - SIG-05
  - SIG-06

must_haves:
  truths:
    - "Clicar em linha do AG Grid navega para /sinal?id=<n> sem full page reload"
    - "Pagina exibe card da entrada principal com mercado, odd, stake, resultado, horario e lucro"
    - "Pagina exibe uma linha por complementar com nome, odd, stake, resultado, lucro"
    - "Totais consolidados no rodape: investido total, retorno total, lucro liquido"
    - "Botao Voltar retorna ao dashboard"
    - "ID inexistente exibe mensagem amigavel sem erro 500"
  artifacts:
    - path: "helpertips/pages/sinal.py"
      provides: "Pagina de detalhe do sinal"
      contains: "register_page"
    - path: "helpertips/pages/home.py"
      provides: "Navegacao AG Grid para detalhe + campo id no rowData"
      contains: "navigate_to_sinal"
    - path: "tests/test_sinal_page.py"
      provides: "Testes estruturais da pagina de detalhe"
      contains: "test_sinal_page"
    - path: "tests/test_dashboard.py"
      provides: "Testes do callback de navegacao"
      contains: "test_navigate_to_sinal"
  key_links:
    - from: "helpertips/pages/home.py"
      to: "helpertips/pages/sinal.py"
      via: "Output('url-nav', 'href') -> /sinal?id=N"
      pattern: "url-nav.*href"
    - from: "helpertips/pages/sinal.py"
      to: "helpertips/queries.py"
      via: "get_sinal_detalhado + calculate_pl_detalhado_por_sinal"
      pattern: "get_sinal_detalhado"
    - from: "helpertips/pages/sinal.py"
      to: "dashboard.py shell"
      via: "dcc.Location(id='sinal-url') leitura local"
      pattern: "sinal-url.*search"
---

<objective>
Criar a pagina de detalhe do sinal (`pages/sinal.py`) com layout completo (card principal, tabela complementares, totais) e integrar navegacao do AG Grid em `home.py` para `/sinal?id=<n>`.

Purpose: O usuario precisa clicar em qualquer sinal no historico e ver o breakdown completo de P&L com todas as entradas complementares individualizadas.

Output: Pagina funcional em `/sinal?id=<n>`, callback de navegacao no AG Grid, testes estruturais e visuais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-p-gina-de-detalhe-do-sinal/15-RESEARCH.md
@.planning/phases/15-p-gina-de-detalhe-do-sinal/15-01-SUMMARY.md

@helpertips/dashboard.py
@helpertips/pages/home.py
@helpertips/queries.py
@tests/test_dashboard.py

<interfaces>
<!-- Shell do dashboard — ja tem url-nav para navegacao cross-page -->
From helpertips/dashboard.py:
```python
app.layout = dbc.Container([
    dcc.Location(id="url-nav", refresh="callback-nav"),
    dash.page_container,
], fluid=True)
```

<!-- Funcoes novas do Plan 01 -->
From helpertips/queries.py (criadas no Plan 01):
```python
def get_sinal_detalhado(conn, signal_id: int) -> dict | None:
    """Retorna sinal por ID com keys: id, liga, entrada, resultado, placar,
    tentativa, received_at, mercado_id, mercado_slug. None se nao existir."""

def calculate_pl_detalhado_por_sinal(
    sinal: dict,
    complementares_config: list[dict],
    stake: float,
    odd_principal: float,
    gale_on: bool,
) -> dict:
    """Retorna {"principal": {...}, "complementares": [...], "totais": {...}}"""
```

<!-- Funcoes existentes necessarias -->
From helpertips/queries.py:
```python
def get_complementares_config(conn, mercado_slug: str) -> list[dict]:
def get_mercado_config(conn, mercado_slug: str) -> dict | None:
```

<!-- AG Grid atual em home.py — id="history-table" -->
<!-- rowData NAO inclui campo "id" — deve ser adicionado -->
<!-- columnDefs: data_hora, liga, entrada, time_casa, time_fora, resultado, tentativa, placar, lucro -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navegacao AG Grid + pagina sinal.py + testes</name>
  <files>helpertips/pages/home.py, helpertips/pages/sinal.py, tests/test_sinal_page.py, tests/test_dashboard.py</files>

  <read_first>
    - helpertips/pages/home.py (ver rowData linhas 919-929 para adicionar "id"; ver imports e callbacks existentes para padrao)
    - helpertips/dashboard.py (ver shell com url-nav e page_container)
    - helpertips/queries.py (ver get_sinal_detalhado e calculate_pl_detalhado_por_sinal — criados no Plan 01)
    - tests/test_dashboard.py (ver padrao de testes existente para callbacks)
    - helpertips/assets/ (ver CSS existente para manter consistencia visual)
  </read_first>

  <action>
    **Parte A — Adicionar campo "id" ao rowData do AG Grid em home.py (SIG-01):**

    Localizar o bloco de construcao de row_data (linha ~919). Adicionar `"id": sig.get("id")` ao dict:
    ```python
    row_data.append({
        "id": sig.get("id"),  # NOVO — necessario para navegacao /sinal?id=N
        "data_hora": data_hora,
        "liga": sig.get("liga", ""),
        ...
    })
    ```
    O campo `id` NAO esta em columnDefs — AG Grid inclui no rowData mas nao exibe na tabela.

    **Parte B — Novo callback navigate_to_sinal em home.py (SIG-01):**

    Adicionar APOS os callbacks existentes (final do arquivo):
    ```python
    @callback(
        Output("url-nav", "href"),
        Input("history-table", "cellClicked"),
        prevent_initial_call=True,
    )
    def navigate_to_sinal(cell_clicked):
        if not cell_clicked:
            return no_update
        row_data = cell_clicked.get("rowData", {})
        signal_id = row_data.get("id")
        if signal_id is None:
            return no_update
        return f"/sinal?id={signal_id}"
    ```

    **Parte C — Criar helpertips/pages/sinal.py (SIG-02, SIG-03, SIG-04, SIG-05, SIG-06):**

    Arquivo novo com esta estrutura:

    ```python
    """
    pages/sinal.py — Pagina de detalhe individual do sinal.

    Exibe breakdown completo de P&L: card da entrada principal,
    tabela de complementares e totais consolidados.
    Rota: /sinal?id=<n>
    """
    import dash
    from dash import dcc, html, callback, Input, Output, no_update
    import dash_bootstrap_components as dbc
    from urllib.parse import parse_qs

    from helpertips.db import get_connection
    from helpertips.queries import (
        get_sinal_detalhado,
        get_complementares_config,
        get_mercado_config,
        calculate_pl_detalhado_por_sinal,
    )

    dash.register_page(__name__, path="/sinal")

    # Stake e gale defaults (mesmos valores do home.py)
    _DEFAULT_STAKE = 10.0
    _DEFAULT_GALE_ON = True

    layout = html.Div([
        dcc.Location(id="sinal-url", refresh=False),
        html.Div(id="sinal-content"),
    ])
    ```

    Callback `render_sinal`:
    ```python
    @callback(
        Output("sinal-content", "children"),
        Input("sinal-url", "search"),
    )
    def render_sinal(search):
        params = parse_qs((search or "").lstrip("?"))
        try:
            signal_id = int(params.get("id", [None])[0])
        except (TypeError, ValueError):
            return _layout_not_found(None)

        conn = get_connection()
        try:
            sinal = get_sinal_detalhado(conn, signal_id)
            if sinal is None:
                return _layout_not_found(signal_id)

            mercado_slug = sinal.get("mercado_slug") or "over_2_5"
            comps_config = get_complementares_config(conn, mercado_slug)
            mercado_cfg = get_mercado_config(conn, mercado_slug)
            odd_principal = float(mercado_cfg["odd_ref"]) if mercado_cfg else 2.30
        finally:
            conn.close()

        pl = calculate_pl_detalhado_por_sinal(
            sinal, comps_config, _DEFAULT_STAKE, odd_principal, _DEFAULT_GALE_ON,
        )

        return _build_detail_layout(sinal, pl)
    ```

    Helper `_layout_not_found(signal_id)` (SIG-06):
    ```python
    def _layout_not_found(signal_id):
        msg = f"O sinal #{signal_id} nao existe ou foi removido." if signal_id else "ID de sinal invalido."
        return dbc.Container([
            dbc.Row(dbc.Col([
                html.H4("Sinal nao encontrado", className="text-warning mt-4"),
                html.P(msg, className="text-muted"),
                dcc.Link(
                    dbc.Button("Voltar ao Dashboard", color="primary", className="mt-3"),
                    href="/",
                ),
            ], width={"size": 6, "offset": 3})),
        ])
    ```

    Helper `_build_detail_layout(sinal, pl)` — layout completo:

    1. **Header** com botao Voltar (SIG-05):
       ```python
       header = dbc.Row(dbc.Col([
           dcc.Link(
               dbc.Button("← Voltar", color="secondary", size="sm", className="mb-3"),
               href="/",
           ),
           html.H4(f"Sinal #{sinal['id']} — {sinal.get('entrada', '')}",
                    className="text-light"),
           html.P(
               f"Liga: {sinal.get('liga', 'N/A')} | "
               f"Horario: {sinal['received_at'].strftime('%d/%m/%Y %H:%M') if sinal.get('received_at') else 'N/A'} | "
               f"Tentativa: {sinal.get('tentativa', 1)}",
               className="text-muted",
           ),
       ]))
       ```

    2. **Card da entrada principal** (SIG-02):
       ```python
       p = pl["principal"]
       resultado_color = "success" if p["resultado"] == "GREEN" else "danger"
       card_principal = dbc.Card(dbc.CardBody([
           html.H5("Entrada Principal", className="card-title"),
           dbc.Row([
               dbc.Col([html.Small("Mercado", className="text-muted"), html.P(sinal.get("entrada", ""))]),
               dbc.Col([html.Small("Odd", className="text-muted"), html.P(f"{p['odd']:.2f}")]),
               dbc.Col([html.Small("Stake", className="text-muted"), html.P(f"R$ {p['investido']:.2f}")]),
               dbc.Col([html.Small("Resultado", className="text-muted"),
                        html.P(p["resultado"], className=f"text-{resultado_color} fw-bold")]),
               dbc.Col([html.Small("Lucro/Prejuizo", className="text-muted"),
                        html.P(f"R$ {p['lucro']:+.2f}", className=f"text-{resultado_color}")]),
           ]),
       ]), className="mb-3")
       ```

    3. **Tabela de complementares** (SIG-03):
       ```python
       comp_rows = []
       for c in pl["complementares"]:
           if c["resultado"] == "GREEN":
               cls = "text-success"
           elif c["resultado"] == "RED":
               cls = "text-danger"
           else:
               cls = "text-muted"
           comp_rows.append(html.Tr([
               html.Td(c["nome"]),
               html.Td(f"{c['odd']:.2f}"),
               html.Td(f"R$ {c['stake']:.2f}"),
               html.Td(c["resultado"], className=cls),
               html.Td(f"R$ {c['lucro']:+.2f}", className=cls),
           ]))

       tabela_comps = dbc.Card(dbc.CardBody([
           html.H5("Entradas Complementares", className="card-title"),
           dbc.Table([
               html.Thead(html.Tr([
                   html.Th("Nome"), html.Th("Odd"), html.Th("Stake"),
                   html.Th("Resultado"), html.Th("Lucro/Prejuizo"),
               ])),
               html.Tbody(comp_rows),
           ], bordered=True, hover=True, responsive=True, className="table-dark"),
       ]), className="mb-3")
       ```

    4. **Totais consolidados** (SIG-04):
       ```python
       t = pl["totais"]
       lucro_cls = "text-success" if t["lucro"] >= 0 else "text-danger"
       totais_card = dbc.Card(dbc.CardBody([
           html.H5("Totais Consolidados", className="card-title"),
           dbc.Row([
               dbc.Col([html.Small("Investido Total", className="text-muted"),
                        html.P(f"R$ {t['investido']:.2f}", className="fs-5")]),
               dbc.Col([html.Small("Retorno Total", className="text-muted"),
                        html.P(f"R$ {t['retorno']:.2f}", className="fs-5")]),
               dbc.Col([html.Small("Lucro Liquido", className="text-muted"),
                        html.P(f"R$ {t['lucro']:+.2f}", className=f"fs-5 fw-bold {lucro_cls}")]),
           ]),
       ]), className="mb-3")
       ```

    5. Montar e retornar layout completo:
       ```python
       return dbc.Container([header, card_principal, tabela_comps, totais_card], className="mt-3")
       ```

    **Parte D — Testes estruturais:**

    Criar `tests/test_sinal_page.py`:
    - `test_sinal_page_registered`: importar `helpertips.pages.sinal`, verificar `dash.page_registry` contem path "/sinal"
    - `test_sinal_page_has_back_button`: verificar que o layout contem `dcc.Location(id="sinal-url")` e `html.Div(id="sinal-content")`
    - `test_render_sinal_invalid_id`: chamar render_sinal com search="?id=abc", verificar retorno contem "invalido" (sem excecao)
    - `test_render_sinal_missing_id`: chamar render_sinal com search="", verificar retorno contem "invalido"

    Adicionar a `tests/test_dashboard.py`:
    - `test_navigate_to_sinal`: chamar navigate_to_sinal com cell_clicked={"rowData": {"id": 42}}, assert retorno == "/sinal?id=42"
    - `test_navigate_to_sinal_no_id`: chamar com cell_clicked={"rowData": {}}, assert retorno == no_update
    - `test_navigate_to_sinal_none`: chamar com cell_clicked=None, assert retorno == no_update
    - `test_history_rowdata_includes_id`: verificar que o callback master inclui "id" no rowData (testar via mock do get_signal_history retornando sinais com id)
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_sinal_page.py tests/test_dashboard.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - test -f helpertips/pages/sinal.py retorna 0
    - grep 'register_page.*path="/sinal"' helpertips/pages/sinal.py retorna match
    - grep 'id="sinal-url"' helpertips/pages/sinal.py retorna match
    - grep 'id="sinal-content"' helpertips/pages/sinal.py retorna match
    - grep '"id": sig.get("id")' helpertips/pages/home.py retorna match
    - grep 'def navigate_to_sinal' helpertips/pages/home.py retorna match
    - grep 'prevent_initial_call=True' helpertips/pages/home.py retorna match (no callback navigate_to_sinal)
    - grep 'Sinal nao encontrado' helpertips/pages/sinal.py retorna match
    - grep 'href="/"' helpertips/pages/sinal.py retorna match (botao Voltar)
    - grep 'Totais Consolidados' helpertips/pages/sinal.py retorna match
    - grep 'Entrada Principal' helpertips/pages/sinal.py retorna match
    - grep 'Entradas Complementares' helpertips/pages/sinal.py retorna match
    - python3 -m pytest tests/test_sinal_page.py tests/test_dashboard.py -x -q retorna 0
    - python3 -m pytest -x -q retorna 0 (suite completa)
  </acceptance_criteria>

  <done>
    Pagina sinal.py registrada em /sinal com card principal, tabela complementares e totais. AG Grid navega via cellClicked para /sinal?id=N. Botao Voltar retorna a /. ID inexistente mostra mensagem amigavel. Todos os testes passam.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao visual da pagina de detalhe</name>
  <files>helpertips/pages/sinal.py, helpertips/pages/home.py</files>
  <action>
    Verificacao visual humana da pagina de detalhe do sinal. O usuario deve testar o fluxo completo:
    navegacao via AG Grid, layout do detalhe (card principal, tabela complementares, totais),
    botao Voltar, e tratamento de IDs inexistentes/invalidos.

    Passos de verificacao:
    1. Abrir o dashboard: `python -m helpertips.dashboard` (ou via gunicorn se em producao)
    2. Na pagina principal, clicar em qualquer linha do AG Grid (historico de sinais)
    3. Verificar que a URL muda para `/sinal?id=N` sem recarregar a pagina
    4. Na pagina de detalhe, verificar:
       a. Card "Entrada Principal" mostra mercado, odd, stake, resultado (GREEN/RED colorido), horario e lucro
       b. Tabela "Entradas Complementares" mostra uma linha por complementar com nome, odd, stake, resultado e lucro
       c. Card "Totais Consolidados" mostra investido total, retorno total e lucro liquido
       d. Resultado GREEN aparece em verde, RED em vermelho, N/A em cinza
    5. Clicar no botao "Voltar" — deve retornar ao dashboard (pagina /)
    6. Acessar manualmente `/sinal?id=999999` — deve exibir "Sinal nao encontrado" sem erro
    7. Acessar manualmente `/sinal?id=abc` — deve exibir "ID de sinal invalido" sem erro
  </action>
  <verify>
    <automated>python3 -m pytest -x -q</automated>
  </verify>
  <done>Usuario aprovou visualmente o layout e funcionalidade da pagina de detalhe do sinal.</done>
</task>

</tasks>

<verification>
python3 -m pytest tests/test_sinal_page.py tests/test_dashboard.py -x -q
python3 -m pytest -x -q
Verificacao visual manual da pagina de detalhe
</verification>

<success_criteria>
- Clicar em linha do AG Grid navega para /sinal?id=N sem reload
- Pagina exibe card principal com todos os campos de SIG-02
- Tabela de complementares exibe breakdown individual (SIG-03)
- Totais consolidados no rodape (SIG-04)
- Botao Voltar retorna a / (SIG-05)
- ID inexistente exibe mensagem amigavel (SIG-06)
- Suite completa de testes verde
</success_criteria>

<output>
After completion, create `.planning/phases/15-p-gina-de-detalhe-do-sinal/15-02-SUMMARY.md`
</output>
