---
phase: 16-navega-o-schema-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpertips/db.py
  - helpertips/dashboard.py
  - helpertips/pages/config.py
  - tests/test_dashboard.py
  - tests/test_db.py
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "Menu/tabs visivel no topo permite alternar entre Dashboard (/) e Configuracoes (/config)"
    - "Tab ativa fica destacada visualmente indicando pagina atual"
    - "Tabela mercados tem colunas stake_base, fator_progressao e max_tentativas"
    - "Migracao idempotente: rodar ensure_schema duas vezes nao gera erro"
  artifacts:
    - path: "helpertips/db.py"
      provides: "Migration adicionando 3 colunas a tabela mercados"
      contains: "stake_base"
    - path: "helpertips/dashboard.py"
      provides: "Shell com navegacao por tabs entre / e /config"
      contains: "dbc.Nav"
    - path: "helpertips/pages/config.py"
      provides: "Pagina placeholder /config registrada via Dash Pages"
      contains: "register_page"
    - path: "tests/test_dashboard.py"
      provides: "Testes de navegacao e estrutura"
      contains: "nav-tabs"
  key_links:
    - from: "helpertips/dashboard.py"
      to: "helpertips/pages/config.py"
      via: "Dash Pages auto-discovery (use_pages=True)"
      pattern: "register_page.*path=./config"
    - from: "helpertips/dashboard.py"
      to: "helpertips/pages/home.py"
      via: "dbc.NavLink href=/"
      pattern: "NavLink.*href=./"
---

<objective>
Adicionar navegacao por tabs entre Dashboard e Configuracoes no shell do dashboard, criar pagina placeholder /config, e migrar schema DB adicionando colunas de config editavel na tabela mercados.

Purpose: Desbloquear Phase 17 (formulario /config) com navegacao funcional e schema pronto para persistir configuracoes.
Output: Menu de navegacao no topo, pagina /config placeholder, colunas stake_base/fator_progressao/max_tentativas em mercados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@helpertips/dashboard.py
@helpertips/db.py
@helpertips/pages/__init__.py
@helpertips/pages/home.py
@helpertips/pages/sinal.py
@tests/test_dashboard.py
@tests/test_db.py

<interfaces>
<!-- dashboard.py — shell atual sem navegacao -->
app = dash.Dash(__name__, use_pages=True, suppress_callback_exceptions=True, ...)
app.layout = dbc.Container([dcc.Location(id="url-nav", refresh="callback-nav"), dash.page_container], fluid=True)

<!-- pages/home.py — registrada em / -->
dash.register_page(__name__, path="/")

<!-- pages/sinal.py — registrada em /sinal -->
dash.register_page(__name__, path="/sinal")

<!-- db.py — tabela mercados atual -->
CREATE TABLE IF NOT EXISTS mercados (
    id           SERIAL PRIMARY KEY,
    slug         TEXT UNIQUE NOT NULL,
    nome_display TEXT NOT NULL,
    odd_ref      NUMERIC(6,2) NOT NULL,
    ativo        BOOLEAN NOT NULL DEFAULT TRUE
);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration DB — colunas de config em mercados + testes</name>
  <files>helpertips/db.py, tests/test_db.py</files>
  <read_first>
    - helpertips/db.py (schema atual com ensure_schema)
    - tests/test_db.py (testes de schema existentes)
  </read_first>
  <action>
Em `helpertips/db.py`, dentro da funcao `ensure_schema`, APOS os blocos existentes de migration Phase 09 (apos a linha `CREATE INDEX IF NOT EXISTS idx_signals_mercado_id`), adicionar um novo bloco de migration Phase 16:

```python
# --- Migration Phase 16: config editavel em mercados ---
cur.execute("ALTER TABLE mercados ADD COLUMN IF NOT EXISTS stake_base NUMERIC(10,2) NOT NULL DEFAULT 10.00")
cur.execute("ALTER TABLE mercados ADD COLUMN IF NOT EXISTS fator_progressao NUMERIC(4,2) NOT NULL DEFAULT 2.00")
cur.execute("ALTER TABLE mercados ADD COLUMN IF NOT EXISTS max_tentativas SMALLINT NOT NULL DEFAULT 4")
```

Valores default:
- `stake_base`: 10.00 (R$10 — valor padrao atual hardcoded)
- `fator_progressao`: 2.00 (dobra a cada tentativa — Martingale padrao)
- `max_tentativas`: 4 (T1 a T4 — padrao atual)

Usar `NOT NULL DEFAULT` garante que registros existentes recebem o default automaticamente. `ADD COLUMN IF NOT EXISTS` garante idempotencia.

Em `tests/test_db.py`, adicionar teste verificando que as 3 colunas existem apos ensure_schema:

```python
def test_mercados_config_columns_exist(db_conn):
    """Phase 16: mercados tem colunas stake_base, fator_progressao, max_tentativas."""
    ensure_schema(db_conn)
    with db_conn.cursor() as cur:
        cur.execute("""
            SELECT column_name, data_type, column_default, is_nullable
            FROM information_schema.columns
            WHERE table_name = 'mercados'
            AND column_name IN ('stake_base', 'fator_progressao', 'max_tentativas')
            ORDER BY column_name
        """)
        cols = {row[0]: row for row in cur.fetchall()}
    assert 'fator_progressao' in cols
    assert 'max_tentativas' in cols
    assert 'stake_base' in cols
```

Adicionar teste de idempotencia (chamar ensure_schema duas vezes sem erro):

```python
def test_ensure_schema_idempotent(db_conn):
    """ensure_schema pode rodar multiplas vezes sem erro."""
    ensure_schema(db_conn)
    ensure_schema(db_conn)  # Segunda vez nao deve levantar excecao
```
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python -m pytest tests/test_db.py -x -v -k "mercados_config or idempotent" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "stake_base" helpertips/db.py retorna 0
    - grep -q "fator_progressao" helpertips/db.py retorna 0
    - grep -q "max_tentativas" helpertips/db.py retorna 0
    - grep -q "IF NOT EXISTS" helpertips/db.py (para cada ALTER TABLE da Phase 16)
    - grep -q "test_mercados_config_columns_exist" tests/test_db.py retorna 0
    - grep -q "test_ensure_schema_idempotent" tests/test_db.py retorna 0
    - python -m pytest tests/test_db.py -x passa sem erro
  </acceptance_criteria>
  <done>Tabela mercados tem colunas stake_base (DEFAULT 10.00), fator_progressao (DEFAULT 2.00) e max_tentativas (DEFAULT 4). Migration idempotente confirmada por testes.</done>
</task>

<task type="auto">
  <name>Task 2: Navegacao por tabs no shell + pagina placeholder /config + testes (NAV-01)</name>
  <files>helpertips/dashboard.py, helpertips/pages/config.py, tests/test_dashboard.py</files>
  <read_first>
    - helpertips/dashboard.py (shell atual — 34 linhas, sem navegacao)
    - helpertips/pages/home.py (linhas 1-45 — para ver register_page pattern)
    - helpertips/pages/sinal.py (linhas 1-30 — para ver register_page pattern)
    - tests/test_dashboard.py (testes existentes de layout e IDs)
  </read_first>
  <action>
**1. Criar `helpertips/pages/config.py`** — pagina placeholder registrada em `/config`:

```python
"""
pages/config.py — Pagina de Configuracoes de Mercado (placeholder Phase 16).

Conteudo real sera implementado na Phase 17.
"""
import dash
import dash_bootstrap_components as dbc
from dash import html

dash.register_page(__name__, path="/config", name="Configuracoes")

layout = dbc.Container([
    html.H2("Configuracoes de Mercado", className="mt-4 mb-3"),
    dbc.Alert(
        "Pagina em construcao. Configuracoes editaveis serao adicionadas na proxima versao.",
        color="info",
    ),
], fluid=True)
```

**2. Atualizar `helpertips/dashboard.py`** — adicionar barra de navegacao com tabs no topo ANTES de `dash.page_container`:

Importar `html` de dash e `Input, Output` de dash (para callback de tab ativa). Adicionar callback que marca tab ativa baseado no pathname.

Layout atualizado:

```python
import os

import dash
import dash_bootstrap_components as dbc
from dash import dcc, html, Input, Output

app = dash.Dash(
    __name__,
    use_pages=True,
    suppress_callback_exceptions=True,
    external_stylesheets=[dbc.themes.DARKLY],
    title="HelperTips \u2014 Futebol Virtual",
)
server = app.server

app.layout = dbc.Container([
    dcc.Location(id="url-nav", refresh="callback-nav"),
    dbc.Nav(
        [
            dbc.NavLink("Dashboard", href="/", id="nav-link-home", active="exact"),
            dbc.NavLink("Configuracoes", href="/config", id="nav-link-config", active="exact"),
        ],
        id="nav-tabs",
        pills=True,
        className="my-3",
    ),
    dash.page_container,
], fluid=True)

if __name__ == "__main__":
    debug = os.getenv("DASH_DEBUG", "false").lower() == "true"
    app.run(debug=debug)
```

Nota: `active="exact"` no dbc.NavLink destaca automaticamente o link cuja `href` corresponde exatamente ao pathname atual — NAO precisa de callback manual. Isso funciona nativamente com Dash Pages + dbc 2.x.

**3. Atualizar `tests/test_dashboard.py`** — adicionar testes para NAV-01:

```python
def test_nav_tabs_present():
    """NAV-01: nav-tabs presente no layout do shell."""
    ids = collect_ids(app.layout)
    assert "nav-tabs" in ids

def test_nav_links_present():
    """NAV-01: links Dashboard e Configuracoes presentes no nav."""
    ids = collect_ids(app.layout)
    assert "nav-link-home" in ids
    assert "nav-link-config" in ids

def test_config_page_registered():
    """NAV-01: pagina /config registrada no Dash Pages."""
    page_paths = [p["path"] for p in dash.page_registry.values()]
    assert "/config" in page_paths
```
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python -m pytest tests/test_dashboard.py -x -v -k "nav_tabs or nav_links or config_page" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "dbc.Nav" helpertips/dashboard.py retorna 0
    - grep -q "nav-tabs" helpertips/dashboard.py retorna 0
    - grep -q "nav-link-home" helpertips/dashboard.py retorna 0
    - grep -q "nav-link-config" helpertips/dashboard.py retorna 0
    - grep -q 'active="exact"' helpertips/dashboard.py retorna 0
    - grep -q "register_page" helpertips/pages/config.py retorna 0
    - grep -q '/config' helpertips/pages/config.py retorna 0
    - grep -q "test_nav_tabs_present" tests/test_dashboard.py retorna 0
    - grep -q "test_config_page_registered" tests/test_dashboard.py retorna 0
    - python -m pytest tests/test_dashboard.py -x passa sem erro
    - python -m pytest tests/ -x passa sem regressao
  </acceptance_criteria>
  <done>Menu com pills "Dashboard" e "Configuracoes" visivel no topo de todas as paginas. Tab ativa destacada via active="exact". Pagina /config exibe placeholder. Testes NAV-01 passando.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -x -v` — todos os testes passam sem regressao
2. `grep -c "stake_base\|fator_progressao\|max_tentativas" helpertips/db.py` — retorna 3 (uma linha por coluna)
3. `grep -c "nav-tabs\|nav-link-home\|nav-link-config" helpertips/dashboard.py` — retorna 3
4. `grep -c "register_page" helpertips/pages/config.py` — retorna 1
</verification>

<success_criteria>
- Menu de navegacao funcional no topo alternando entre / e /config
- Tab ativa destacada visualmente (active="exact" do dbc.NavLink)
- Pagina /config acessivel com placeholder informativo
- Colunas stake_base, fator_progressao, max_tentativas existem em mercados com defaults corretos
- Migration idempotente (ensure_schema roda N vezes sem erro)
- Zero regressoes nos testes existentes
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/16-navega-o-schema-db/16-01-SUMMARY.md`
</output>
