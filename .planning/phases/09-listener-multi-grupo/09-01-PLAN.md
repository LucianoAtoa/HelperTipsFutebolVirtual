---
phase: 09-listener-multi-grupo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpertips/db.py
  - helpertips/store.py
  - tests/test_store.py
  - tests/test_db.py
autonomous: true
requirements: [LIST-01, LIST-02]
must_haves:
  truths:
    - "Schema signals tem colunas group_id BIGINT NOT NULL e mercado_id INTEGER REFERENCES mercados(id)"
    - "Constraint UNIQUE(group_id, message_id) substitui UNIQUE(message_id)"
    - "upsert_signal usa ON CONFLICT (group_id, message_id) e insere mercado_id"
    - "_resolve_mercado_id('Over 2.5') retorna 1, 'Ambas Marcam' retorna 2, desconhecido retorna None"
    - "Dados historicos recebem group_id do grupo original via UPDATE retroativo"
    - "ensure_schema() e idempotente — pode rodar N vezes sem erro"
  artifacts:
    - path: "helpertips/db.py"
      provides: "Migration SQL com group_id, mercado_id, constraint composta, UPDATE retroativo"
      contains: "ADD COLUMN IF NOT EXISTS group_id"
    - path: "helpertips/store.py"
      provides: "Dicionario ENTRADA_PARA_MERCADO_ID, _resolve_mercado_id(), upsert atualizado"
      contains: "ENTRADA_PARA_MERCADO_ID"
    - path: "tests/test_store.py"
      provides: "Testes de upsert com group_id, mercado_id, deduplicacao multi-grupo"
      contains: "test_upsert_two_groups_same_message_id"
    - path: "tests/test_db.py"
      provides: "Testes de migration novas colunas e constraint"
      contains: "test_migration_adds_group_id_column"
  key_links:
    - from: "helpertips/store.py"
      to: "helpertips/db.py"
      via: "upsert_signal depende do schema com group_id e mercado_id"
      pattern: "ON CONFLICT \\(group_id, message_id\\)"
    - from: "helpertips/store.py"
      to: "tabela mercados"
      via: "_resolve_mercado_id mapeia entrada para mercado_id FK"
      pattern: "ENTRADA_PARA_MERCADO_ID"
---

<objective>
Migrar schema PostgreSQL para suportar multi-grupo e adicionar logica de resolucao entrada->mercado_id no store.

Purpose: Sem as colunas group_id e mercado_id na tabela signals, o listener nao consegue armazenar sinais de multiplos grupos nem vincular ao mercado correto. Esta e a fundacao de dados para toda a fase.
Output: db.py com migration idempotente, store.py com upsert atualizado e dicionario de mercados, testes cobrindo os novos comportamentos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-listener-multi-grupo/09-CONTEXT.md
@.planning/phases/09-listener-multi-grupo/09-RESEARCH.md

@helpertips/db.py
@helpertips/store.py
@tests/test_store.py
@tests/test_db.py

<interfaces>
<!-- Tabela mercados ja existente (seeds em db.py) -->
<!-- mercados: id=1 slug='over_2_5', id=2 slug='ambas_marcam' -->

From helpertips/store.py (assinatura atual):
```python
def upsert_signal(conn, data: dict) -> None:
    # ON CONFLICT (message_id) DO UPDATE
```

From helpertips/db.py (schema atual):
```python
# signals: message_id BIGINT UNIQUE NOT NULL
# mercados: id SERIAL PRIMARY KEY, slug TEXT UNIQUE NOT NULL
```

From tests/test_store.py (helper atual):
```python
def _make_signal(message_id: int, resultado=None, placar=None, tentativa=None) -> dict:
    # Nao inclui group_id — precisa ser adicionado
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration SQL + store._resolve_mercado_id + upsert atualizado</name>
  <files>helpertips/db.py, helpertips/store.py, tests/test_store.py, tests/test_db.py</files>
  <read_first>
    - helpertips/db.py (schema atual com ensure_schema, ver estrutura da migration existente)
    - helpertips/store.py (upsert_signal atual com ON CONFLICT message_id)
    - tests/test_store.py (_make_signal helper e testes existentes)
    - tests/test_db.py (testes de schema e seeds existentes)
  </read_first>
  <behavior>
    - test_resolve_mercado_id_over: _resolve_mercado_id("Over 2.5") == 1
    - test_resolve_mercado_id_ambas: _resolve_mercado_id("Ambas Marcam") == 2
    - test_resolve_mercado_id_ambas_variante: _resolve_mercado_id("Ambas as equipes marcam") == 2
    - test_resolve_mercado_id_case_insensitive: _resolve_mercado_id("over 2.5") == 1, _resolve_mercado_id("AMBAS MARCAM") == 2
    - test_resolve_mercado_id_unknown: _resolve_mercado_id("Outro Mercado") is None (D-06: logar warning mas nao rejeitar)
    - test_resolve_mercado_id_none: _resolve_mercado_id(None) is None
    - test_upsert_with_group_id: upsert_signal com group_id=12345 insere corretamente, row tem group_id=12345
    - test_upsert_with_mercado_id: upsert_signal com entrada="Over 2.5" insere mercado_id=1
    - test_upsert_two_groups_same_message_id: group_id=111 message_id=1000 e group_id=222 message_id=1000 geram 2 rows distintas (LIST-01)
    - test_migration_adds_group_id_column: ensure_schema() cria coluna group_id na tabela signals
    - test_migration_adds_mercado_id_column: ensure_schema() cria coluna mercado_id na tabela signals
    - test_migration_idempotent_with_new_columns: ensure_schema() 2x nao gera erro
  </behavior>
  <action>
    **Em helpertips/db.py — ensure_schema():**

    Adicionar ao final do bloco SQL principal (antes do conn.commit()), APOS os CREATE TABLE e seeds existentes:

    ```python
    # --- Migration Phase 09: multi-grupo ---
    # Passo 1: Adicionar colunas (per D-10)
    cur.execute("ALTER TABLE signals ADD COLUMN IF NOT EXISTS group_id BIGINT")
    cur.execute("ALTER TABLE signals ADD COLUMN IF NOT EXISTS mercado_id INTEGER REFERENCES mercados(id)")

    # Passo 2: UPDATE retroativo — dados historicos recebem group_id original (per D-11)
    # Usa TELEGRAM_GROUP_ID (singular) como fallback — e o unico grupo que existia antes
    original_group_id = os.environ.get('TELEGRAM_GROUP_IDS', os.environ.get('TELEGRAM_GROUP_ID', '')).split(',')[0].strip()
    if original_group_id:
        cur.execute(
            "UPDATE signals SET group_id = %s WHERE group_id IS NULL",
            (int(original_group_id),)
        )

    # Passo 3: SET NOT NULL somente se nao ha NULLs restantes (per D-10 pitfall)
    cur.execute("SELECT COUNT(*) FROM signals WHERE group_id IS NULL")
    null_count = cur.fetchone()[0]
    if null_count == 0:
        cur.execute("""
            DO $$
            BEGIN
                IF EXISTS (
                    SELECT 1 FROM information_schema.columns
                    WHERE table_name='signals' AND column_name='group_id'
                    AND is_nullable='YES'
                ) THEN
                    ALTER TABLE signals ALTER COLUMN group_id SET NOT NULL;
                END IF;
            END $$;
        """)

    # Passo 4: Drop constraint antiga e criar nova (per D-08, D-12)
    cur.execute("ALTER TABLE signals DROP CONSTRAINT IF EXISTS signals_message_id_key")
    cur.execute("""
        DO $$
        BEGIN
            IF NOT EXISTS (
                SELECT 1 FROM pg_constraint WHERE conname = 'signals_group_message_unique'
            ) THEN
                ALTER TABLE signals ADD CONSTRAINT signals_group_message_unique UNIQUE (group_id, message_id);
            END IF;
        END $$;
    """)

    # Passo 5: Indice em group_id (Claude's Discretion)
    cur.execute("CREATE INDEX IF NOT EXISTS idx_signals_group_id ON signals(group_id)")
    cur.execute("CREATE INDEX IF NOT EXISTS idx_signals_mercado_id ON signals(mercado_id)")
    ```

    Adicionar `import os` no topo de db.py (ja tem `import os` — verificar).

    **Em helpertips/store.py:**

    1. Adicionar import logging no topo:
       ```python
       import logging
       logger = logging.getLogger("helpertips.store")
       ```

    2. Adicionar dicionario ENTRADA_PARA_MERCADO_ID e funcao _resolve_mercado_id (per D-04):
       ```python
       ENTRADA_PARA_MERCADO_ID = {
           "over 2.5": 1,
           "ambas marcam": 2,
           "ambas as equipes marcam": 2,
       }

       def _resolve_mercado_id(entrada: str | None) -> int | None:
           if not entrada:
               return None
           normalized = entrada.lower().strip()
           mercado_id = ENTRADA_PARA_MERCADO_ID.get(normalized)
           if mercado_id is None:
               logger.warning("Mercado desconhecido para entrada '%s' — inserindo com mercado_id=NULL", entrada)
           return mercado_id
       ```

    3. Atualizar upsert_signal para:
       - Chamar `_resolve_mercado_id(data.get('entrada'))` para obter mercado_id
       - Adicionar `group_id` e `mercado_id` no INSERT e no dict de parametros
       - Mudar `ON CONFLICT (message_id)` para `ON CONFLICT (group_id, message_id)` (per D-09)
       - Adicionar `mercado_id = COALESCE(EXCLUDED.mercado_id, signals.mercado_id)` no DO UPDATE
       - Remover a linha duplicada `OR signals.resultado IS NULL` no WHERE (bug existente)

       SQL final do upsert:
       ```sql
       INSERT INTO signals (
           message_id, group_id, mercado_id, liga, entrada, horario, periodo,
           dia_semana, resultado, placar, tentativa, raw_text, received_at, updated_at
       ) VALUES (
           %(message_id)s, %(group_id)s, %(mercado_id)s, %(liga)s, %(entrada)s,
           %(horario)s, %(periodo)s, %(dia_semana)s, %(resultado)s, %(placar)s,
           %(tentativa)s, %(raw_text)s, NOW(), NOW()
       )
       ON CONFLICT (group_id, message_id) DO UPDATE SET
           resultado  = COALESCE(EXCLUDED.resultado, signals.resultado),
           placar     = COALESCE(EXCLUDED.placar, signals.placar),
           tentativa  = COALESCE(EXCLUDED.tentativa, signals.tentativa),
           mercado_id = COALESCE(EXCLUDED.mercado_id, signals.mercado_id),
           liga       = EXCLUDED.liga,
           entrada    = EXCLUDED.entrada,
           updated_at = NOW()
       WHERE
           EXCLUDED.resultado IS NOT NULL
           OR signals.resultado IS NULL
       ```
       Passar `{**data, 'mercado_id': mercado_id}` como parametro.

    **Em tests/test_store.py:**

    4. Atualizar `_make_signal` para aceitar `group_id` com default `99999`:
       ```python
       def _make_signal(message_id: int, resultado=None, placar=None, tentativa=None, group_id=99999) -> dict:
           return {
               "message_id": message_id,
               "group_id": group_id,
               ...  # demais campos iguais
           }
       ```

    5. Adicionar testes unitarios para _resolve_mercado_id (importar de store):
       - test_resolve_mercado_id_over, test_resolve_mercado_id_ambas, etc. (ver <behavior>)

    6. Adicionar testes de integracao:
       - test_upsert_with_group_id: insere com group_id=12345, verifica SELECT
       - test_upsert_with_mercado_id: insere com entrada="Over 2.5", verifica mercado_id=1 no row
       - test_upsert_two_groups_same_message_id: insere group_id=111/msg=1000 e group_id=222/msg=1000, verifica COUNT(*)=2

    **Em tests/test_db.py:**

    7. Adicionar testes para migration:
       - test_migration_adds_group_id_column: verifica coluna group_id em information_schema.columns
       - test_migration_adds_mercado_id_column: verifica coluna mercado_id em information_schema.columns
       - test_migration_idempotent_with_new_columns: ensure_schema() 2x sem erro, colunas existem

    **IMPORTANTE:** Os testes existentes em test_store.py precisam continuar passando. Os testes que usam _make_signal sem group_id usarao o default 99999, que sera aceito pelo schema.
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_store.py tests/test_db.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/store.py contem `ENTRADA_PARA_MERCADO_ID = {`
    - helpertips/store.py contem `def _resolve_mercado_id(entrada`
    - helpertips/store.py contem `ON CONFLICT (group_id, message_id)`
    - helpertips/store.py contem `mercado_id = COALESCE(EXCLUDED.mercado_id`
    - helpertips/db.py contem `ADD COLUMN IF NOT EXISTS group_id BIGINT`
    - helpertips/db.py contem `ADD COLUMN IF NOT EXISTS mercado_id INTEGER REFERENCES mercados(id)`
    - helpertips/db.py contem `signals_group_message_unique`
    - helpertips/db.py contem `DROP CONSTRAINT IF EXISTS signals_message_id_key`
    - tests/test_store.py contem `def test_resolve_mercado_id`
    - tests/test_store.py contem `def test_upsert_two_groups_same_message_id`
    - tests/test_store.py::_make_signal tem parametro `group_id=99999`
    - tests/test_db.py contem `def test_migration_adds_group_id_column`
    - `python3 -m pytest tests/test_store.py tests/test_db.py -x -q` exit code 0
  </acceptance_criteria>
  <done>
    Schema signals tem group_id BIGINT NOT NULL e mercado_id INTEGER REFERENCES mercados(id). Constraint UNIQUE(group_id, message_id) ativa. upsert_signal usa ON CONFLICT (group_id, message_id) com mercado_id resolvido pelo dicionario. Todos os testes existentes e novos passam.
  </done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_store.py tests/test_db.py -x -q` — todos os testes passam
- `grep -c "ON CONFLICT (group_id, message_id)" helpertips/store.py` retorna 1
- `grep -c "ENTRADA_PARA_MERCADO_ID" helpertips/store.py` retorna pelo menos 2
- `grep -c "signals_group_message_unique" helpertips/db.py` retorna pelo menos 1
</verification>

<success_criteria>
Schema migrado com group_id e mercado_id. Store resolve entrada para mercado_id. Upsert usa constraint composta. Testes cobrem deduplicacao multi-grupo, resolucao de mercado, migration idempotente. Suite completa verde.
</success_criteria>

<output>
After completion, create `.planning/phases/09-listener-multi-grupo/09-01-SUMMARY.md`
</output>
