---
phase: 09-listener-multi-grupo
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - helpertips/listener.py
  - helpertips/list_groups.py
  - .env.example
autonomous: true
requirements: [LIST-01, LIST-02]
must_haves:
  truths:
    - "Listener parseia TELEGRAM_GROUP_IDS=id1,id2 do .env em lista de ints"
    - "Fallback le TELEGRAM_GROUP_ID se TELEGRAM_GROUP_IDS nao existe (D-14)"
    - "Handler unico registrado com chats=group_ids (lista) para NewMessage e MessageEdited"
    - "event.chat_id e passado como group_id no dict do parsed signal antes do upsert"
    - "Startup verifica acesso a cada grupo via get_entity, remove invalidos, aborta se nenhum valido"
    - "Startup summary mostra nomes de todos os grupos conectados"
    - ".env.example documenta TELEGRAM_GROUP_IDS no lugar de TELEGRAM_GROUP_ID"
  artifacts:
    - path: "helpertips/listener.py"
      provides: "Handler multi-grupo com chats=[id1,id2], startup multi-grupo, group_id no parsed dict"
      contains: "TELEGRAM_GROUP_IDS"
    - path: ".env.example"
      provides: "Template atualizado com TELEGRAM_GROUP_IDS"
      contains: "TELEGRAM_GROUP_IDS="
  key_links:
    - from: "helpertips/listener.py"
      to: "helpertips/store.py"
      via: "parsed['group_id'] = event.chat_id passado para upsert_signal"
      pattern: "parsed\\['group_id'\\] = event\\.chat_id"
    - from: "helpertips/listener.py"
      to: ".env"
      via: "os.environ.get('TELEGRAM_GROUP_IDS') com fallback para TELEGRAM_GROUP_ID"
      pattern: "TELEGRAM_GROUP_IDS.*TELEGRAM_GROUP_ID"
---

<objective>
Atualizar listener.py para escutar multiplos grupos Telegram simultaneamente via handler unico e configuracao por .env.

Purpose: Com o schema pronto (Plan 01), o listener precisa ser adaptado para receber sinais de Over 2.5 e Ambas Marcam no mesmo processo, passando group_id para o upsert e verificando acesso a cada grupo no startup.
Output: listener.py multi-grupo funcional, .env.example atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-listener-multi-grupo/09-CONTEXT.md
@.planning/phases/09-listener-multi-grupo/09-RESEARCH.md
@.planning/phases/09-listener-multi-grupo/09-01-SUMMARY.md

@helpertips/listener.py
@helpertips/store.py
@.env.example

<interfaces>
<!-- De store.py (atualizado no Plan 01): -->
```python
def upsert_signal(conn, data: dict) -> None:
    # data DEVE conter 'group_id' (int) — obrigatorio apos Plan 01
    # data['entrada'] e usado internamente para resolver mercado_id
    # ON CONFLICT (group_id, message_id) DO UPDATE
```

<!-- De parser.py (nao muda): -->
```python
def parse_message(text: str, message_id: int) -> dict | None:
    # Retorna dict com: message_id, liga, entrada, horario, periodo,
    # dia_semana, resultado, placar, tentativa, raw_text
    # NAO inclui group_id — listener deve adicionar
```

<!-- De db.py (nao muda nesta task): -->
```python
def validate_config() -> None:  # REQUIRED_VARS nao inclui TELEGRAM_GROUP_IDS
def ensure_schema(conn) -> None:  # Migration ja cria group_id, mercado_id
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Listener multi-grupo + .env.example</name>
  <files>helpertips/listener.py, .env.example</files>
  <read_first>
    - helpertips/listener.py (handler atual com single group_id, startup, print_startup_summary)
    - helpertips/store.py (upsert_signal atualizado no Plan 01 — confirmar que aceita group_id)
    - helpertips/parser.py (parse_message — confirmar que NAO retorna group_id)
    - .env.example (template atual com TELEGRAM_GROUP_ID)
  </read_first>
  <action>
    **Em helpertips/listener.py — funcao main():**

    1. Substituir o bloco de parse de group_id (linhas ~122-143 atuais) por parse multi-grupo (per D-01, D-02, D-14):

       ```python
       # Parse TELEGRAM_GROUP_IDS (CSV) com fallback para TELEGRAM_GROUP_ID (D-14)
       group_ids_str = os.environ.get('TELEGRAM_GROUP_IDS') or os.environ.get('TELEGRAM_GROUP_ID', '')
       group_ids = [int(g.strip()) for g in group_ids_str.split(',') if g.strip()]

       if not group_ids:
           if sys.stdout.isatty():
               console.print("[yellow]TELEGRAM_GROUP_IDS nao configurado.[/yellow]")
               # Oferecer selecao interativa se TTY
               selected = await selecionar_grupo(client)
               if selected is None:
                   console.print("[red]Nenhum grupo selecionado. Encerrando.[/red]")
                   await client.disconnect()
                   sys.exit(1)
               group_ids = [selected]
           else:
               logger.error("TELEGRAM_GROUP_IDS vazio ou invalido. Encerrando.")
               await client.disconnect()
               sys.exit(1)
       ```

    2. Substituir verificacao de acesso single-group (linhas ~190-197 atuais) por verificacao multi-grupo (per D-03):

       ```python
       # Verificar acesso a cada grupo (D-03)
       group_titles = []
       invalid_ids = []
       for gid in group_ids:
           try:
               entity = await client.get_entity(gid)
               group_titles.append(f"{entity.title} ({gid})")
           except Exception as e:
               logger.error("Nao foi possivel acessar grupo %s: %s", gid, e)
               invalid_ids.append(gid)

       if invalid_ids:
           logger.warning("Grupos invalidos ignorados: %s", invalid_ids)
           group_ids = [g for g in group_ids if g not in invalid_ids]

       if not group_ids:
           logger.error("Nenhum grupo valido encontrado. Encerrando.")
           conn.close()
           await client.disconnect()
           sys.exit(1)
       ```

    3. Atualizar print_startup_summary para aceitar lista de titulos:

       Mudar assinatura de `print_startup_summary(conn, group_title)` para `print_startup_summary(conn, group_titles)` onde `group_titles` e uma lista de strings.

       No Panel title, usar: `title=f"[bold]HelperTips[/bold] - {', '.join(group_titles)}"` (TTY).
       No logger.info, usar: `grupo: %s` com `', '.join(group_titles)`.

       Chamada na main: `print_startup_summary(conn, group_titles)` (ja e lista).

    4. Atualizar o handler para multi-grupo (per D-01):

       ```python
       @client.on(events.NewMessage(chats=group_ids))
       @client.on(events.MessageEdited(chats=group_ids))
       async def handle_signal(event):
           global parse_total, parse_success, parse_fail_count

           if not event.message.text:
               return

           parse_total += 1
           parsed = parse_message(event.message.text, event.message.id)

           if parsed is None:
               parse_fail_count += 1
               reason = "no_liga_match"
               logger.warning("Parse falhou: %.80s...", event.message.text.replace('\n', ' '))
               await asyncio.to_thread(log_parse_failure, conn, event.message.text, reason)
               return

           parse_success += 1

           # Adicionar group_id ao dict do parsed signal (D-01)
           parsed['group_id'] = event.chat_id

           await asyncio.to_thread(upsert_signal, conn, parsed)

           # Log colorido (igual ao atual, sem mudanca)
           resultado = parsed.get('resultado')
           if sys.stdout.isatty():
               if resultado == 'GREEN':
                   console.print(f"  [bold green]GREEN[/bold green] | {parsed['liga']} | {parsed['entrada']}")
               elif resultado == 'RED':
                   console.print(f"  [bold red]RED[/bold red] | {parsed['liga']} | {parsed['entrada']}")
               else:
                   console.print(f"  [yellow]NOVO[/yellow] | {parsed['liga']} | {parsed['entrada']}")
           else:
               logger.info("%s | %s | %s", resultado or "NOVO", parsed['liga'], parsed['entrada'])
       ```

    5. Mover o registro do handler para DEPOIS da verificacao de acesso (atualmente esta antes do conn/ensure_schema — precisa estar depois pois group_ids pode ter sido filtrado).

       Ordem correta na main():
       a. Parse group_ids do env
       b. client.start() + Telegram conectado
       c. Se group_ids vazio, selecao interativa
       d. conn = get_connection() + ensure_schema(conn)
       e. Verificar acesso a cada grupo (get_entity loop)
       f. Registrar handler com @client.on(chats=group_ids) — NOTA: Como o handler e definido com decorators que capturam group_ids por closure, o handler deve ser definido APOS group_ids estar finalizado. Usar `client.add_event_handler` ao inves de decorators para permitir registro dinamico:
       ```python
       async def handle_signal(event):
           # ... corpo do handler ...

       client.add_event_handler(handle_signal, events.NewMessage(chats=group_ids))
       client.add_event_handler(handle_signal, events.MessageEdited(chats=group_ids))
       ```
       g. print_startup_summary(conn, group_titles)
       h. SIGINT handler + await stop

    **Em .env.example:**

    6. Substituir `TELEGRAM_GROUP_ID=` por:
       ```
       # Telegram group IDs (CSV) — obter via python3 -m helpertips.list_groups
       # Exemplo: TELEGRAM_GROUP_IDS=1234567890,-1001234567890
       TELEGRAM_GROUP_IDS=
       # Fallback (deprecated): TELEGRAM_GROUP_ID tambem e aceito se TELEGRAM_GROUP_IDS nao existir
       ```

    **NAO modificar:** helpertips/db.py, helpertips/store.py, helpertips/parser.py (ja atualizados ou nao precisam de mudanca).
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -c "
import ast, sys
tree = ast.parse(open('helpertips/listener.py').read())
source = open('helpertips/listener.py').read()
checks = [
    'TELEGRAM_GROUP_IDS' in source,
    'TELEGRAM_GROUP_ID' in source,  # fallback D-14
    'event.chat_id' in source,
    'group_id' in source,
    'add_event_handler' in source or 'chats=group_ids' in source,
]
print(f'Checks passed: {sum(checks)}/{len(checks)}')
if not all(checks): sys.exit(1)
print('All listener checks OK')
" && grep -q "TELEGRAM_GROUP_IDS=" .env.example && echo ".env.example OK" && python3 -m pytest tests/test_store.py tests/test_db.py tests/test_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/listener.py contem `os.environ.get('TELEGRAM_GROUP_IDS')`
    - helpertips/listener.py contem `os.environ.get('TELEGRAM_GROUP_ID'` (fallback D-14)
    - helpertips/listener.py contem `parsed['group_id'] = event.chat_id`
    - helpertips/listener.py contem `group_ids = [int(g.strip()) for g in group_ids_str.split`
    - helpertips/listener.py contem `client.add_event_handler` ou `chats=group_ids`
    - helpertips/listener.py contem `for gid in group_ids:` (loop de verificacao D-03)
    - helpertips/listener.py contem `get_entity(gid)` (verificacao por grupo)
    - .env.example contem `TELEGRAM_GROUP_IDS=`
    - .env.example NAO contem `TELEGRAM_GROUP_ID=` como variavel principal (pode ter como comentario de fallback)
    - `python3 -m pytest tests/ -x -q` exit code 0 (suite completa verde)
  </acceptance_criteria>
  <done>
    Listener escuta multiplos grupos via handler unico com chats=[group_ids]. event.chat_id e passado como group_id no dict antes do upsert. Startup verifica cada grupo individualmente. .env.example documenta TELEGRAM_GROUP_IDS. Fallback para TELEGRAM_GROUP_ID funciona (D-14). Suite de testes completa verde.
  </done>
</task>

</tasks>

<verification>
- `grep -c "TELEGRAM_GROUP_IDS" helpertips/listener.py` retorna pelo menos 1
- `grep -c "event.chat_id" helpertips/listener.py` retorna pelo menos 1
- `grep -c "parsed\['group_id'\]" helpertips/listener.py` retorna 1
- `grep "TELEGRAM_GROUP_IDS=" .env.example` retorna a linha
- `python3 -m pytest tests/ -x -q` — suite completa verde
</verification>

<success_criteria>
Listener multi-grupo funcional: parseia TELEGRAM_GROUP_IDS do .env, verifica acesso a cada grupo, registra handler unico com chats=[lista], passa event.chat_id como group_id para upsert. Fallback D-14 ativo. .env.example atualizado. Suite de testes completa verde.
</success_criteria>

<output>
After completion, create `.planning/phases/09-listener-multi-grupo/09-02-SUMMARY.md`
</output>
