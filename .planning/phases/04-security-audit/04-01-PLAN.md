---
phase: 04-security-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpertips/dashboard.py
  - .env.example
  - tests/test_dashboard.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "Historico git nao contem nenhum secret (.env, .session, API keys) em nenhum commit"
    - "Dashboard roda com debug=False quando DASH_DEBUG nao esta definido"
    - "Dashboard roda com debug=True quando DASH_DEBUG=true esta no .env"
    - ".env.example lista DASH_DEBUG e variaveis AWS comentadas"
  artifacts:
    - path: "helpertips/dashboard.py"
      provides: "Debug mode controlado por env var"
      contains: "os.getenv('DASH_DEBUG'"
    - path: ".env.example"
      provides: "Template de variaveis de ambiente completo"
      contains: "DASH_DEBUG"
    - path: "tests/test_dashboard.py"
      provides: "Testes unitarios para debug mode"
      contains: "test_debug_mode"
  key_links:
    - from: "helpertips/dashboard.py"
      to: ".env"
      via: "os.getenv('DASH_DEBUG', 'false')"
      pattern: "os\\.getenv\\('DASH_DEBUG'"
    - from: ".env.example"
      to: "helpertips/dashboard.py"
      via: "DASH_DEBUG documenta a variavel usada no codigo"
      pattern: "DASH_DEBUG"
---

<objective>
Auditar historico git para confirmar ausencia de secrets, corrigir debug=True hardcoded no dashboard para usar variavel de ambiente, atualizar .env.example com DASH_DEBUG e variaveis AWS, e adicionar testes para o debug mode.

Purpose: Eliminar o unico blocker de seguranca (debug=True expoe REPL remoto) e confirmar que o repositorio esta limpo para publicacao publica.
Output: dashboard.py seguro, .env.example completo, testes de debug mode passando, auditoria git documentada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-security-audit/04-CONTEXT.md
@.planning/phases/04-security-audit/04-RESEARCH.md
@.planning/phases/04-security-audit/04-VALIDATION.md
@helpertips/dashboard.py
@.env.example
@tests/test_dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar historico git e corrigir debug mode no dashboard</name>
  <files>helpertips/dashboard.py</files>
  <read_first>
    - helpertips/dashboard.py (ver estado atual da linha 997 e verificar se `import os` existe)
    - .planning/phases/04-security-audit/04-CONTEXT.md (decisoes D-01 e D-03)
  </read_first>
  <action>
**Parte A — Auditoria git (SEC-01, per D-03):**

Executar os 3 comandos de auditoria e registrar os resultados:

```bash
# 1. Verificar se .env ou .session foram alguma vez rastreados
git log --all --name-only --format="" | grep -E "\.env$|\.session$"

# 2. Verificar conteudo do historico por padroes de secrets reais
git log --all -p | grep -E "^\+" | grep -iE "(api_id|api_hash|db_password)\s*=\s*[^$'\"]" | head -20

# 3. Comando oficial do success criteria do ROADMAP
git log --all --full-diff -p -- .env '*.session'
```

Resultado esperado: saida vazia para todos os 3. Se algum retornar conteudo, parar e reportar — nao prosseguir sem resolver.

**Parte B — Corrigir debug mode (SEC-02, per D-01):**

1. Verificar se `import os` existe no topo de `helpertips/dashboard.py`. NAO existe atualmente — adicionar `import os` logo apos o docstring do modulo (linha 20, antes dos outros imports).

2. Substituir a linha 997:
```python
# ANTES (linha 997):
    app.run(debug=True, host="0.0.0.0", port=8050)

# DEPOIS:
    app.run(
        debug=os.getenv('DASH_DEBUG', 'false').lower() == 'true',
        host="0.0.0.0",
        port=8050,
    )
```

Exatamente esta expressao — `os.getenv('DASH_DEBUG', 'false').lower() == 'true'` — conforme decisao D-01 bloqueada.
  </action>
  <verify>
    <automated>grep -n "import os" helpertips/dashboard.py && grep "DASH_DEBUG" helpertips/dashboard.py && git log --all --full-diff -p -- .env '*.session' | wc -l | grep "^0$" && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `helpertips/dashboard.py` contem `import os` nas primeiras 25 linhas
    - `helpertips/dashboard.py` contem `os.getenv('DASH_DEBUG', 'false').lower() == 'true'`
    - `helpertips/dashboard.py` NAO contem `debug=True` (hardcoded)
    - `git log --all --full-diff -p -- .env '*.session'` retorna zero linhas
    - Suite de testes existente continua passando (132+ testes)
  </acceptance_criteria>
  <done>Auditoria git confirmou historico limpo. dashboard.py usa DASH_DEBUG env var para controlar debug mode, desligado por padrao.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar .env.example e adicionar testes de debug mode</name>
  <files>.env.example, tests/test_dashboard.py</files>
  <read_first>
    - .env.example (ver estado atual com 8 variaveis)
    - tests/test_dashboard.py (ver estrutura existente para adicionar testes)
    - .planning/phases/04-security-audit/04-RESEARCH.md (secao "Padrao D-04" e "Teste para SEC-02")
  </read_first>
  <action>
**Parte A — Atualizar .env.example (SEC-03, per D-04):**

Substituir o conteudo completo de `.env.example` por:

```
# Telegram API credentials — get from https://my.telegram.org
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_GROUP_ID=

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=helpertips
DB_USER=helpertips_user
DB_PASSWORD=

# Dashboard
# Set to true for local development hot-reload; false (default) in production
DASH_DEBUG=false

# AWS (Phase 6 — deixar vazio ate o deploy)
# AWS_REGION=
# AWS_S3_BUCKET=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
```

Total: 12 variaveis (8 existentes + DASH_DEBUG + 4 AWS comentadas).

**Parte B — Adicionar testes de debug mode (Wave 0 gap, SEC-02):**

Adicionar ao final de `tests/test_dashboard.py` dois testes:

```python
import os


def test_debug_mode_off_by_default(monkeypatch):
    """DASH_DEBUG ausente ou 'false' resulta em debug=False."""
    monkeypatch.delenv('DASH_DEBUG', raising=False)
    debug = os.getenv('DASH_DEBUG', 'false').lower() == 'true'
    assert debug is False


def test_debug_mode_on_with_env(monkeypatch):
    """DASH_DEBUG=true resulta em debug=True."""
    monkeypatch.setenv('DASH_DEBUG', 'true')
    debug = os.getenv('DASH_DEBUG', 'false').lower() == 'true'
    assert debug is True
```

Se `import os` ja estiver no topo do arquivo de testes, nao duplicar o import — apenas adicionar as funcoes.
  </action>
  <verify>
    <automated>grep "DASH_DEBUG" .env.example && grep "AWS_REGION" .env.example && python3 -m pytest tests/test_dashboard.py -k "debug" -v && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contem `DASH_DEBUG=false`
    - `.env.example` contem `# AWS_REGION=` (comentado)
    - `.env.example` contem `# AWS_SECRET_ACCESS_KEY=` (comentado)
    - `.env.example` NAO contem nenhum valor real de credencial
    - `tests/test_dashboard.py` contem `def test_debug_mode_off_by_default`
    - `tests/test_dashboard.py` contem `def test_debug_mode_on_with_env`
    - `python3 -m pytest tests/test_dashboard.py -k "debug" -x` retorna 2 testes passando
    - Suite completa continua verde
  </acceptance_criteria>
  <done>.env.example expandido com DASH_DEBUG e variaveis AWS. Testes unitarios para debug mode adicionados e passando.</done>
</task>

</tasks>

<verification>
- `git log --all --full-diff -p -- .env '*.session' | wc -l` retorna 0
- `grep "debug=True" helpertips/dashboard.py` retorna vazio (nenhum hardcoded)
- `grep "DASH_DEBUG" helpertips/dashboard.py .env.example` retorna matches em ambos
- `python3 -m pytest tests/test_dashboard.py -k "debug" -v` mostra 2 testes passando
- `python3 -m pytest tests/ -q` suite completa verde
</verification>

<success_criteria>
- Historico git auditado e confirmado limpo (SEC-01)
- Dashboard usa env var para debug mode, desligado por padrao (SEC-02)
- .env.example completo com DASH_DEBUG e variaveis AWS comentadas (SEC-03)
- Testes de debug mode passando
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-audit/04-01-SUMMARY.md`
</output>
