---
phase: 10-l-gica-financeira
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - helpertips/queries.py
  - tests/test_queries.py
autonomous: true
requirements: [FIN-01, FIN-02, FIN-03]

must_haves:
  truths:
    - "calculate_pl_por_entrada retorna P&L por sinal com breakdown principal vs complementar"
    - "GREEN T1 stake=100 odd=2.30: investido_principal=100, lucro_principal=130"
    - "GREEN T2 Gale stake=100 odd=2.30: investido_principal=300, lucro_principal=160"
    - "RED T4 Gale stake=100: investido_principal=1500, lucro_principal=-1500"
    - "Complementares Over 2.5 usam stakes 20%/1%/10%/1%/1%/1%/1% do principal"
    - "Complementares Ambas Marcam usam stakes 10%/1%/5%/1%/1%/1%/1% distintos"
    - "calculate_equity_curve_breakdown retorna 3 series: y_principal, y_complementar, y_total"
    - "y_total[i] == y_principal[i] + y_complementar[i] para todo i"
  artifacts:
    - path: "helpertips/queries.py"
      provides: "calculate_pl_por_entrada, calculate_equity_curve_breakdown"
      contains: "def calculate_pl_por_entrada"
    - path: "tests/test_queries.py"
      provides: "Testes de P&L e equity curve breakdown"
      contains: "test_pl_green_t1"
  key_links:
    - from: "helpertips/queries.py::calculate_pl_por_entrada"
      to: "helpertips/queries.py::validar_complementar"
      via: "chamada para cada complementar de cada sinal"
      pattern: "validar_complementar"
    - from: "helpertips/queries.py::calculate_equity_curve_breakdown"
      to: "helpertips/queries.py::validar_complementar"
      via: "chamada para P&L de complementares na curva"
      pattern: "validar_complementar"
---

<objective>
Criar funcoes puras de calculo de P&L por entrada e equity curve com breakdown por mercado.

Purpose: FIN-02 requer calculo de P&L discriminado (investido, retorno, lucro) por sinal individual com Martingale progressivo. D-02 requer equity curve com 3 linhas (principal, complementar, total). Estas funcoes alimentam diretamente DASH-04 (tabela P&L) e DASH-06 (equity curve) nas Phases 11-13.

Output: `calculate_pl_por_entrada()` e `calculate_equity_curve_breakdown()` em queries.py com testes cobrindo todos os cenarios de Gale (T1-T4), RED, e breakdown por mercado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-l-gica-financeira/10-CONTEXT.md
@.planning/phases/10-l-gica-financeira/10-RESEARCH.md
@.planning/phases/10-l-gica-financeira/10-01-SUMMARY.md

@helpertips/queries.py
@tests/test_queries.py

<interfaces>
<!-- Funcoes existentes que as novas funcoes DEVEM reutilizar -->

From helpertips/queries.py:
```python
def validar_complementar(regra: str, placar: str | None, resultado_principal: str | None) -> str | None:
    """Determine GREEN/RED/None for a complementary market entry."""

def calculate_roi(signals: list, stake: float, odd: float, gale_on: bool) -> dict:
    """Compute ROI. Gale formula:
       accumulated_stake = stake * (2 ** tentativa - 1)
       winning_stake = stake * (2 ** (tentativa - 1))
       net = winning_stake * (odd - 1) - (accumulated_stake - winning_stake)"""

def calculate_roi_complementares(signals, complementares_config, stake, gale_on) -> dict:
    """ROI para complementares. Gale:
       accumulated_stake = stake * (2 ** tentativa - 1) * percentual
       winning_stake = stake * (2 ** (tentativa - 1)) * percentual"""

def calculate_equity_curve(signals_desc, stake, odd) -> dict:
    """Curva equity para Stake Fixa e Gale. Retorna {x, y_fixa, y_gale, colors, annotations}."""

def get_signals_com_placar(conn, liga=None, entrada=None, date_start=None, date_end=None, mercado_id=None) -> list[dict]:
    """Sinais resolvidos com mercado_slug. Retorna list[dict] com:
       id, resultado, placar, tentativa, mercado_id, mercado_slug, entrada, liga, received_at"""

def get_complementares_config(conn, mercado_slug: str) -> list[dict]:
    """Config complementares. Retorna list[dict] com:
       id, slug, nome_display, percentual, odd_ref, regra_validacao"""
```

Formulas de Gale (de calculate_roi, REPLICAR exatamente):
```python
accumulated_stake = stake * (2 ** tentativa - 1)   # total investido
winning_stake = stake * (2 ** (tentativa - 1))      # stake da tentativa vencedora
# GREEN: net = winning_stake * (odd - 1) - (accumulated_stake - winning_stake)
# RED:   net = -accumulated_stake
```

Percentuais por mercado (seeds do banco, per D-09):
- Over 2.5:     [0.20, 0.01, 0.10, 0.01, 0.01, 0.01, 0.01] (slugs: over_3_5, empate_3_3_4_4, over_5_plus, gols_casa_4, gols_fora_4, gols_casa_5_plus, gols_fora_5_plus)
- Ambas Marcam: [0.10, 0.01, 0.05, 0.01, 0.01, 0.01, 0.01] (mesmos slugs)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar calculate_pl_por_entrada com testes de P&L por sinal</name>
  <files>helpertips/queries.py, tests/test_queries.py</files>
  <read_first>
    - helpertips/queries.py (calculate_roi linhas 201-276 para formula Gale, calculate_roi_complementares linhas 415-530 para complementares, validar_complementar linhas 315-365)
    - tests/test_queries.py (fixtures de sinais e padrao de testes puros sem DB)
    - .planning/phases/10-l-gica-financeira/10-RESEARCH.md (Pattern 3: calculate_pl_por_entrada, exemplos de calculo nas linhas 290-330)
  </read_first>
  <behavior>
    - test_pl_green_t1: sinal GREEN T1, stake=100, odd=2.30, gale_on=True -> investido_principal=100, retorno_principal=230, lucro_principal=130
    - test_pl_green_t2_gale: sinal GREEN T2, stake=100, odd=2.30, gale_on=True -> investido_principal=300, winning_stake=200, lucro_principal=160 (200*1.30 - 100)
    - test_pl_green_t3_gale: sinal GREEN T3, stake=100, odd=2.30, gale_on=True -> investido_principal=700
    - test_pl_green_t4_gale: sinal GREEN T4, stake=100, odd=2.30, gale_on=True -> investido_principal=1500
    - test_pl_red_t4: sinal RED T4, stake=100, gale_on=True -> lucro_principal=-1500, retorno_principal=0
    - test_pl_red_stake_fixa: sinal RED T4, stake=100, gale_on=False -> lucro_principal=-100 (stake fixa ignora tentativa)
    - test_pl_complementares_over_2_5: sinal GREEN T1, placar="4-0", mercado_slug="over_2_5" -> over_3_5 GREEN (4>3.5), investido_comp para over_3_5 = 100*0.20=20
    - test_pl_complementares_ambas_marcam: sinal GREEN T1, mercado_slug="ambas_marcam" -> over_3_5 usa 10% (nao 20%)
    - test_pl_red_sem_placar_complementares: sinal RED sem placar -> todos complementares RED
    - test_pl_retorno_lista_dicts: retorno e lista de dicts com chaves investido_principal, retorno_principal, lucro_principal, investido_comp, lucro_comp, lucro_total
  </behavior>
  <action>
    1. Em `helpertips/queries.py`, adicionar funcao `calculate_pl_por_entrada()` apos `calculate_roi_complementares`:
       ```python
       def calculate_pl_por_entrada(
           signals: list[dict],
           complementares_por_mercado: dict[str, list[dict]],
           stake: float,
           odd_por_mercado: dict[str, float],
           gale_on: bool,
       ) -> list[dict]:
           """
           Calcula P&L detalhado por sinal individual.

           Parametros
           ----------
           signals : list[dict]
               Sinais de get_signals_com_placar(). Cada dict: resultado, placar, tentativa, mercado_slug.
           complementares_por_mercado : dict[str, list[dict]]
               Mapa mercado_slug -> list[dict config complementar] (de get_complementares_config).
           stake : float
               Stake base do principal.
           odd_por_mercado : dict[str, float]
               Mapa mercado_slug -> odd de referencia do principal (ex: {"over_2_5": 2.30, "ambas_marcam": 2.10}).
           gale_on : bool
               Se True, aplica Gale progressivo (1x, 2x, 4x, 8x).

           Retorna
           -------
           list[dict] com uma linha por sinal resolvido:
               mercado_slug, resultado, tentativa, placar, liga, entrada,
               investido_principal, retorno_principal, lucro_principal,
               investido_comp, retorno_comp, lucro_comp,
               investido_total, lucro_total
           """
       ```

    2. Implementacao interna (REPLICAR formulas exatas de calculate_roi):
       - Para cada sinal resolvido (pular resultado=None):
         - `tentativa = signal.get("tentativa") or 1`
         - `mercado_slug = signal.get("mercado_slug") or "over_2_5"` (fallback para sinais historicos sem mercado)
         - `odd = odd_por_mercado.get(mercado_slug, 2.30)` (default 2.30)
         - Se gale_on:
           - `accumulated_stake = stake * (2 ** tentativa - 1)`
           - `winning_stake = stake * (2 ** (tentativa - 1))`
           - Se GREEN: `retorno_principal = winning_stake * odd`, `lucro_principal = winning_stake * (odd - 1) - (accumulated_stake - winning_stake)`
           - Se RED: `retorno_principal = 0`, `lucro_principal = -accumulated_stake`
           - `investido_principal = accumulated_stake`
         - Se nao gale_on:
           - `investido_principal = stake`
           - Se GREEN: `retorno_principal = stake * odd`, `lucro_principal = stake * (odd - 1)`
           - Se RED: `retorno_principal = 0`, `lucro_principal = -stake`
         - Para complementares:
           - `comps = complementares_por_mercado.get(mercado_slug, [])`
           - Para cada comp: `percentual = float(comp["percentual"])`, `odd_ref = float(comp["odd_ref"])`
           - Se gale_on: `acc_comp = stake * (2 ** tentativa - 1) * percentual`, `win_comp = stake * (2 ** (tentativa - 1)) * percentual`
           - Se nao gale_on: `stake_comp = stake * percentual`
           - `resultado_comp = validar_complementar(comp["regra_validacao"], signal.get("placar"), signal.get("resultado"))`
           - Somar investido_comp, retorno_comp, lucro_comp de todos os 7 complementares
         - Montar dict com todos os campos e appendar na lista resultado

    3. Em `tests/test_queries.py`, adicionar import de `calculate_pl_por_entrada` e criar testes puros (sem DB):
       - Usar sinais mock: `[{"resultado": "GREEN", "tentativa": 1, "placar": "4-0", "mercado_slug": "over_2_5", "entrada": "Over 2.5", "liga": "Superliga"}]`
       - Usar config mock de complementares (copiar estrutura de get_complementares_config):
         ```python
         COMP_OVER_2_5 = [
             {"slug": "over_3_5", "nome_display": "Over 3.5", "percentual": 0.20, "odd_ref": 4.00, "regra_validacao": "over_3_5"},
             {"slug": "empate_3_3_4_4", "nome_display": "Empate 3-3/4-4", "percentual": 0.01, "odd_ref": 40.00, "regra_validacao": "empate_3_3_4_4"},
             {"slug": "over_5_plus", "nome_display": "Over 5+", "percentual": 0.10, "odd_ref": 8.00, "regra_validacao": "over_5_plus"},
             {"slug": "gols_casa_4", "nome_display": "Gols Casa=4", "percentual": 0.01, "odd_ref": 20.00, "regra_validacao": "gols_casa_4"},
             {"slug": "gols_fora_4", "nome_display": "Gols Fora=4", "percentual": 0.01, "odd_ref": 20.00, "regra_validacao": "gols_fora_4"},
             {"slug": "gols_casa_5_plus", "nome_display": "Gols Casa 5+", "percentual": 0.01, "odd_ref": 25.00, "regra_validacao": "gols_casa_5_plus"},
             {"slug": "gols_fora_5_plus", "nome_display": "Gols Fora 5+", "percentual": 0.01, "odd_ref": 25.00, "regra_validacao": "gols_fora_5_plus"},
         ]
         ```
       - Verificar valores exatos com `pytest.approx(valor, abs=0.01)`
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py -k "test_pl_" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/queries.py contem `def calculate_pl_por_entrada(`
    - helpertips/queries.py contem `complementares_por_mercado: dict[str, list[dict]]`
    - helpertips/queries.py contem `odd_por_mercado: dict[str, float]`
    - helpertips/queries.py contem `accumulated_stake = stake * (2 ** tentativa - 1)` dentro de calculate_pl_por_entrada
    - helpertips/queries.py contem `validar_complementar(` dentro de calculate_pl_por_entrada
    - tests/test_queries.py contem `def test_pl_green_t1`
    - tests/test_queries.py contem `def test_pl_green_t2_gale`
    - tests/test_queries.py contem `def test_pl_red_t4`
    - tests/test_queries.py contem `def test_pl_complementares_over_2_5`
    - `python3 -m pytest tests/test_queries.py -k "test_pl_" -x -q` exit code 0
  </acceptance_criteria>
  <done>calculate_pl_por_entrada calcula P&L correto para GREEN T1-T4 e RED com Gale e Stake Fixa, incluindo complementares diferenciados por mercado</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar calculate_equity_curve_breakdown com 3 series</name>
  <files>helpertips/queries.py, tests/test_queries.py</files>
  <read_first>
    - helpertips/queries.py (calculate_equity_curve linhas 641-726 como referencia de padrao, calculate_pl_por_entrada ja criada pela Task 1)
    - tests/test_queries.py (testes existentes de equity curve e novos testes de P&L)
    - .planning/phases/10-l-gica-financeira/10-RESEARCH.md (Pattern 4: calculate_equity_curve_breakdown)
  </read_first>
  <behavior>
    - test_equity_curve_breakdown_retorna_chaves: retorno tem chaves x, y_principal, y_complementar, y_total, colors
    - test_equity_curve_breakdown_total_soma: y_total[i] == y_principal[i] + y_complementar[i] para todo i
    - test_equity_curve_breakdown_green_t1: primeiro sinal GREEN T1, odd=2.30, stake=100 -> y_principal[0]=130, y_complementar[0]>0 (soma dos comps GREEN)
    - test_equity_curve_breakdown_red_acumula: sinal RED diminui equity principal e complementar
    - test_equity_curve_breakdown_lista_vazia: sinais vazio -> todas as listas vazias
    - test_equity_curve_breakdown_colors: GREEN -> "#28a745", RED -> "#dc3545"
  </behavior>
  <action>
    1. Em `helpertips/queries.py`, adicionar funcao `calculate_equity_curve_breakdown()` apos `calculate_equity_curve`:
       ```python
       def calculate_equity_curve_breakdown(
           signals: list[dict],
           complementares_por_mercado: dict[str, list[dict]],
           stake: float,
           odd_por_mercado: dict[str, float],
           gale_on: bool,
       ) -> dict:
           """
           Equity curve com breakdown principal / complementar / total (per D-02).

           Recebe sinais em ordem ASC (saida de get_signals_com_placar).

           Parametros
           ----------
           signals : list[dict]
               Sinais resolvidos em ordem ASC. Cada dict: resultado, placar, tentativa, mercado_slug.
           complementares_por_mercado : dict[str, list[dict]]
               Mapa mercado_slug -> config complementares.
           stake : float
               Stake base.
           odd_por_mercado : dict[str, float]
               Mapa mercado_slug -> odd referencia principal.
           gale_on : bool
               Aplica Gale progressivo.

           Retorna
           -------
           dict com chaves:
               x              — indices cronologicos [1, 2, ...]
               y_principal    — equity acumulado do principal
               y_complementar — equity acumulado dos complementares
               y_total        — y_principal + y_complementar
               colors         — "#28a745" (GREEN) ou "#dc3545" (RED) por sinal
           """
       ```

    2. Implementacao interna:
       - Filtrar sinais resolvidos: `resolved = [s for s in signals if s.get("resultado") in ("GREEN", "RED")]`
       - Se lista vazia: retornar `{"x": [], "y_principal": [], "y_complementar": [], "y_total": [], "colors": []}`
       - Inicializar `bk_principal = bk_comp = 0.0`
       - Para cada sinal (ja em ordem ASC):
         - `tentativa = signal.get("tentativa") or 1`
         - `mercado_slug = signal.get("mercado_slug") or "over_2_5"`
         - `odd = odd_por_mercado.get(mercado_slug, 2.30)`
         - Calcular net_principal usando formula Gale exata (mesma de calculate_roi e calculate_pl_por_entrada):
           - Se gale_on: `accumulated = stake * (2 ** tentativa - 1)`, `winning = stake * (2 ** (tentativa - 1))`
           - GREEN: `net_principal = winning * (odd - 1) - (accumulated - winning)`
           - RED: `net_principal = -accumulated`
           - Se nao gale_on: GREEN: `net_principal = stake * (odd - 1)`, RED: `net_principal = -stake`
         - Calcular net_comp somando todos complementares do mercado (mesma logica de calculate_roi_complementares):
           - `comps = complementares_por_mercado.get(mercado_slug, [])`
           - Para cada comp: aplicar Gale ao `stake * percentual`, chamar `validar_complementar()`, somar net
         - `bk_principal += net_principal`, `bk_comp += net_comp`
         - Appendar valores arredondados com `round(x, 2)`
       - Retornar dict com x, y_principal, y_complementar, y_total (soma), colors

    3. Em `tests/test_queries.py`, adicionar import de `calculate_equity_curve_breakdown` e testes puros:
       - Reutilizar as mesmas fixtures COMP_OVER_2_5 da Task 1
       - Verificar y_total[i] == pytest.approx(y_principal[i] + y_complementar[i], abs=0.01)
       - Verificar que lista vazia retorna listas vazias
       - Verificar colors corretas

    4. Rodar suite completa para garantir zero regressao: `python3 -m pytest tests/ -q`
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py -k "test_equity_curve_breakdown" -x -q && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/queries.py contem `def calculate_equity_curve_breakdown(`
    - helpertips/queries.py contem `y_principal` e `y_complementar` e `y_total` dentro da funcao
    - helpertips/queries.py contem `complementares_por_mercado: dict[str, list[dict]]` na assinatura
    - helpertips/queries.py contem `odd_por_mercado: dict[str, float]` na assinatura
    - helpertips/queries.py contem chamada a `validar_complementar(` dentro de calculate_equity_curve_breakdown
    - tests/test_queries.py contem `def test_equity_curve_breakdown_retorna_chaves`
    - tests/test_queries.py contem `def test_equity_curve_breakdown_total_soma`
    - `python3 -m pytest tests/test_queries.py -k "test_equity_curve_breakdown" -x -q` exit code 0
    - `python3 -m pytest tests/ -q` exit code 0 (suite completa green, 150+ existentes + todos novos)
  </acceptance_criteria>
  <done>calculate_equity_curve_breakdown retorna 3 series (principal, complementar, total) com y_total = y_principal + y_complementar, todos os testes da phase passam, zero regressao na suite completa</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -q` — suite completa green (150+ existentes + ~15 novos)
- `grep "def calculate_pl_por_entrada" helpertips/queries.py` — funcao existe
- `grep "def calculate_equity_curve_breakdown" helpertips/queries.py` — funcao existe
- Validar P&L GREEN T2 manualmente: stake=100, odd=2.30, invested=300, winning=200, net=200*1.30-100=160
- Validar equity total = principal + complementar em todos os pontos
</verification>

<success_criteria>
1. calculate_pl_por_entrada retorna P&L discriminado por sinal (FIN-02) com Gale correto T1-T4
2. calculate_equity_curve_breakdown retorna 3 series sobrepostas (D-02, FIN-02)
3. Complementares usam percentuais diferenciados por mercado (FIN-01, FIN-03, D-09)
4. Formulas de Gale replicam exatamente o padrao de calculate_roi existente
5. Zero regressao — suite completa green
6. Funcoes sao puras Python sem acesso ao banco (per D-01, D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/10-l-gica-financeira/10-02-SUMMARY.md`
</output>
