---
phase: 10-l-gica-financeira
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpertips/queries.py
  - tests/test_queries.py
autonomous: true
requirements: [FIN-01, FIN-03]

must_haves:
  truths:
    - "_build_where(mercado_id=1) gera clausula SQL com mercado_id = %s"
    - "get_signals_com_placar() retorna sinais resolvidos com mercado_id e mercado_slug"
    - "get_mercado_config() retorna config de um mercado por slug"
    - "Queries filtradas por mercado retornam apenas sinais do mercado especificado"
    - "Queries sem filtro de mercado retornam todos os sinais (backward compatible)"
  artifacts:
    - path: "helpertips/queries.py"
      provides: "_build_where com mercado_id, get_signals_com_placar, get_mercado_config"
      contains: "def get_signals_com_placar"
    - path: "tests/test_queries.py"
      provides: "Testes das novas queries"
      contains: "test_build_where_mercado_id"
  key_links:
    - from: "helpertips/queries.py::get_signals_com_placar"
      to: "helpertips/queries.py::_build_where"
      via: "chamada com mercado_id"
      pattern: "_build_where.*mercado_id"
    - from: "helpertips/queries.py::get_signals_com_placar"
      to: "tabela signals JOIN mercados"
      via: "LEFT JOIN mercados m ON s.mercado_id = m.id"
      pattern: "LEFT JOIN mercados"
---

<objective>
Estender a camada de queries para suportar filtro por mercado e fornecer dados base para calculo financeiro.

Purpose: FIN-01 e FIN-03 precisam que as queries filtrem por mercado (Over 2.5 vs Ambas Marcam) e retornem sinais com mercado_slug para alimentar as funcoes de P&L do Plan 02. Sem isso, o dashboard redesign (Phases 11-13) nao pode filtrar por mercado.

Output: `_build_where` estendido, `get_signals_com_placar()`, `get_mercado_config()` e testes cobrindo todas as novas funcoes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-l-gica-financeira/10-CONTEXT.md
@.planning/phases/10-l-gica-financeira/10-RESEARCH.md

@helpertips/queries.py
@helpertips/db.py
@tests/test_queries.py

<interfaces>
<!-- Assinaturas existentes que o executor precisa respeitar -->

From helpertips/queries.py:
```python
def _build_where(liga=None, entrada=None, date_start=None, date_end=None):
    """Build a parameterized WHERE clause. Returns (where_sql, params)."""

def get_filtered_stats(conn, liga=None, entrada=None, date_start=None, date_end=None) -> dict:
    """Return aggregate signal counts with optional filters."""

def get_signal_history(conn, liga=None, entrada=None, date_start=None, date_end=None, limit=500) -> list:
    """Return paginated signal rows ordered by received_at DESC."""

def get_complementares_config(conn, mercado_slug: str) -> list[dict]:
    """Retorna lista de complementares para um mercado_slug."""

def validar_complementar(regra, placar, resultado_principal) -> str | None:
    """Determine GREEN/RED/None for a complementary market entry."""
```

From helpertips/db.py (schema relevante):
```sql
-- tabela signals tem: mercado_id (INT FK), group_id (BIGINT) -- adicionadas Phase 9
-- tabela mercados: id SERIAL, slug TEXT UNIQUE, nome_display TEXT, odd_ref NUMERIC, ativo BOOLEAN
-- tabela complementares: id SERIAL, mercado_id INT FK, slug TEXT, percentual NUMERIC, odd_ref NUMERIC, regra_validacao TEXT
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender _build_where com mercado_id e criar get_mercado_config</name>
  <files>helpertips/queries.py, tests/test_queries.py</files>
  <read_first>
    - helpertips/queries.py (estado atual de _build_where na linha 40-66, e todas as funcoes que chamam _build_where)
    - tests/test_queries.py (padroes de teste existentes, fixtures, imports)
    - helpertips/db.py (schema da tabela mercados para confirmar colunas)
  </read_first>
  <behavior>
    - test_build_where_mercado_id: `_build_where(mercado_id=1)` retorna (`"1=1 AND mercado_id = %s"`, `[1]`)
    - test_build_where_mercado_id_none: `_build_where(mercado_id=None)` retorna (`"1=1"`, `[]`) — backward compatible
    - test_build_where_mercado_id_com_liga: `_build_where(liga="Superliga", mercado_id=2)` retorna ambos filtros
    - test_get_mercado_config_over: `get_mercado_config(conn, "over_2_5")` retorna dict com `slug="over_2_5"`, `odd_ref` numerico, `ativo=True`
    - test_get_mercado_config_inexistente: `get_mercado_config(conn, "nao_existe")` retorna `None`
  </behavior>
  <action>
    1. Em `helpertips/queries.py`, adicionar parametro `mercado_id=None` a `_build_where()`:
       ```python
       def _build_where(liga=None, entrada=None, date_start=None, date_end=None, mercado_id=None):
       ```
       Dentro da funcao, apos o bloco de `date_end`, adicionar:
       ```python
       if mercado_id is not None:
           conditions.append("mercado_id = %s")
           params.append(mercado_id)
       ```
       Isso e backward compatible — todas as chamadas existentes continuam funcionando (per D-07, zero breaking change).

    2. Adicionar funcao `get_mercado_config()` em queries.py (apos `get_complementares_config`):
       ```python
       def get_mercado_config(conn, mercado_slug: str) -> dict | None:
           """Retorna config de um mercado por slug. None se nao existir ou inativo."""
           sql = """
               SELECT id, slug, nome_display, odd_ref, ativo
               FROM mercados
               WHERE slug = %s AND ativo = TRUE
           """
           with conn.cursor() as cur:
               cur.execute(sql, (mercado_slug,))
               row = cur.fetchone()
           if row is None:
               return None
           columns = ["id", "slug", "nome_display", "odd_ref", "ativo"]
           return dict(zip(columns, row))
       ```

    3. Em `tests/test_queries.py`, adicionar imports `get_mercado_config` no bloco try. Adicionar testes unitarios para `_build_where` com mercado_id (sem DB) e testes DB para `get_mercado_config`.
       - Testes de `_build_where` sao puros (sem DB) — usar chamada direta
       - Testes de `get_mercado_config` precisam de DB (usar `@pytest.mark.skipif(not _IMPORTS_OK...)` e `conn` fixture existente)
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py -k "test_build_where_mercado_id or test_get_mercado_config" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/queries.py contem `def _build_where(liga=None, entrada=None, date_start=None, date_end=None, mercado_id=None)`
    - helpertips/queries.py contem `conditions.append("mercado_id = %s")` dentro de `_build_where`
    - helpertips/queries.py contem `def get_mercado_config(conn, mercado_slug: str) -> dict | None:`
    - helpertips/queries.py contem `FROM mercados` dentro de get_mercado_config
    - tests/test_queries.py contem `def test_build_where_mercado_id`
    - tests/test_queries.py contem `def test_get_mercado_config`
    - `python3 -m pytest tests/test_queries.py -k "test_build_where_mercado_id" -x -q` exit code 0
    - `python3 -m pytest tests/ -q` exit code 0 (suite completa sem regressao)
  </acceptance_criteria>
  <done>_build_where aceita mercado_id, get_mercado_config retorna config de mercado, todos os testes passam sem regressao</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar get_signals_com_placar com filtro por mercado</name>
  <files>helpertips/queries.py, tests/test_queries.py</files>
  <read_first>
    - helpertips/queries.py (funcao get_signal_history como referencia de padrao, _build_where ja estendido pela Task 1)
    - helpertips/db.py (schema de signals e mercados para confirmar colunas do JOIN)
    - tests/test_queries.py (fixtures existentes e padrao de testes DB)
  </read_first>
  <behavior>
    - test_get_signals_com_placar_retorna_campos: resultado inclui chaves `id, resultado, placar, tentativa, mercado_id, mercado_slug, entrada, liga`
    - test_get_signals_com_placar_ordem_asc: sinais retornam em ordem cronologica (received_at ASC)
    - test_get_signals_com_placar_exclui_pendentes: sinais com resultado=NULL nao aparecem
    - test_get_signals_com_placar_filtro_mercado: com `mercado_id=1` retorna apenas sinais Over 2.5
    - test_get_signals_com_placar_sem_filtro: sem mercado_id retorna todos os sinais resolvidos
  </behavior>
  <action>
    1. Em `helpertips/queries.py`, adicionar funcao `get_signals_com_placar()` (apos `get_mercado_config`):
       ```python
       def get_signals_com_placar(
           conn,
           liga=None, entrada=None, date_start=None, date_end=None, mercado_id=None,
       ) -> list[dict]:
           """
           Retorna sinais resolvidos (GREEN/RED) com mercado_id e mercado_slug.
           Ordenado por received_at ASC para calculo cronologico.
           Inclui sinais com placar=NULL (REDs sem placar).

           Retorna list[dict] com chaves:
               id, resultado, placar, tentativa, mercado_id, mercado_slug, entrada, liga, received_at
           """
           where, params = _build_where(
               liga=liga, entrada=entrada,
               date_start=date_start, date_end=date_end,
               mercado_id=mercado_id,
           )
           sql = f"""
               SELECT s.id, s.resultado, s.placar, s.tentativa,
                      s.mercado_id, m.slug AS mercado_slug, s.entrada, s.liga, s.received_at
               FROM signals s
               LEFT JOIN mercados m ON s.mercado_id = m.id
               WHERE s.resultado IN ('GREEN', 'RED') AND {where}
               ORDER BY s.received_at ASC
           """
           with conn.cursor() as cur:
               cur.execute(sql, params)
               columns = [desc[0] for desc in cur.description]
               rows = cur.fetchall()
           return [dict(zip(columns, row)) for row in rows]
       ```

    2. Adicionar import de `get_signals_com_placar` no bloco try de tests/test_queries.py.

    3. Adicionar testes DB para `get_signals_com_placar` seguindo o padrao existente:
       - Usar `conn` fixture com `ensure_schema(conn)` no setup
       - Inserir sinais de teste com mercado_id=1 e mercado_id=2 via INSERT direto
       - Verificar campos retornados, ordem ASC, exclusao de pendentes, filtro por mercado_id
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -m pytest tests/test_queries.py -k "test_get_signals_com_placar" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/queries.py contem `def get_signals_com_placar(`
    - helpertips/queries.py contem `LEFT JOIN mercados m ON s.mercado_id = m.id`
    - helpertips/queries.py contem `ORDER BY s.received_at ASC`
    - helpertips/queries.py contem `s.resultado IN ('GREEN', 'RED')`
    - tests/test_queries.py contem `def test_get_signals_com_placar`
    - `python3 -m pytest tests/test_queries.py -k "test_get_signals_com_placar" -x -q` exit code 0
    - `python3 -m pytest tests/ -q` exit code 0 (suite completa sem regressao)
  </acceptance_criteria>
  <done>get_signals_com_placar retorna sinais resolvidos com mercado_slug em ordem ASC, filtro por mercado_id funciona, testes passam</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -q` — suite completa (150+ existentes + novos) sem regressao
- `grep -c "def test_" tests/test_queries.py` — contagem de testes aumentou
- `grep "def get_signals_com_placar" helpertips/queries.py` — funcao existe
- `grep "def get_mercado_config" helpertips/queries.py` — funcao existe
- `grep "mercado_id" helpertips/queries.py` — parametro presente em _build_where
</verification>

<success_criteria>
1. _build_where aceita mercado_id e gera clausula SQL correta (FIN-03)
2. get_signals_com_placar retorna sinais com mercado_slug para alimentar P&L (FIN-01)
3. get_mercado_config retorna odd_ref e config de mercado por slug (FIN-01)
4. Zero regressao — todos os 150+ testes existentes continuam passando
5. Novas funcoes seguem padroes existentes (parametrizado, sem f-string injection)
</success_criteria>

<output>
After completion, create `.planning/phases/10-l-gica-financeira/10-01-SUMMARY.md`
</output>
