---
phase: 02.1-market-config
plan: 04
type: execute
wave: 3
depends_on:
  - 02.1-03
files_modified:
  - helpertips/dashboard.py
autonomous: false
requirements:
  - MKT-06
  - MKT-07
must_haves:
  truths:
    - "Dashboard exibe card ROI Complementares abaixo do card ROI Principal"
    - "Card mostra tabela de breakdown com 7 complementares quando entrada filtrada"
    - "Card mostra alerta 'Selecione uma entrada' quando sem filtro de entrada"
    - "Valores de lucro sao coloridos: text-success para positivo, text-danger para negativo"
    - "Filtros de liga/entrada/data afetam o calculo de ROI complementares"
  artifacts:
    - path: "helpertips/dashboard.py"
      provides: "Card ROI Complementares com tabela breakdown e outputs no master callback"
      contains: "roi-comp"
  key_links:
    - from: "helpertips/dashboard.py"
      to: "helpertips/queries.py:get_complementares_config"
      via: "import e chamada no callback"
      pattern: "get_complementares_config"
    - from: "helpertips/dashboard.py"
      to: "helpertips/queries.py:calculate_roi_complementares"
      via: "import e chamada no callback"
      pattern: "calculate_roi_complementares"
---

<objective>
Adicionar card ROI Complementares ao dashboard com tabela de breakdown por mercado, integrado ao master callback existente e respeitando todos os filtros.

Purpose: Dar visibilidade ao retorno das entradas complementares — valor direto para decisao de aposta.
Output: Card ROI Complementares funcional no dashboard com coloracao dinamica e estado vazio tratado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-market-config/02.1-CONTEXT.md
@.planning/phases/02.1-market-config/02.1-RESEARCH.md
@.planning/phases/02.1-market-config/02.1-UI-SPEC.md
@.planning/phases/02.1-market-config/02.1-03-SUMMARY.md

<interfaces>
<!-- From Plan 03: funcoes disponiveis em queries.py -->
From helpertips/queries.py:
```python
def get_complementares_config(conn, mercado_slug: str) -> list[dict]:
    """Retorna lista de complementares ativas para um mercado principal."""

def calculate_roi_complementares(
    signals: list[dict],
    complementares_config: list[dict],
    stake: float,
    gale_on: bool,
) -> dict:
    """Returns: {profit, roi_pct, total_invested, por_mercado: [{slug, nome_display, greens, reds, lucro, investido}]}"""
```

<!-- Existing dashboard master callback -->
From helpertips/dashboard.py:
```python
@callback(
    Output("kpi-total", "children"),
    Output("kpi-greens", "children"),
    Output("kpi-reds", "children"),
    Output("kpi-pending", "children"),
    Output("kpi-winrate", "children"),
    Output("roi-profit", "children"),
    Output("roi-pct", "children"),
    Output("roi-invested", "children"),
    Output("history-table", "rowData"),
    Output("bar-chart", "figure"),
    Output("winrate-chart", "figure"),
    Output("filter-liga", "options"),
    Output("filter-entrada", "options"),
    Input("filter-liga", "value"),
    Input("filter-entrada", "value"),
    Input("filter-date", "start_date"),
    Input("filter-date", "end_date"),
    Input("gale-toggle", "value"),
    Input("stake-input", "value"),
    Input("odd-input", "value"),
    Input("interval-refresh", "n_intervals"),
)
def update_dashboard(liga, entrada, date_start, date_end, gale_on, stake, odd, _n):
```

<!-- Existing import block -->
From helpertips/dashboard.py:
```python
from helpertips.queries import (
    get_filtered_stats,
    get_signal_history,
    get_distinct_values,
    calculate_roi,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar card ROI Complementares ao layout e expandir master callback</name>
  <files>helpertips/dashboard.py</files>
  <read_first>
    - helpertips/dashboard.py (layout completo e callback atual)
    - helpertips/queries.py (assinaturas de get_complementares_config e calculate_roi_complementares)
    - .planning/phases/02.1-market-config/02.1-UI-SPEC.md (contrato visual completo)
  </read_first>
  <action>
    1. IMPORTS — Adicionar ao bloco de import existente:
       ```python
       from helpertips.queries import (
           get_filtered_stats,
           get_signal_history,
           get_distinct_values,
           calculate_roi,
           get_complementares_config,
           calculate_roi_complementares,
       )
       ```

    2. HELPER — Adicionar funcao helper _entrada_para_slug():
       ```python
       _ENTRADA_SLUG_MAP = {
           "Over 2.5": "over_2_5",
           "Ambas Marcam": "ambas_marcam",
       }

       def _entrada_para_slug(entrada: str | None) -> str | None:
           """Mapeia nome de entrada (dropdown) para slug do mercado."""
           if entrada is None:
               return None
           return _ENTRADA_SLUG_MAP.get(entrada)
       ```

    3. HELPER — Adicionar funcao _build_comp_table() para construir dbc.Table:
       ```python
       def _build_comp_table(por_mercado: list[dict]) -> dbc.Table:
           """Constroi dbc.Table com breakdown de complementares."""
           header = html.Thead(html.Tr([
               html.Th("Mercado"),
               html.Th("Greens", className="text-end"),
               html.Th("Reds", className="text-end"),
               html.Th("Lucro (R$)", className="text-end"),
           ]))

           rows = []
           total_greens = 0
           total_reds = 0
           total_lucro = 0.0
           for m in por_mercado:
               lucro = m["lucro"]
               lucro_class = "text-success" if lucro > 0 else ("text-danger" if lucro < 0 else "text-muted")
               rows.append(html.Tr([
                   html.Td(m["nome_display"]),
                   html.Td(str(m["greens"]), className="text-end"),
                   html.Td(str(m["reds"]), className="text-end"),
                   html.Td(f"R$ {lucro:+.2f}", className=f"text-end {lucro_class}"),
               ]))
               total_greens += m["greens"]
               total_reds += m["reds"]
               total_lucro += lucro

           # Linha de totais
           total_class = "text-success" if total_lucro > 0 else ("text-danger" if total_lucro < 0 else "text-muted")
           rows.append(html.Tr([
               html.Td("Total", className="fw-bold"),
               html.Td(str(total_greens), className="text-end fw-bold"),
               html.Td(str(total_reds), className="text-end fw-bold"),
               html.Td(f"R$ {total_lucro:+.2f}", className=f"text-end fw-bold {total_class}"),
           ]))

           body = html.Tbody(rows)
           return dbc.Table(
               [header, body],
               size="sm",
               bordered=False,
               hover=True,
               dark=True,
               className="mt-3",
           )
       ```

    4. LAYOUT — Adicionar card ROI Complementares APOS o card "Simulacao de ROI" existente (antes do Charts Row). O card usa um html.Div com id="roi-comp-container" que recebe o conteudo dinamicamente:
       ```python
       # Card ROI Complementares
       dbc.Card(
           dbc.CardBody([
               html.H5("ROI Complementares", className="card-title text-muted mb-3"),
               html.Div(id="roi-comp-container"),
           ]),
           className="mb-3",
       ),
       ```

    5. CALLBACK — Adicionar novo Output ao master callback:
       ```python
       Output("roi-comp-container", "children"),
       ```
       Inserir APOS `Output("filter-entrada", "options")` (ultimo output atual).

    6. CALLBACK LOGIC — Dentro de update_dashboard(), apos o calculo de ROI existente, adicionar:
       ```python
       # --- ROI Complementares ---
       mercado_slug = _entrada_para_slug(entrada)
       if mercado_slug:
           comp_config = get_complementares_config(conn, mercado_slug)
           if comp_config:
               roi_comp = calculate_roi_complementares(
                   history, comp_config, stake or 10.0, bool(gale_on)
               )
               if roi_comp["total_invested"] > 0:
                   comp_profit = roi_comp["profit"]
                   comp_pct = roi_comp["roi_pct"]
                   comp_invested = roi_comp["total_invested"]
                   profit_class = "text-success" if comp_profit > 0 else ("text-danger" if comp_profit < 0 else "text-muted")
                   pct_class = "text-success" if comp_pct > 0 else ("text-danger" if comp_pct < 0 else "text-info")

                   comp_content = html.Div([
                       dbc.Row([
                           dbc.Col([
                               dbc.Label("Mercado"),
                               html.H5(entrada, className="fw-bold text-light"),
                           ], md=2),
                           dbc.Col([
                               dbc.Label("Lucro/Prejuizo"),
                               html.H5(f"R$ {comp_profit:+.2f}", className=f"fw-bold {profit_class}"),
                           ], md=2),
                           dbc.Col([
                               dbc.Label("ROI %"),
                               html.H5(f"{comp_pct:+.1f}%", className=f"fw-bold {pct_class}"),
                           ], md=2),
                           dbc.Col([
                               dbc.Label("Total Investido"),
                               html.H5(f"R$ {comp_invested:.2f}", className="fw-bold text-muted"),
                           ], md=2),
                       ], className="g-2"),
                       _build_comp_table(roi_comp["por_mercado"]),
                   ])
               else:
                   comp_content = dbc.Alert(
                       [html.H6("Sem sinais no periodo", className="alert-heading"),
                        html.P("Nenhum sinal com resultado encontrado para os filtros selecionados.")],
                       color="warning",
                   )
           else:
               comp_content = dbc.Alert(
                   [html.H6("Sem complementares configuradas", className="alert-heading"),
                    html.P(f"Nenhuma complementar configurada para '{entrada}'.")],
                   color="info",
               )
       else:
           comp_content = dbc.Alert(
               [html.H6("Selecione uma entrada", className="alert-heading"),
                html.P("Escolha Over 2.5 ou Ambas Marcam no filtro acima para ver o breakdown de complementares.")],
               color="info",
           )
       ```

    7. RETURN — Atualizar o return do callback para incluir comp_content como ultimo valor:
       ```python
       return (
           str(total),
           str(greens),
           str(reds),
           str(pending),
           winrate,
           profit_text,
           roi_pct_text,
           invested_text,
           history,
           bar_fig,
           winrate_fig,
           liga_options,
           entrada_options,
           comp_content,     # NOVO — roi-comp-container
       )
       ```

    IMPORTANTE: O `history` ja esta filtrado pelos filtros de liga/entrada/data (per Pitfall 5 do RESEARCH.md). Basta passa-lo para calculate_roi_complementares().
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -c "from helpertips.dashboard import app; print('Dashboard imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/dashboard.py contem `from helpertips.queries import` com `get_complementares_config` e `calculate_roi_complementares`
    - helpertips/dashboard.py contem `id="roi-comp-container"`
    - helpertips/dashboard.py contem `Output("roi-comp-container", "children")`
    - helpertips/dashboard.py contem `_entrada_para_slug`
    - helpertips/dashboard.py contem `_build_comp_table`
    - helpertips/dashboard.py contem `"Selecione uma entrada"` (estado vazio)
    - helpertips/dashboard.py contem `calculate_roi_complementares(` (chamada no callback)
    - helpertips/dashboard.py contem `text-success` e `text-danger` (coloracao dinamica de lucro)
    - `python3 -c "from helpertips.dashboard import app"` nao levanta erro
  </acceptance_criteria>
  <done>Card ROI Complementares adicionado ao dashboard com tabela de breakdown, coloracao dinamica, e tratamento de estado vazio. Import do modulo funciona sem erro.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao visual do card ROI Complementares</name>
  <files>helpertips/dashboard.py</files>
  <action>
    Verificacao visual e funcional do card ROI Complementares no dashboard.
    O usuario deve iniciar o dashboard e validar que:
    - Card aparece abaixo do ROI Principal
    - Estado vazio mostra alerta azul "Selecione uma entrada"
    - Selecionar Over 2.5 ou Ambas Marcam mostra tabela com 7 complementares
    - Valores de lucro coloridos dinamicamente
    - Filtros de liga/entrada/data afetam os calculos
    - Toggle Gale recalcula valores
  </action>
  <verify>
    <automated>cd /Users/luciano/helpertips && python3 -c "from helpertips.dashboard import app; print('OK')"</automated>
  </verify>
  <what-built>Card ROI Complementares integrado ao dashboard com tabela de breakdown por mercado, coloracao dinamica de lucro, e estado vazio tratado.</what-built>
  <how-to-verify>
    1. Iniciar o dashboard: `python -m helpertips.dashboard`
    2. Abrir http://localhost:8050 no browser
    3. Verificar que o card "ROI Complementares" aparece abaixo do card "Simulacao de ROI"
    4. Sem filtro de entrada: card deve mostrar alerta azul "Selecione uma entrada"
    5. Selecionar "Over 2.5" no filtro de entrada: card deve mostrar tabela com 7 complementares
    6. Verificar que valores de lucro estao coloridos (verde=positivo, vermelho=negativo)
    7. Selecionar "Ambas Marcam": tabela deve atualizar com percentuais diferentes
    8. Ativar toggle "Com Gale": valores devem recalcular
    9. Aplicar filtro de liga: valores devem atualizar (filtros afetam complementares)
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva problemas encontrados</resume-signal>
  <done>Usuario aprovou visualmente o card ROI Complementares no dashboard.</done>
</task>

</tasks>

<verification>
- Dashboard inicia sem erros: `python -m helpertips.dashboard`
- Card ROI Complementares renderiza com tabela de 7 linhas quando entrada filtrada
- Estado vazio mostra alerta informativo
- Filtros de liga/entrada/data afetam calculo de complementares
- Suite de testes completa passa: `python3 -m pytest tests/ -x -q`
</verification>

<success_criteria>
- Card ROI Complementares visivel no dashboard com breakdown por mercado
- Tabela mostra Mercado, Greens, Reds, Lucro para cada complementar
- Valores monetarios coloridos dinamicamente (verde/vermelho/cinza)
- Estado vazio tratado com alerta informativo
- Filtros do dashboard afetam calculo de ROI complementares
- Aprovacao visual do usuario
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-market-config/02.1-04-SUMMARY.md`
</output>
