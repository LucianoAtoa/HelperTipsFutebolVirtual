---
phase: 11-dashboard-funda-o
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dashboard.py
  - helpertips/assets/custom.css
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "Testes definem os novos IDs esperados no layout (periodo-selector, filter-mercado, filter-liga, kpi-pl-total, kpi-roi, kpi-streak-green, kpi-streak-red)"
    - "Testes verificam _resolve_periodo para todos os periodos (hoje, semana, mes, mes_passado, toda_vida, personalizado)"
    - "Testes verificam formatacao de P&L (R$ +X.XX) e streaks (Nx ou em-dash)"
    - "CSS override garante contraste dos RadioItems no tema DARKLY"
  artifacts:
    - path: "tests/test_dashboard.py"
      provides: "Testes estruturais e de formatacao para o novo layout"
      contains: "periodo-selector"
    - path: "helpertips/assets/custom.css"
      provides: "Override de contraste para btn-outline-secondary no DARKLY"
      contains: "btn-check"
  key_links:
    - from: "tests/test_dashboard.py"
      to: "helpertips/dashboard.py"
      via: "import app, collect_ids, _resolve_periodo"
      pattern: "from helpertips.dashboard import"
---

<objective>
Criar os testes do novo layout e o CSS de suporte antes de reescrever o dashboard.py.

Purpose: Estabelecer as expectativas (test-first) para o novo layout v1.2 e o arquivo CSS necessario para contraste no tema DARKLY. Os testes falharao ate o Plan 02 reescrever o dashboard — isso e intencional (abordagem red-first).
Output: tests/test_dashboard.py reescrito com novos IDs + helpertips/assets/custom.css
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-funda-o/11-CONTEXT.md
@.planning/phases/11-dashboard-funda-o/11-RESEARCH.md

<interfaces>
<!-- Funcoes existentes em queries.py que os testes de formatacao referenciam -->
From helpertips/queries.py:
```python
def calculate_roi(signals: list, stake: float, odd: float, gale_on: bool) -> dict:
def calculate_streaks(signals_desc: list) -> dict:
    # returns: {current, current_type, max_green, max_red}
```

From helpertips/dashboard.py (helpers a preservar):
```python
def make_kpi_card(title: str, value_id: str, color_class: str = "text-light"):
def _format_streak(count: int, streak_type: str | None) -> str:
def _entrada_para_slug(entrada: str | None) -> str | None:
```

From helpertips/store.py:
```python
ENTRADA_PARA_MERCADO_ID = {
    "Over 2.5": 1,
    "Ambas Marcam": 2,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar helpertips/assets/custom.css com override de contraste DARKLY</name>
  <files>helpertips/assets/custom.css</files>
  <read_first>
    - helpertips/dashboard.py (confirmar que usa dash.Dash(__name__) — Dash serve assets/ relativo ao modulo)
  </read_first>
  <action>
Criar o diretorio `helpertips/assets/` e o arquivo `helpertips/assets/custom.css` com o seguinte conteudo exato (per D-08):

```css
/* Override contraste RadioItems btn-outline-secondary no tema DARKLY */
.btn-check:not(:checked) + .btn-outline-secondary {
    color: #e0e0e0;
    border-color: #6c757d;
}
.btn-check:checked + .btn-outline-secondary {
    background-color: #6c757d;
    border-color: #6c757d;
    color: #fff;
}
```

Dash carrega automaticamente qualquer arquivo em `assets/` — nenhuma configuracao adicional necessaria.
  </action>
  <verify>
    <automated>test -f helpertips/assets/custom.css && grep -q "btn-check" helpertips/assets/custom.css && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - helpertips/assets/custom.css existe
    - Contem `.btn-check:not(:checked) + .btn-outline-secondary`
    - Contem `.btn-check:checked + .btn-outline-secondary`
    - Contem `color: #e0e0e0`
    - Contem `background-color: #6c757d`
  </acceptance_criteria>
  <done>Arquivo CSS criado com override de contraste para RadioItems no DARKLY</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever tests/test_dashboard.py para o novo layout v1.2</name>
  <files>tests/test_dashboard.py</files>
  <read_first>
    - tests/test_dashboard.py (estado atual completo — preservar collect_ids helper e testes que nao dependem de IDs antigos)
    - helpertips/dashboard.py (linhas 1-140 — confirmar helpers existentes e seus nomes)
    - helpertips/queries.py (linhas 207-230 — assinatura calculate_roi para test_gale)
  </read_first>
  <behavior>
    - test_app_layout_renders: app.layout is not None, title == "HelperTips — Futebol Virtual" (PRESERVAR)
    - test_layout_has_required_component_ids: required_ids atualizado para novos IDs do v1.2:
      KPIs: kpi-total, kpi-winrate, kpi-pl-total, kpi-roi, kpi-streak-green, kpi-streak-red
      Filtros: periodo-selector, filter-mercado, filter-liga, collapse-datepicker, filter-date-custom
      Simulacao: stake-input, odd-input, gale-toggle
      Tabela: history-table
      Infra: interval-refresh
      Modal: modal-parse-failures
    - test_datepicker_collapse_initial_closed: verifica que collapse-datepicker.is_open == False no layout
    - test_resolve_periodo_hoje: _resolve_periodo("hoje") retorna (str(date.today()), str(date.today()))
    - test_resolve_periodo_semana: _resolve_periodo("semana") retorna (segunda da semana, hoje)
    - test_resolve_periodo_mes: _resolve_periodo("mes") retorna (primeiro dia do mes, hoje)
    - test_resolve_periodo_mes_passado: _resolve_periodo("mes_passado") retorna (primeiro, ultimo) do mes anterior
    - test_resolve_periodo_toda_vida: _resolve_periodo("toda_vida") retorna (None, None)
    - test_resolve_periodo_personalizado: _resolve_periodo("personalizado", "2025-01-01", "2025-01-31") retorna ("2025-01-01", "2025-01-31")
    - test_kpi_pl_formatting: "R$ +123.45" para positivo, "R$ -50.00" para negativo, classe text-success/text-danger
    - test_kpi_streak_formatting: "12x" para max > 0, em-dash para 0
    - PRESERVAR: test_kpi_formatting_winrate_with_results, test_kpi_formatting_winrate_no_results, test_kpi_formatting_roi_strings, test_gale_accumulated_cost_model, test_debug_mode_off_by_default, test_debug_mode_on_with_env
    - REMOVER: test_coverage_badge_thresholds (badge-coverage removido do layout novo)
    - REMOVER: test_format_streak (funcao _format_streak sera substituida por formatacao inline no novo callback)
  </behavior>
  <action>
Reescrever `tests/test_dashboard.py` mantendo a estrutura existente e o helper `collect_ids()`.

**Imports atualizados:**
```python
from datetime import date, timedelta
from helpertips.dashboard import app
from helpertips.queries import calculate_roi
```
Nota: `_resolve_periodo` sera importado quando existir (Plan 02). Por enquanto, os testes de _resolve_periodo devem ser escritos como funcoes puras locais que testam a logica esperada — serao convertidos para importar do dashboard no Plan 02 quando a funcao existir. Alternativamente, usar `pytest.importorskip` ou wrapping com try/except para que o teste falhe graciosamente.

**Abordagem recomendada para _resolve_periodo:** Escrever os testes importando `_resolve_periodo` do dashboard.py. Esses testes falharao (RED) ate o Plan 02 implementar a funcao — isso e o comportamento esperado de test-first.

```python
from helpertips.dashboard import _resolve_periodo  # Falhara ate Plan 02
```

**Novo required_ids para test_layout_has_required_component_ids:**
```python
required_ids = {
    # KPI cards (D-09)
    "kpi-total",
    "kpi-winrate",
    "kpi-pl-total",
    "kpi-roi",
    "kpi-streak-green",
    "kpi-streak-red",
    # Filtros globais (D-04 a D-07)
    "periodo-selector",
    "filter-mercado",
    "filter-liga",
    "collapse-datepicker",
    "filter-date-custom",
    # Simulacao (D-13, D-14)
    "stake-input",
    "odd-input",
    "gale-toggle",
    # Tabela historico
    "history-table",
    # Infra
    "interval-refresh",
    # Modal
    "modal-parse-failures",
}
```

**Novo teste test_datepicker_collapse_initial_closed:**
```python
def test_datepicker_collapse_initial_closed():
    """DASH-01 D-05: DatePickerRange oculto por padrao."""
    def find_collapse(component, target_id):
        if hasattr(component, 'id') and component.id == target_id:
            return component
        children = getattr(component, 'children', None)
        if children is None:
            return None
        if isinstance(children, (list, tuple)):
            for child in children:
                result = find_collapse(child, target_id)
                if result:
                    return result
        elif hasattr(children, 'children') or hasattr(children, 'id'):
            return find_collapse(children, target_id)
        return None

    collapse = find_collapse(app.layout, "collapse-datepicker")
    assert collapse is not None, "collapse-datepicker nao encontrado no layout"
    assert collapse.is_open is False, "collapse-datepicker deve iniciar fechado (is_open=False)"
```

**Novos testes de _resolve_periodo (6 testes):**
Testar cada opcao: hoje, semana, mes, mes_passado, toda_vida, personalizado. Usar `from datetime import date, timedelta` para calcular valores esperados.

**Novos testes de KPI formatting:**
- test_kpi_pl_formatting: verificar strings "R$ +123.45", "R$ -50.00", "R$ +0.00" e classes CSS "card-text fw-bold text-success", "card-text fw-bold text-danger", "card-text fw-bold text-muted"
- test_kpi_streak_formatting: verificar "12x" para count > 0, em-dash U+2014 para count == 0

**PRESERVAR sem alteracao:** test_kpi_formatting_winrate_with_results, test_kpi_formatting_winrate_no_results, test_kpi_formatting_roi_strings, test_gale_accumulated_cost_model, test_debug_mode_off_by_default, test_debug_mode_on_with_env, collect_ids helper, _make_sig helper, _winrate helper.

**REMOVER:** test_coverage_badge_thresholds, test_format_streak (e o import de _format_streak).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_dashboard.py --collect-only -q 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_dashboard.py contem "periodo-selector" no required_ids
    - tests/test_dashboard.py contem "kpi-pl-total" no required_ids
    - tests/test_dashboard.py contem "kpi-roi" no required_ids
    - tests/test_dashboard.py contem "kpi-streak-green" no required_ids
    - tests/test_dashboard.py contem "kpi-streak-red" no required_ids
    - tests/test_dashboard.py contem "collapse-datepicker" no required_ids
    - tests/test_dashboard.py contem "filter-date-custom" no required_ids
    - tests/test_dashboard.py contem "filter-mercado" no required_ids
    - tests/test_dashboard.py contem def test_datepicker_collapse_initial_closed
    - tests/test_dashboard.py contem def test_resolve_periodo_hoje
    - tests/test_dashboard.py contem def test_resolve_periodo_mes_passado
    - tests/test_dashboard.py contem def test_resolve_periodo_personalizado
    - tests/test_dashboard.py contem def test_kpi_pl_formatting
    - tests/test_dashboard.py contem def test_kpi_streak_formatting
    - tests/test_dashboard.py NAO contem test_coverage_badge_thresholds
    - tests/test_dashboard.py NAO contem test_format_streak
    - pytest --collect-only executa sem SyntaxError
  </acceptance_criteria>
  <done>Testes reescritos com novos IDs v1.2, testes de _resolve_periodo, testes de formatacao KPI. Testes falharao (RED) ate Plan 02 implementar o novo layout.</done>
</task>

</tasks>

<verification>
- `test -f helpertips/assets/custom.css` — arquivo CSS existe
- `grep -c "btn-check" helpertips/assets/custom.css` — pelo menos 2 ocorrencias
- `python3 -m pytest tests/test_dashboard.py --collect-only -q` — coleta testes sem SyntaxError
- Testes de layout e _resolve_periodo DEVEM falhar neste ponto (RED) — Plan 02 os fara passar
</verification>

<success_criteria>
- CSS override criado em helpertips/assets/custom.css com regras de contraste DARKLY
- tests/test_dashboard.py reescrito com novos IDs (periodo-selector, filter-mercado, kpi-pl-total, kpi-roi, kpi-streak-green, kpi-streak-red, collapse-datepicker, filter-date-custom)
- Testes de _resolve_periodo cobrindo 6 opcoes de periodo
- Testes de formatacao KPI (P&L R$, streaks Nx)
- Testes existentes preservados (winrate, ROI strings, gale, debug mode)
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-funda-o/11-01-SUMMARY.md`
</output>
